// MDXEditor JsxComponentDescriptor for <Quiz> — deferred until the mentor
// authoring surface (MDXEditor, docs/02-content-authoring.mdx) is wired up in
// a later M1 pass. Kept as an empty placeholder so this folder matches the
// five-file shape every block type follows (docs/03-blocks-registry.mdx),
// rather than special-casing Quiz as "the block type without an editor".
export const quizEditorDescriptor = null;
