---
title: "Map actors to collections"
module: 3
lesson: 9
summary: "**Why this step matters:** Diagrams show structure; actor paragraphs show **behaviour** — who reads which collection, how checkout splits orders, why Vendor B must never see Vendor A's products. Chapter 8 isolation tests start as sentences you write here."
---
**Why this step matters:** Diagrams show structure; actor paragraphs show **behaviour** — who reads which collection, how checkout splits orders, why Vendor B must never see Vendor A's products. Chapter 8 isolation tests start as sentences you write here.

**Where the project is now:** `docs/erd-draft.md` exists with diagram and tables.

**After this step:** Same file (or new section in it) includes **actor-driven narratives** — what each role reads/writes in which collection. Chapter 5 seed data and Chapter 8 isolation tests make sense only when actors map cleanly to data.

## Why this matters for absolute beginners

Tables of collections describe **structure**; actor paragraphs describe **behaviour**. "Vendor reads orders where storeId matches their store" becomes the isolation test you write in Chapter 8. "Customer checkout creates one order per store in cart" becomes the checkout spec in Chapter 18. Without actor language, your ERD is a static picture — this step makes it a **story you will implement**.

## Common beginner questions

<FaqGroup>

<FaqItem question="Can I copy the sample vendor paragraph?">

Use it as a template only — rewrite in your words; viva checks understanding, not copy-paste.

</FaqItem>

<FaqItem question="Why note seed counts (2 vendors, 2 stores)?">

Chapter 5 seed must support **Vendor A vs Vendor B** isolation tests — one vendor is not enough to prove boundaries.

</FaqItem>

<FaqItem question="What is IDOR in plain English?">

Changing an id in the URL to access someone else's resource — your vendor paragraph should say queries are **scoped**, never global `find()` on products.

</FaqItem>

</FaqGroup>


## What you must add

Extend **`docs/erd-draft.md`** with a section **## Actors and collections** containing **one paragraph per bullet** below — customize to your product name and field names.

Write in complete sentences — this text becomes seed script notes and isolation test checklist.

## Vendor (role: `vendor`)

**Paragraph must cover:**

- Registers as `users` document with role vendor; profile in `profiles`  
- After OTP verification (Chapter 6), creates exactly one `stores` document linked by `userId`  
- Creates `products` documents with `storeId` pointing to their store only  
- Updates own products' `stockQty`, price, `imageUrl`  
- Reads `orders` where `storeId` matches their store — never other vendors' orders  
- Must not query products by id without ownership check — IDOR in Chapter 8  

**Sample opening (rewrite in your words):**

> *A vendor authenticates against the users collection. Their shop is a stores document whose userId equals the vendor's _id. Every product they list stores their storeId. When they open the vendor dashboard, the API queries products with their storeId and orders with their storeId — never a global find().*

## Customer (role: `customer`)

**Paragraph must cover:**

- Registers as `users` with role customer; profile optional fields  
- Browses `products` across all stores — public catalogue, no auth or auth optional  
- Creates `cartitems` linked to their userId; multiple vendors in one cart via different `storeId` on lines  
- Checkout creates one `orders` document per distinct `storeId` in cart, with embedded snapshot lines  
- Reads own `orders` by customerId — not other customers' orders  

**Sample opening:**

> *A customer adds cartitems that reference products and duplicate storeId for split. Checkout does not create one order for the whole cart — it groups cartitems by storeId and inserts multiple order documents, each with embedded orderItems capturing price and unit at purchase time.*

## System / API (no human actor)

**Short paragraph:**

- Health check — no DB yet; Chapter 4 adds DB ping  
- Catalogue read — `products` find with filters; populate `storeId` for name  
- Stock decrement on checkout — atomic update on `products.stockQty`  
- Cache (Chapter 21) — fronting catalogue reads, not replacing MongoDB as source of truth  

## Table — actor × collection × operations

Add this table to your doc (fill "allowed?" honestly):

| Collection | Vendor read | Vendor write | Customer read | Customer write |
|---|---|---|---|---|
| users | own | own register | own | own register |
| profiles | own | own | own | own |
| stores | own | own create one | public info? | no |
| products | own store | own store CRUD | published only | no |
| cartitems | no | no | own | own |
| orders | own store's | status update | own as customer | create via checkout |

Public store info = store name on product cards via populate — not full vendor PII.

## Seed data preview (Chapter 5)

Your paragraphs should imply seed script creates at minimum:

| Entity | Count | Purpose |
|---|---|---|
| Vendor users | 2 | Isolation tests |
| Customer users | 2 | Cart scoping tests |
| Stores | 2 | One per vendor |
| Products | 5–10 per store | Catalogue + cart |
| Cartitems | 0 initially | Created in tests / manual |
| Orders | 0 initially | Created at checkout demo |

Note this in ERD doc under **## Seed plan**.

## Isolation test preview (Chapter 8)

From vendor paragraph, derive test:

- Vendor B token + Vendor A `product._id` on PATCH → must fail  

Document under **## Isolation tests to implement**:

```
- [ ] Vendor cannot PATCH another vendor's product
- [ ] Vendor cannot PATCH another vendor's store
- [ ] Customer cannot GET another customer's cart
```

## Cross-check with Chapter 1 scope

Ensure paragraphs don't promise payments, admin, or reviews — orders may have `paymentStatus: 'pending'` only.

## Commit

```bash
git add docs/erd-draft.md
git commit -m "docs: map actors to collections in ERD draft"
```

## Verify

- [ ] Vendor paragraph committed  
- [ ] Customer paragraph committed  
- [ ] Actor × collection table present  
- [ ] Seed plan noted  
- [ ] Isolation test list started  

## ✅ Do / ❌ Don't

**✅ Do:** Use `_id` / ObjectId vocabulary — matches MongoDB.

**✅ Do:** Mention snapshot fields in customer checkout paragraph.

**❌ Don't:** Describe SQL tables — stay document collections.

**❌ Don't:** Skip vendor isolation language — marketplace core.


## Before you continue

<OpenQuestion id="01KZ5FW3G0DNWQ5BMGFXMW8DVD" />

## Next

**3.10:** Key takeaways checklist + learning log — gate to Chapter 4.
