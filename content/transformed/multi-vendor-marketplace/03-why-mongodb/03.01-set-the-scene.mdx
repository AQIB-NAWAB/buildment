---
title: "Set the scene"
module: 3
lesson: 1
summary: "**Where the project is now:** Chapter 2 left you with a running skeleton — Express API, React client, `GET /health` working in curl and in the browser. Data does not persist yet. Restart the server and the only truth is \"the process is up.\" Vendors, products, carts, and orders ex"
---
# 3.1 — Set the scene

> **[Read]** · Chapter 03 — Why MongoDB · *Every marketplace needs a database — which shape fits?*

**Where the project is now:** Chapter 2 left you with a running skeleton — Express API, React client, `GET /health` working in curl and in the browser. Data does not persist yet. Restart the server and the only truth is "the process is up." Vendors, products, carts, and orders exist only as empty folders under `server/src/modules/`.

**Where this chapter goes:** Before you install Mongoose or paste a connection string (Chapter 4), you decide **what kind of database** this marketplace needs — and you sketch **what collections** you'll create in Chapter 5.

This is a **concept chapter**. There is no new API endpoint to demo. The gate is **understanding + applied design work** in your repo: an ERD draft committed to `docs/erd-draft.md`.

## Bridge from last chapter

Chapter 2 left you with something you can **run and demo**: Express on one port, React on another, `GET /health` returning JSON in curl and on the Home page. You created `server/src/modules/` with empty placeholders — auth, stores, products, cart, orders — and a `docs/` folder waiting for design work. That skeleton proves HTTP works; it does not **remember** anything. Restart the server and no vendor, product, or cart survives.

What carries forward: your folder names, your two-app habit, and the module boundaries you committed to. What you add now is **deliberate data design** before installing Mongoose or pasting a connection string. Professional teams sketch how entities relate before they write schemas — you will do the same in `docs/erd-draft.md`.

The hat you wear now is **data architect for FreshMarket**. You are not wiring MongoDB yet (Chapter 4) or writing Mongoose models (Chapter 5). You are deciding *why* documents fit this grocery marketplace, *what* collections you need, and *when* to embed vs reference — especially for order receipts. When connection code arrives, it implements a blueprint you already understand.

## In plain English

| Term | Meaning |
|---|---|
| **Database** | Where the app stores data permanently — survives server restarts |
| **Document** | One JSON-like record in MongoDB — e.g. one product, one order |
| **Collection** | A named group of documents — like a table, but without fixed columns for every row |
| **Schema / model** | The rules for what fields a document must have — enforced in code (Chapter 5) |
| **Reference** | Storing another document's id and looking it up when needed — like a foreign key |
| **Embed** | Copying sub-objects inside a parent document — e.g. order line items inside an order |
| **ERD** | Entity-relationship diagram — a picture of collections and how they connect |
| **ODM (Mongoose)** | A library that defines schemas and talks to MongoDB from Node |

## Think of it like…

**Planning shelf labels before the warehouse opens.** Chapter 2 built the building (skeleton). This chapter decides what goes on each shelf — users here, stores there, products with grocery units, carts, orders with frozen receipt lines. You are not moving stock in yet; you are drawing the layout so Chapter 4 can connect the warehouse and Chapter 5 can fill shelves without guessing where things belong.

## Common beginner questions

**Q: Why not install MongoDB in Chapter 2?**  
A: Because you would connect before knowing **what shape** your data needs. Grocery products need units and perishability; orders need price snapshots. Designing first prevents copy-paste schemas that break at checkout.

**Q: Is this chapter just reading — does my repo change?**  
A: You **commit** `docs/erd-draft.md` and actor narratives in sub-chapters 3.8–3.9. That is real project progress — design artifacts live beside code in professional teams.

**Q: MongoDB was chosen for MERN — do I need to agree?**  
A: For **this repo**, yes — chapter paths assume Mongoose and documents. You must also articulate **when SQL would compete** (reporting, strict FK enforcement). Interviewers want trade-offs, not fanboyism.

**Q: What's the difference between `erd-draft.md` and code in Chapter 5?**  
A: Draft is your **plan**; Mongoose models are the **enforceable plan**. They should match. If they diverge, fix the draft or the model — do not leave both telling different stories.

**Q: I don't know SQL or MongoDB deeply. Can I still do this chapter?**  
A: Yes. Sub-chapters 3.4–3.6 explain both styles using **the same marketplace entities** side by side. You need curiosity, not prior DBA experience.

## Why every marketplace needs a deliberate database choice

Your app will store:

- **Users** with roles (vendor vs customer)  
- **Stores** owned by vendors  
- **Products** with grocery-specific fields (unit, weight, perishability, stock)  
- **Cart items** tied to customers and products  
- **Orders** split per vendor with **snapshotted prices**  

You could store that in PostgreSQL, MySQL, MongoDB, or something else. The choice affects:

- How you model **ownership** (vendor → store → product)  
- How you model **order lines** (embedded snapshots vs join tables)  
- How you enforce **multi-vendor checkout** (one cart → many orders)  
- How you query the **public catalogue** (filters, pagination, search, cache)

Picking a database because "MERN starts with M" is how tutorials teach. Picking a database because **this product's read/write patterns fit** is how engineers ship.

## The question this chapter answers

> **For a multi-vendor local grocery marketplace, why MongoDB — and when would relational be the better call?**

By the end you will:

- Compare relational and document models **honestly**  
- Mandate **MongoDB + Mongoose** for this course (with reasons tied to the domain)  
- Decide **embed vs reference** for orders, cart, and products  
- Produce **`docs/erd-draft.md`** — collections, refs, embedded shapes — before writing schemas  

Chapter 5 implements what you sketch here. Chapter 4 connects the wire. **Order matters:** decide → connect → implement.

## What this chapter is not

- **Not** installing MongoDB — Chapter 4  
- **Not** writing Mongoose schemas — Chapter 5  
- **Not** a MongoDB tutorial from zero — you revise document basics in 1.5; we go deep on **modelling for this app**  
- **Not** an essay you read without touching your repo — sub-chapters 3.8–3.9 **write files in your project**

## How concept chapters still move the project (Rule 28)

Concept chapters that only produce paragraphs fail the course bar. Here, applied work means:

1. **List collections** your marketplace needs  
2. **Sketch relationships** — User owns Store, Store has Products, Customer has CartItems, Checkout creates Orders  
3. **Commit** `docs/erd-draft.md` (paper photo, Excalidraw export, or Mermaid in markdown — your choice)  
4. **Write learning log** proving you can explain the decision, not parrot it  

When Chapter 5 says *"create `products.model.js` with unit enum"*, you'll already know **why** that file lives in `modules/products/` and **what** references it holds.

## Continuity from Chapter 2

Your module folders were empty placeholders:

```
server/src/modules/
  auth/       ← User, Profile, tokens (Ch 5–6)
  stores/     ← Store (Ch 5, 9)
  products/   ← Product (Ch 5, 11)
  cart/       ← CartItem (Ch 5, 16)
  orders/     ← Order + embedded lines (Ch 5, 18)
```

This chapter names **what documents** live in each module before code arrives.

## Reading pace

| Sub-chapter | Focus |
|---|---|
| 3.1–3.3 | Problem framing |
| 3.4–3.7 | Compare, decide, embed vs ref, reads |
| 3.8–3.9 | **Your ERD** in repo |
| 3.10 | Key takeaways gate |

Estimated time: **half a day to one day** — less typing than Chapter 2, more thinking.

## Self-check before continuing

- [ ] Chapter 2 gate cleared (health demo works)  
- [ ] You can name the two actors and the ownership chain at a high level  
- [ ] `docs/` folder exists in your repo (created in 2.8)  

Next: why grocery specifically pushes on data shape — not generic e-commerce.
