---
title: "Where the project is now"
module: 12
lesson: 5
summary: "Chapter 12 wires React to Chapter 11's product API. Confirm curl can create, list, and PATCH before building tables and forms — otherwise you'll debug API contracts inside JSX error boundaries."
---
## Why this step matters

Chapter 12 wires React to Chapter 11's product API. Confirm curl can create, list, and PATCH before building tables and forms — otherwise you'll debug API contracts inside JSX error boundaries.

**Where the project is now:** Chapter 11 product API complete and curl-tested. Client has vendor shell and store gate from Ch 10 — **no product pages, no `api/products.js`**.

**After this chapter:** Vendor manages inventory in browser — list, create, edit, publish — with errors per 12.4.

**Build cluster starts here.** Confirm Chapter 11 gate before client work.

## Server — unchanged this chapter

```
server/src/modules/products/
  products.routes.js    ← POST, GET /vendor/products, PATCH, DELETE
```

### Product API contracts (reference)

| Method | Path | Role | Success | Key failure |
|---|---|---|---|---|
| `POST` | `/api/products` | vendor | 201 | 400, 404 no store |
| `GET` | `/api/vendor/products` | vendor | 200 array | 403 customer |
| `PATCH` | `/api/products/:id` | vendor | 200 | 403 cross-vendor |
| `DELETE` | `/api/products/:id` | vendor | 200/204 | 403 cross-vendor |

**Body rule:** Never send `storeId` — server resolves from vendor's store.

### Pre-build curl gate

```bash
curl -s http://localhost:5000/api/vendor/products -H "Authorization: Bearer <vendor-token>"
curl -s -X POST http://localhost:5000/api/products \
  -H "Authorization: Bearer <vendor-token>" \
  -H "Content-Type: application/json" \
  -d '{"name":"Organic Kale","price":2.99,"unit":"bunch","stockQty":20}'
```

- [ ] GET /vendor/products returns array
- [ ] POST creates draft (`isPublished: false`)
- [ ] Vendor B PATCH Vendor A product → 403

## Client entering Chapter 12

```
layouts/VendorLayout.jsx     ← store gate
pages/vendor/OpenShop.jsx, StoreSettings.jsx, (products stub)
api/stores.js
```

**Missing:**

| Deliverable | Sub-chapter |
|---|---|
| `api/products.js` | 12.6 |
| `ProductList.jsx` | 12.6 |
| `CreateProduct.jsx` | 12.7 |
| `EditProduct.jsx` + publish toggle | 12.8–12.9 |

## Route map (target)

| Path | Page |
|---|---|
| `/vendor/products` | ProductList |
| `/vendor/products/new` | CreateProduct |
| `/vendor/products/:id/edit` | EditProduct |

All under `VendorLayout` — store gate inherited.

## Vendor journey checkpoint

| Checkpoint | Expected |
|---|---|
| Seed vendor → `/vendor/products` | Renders inside layout |
| Storeless vendor → `/vendor/products` | Redirect open-shop |
| Customer → `/vendor/products` | Role guard blocks |

Fix Ch 10 gate before continuing if storeless vendor sees product stub.

## Troubleshooting

| Symptom | Likely cause |
|---|---|
| GET /vendor/products 404 | Vendor has no store |
| Empty array but Compass has rows | Wrong token or DB |
| 403 on own list | Customer token |
| Products stub still showing | Router not updated in 12.6 |

## ✅ Do / ❌ Don't

**✅ Do:** Confirm Ch 11 log committed; use vendor with store; know `:id` param pattern.

**❌ Don't:** Call public `GET /products`; add image upload (Ch 13); change server routes.

## Verify before 12.6

- [ ] Product API curl tests pass
- [ ] Vendor with store reaches `/vendor/products`
- [ ] 12.4 error mapping reviewed
- [ ] `unit` enum noted from Product model


## Before you continue

<OpenQuestion id="01KZ63P5C5W6CCZ6CSAVE6CYGE" />

## Next

**Sub-chapter 12.6:** Product list page.
