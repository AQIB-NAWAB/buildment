---
title: "Edit product page"
module: 12
lesson: 8
summary: "Vendors fix prices and stock daily. Pre-filled edit forms test your PATCH contract and prove the vendor can round-trip data — load, change, save, see update in the list. Chapter 13's image upload attaches to this page."
---
## Why this step matters

Vendors fix prices and stock daily. Pre-filled edit forms test your PATCH contract and prove the vendor can round-trip data — load, change, save, see update in the list. Chapter 13's image upload attaches to this page.

**After this step:** `/vendor/products/:id/edit` **pre-fills form** from product data and saves via `PATCH /api/products/:id`.

## Loading product data

| Strategy | Pros |
|---|---|
| A — from list via route state | No extra request |
| B — refetch list and find id | Simple; works on direct URL |
| C — GET single product (if added) | Best deep links |

**MVP:** Strategy B — on mount call `listVendorProducts()`, find `params.id`, or 404 if missing.

## Page: EditProduct.jsx

Same fields as create — all editable except id.

### Submit flow

```
onSubmit:
  1. updateProduct(id, dirtyFields) → PATCH
  2. 200 → success toast/banner + stay or navigate list
  3. 400 → field errors
  4. 403/404 → banner per 12.4
```

Recommend navigate back to list on success — table shows updates.

## Pre-fill

Populate controlled inputs from loaded product object — handle loading until data ready.

## api/products.js

Add `updateProduct(id, data)` — PATCH JSON.

## Delete button (optional)

If included:

- Confirm modal  
- `deleteProduct(id)` → DELETE  
- Navigate to list on 204  

Uses Ch 11 delete route — optional for gate if edit + publish sufficient.

## Breadcrumb

Products → Edit [name] — optional navigation aid.

## Commit

```bash
git add client/src/pages/vendor/EditProduct.jsx
git commit -m "feat(client): add edit product form with PATCH products"
```

## ✅ Do / ❌ Don't

**✅ Do:** Validate id param — invalid ObjectId shows friendly 404.

**✅ Do:** Keep publish toggle on this page (12.9) — same form OK.

**❌ Don't:** Allow editing `_id` or `storeId` fields.

## Verify

- [ ] Edit price → save → list shows new price  
- [ ] Hard refresh edit page — still loads correct product  
- [ ] Wrong id in URL → not found UX  


## Before you continue

<OpenQuestion id="01KZ5P6AR3K8TB82TBTVP2YD6N" />

## Next

**Sub-chapter 12.9:** Publish toggle.
