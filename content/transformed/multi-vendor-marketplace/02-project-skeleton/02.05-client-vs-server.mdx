---
title: "Client vs server"
module: 2
lesson: 5
summary: "Full-stack beginners often mix **what runs in the browser** with **what runs on your machine in Node**. This sub-chapter draws the line once, using **your** `client/` and `server/` folders — not a generic diagram you'll forget."
---
Full-stack beginners often mix **what runs in the browser** with **what runs on your machine in Node**. This sub-chapter draws the line once, using **your** `client/` and `server/` folders — not a generic diagram you'll forget.

<BigWordAlert term="Client-server boundary" plainEnglish="The browser runs UI code; the server runs secrets, database access, and business rules — never the reverse." whyItMatters="Stripe keys and MongoDB URLs live only on the server; the React app calls HTTP APIs for everything sensitive." />

## Why this matters for absolute beginners

If you think React "talks to MongoDB," you will put connection strings in the client — a serious security mistake. The browser runs **untrusted code** anyone can inspect in DevTools. The server runs **trusted code** on your machine (later, a host) and holds secrets. Every marketplace feature follows the same loop: click in React → HTTP request → Express → database → JSON response → React updates the screen. Memorize that loop now; every chapter extends it with new routes, not new architecture.

## Think of it like…

**A restaurant dining room vs the kitchen.** Customers (browser) see the menu and place orders — they never walk into the walk-in freezer. The kitchen (server) holds ingredients, recipes, and keys to suppliers (MongoDB, Cloudinary). Waiters carry plates through a pass (HTTP/JSON). You would not put the freezer in the dining room; you do not put `MONGODB_URI` in React.

## Common beginner questions

<FaqGroup>

<FaqItem question="Why two ports (5173 and 5000)?">

Because Vite and Express are two separate programs running on the same machine, and each program listens on its own port — its own door. Port 5173 serves the React UI to the browser; port 5000 serves your API. Think of a restaurant where the dining room has one door and the kitchen has another: customers go in one, orders go in the other, waiters carry plates between them. Different doors also mean different *origins*, which is exactly why CORS shows up next. Two ports, two processes, one product.

</FaqItem>

<FaqItem question="What is CORS and why do I need it?">

CORS (Cross-Origin Resource Sharing) is the browser's rule about letting JavaScript on one origin call another. `http://localhost:5173` and `http://localhost:5000` count as different origins — same machine, different port — so by default the browser blocks your React app from calling your API. You fix it with the `cors` package on the server, telling Express "this client origin is allowed." The classic symptom of skipping it: the same URL works fine in curl but fails in the browser with a CORS error. That difference — the browser enforces it, curl doesn't — is why curl alone can lull you into false confidence.

</FaqItem>

<FaqItem question="Can I put API logic inside a React `useEffect`?">

You can *call* your API from React — that's normal — but you must never implement business rules or database access there. The browser runs untrusted code anyone can read in DevTools, so anything like "which vendor owns this product?" has to live on the server, where it can enforce the rules. Keep React as the menu — display data, handle clicks and forms — and let the server be the kitchen that owns the ingredients and recipes. All persistence stays server-side; the client just asks and renders.

</FaqItem>

<FaqItem question="Is `VITE_API_URL` a secret?">

No — and it's worth seeing why. The browser has to know where to send requests, and `VITE_API_URL` is just the public address of your API, like a shop's street address. That's fine to ship. Secrets — `MONGODB_URI`, `JWT_SECRET`, a Cloudinary API secret — are the *keys*, and they live only in `server/.env`. Vite only exposes variables prefixed with `VITE_` to the client bundle precisely so you can't accidentally leak server secrets into code every user can download. The address is public; the keys stay in the kitchen.

</FaqItem>

<FaqItem question="Why can't React talk to MongoDB directly?">

Because MongoDB needs a connection string with credentials, and the only place that should live is the server — anyone could read it out of the client bundle in DevTools. Beyond security, direct access would let every user run any query, destroying the vendor isolation Chapter 8 depends on. Instead every persistence operation flows through HTTP: React sends `POST /api/products`, Express routes it to a service, the service calls Mongoose, and the response travels back. React is the menu, Express is the kitchen, MongoDB is the pantry — only the kitchen opens the pantry.

</FaqItem>

</FaqGroup>


## Two processes, two terminals

After Chapter 2 you'll run **two commands in two terminals**:

| Terminal | Directory | Command | Serves |
|---|---|---|---|
| 1 | `server/` | `npm run dev` | Express API on port **5000** (default) |
| 2 | `client/` | `npm run dev` | Vite + React on port **5173** (default) |

Two processes. Two ports. **One product.**

Closing one terminal doesn't stop the other — which is why half the bugs this chapter teaches you to debug are "client works, API down" or the reverse.

## What runs where

```
┌─────────────────────────────────────────────────────────────┐
│  BROWSER (customer or vendor)                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  client/  — React, JSX, CSS, Vite bundle              │  │
│  │  • Renders pages                                      │  │
│  │  • Handles clicks, forms, routing                     │  │
│  │  • Calls API via fetch/axios using VITE_API_URL       │  │
│  │  • MUST NOT contain database passwords or JWT secrets │  │
│  └───────────────────────────────────────────────────────┘  │
└───────────────────────────┬─────────────────────────────────┘
                            │ HTTP (JSON)
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  YOUR MACHINE / SERVER (Node.js)                            │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  server/  — Express, Mongoose, business rules         │  │
│  │  • Talks to MongoDB (from Chapter 4)                  │  │
│  │  • Hashes passwords, signs JWTs (Chapter 6)           │  │
│  │  • Enforces vendor isolation (Chapter 8)              │  │
│  │  • Reads secrets from server/.env                     │  │
│  └───────────────────────────────────────────────────────┘  │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
                     MongoDB, Redis, Cloudinary
                     (later chapters)
```

## HTTP — the contract between them

The browser **does not** call Mongoose. React **does not** open MongoDB. Every persistence operation flows:

```
React component
  → fetch POST /api/products  (HTTP + JSON)
    → Express route
      → service
        → Mongoose
          → MongoDB
```

Response travels back up the same chain.

**REST** in this course means: resources identified by URLs, verbs mean something (`GET` read, `POST` create, `PATCH` update, `DELETE` remove), status codes tell the story (`200` OK, `201` created, `400` bad input, `401` not logged in, `403` not allowed, `404` not found).

Chapter 2's only API endpoint is **`GET /health`** — no database, no auth. Still HTTP. Still JSON.

## Why secrets stay on the server

Anything bundled into the React app is **visible to every user**. They can open DevTools → Sources and read minified JS. Therefore:

| Secret | Where it lives |
|---|---|
| `MONGODB_URI` | `server/.env` only |
| `JWT_SECRET` | `server/.env` only |
| `CLOUDINARY_API_SECRET` | `server/.env` only |
| Public API base URL | `client/.env` as `VITE_API_URL` — not secret |

Vite only exposes env vars prefixed with `VITE_` to the client bundle — and you still only put **non-secret** values there.

**❌ Never:** paste Mongo connection string into `client/src/api/client.js`.

## CORS — why the browser blocks you (until you fix it)

By default, browsers restrict JavaScript on `http://localhost:5173` from calling `http://localhost:5000` — **different ports = different origins**.

You'll install the `cors` package on the server and allow the client origin. That's infrastructure setup (Chapter 2 build step), not business logic.

Symptom if you skip CORS: fetch fails in browser with a CORS error; same URL works fine in curl.

## `app.js` vs `server.js` — split on the server

| File | Job |
|---|---|
| `app.js` | Create Express app; register middleware; mount routes; **export** app |
| `server.js` | Import app; read `PORT`; call `app.listen()` |

**Why split?** Later you'll import `app` in tests and send fake HTTP requests **without** opening a real port — the style of isolation testing used on real server codebases. Cost now: one extra file. Benefit later: no refactor.

## Client folder roles (Chapter 2)

Each file in `client/src` has one job now and a clear job later — the table shows both, so you know why each file exists rather than copying an empty structure:

| Path | Chapter 2 | Later |
|---|---|---|
| `src/main.jsx` | Mount React to DOM | Unchanged |
| `src/App.jsx` | Router shell | Add routes for shop, vendor |
| `src/pages/Home.jsx` | Health check demo | Customer shop replaces/extends |
| `src/api/client.js` | Base URL wrapper | `get`, `post` helpers |
| `src/components/` | Empty | ProductCard, etc. |
| `src/hooks/` | Empty | `useAuth` |

## What the Home page proves in 2.15

When Home fetches `/health` and displays the result:

- React `useEffect` + fetch works  
- `VITE_API_URL` is correct  
- Server is up and CORS allows the browser  
- JSON parsing in UI works  

That's the **minimum full-stack loop** — everything else is more routes and more pages on the same pattern.

## Common beginner mistakes

| Mistake | Symptom |
|---|---|
| Only running client | API calls fail; or you hardcode fake data |
| Wrong `VITE_API_URL` | Network error, connection refused |
| Forgot CORS | Browser console CORS policy error |
| Put API code in React | Can't enforce isolation; secrets leak |
| One terminal, one port | Can't serve Vite and Express on same port without proxy (we use two ports + CORS for clarity) |

Optional advanced: Vite `proxy` in `vite.config.js` instead of CORS — this course uses explicit CORS to teach cross-origin reality.

## ✅ Do / ❌ Don't

**✅ Do:** Keep all MongoDB and JWT logic in `server/src/modules/` or `shared/`.

**✅ Do:** Put every `fetch` to your API through `api/client.js` (or helpers built on it).

**❌ Don't:** Call Cloudinary upload secret from React — server uploads in Chapter 13.

**❌ Don't:** Store refresh tokens in `localStorage` — Chapter 7 covers safer patterns.

<RealWorldEvent title="Shopify's modular commerce platform" when="2006–present" summary="Shopify grew from a single-product store builder into a platform with separate admin, storefront, and API surfaces — all coordinated through clear project boundaries." lesson="Starting with a deliberate folder skeleton prevents painful renames later when vendor, cart, and payment code all need room to grow." />

## Key ideas

- **client/** = browser; **server/** = Node API  
- They talk over **HTTP + JSON**  
- **Secrets** server-only; **VITE_** vars client-safe only  
- **CORS** required for localhost cross-port  
- **app.js** builds; **server.js** listens  

## Knowledge self-check

Answer in your own words in the fields below:

1. Why must database connection strings live in `server/.env` and never in the React app?
2. What happens when the browser on port 5173 calls the API on port 5000 — and why is CORS involved?
3. Name one thing the client is allowed to do and one thing only the server should do in this architecture.

Next: mandatory read before you create folders — continue to **Prime your thinking** in the sidebar.
## Before you continue

<OpenQuestion id="01KZP7KNTZCYE0FB94M7KSPMRT" />
