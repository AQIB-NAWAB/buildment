---
title: "Prime your thinking"
module: 2
lesson: 6
summary: "Before you run `git init` and create folders, read **one** short piece on Node project structure so the module-based choice in 2.4 isn't only this course's opinion — it's a decision you've seen argued elsewhere too."
---
Before you run `git init` and create folders, read **one** short piece on Node project structure so the module-based choice in 2.4 isn't only this course's opinion — it's a decision you've seen argued elsewhere too.

## Why this matters for absolute beginners

Reading fifteen minutes before `git init` feels slow when you want to code. It prevents **false confidence** — copying a layer-based tutorial and discovering at Chapter 11 that "products module" does not exist in your repo. The mandatory reads connect this course's module mandate to how Express routing actually works, so sub-chapter 2.11 is not your first time seeing `app.get` and middleware.

## Common beginner questions

<FaqGroup>

<FaqItem question="Do I need to memorize the Express docs?">

No — know where to look. Focus on basic routing, `express.json()`, and `Router()` — enough to mount `/health` and understand Chapter 6 POST bodies.

</FaqItem>

<FaqItem question="Can I skip the feature-vs-layer article if I trust the course?">

Read it anyway — one paragraph in your learning log comparing both approaches proves you chose modules with eyes open, not because a checklist said so.

</FaqItem>

<FaqItem question="Should I read Mongoose or JWT now?">

No — scattered reading derails focus. Trust chapter order; those topics get full chapters when your repo is ready.

</FaqItem>

<FaqItem question="What if I finish reading and still feel unsure?">

Re-read 2.4–2.5 and complete the sanity-check sentences below. Wrong answers now are cheaper than wrong folders after 2.11.

</FaqItem>

</FaqGroup>


## Mandatory read — before you scaffold the repo

Read **both** of the following (total time ~30–45 minutes):

### 1. Express application structure

**Topic to search:** *Express.js routing guide* — official Express documentation section on routing and middleware.

**Why now:** Chapter 2.11 mounts your first route. You should know what `app.get`, `app.use`, and `Router()` mean before you write them.

**After reading, you should be able to answer:**
- What does middleware do in the request pipeline?
- What's the difference between `app.listen` and defining routes on `app`?

### 2. Feature-based vs layered folder structure

**Topic to search:** *feature based vs layered folder structure node express*

**Why now:** You just chose module-based layout. This read shows you the alternative teams use and **when they switch**.

**After reading, you should be able to answer:**
- What problem does layer-based structure solve at small scale?
- Why do growing codebases often move toward feature folders?

## Optional depth — if you have extra time

| Topic | Search | Why |
|---|---|---|
| Vite env variables | *Vite env and mode* official docs | Before setting `VITE_API_URL` in 2.14 |
| Monorepo vs polyrepo | *monorepo frontend backend* | Context for one Git repo, two packages |
| CORS explained | *MDN CORS* | Before enabling cors middleware |

Not blocking — but Chapter 2.15 goes smoother if you've skimmed Vite env docs.

## What not to read yet

| Topic | Wait until |
|---|---|
| Mongoose schemas | Chapter 5 |
| JWT deep dive | Chapter 6 |
| React Router advanced | Chapter 7+ when routes multiply |
| Redis | Chapter 21 |

Reading ahead feels productive but scatters focus. Trust the chapter order.

## After reading — sanity check

Without opening notes, complete these sentences:

1. "In my repo, business logic for products will live in _____________."  
   → Expected: `server/src/modules/products/` (service + routes)

2. "The React app will get data by _____________."  
   → Expected: HTTP requests to the Express API (not direct DB)

3. "Database connection strings live in _____________."  
   → Expected: `server/.env`, not client

If any answer feels fuzzy, re-read 2.5 before 2.8.

## Interesting to read

Teams migrating from layer-based to module-based often publish post-mortems — search *"refactoring to feature folders node"*. Not required; useful if you want war stories.

## Build starts next

Sub-chapter **2.7** is a quiz placeholder — **skip it**.

Sub-chapter **2.8** is your first hands-on step: **create the Git repository** at the root of your marketplace project.

Have your empty parent directory ready. Have Node and Git verified. Don't scaffold inside the course content folder (`multi-vendor-marketplace/` in the guides repo) — that's the textbook, not your app.

Your app repo might live at `~/projects/greenbasket/` or similar — **your choice**.

## Key ideas

- **Read Express routing** before writing routes  
- **Read feature vs layer** to validate the mandated module choice  
- **Skip** Mongoose/JWT reads until their chapters  
- **Next lesson:** Create the repo and root files  

## Reflect before building (5 minutes)

## Knowledge self-check

Answer in your own words in the fields below:

1. What two processes will run when you develop locally?
2. Where will `stores.routes.js` live in a module-based repo?
3. Name one thing that must never be committed to Git.

Wrong answers mean re-read **Layer vs module** and **Client vs server** before **Create the repo** — cheaper than restructuring later.

## Mentor review checkpoint (if applicable)

If a mentor reviews your work, they will ask to see:

- Git log with incremental commits through 2.11 and 2.15  
- curl output for `/health`  
- Browser screenshot with API status  

Reading this sub-chapter doesn't satisfy that — building does.

## Reading log (optional)

You may note your readings in the Chapter 2 learning log on the platform — optional until the gate.

## Express docs — what to focus on

When reading Express routing, pay attention to these sections specifically:

- **Basic routing** — `app.get`, route paths, handler functions  
- **express.json()** — needed before any POST body in Chapter 6  
- **Router** — `express.Router()` — you'll use per module later; health can live on `app` directly for now  

Skip advanced topics (template engines, static files) until needed.

## Feature-folder articles — what to focus on

When reading layer vs feature comparisons, look for:

- How many folders change when adding one feature  
- Where merge conflicts cluster in layer repos  
- Quotes from teams that migrated — what triggered migration  

You are choosing module-based **before** pain forces migration — that's the win.

## Key ideas (summary)

- Mandatory reads: **Express routing** + **feature vs layer structure**  
- Optional: Vite env, CORS MDN  
- **Reflect** with three questions before `git init`  
- **Skip** JWT/Mongoose deep dives until Chapters 4–6  
- **Next lesson:** Create the repo

When reads are done, continue to **Create the repo** in the sidebar.
## Before you continue

<OpenQuestion id="01KZ63P5055PNNA53XC39TFFS7" />
