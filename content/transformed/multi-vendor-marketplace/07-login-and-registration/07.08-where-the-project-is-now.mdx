---
title: "Where the project is now"
module: 7
lesson: 8
summary: "**Why this step matters:** Chapter 7 assumes curl auth already passes — if register or refresh fails in terminal, fix Chapter 6 before building forms. This recap aligns client file paths with the API contract you tested."
---
**Why this step matters:** Chapter 7 assumes curl auth already passes — if register or refresh fails in terminal, fix Chapter 6 before building forms. This recap aligns client file paths with the API contract you tested.

**Where the project is now:** Auth API complete via curl. Client has Home + health fetch only. No auth pages or hooks.

**After this step:** Continuity recap — build cluster from 7.9.

## Client tree entering Chapter 7

```
client/src/
├── api/client.js         ← getHealth only — extend for auth
├── pages/Home.jsx
├── App.jsx               ← router
└── main.jsx
```

Missing: Login, Register, VerifyOtp, useAuth, ProtectedRoute.

## Server baseline

Verify curl still works:

```bash
curl -s -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"vendor-a@test.local","password":"Password123!"}'
```

If cookie mode enabled on server, note Set-Cookie header.

## Build sequence

| Sub-chapter | Deliverable |
|---|---|
| 7.9 | Auth page scaffolds + routes |
| 7.10 | useAuth / AuthProvider |
| 7.11 | Register form wired |
| 7.12 | VerifyOtp wired |
| 7.13 | Login + redirect |
| 7.14 | API interceptor + refresh |
| 7.15 | ProtectedRoute |
| 7.16 | Browser verification |

## ✅ Do / ❌ Don't

**✅ Do:** Test API with curl before blaming UI.

**✅ Do:** Keep VITE_API_URL for all auth calls.

**❌ Don't:** Add new auth endpoints without updating Ch 6 docs.

## Verify checklist

- [ ] Ch 6 gate complete
- [ ] Client and server both run
- [ ] CORS documented


## Before you continue

<OpenQuestion id="01KZAX76EK95RW2J56T41ERCBJ" />

## Next

**Sub-chapter 7.9:** Scaffold auth pages and routes — API auth is done; the browser needs the pages that will call it.
