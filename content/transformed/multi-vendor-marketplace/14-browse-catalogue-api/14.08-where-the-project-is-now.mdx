---
title: "Where the project is now"
module: 14
lesson: 8
summary: "**Why this step matters:** You are about to expose product data to the **public internet** (no auth). This recap confirms seed data, vendor routes, and ownership are solid so you do not accidentally leak drafts or break vendor CRUD while adding browse."
---
**Why this step matters:** You are about to expose product data to the **public internet** (no auth). This recap confirms seed data, vendor routes, and ownership are solid so you do not accidentally leak drafts or break vendor CRUD while adding browse.

**Build cluster starts here.** Confirm vendor product API and seed data exist before adding public read routes.

## Prerequisites gate

| Prior gate | Verify |
|---|---|
| Chapter 11 | Vendor product CRUD; ownership on write |
| Chapter 13 | Products may have `imageUrl` |
| Chapter 5 seed | ≥2 stores, multiple published products, varied categories |
| Chapter 8 | Vendor routes protected; public route will be **open read** |

## Server tree — as of Chapter 13 end

```
server/src/modules/products/
  products.routes.js    ← vendor POST/PATCH/DELETE + GET /vendor/products
  products.service.js
  products.schema.js

server/src/modules/stores/
  stores.model.js       ← Store with name
```

**What changes in Chapter 14:**

```
products.routes.js    ← + GET /products, GET /products/:id (no auth)
products.service.js   ← + listPublicProducts, getPublicProductById
                      ← + buildPublicFilter, pagination helpers
```

## Routes today vs after Chapter 14

| Method | Path | Auth today | After Ch 14 |
|---|---|---|---|
| GET | `/vendor/products` | Vendor | unchanged |
| GET | `/products` | — | **NEW public** |
| GET | `/products/:id` | — | **NEW public** |
| POST | `/products` | Vendor | unchanged |

Register public GET routes **without** `requireAuth` — still apply visibility filter in service.

## Seed data requirements for testing

| Need | Why |
|---|---|
| Products with `isPublished: true` | Public list non-empty |
| At least one `stockQty: 0` | Excluded from catalogue |
| At least one draft/unpublished | Must not appear in public list |
| 2+ categories | Filter test in 14.10 |
| 2+ storeIds | Multi-vendor browse test |

Re-run seed if catalogue empty.

## Client — unchanged this chapter

Chapter 15 adds Shop UI. Optional: test public API with curl or Thunder Client only.

## Build sequence

| Sub-chapter | Action |
|---|---|
| 14.9 | Public list route skeleton |
| 14.10 | category + storeId filters |
| 14.11 | page + limit + metadata |
| 14.12 | populate store — N+1 fix |
| 14.13 | Detail route |
| 14.14 | curl + query count log |

## ✅ Do / ❌ Don't

**✅ Do:** Extract `publicFilter` constant — reuse list, count, detail.

**❌ Don't:** Mount public routes behind vendor middleware by mistake.

**❌ Don't:** Start Chapter 15 before N+1 verified in 14.14.

## Verify before 14.9

- [ ] Seed products visible in Compass with mixed publish/stock states  
- [ ] `GET /vendor/products` still works for vendor JWT  
- [ ] Chapter 13 gate cleared

<RealWorldEvent title="Amazon search index separate from catalog DB" when="2000s–present" summary="Large marketplaces eventually split transactional catalog storage from search-optimized indexes — your module 21 preview." lesson="Build a correct catalogue API first; optimize with cache and search once the happy path works." />


## Before you continue

<OpenQuestion id="01M34MRAH6RFKPKSAP9S23ZSVG" />

## Next

**Sub-chapter 14.9:** Public products route — published, in-stock only.

First public read endpoint.
