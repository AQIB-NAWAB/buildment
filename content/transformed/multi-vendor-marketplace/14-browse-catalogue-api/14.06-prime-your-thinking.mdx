---
title: "Prime your thinking"
module: 14
lesson: 6
summary: "Before implementing pagination in 14.11, read how **offset** and **cursor** pagination differ — you implement offset for this chapter; cursor is the scale-up path mentors may ask about in viva."
---
Before implementing pagination in 14.11, read how **offset** and **cursor** pagination differ — you implement offset for this chapter; cursor is the scale-up path mentors may ask about in viva.

<BigWordAlert term="Query performance" plainEnglish="Designing database queries so catalogue pages load in one or two round-trips, not hundreds." whyItMatters="Slow shop page loses shoppers — index and populate strategy decided here." />


<Predict id="01M34MRAGBVVYKE62GE7SR6EE1" />


## Why read before you build

You are about to write **four related features** in sequence: filters → pagination → populate → detail route. Ten minutes of reading now prevents:

- Applying populate to the **whole collection** instead of the **current page** (slow + wrong mental model)
- Different filters on `countDocuments` vs `find` (pagination shows wrong total pages)
- Skipping query-count verification and shipping N+1 to Chapter 15's shop

This sub-chapter is **not** optional fluff — it is the pause before the build cluster where mistakes compound.

## Mandatory reads

1. **Mongoose — Query populate**  
   Focus: `select`, nested populate, lean queries.

2. **MongoDB — Pagination** (official docs or trusted blog)  
   Focus: skip/limit vs range queries on `_id` or `createdAt`.

3. **Your Chapter 11 product list** — if vendor list exists, compare filters; public list is wider scope.

Skim: MongoDB compound indexes for `{ isPublished: 1, category: 1 }` — optional optimization note in learning log.

## Offset vs cursor — decision table

| | Offset (`page` + `limit`) | Cursor (`after` + `limit`) |
|---|---|---|
| UX | Page numbers — familiar | Infinite scroll — common on mobile |
| Deep pages | Slow (large skip) | Stable speed |
| Implementation | Simple | Needs encoded cursor token |
| Jump to page 5 | Easy | Hard |
| **This chapter** | **✅ mandated** | Interesting read only |

## Questions for learning log draft

1. Why does `skip(10000)` get expensive at scale?  
2. What cursor field would you use for products — `_id` or `createdAt`?  
3. How does populate affect pagination — populate before or after limit? (**After** — limit products first, then populate page only.)  
4. Should `total` count run on every request — cache in Ch 21?  
5. What happens when a product is unpublished between page 1 and page 2 load — acceptable inconsistency for MVP?

## N+1 verification prep

Read how to enable:

```javascript
mongoose.set('debug', (collectionName, method, query, doc) => { ... })
```

Plan to count lines for one `GET /products?limit=20` in 14.14.

## Optional reads

- **Shopify / Stripe API pagination styles** — cursor tokens in the wild.  
- **GraphQL relay cursor spec** — overkill but clarifies "cursor" term.

## ✅ Do / ❌ Don't

**✅ Do:** Apply `.limit()` and `.skip()` on Product query before populate when using find+populate.

**✅ Do:** Document default sort in README when you pick one in 14.11.

**❌ Don't:** Implement cursor pagination unless you finish Ch 14 gate early and update Chapter 15 spec to match.

**❌ Don't:** Skip populate docs — 14.12 depends on them.

## Self-check

- [ ] I read Mongoose populate select syntax  
- [ ] I understand why course uses offset pagination  
- [ ] I know populate runs on limited result set, not whole collection

<InterestingRead title="Public read routes still need discipline" hook="No login does not mean no limits." readMinutes={4}>

Catalogue APIs are public, but they still need rate limits, input validation on query params, and careful projection so internal fields never leak. FreshMarket exposes only what shoppers need to decide and add to cart.

</InterestingRead>

## Next

**Sub-chapter 14.7:** Quiz — reserved.

Continue to **14.8** to start the build cluster.
<RealWorldEvent title="Amazon search index separate from catalog DB" when="2000s–present" summary="Large marketplaces eventually split transactional catalog storage from search-optimized indexes — your module 21 preview." lesson="Build a correct catalogue API first; optimize with cache and search once the happy path works." />
