---
title: "Test catalogue API"
module: 14
lesson: 14
summary: "**Why this step matters:** curl proof and **query-count logging** are your gate evidence — not \"it looks fine in Compass.\" This step catches N+1 and filter bugs before you build React on top of a broken API."
---
**Why this step matters:** curl proof and **query-count logging** are your gate evidence — not "it looks fine in Compass." This step catches N+1 and filter bugs before you build React on top of a broken API.

<BigWordAlert term="Catalogue API test" plainEnglish="curl or Postman checks for pagination, filters, and populate — before wiring React shop." whyItMatters="Isolate slow or wrong JSON here — do not debug catalogue bugs only in the UI." />

**Where the project is now:** Public list and detail routes implemented.

**After this step:** Documented curl tests pass; N+1 query count recorded; ready for Chapter 15 UI.

## Test matrix

Run unauthenticated unless noted.

| # | Request | Expected |
|---|---|---|
| 1 | `GET /products` | 200; items only published+in-stock |
| 2 | `GET /products?limit=5` | ≤5 items; total ≥ items length |
| 3 | `GET /products?category=X` | All items category X |
| 4 | `GET /products?storeId=Y` | All items from store Y |
| 5 | `GET /products?page=2&limit=5` | Correct page metadata |
| 6 | `GET /products/:publishedId` | 200; store populated |
| 7 | `GET /products/:draftId` | 404 |
| 8 | Compare to vendor list | Draft visible vendor-only, not public |

Save example JSON snippets in learning log (redact ids if sharing publicly).

<ApiRequestPanel title="Catalogue API — sample requests (no auth)">

<ApiRequest
  label="Public product list"
  method="GET"
  url="http://localhost:5000/api/products"
  responseStatus={200}
  responseBody='{"items":[{"_id":"...","name":"Organic Kale","store":{"name":"Green Valley"}}],"total":42,"page":1,"limit":20}'
/>

<ApiRequest
  label="Filter by category"
  method="GET"
  url="http://localhost:5000/api/products?category=vegetables&limit=5"
  responseStatus={200}
  responseBody='{"items":[{"category":"vegetables"}],"total":12,"page":1,"limit":5}'
/>

<ApiRequest
  label="Single published product"
  method="GET"
  url="http://localhost:5000/api/products/VALID_ID"
  responseStatus={200}
  responseBody='{"_id":"...","name":"Organic Kale","store":{"name":"Green Valley"}}'
/>

<ApiRequest
  label="Draft product — must 404 on public route"
  method="GET"
  url="http://localhost:5000/api/products/DRAFT_ID"
  responseStatus={404}
  responseBody='{"message":"Product not found"}'
/>

<ApiRequest
  label="Pagination — page 2"
  method="GET"
  url="http://localhost:5000/api/products?page=2&limit=5"
  responseStatus={200}
  responseBody='{"items":[],"total":12,"page":2,"limit":5}'
/>

</ApiRequestPanel>

## Multi-vendor check

Response must include products from **≥2 different** `store.id` values when seed supports it — proves marketplace not single-vendor accidentally.

## N+1 query count — gate evidence

1. Enable Mongoose debug (dev only)  
2. `GET /products?limit=20`  
3. Count store-related queries in log  
4. Write in learning log: **"N products → M queries"**

**Pass:** M is small constant (typically 2), not 21.

If fail: find loop in service — fix before 14.16 gate.

## Response size check

Network tab or curl `-w '%size_download'`:

- JSON should be kilobytes for page of 20 with URLs — not megabytes  
- `imageUrl` present; no embedded image data

## CORS from client origin

Optional pre-Ch15 check:

```javascript
fetch(`${import.meta.env.VITE_API_URL}/products`)
```

From browser console on Vite dev — should not CORS-fail on GET.

## README — API documentation

Add section **Public catalogue**:

| Endpoint | Params |
|---|---|
| GET /products | page, limit, category, storeId |
| GET /products/:id | — |

Example curl commands — copy from this sub-chapter.

## Commit

```bash
git add README.md
git commit -m "docs: add public catalogue API test notes and examples"
```

Include any bugfixes from testing in product module commits.

## ✅ Do / ❌ Don't

**✅ Do:** Test with seed that includes edge cases (draft, OOS).

**✅ Do:** Disable mongoose debug after test — noisy logs.

**❌ Don't:** Mark gate done without query count note.

**❌ Don't:** Test only with limit=2 — N+1 hides until 20.

## Verify checklist

- [ ] Full test matrix passed  
- [ ] Query count documented  
- [ ] Two stores represented in default list  
- [ ] README updated  
- [ ] Vendor routes regression — still 401/403 without token

<RealWorldEvent title="Amazon search index separate from catalog DB" when="2000s–present" summary="Large marketplaces eventually split transactional catalog storage from search-optimized indexes — your module 21 preview." lesson="Build a correct catalogue API first; optimize with cache and search once the happy path works." />


## Before you continue

<OpenQuestion id="01KZP7KQ88182PKYYXFBT2ERQY" />

## Next

**Sub-chapter 14.15:** Recap and what's next — Chapter 15 shop UI.

API complete; frontend consumes it next.
