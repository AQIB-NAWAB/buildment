---
title: "Where the project is now"
module: 5
lesson: 8
summary: "**Why this step matters:** Before creating six models, confirm MongoDB connects, module folders exist, and your ERD is open beside you. Building schemas on a disconnected DB or without a blueprint is the fastest path to rework in Chapter 6."
---
**Why this step matters:** Before creating six models, confirm MongoDB connects, module folders exist, and your ERD is open beside you. Building schemas on a disconnected DB or without a blueprint is the fastest path to rework in Chapter 6.

**Where the project is now:** Chapter 4 complete — MongoDB connects on boot, health reports `db: connected`, client Home shows live status. Module folders exist under `server/src/modules/` but contain no models. Compass shows empty collections (or no collections yet).

**After this step:** Same — this is a **continuity recap** before the build cluster. No new files until 5.9.

## Repo tree as you enter Chapter 5

```
your-marketplace/
├── README.md
├── .gitignore
├── docs/
│   └── erd-draft.md              ← Ch 3 blueprint — open this now
│   └── 04-config-and-database.md
├── server/
│   ├── .env                      ← MONGODB_URI — gitignored
│   ├── .env.example
│   ├── package.json              ← mongoose installed
│   ├── scripts/                  ← does not exist yet — 5.14
│   └── src/
│       ├── app.js                ← GET /health { status, db }
│       ├── server.js             ← connectDB → listen
│       ├── shared/
│       │   ├── config.js
│       │   └── db.js
│       └── modules/
│           ├── auth/             ← empty — Ch 6
│           ├── users/            ← empty — 5.10
│           ├── stores/           ← empty — 5.11
│           ├── products/         ← empty — 5.12
│           ├── cart/             ← empty — 5.13
│           └── orders/           ← empty — 5.13
└── client/
    └── src/
        ├── api/client.js
        └── pages/Home.jsx        ← unchanged this chapter
```

## What still works — verify before building

Run both apps. Confirm baseline:

```bash
# Terminal 1 — server/
npm run dev

# Terminal 2 — client/
npm run dev
```

<ApiRequestPanel title="Baseline before models">

<ApiRequest
  label="GET /health — DB connected"
  method="GET"
  url="http://localhost:5000/health"
  responseStatus={200}
  responseBody='{"status":"ok","db":"connected"}'
/>

</ApiRequestPanel>

Client Home should display the same fields. If not, finish Chapter 4 gate first.

## What Chapter 5 adds (preview)

| Area | Change |
|---|---|
| `server/src/modules/*/` | Six model files |
| `server/scripts/seed.js` | Test data population |
| `docs/erd.md` | Final ERD |
| `server/package.json` | `"seed"` script |
| Client | **No change** |
| Routes | **No change** |

Health endpoint unchanged — still no business logic routes.

## Dependencies from prior chapters

| Prior work | Used in Ch 5 |
|---|---|
| `connectDB()` from `shared/db.js` | Seed script entry |
| `config.js` MONGODB_URI | Same DB as API |
| `docs/erd-draft.md` | Model field names |
| Module folder convention | Model file locations |
| `.gitignore` | Still no `.env` in Git |

## Compass baseline

Connect Compass to your `MONGODB_URI` database. Note:

- Database name matches URI path (e.g. `freshmarket-dev`)
- Collections may be empty or absent — **expected**
- After seed (5.15), you should see: `users`, `profiles`, `stores`, `products` (Mongoose pluralization)

## Chapter 4 gate quick audit

- [ ] `git log --all --full-history -- "**/.env"` empty
- [ ] Server fails start when MongoDB down
- [ ] Learning log 04 committed

## Build cluster sequence

| Sub-chapter | Action |
|---|---|
| 5.9 | Confirm module folders |
| 5.10 | User + Profile models |
| 5.11 | Store model |
| 5.12 | Product model |
| 5.13 | CartItem + Order models |
| 5.14 | Seed script scaffold |
| 5.15 | Run seed + Compass inspect |
| 5.16 | Final `docs/erd.md` |

Commit after each model group — not one giant commit at end.

## ✅ Do / ❌ Don't

**✅ Do:** Keep server running during model work — no need to restart until seed.

**✅ Do:** Have ERD draft visible in second monitor or split editor.

**❌ Don't:** Add routes "to test" models — use seed + Compass or temporary script.

**❌ Don't:** Change health contract this chapter.

## Verify checklist

- [ ] curl `/health` returns 200 with `db: connected`
- [ ] Client Home shows health JSON
- [ ] `docs/erd-draft.md` exists
- [ ] Module folders exist under `server/src/modules/`

<RealWorldEvent title="Amazon's item-level order snapshots" when="Marketplace era" summary="Marketplace order systems persist item details at purchase time so disputes, refunds, and tax reporting reflect the transaction as it happened." lesson="Model for auditability early. Your seed data and ERD should show which fields are live references and which are immutable snapshots." />


## Before you continue

<OpenQuestion id="01M34MR8WJQ9A4G4YVN2YYQT1K" />

## Next

**Sub-chapter 5.9:** Scaffold models folder — confirm module structure for all six collections.

Baseline confirmed — time to create schemas.
