---
title: "Product model"
module: 5
lesson: 12
summary: "**Why this step matters:** Products carry FreshMarket's grocery identity — unit, stock, perishability — not generic e-commerce fields. This schema is what customers browse and vendors edit; catalogue and checkout chapters assume these shapes exist."
---
**Why this step matters:** Products carry FreshMarket's grocery identity — unit, stock, perishability — not generic e-commerce fields. This schema is what customers browse and vendors edit; catalogue and checkout chapters assume these shapes exist.

<BigWordAlert term="Product catalogue record" plainEnglish="The document listing what a vendor sells — name, price, stock, images, and publish status." whyItMatters="Browse, cart, and checkout all read product documents — this is the core inventory entity." />

**Where the project is now:** User, Profile, and Store models exist. No Product schema yet.

**After this step:** `product.model.js` defines grocery catalogue items with `storeId` ref and domain fields from 5.5.

## Product schema specification

| Field | Type | Required | Default | Notes |
|---|---|---|---|---|
| `name` | String | yes | — | Searchable — text index Ch 21 |
| `description` | String | no | — | |
| `price` | Number | yes | — | Per `unit`; min 0 |
| `unit` | String enum | yes | — | See 5.5 enum list |
| `weightOrQuantity` | Number | no | — | Pack size hint |
| `stockQty` | Number | yes | 0 | Integer ≥ 0 |
| `isPerishable` | Boolean | yes | false | |
| `category` | String | yes | — | Controlled vocabulary |
| `isPublished` | Boolean | yes | false | Draft until vendor publishes |
| `imageUrl` | String | no | null | Cloudinary URL — Ch 13 |
| `storeId` | ObjectId ref Store | yes | — | Ownership |
| timestamps | auto | yes | — | |

**Indexes:**

- `{ storeId: 1 }` — vendor product list
- `{ storeId: 1, isPublished: 1 }` — catalogue queries (Ch 14)
- Optional: `{ name: 'text', category: 'text' }` — defer to Ch 21 or note in ERD

## Enum — unit (mandated minimum)

```
each | bunch | kg | g | L | ml | pack
```

Document exact enum in model and `docs/erd.md`.

## Enum — category (suggested)

```
produce | dairy | bakery | pantry | frozen | beverages | household
```

Adjust to your product name — keep list finite for filter dropdown.

## Relationship

```
Store (_id) ←—— Product.storeId
```

**No** `userId` on Product — ownership via Store.userId chain only.

## Published vs draft semantics

| isPublished | Visible in public GET /products (Ch 14) |
|---|---|
| false | Hidden — vendor dashboard only |
| true | Catalogue if stockQty > 0 |

Seed: at least 3 published products per store for Week 2 demo prep.

## Stock rules (schema + app)

Stock is enforced in two places — the schema and the app logic — and both must agree:

- Schema: `stockQty` Number, min 0 validator optional
- App (Ch 16–18): reject add-to-cart and checkout when insufficient

Do not use negative stock to mean "unlimited".

## Seed product examples (spec)

Per store, include variety:

| name | unit | isPerishable | category | isPublished |
|---|---|---|---|---|
| Organic Bananas | bunch | true | produce | true |
| Whole Milk 1L | L | true | dairy | true |
| Sourdough Loaf | each | true | bakery | true |
| Basmati Rice 1kg | pack | false | pantry | true |
| Frozen Peas 500g | pack | false | frozen | false |

Prices realistic for your locale. `stockQty` 10–100.

## File to create

```
server/src/modules/products/product.model.js
```

Model name: `'Product'`

## Commit

```bash
git add server/src/modules/products/
git commit -m "feat(server): add Product model with grocery domain fields"
```

## ✅ Do / ❌ Don't

**✅ Do:** Index storeId — vendor queries are hot path.

**✅ Do:** Default isPublished false — explicit publish action in Ch 12.

**❌ Don't:** Add vendorName or storeName fields — populate storeId.

**❌ Don't:** Store price as String — checkout math needs Number.

## Verify checklist

- [ ] All grocery fields from 5.5 present
- [ ] storeId ref `'Store'` required
- [ ] unit and role enums enforced
- [ ] stockQty defaults sensibly
- [ ] timestamps enabled

<RealWorldEvent title="Amazon's item-level order snapshots" when="Marketplace era" summary="Marketplace order systems persist item details at purchase time so disputes, refunds, and tax reporting reflect the transaction as it happened." lesson="Model for auditability early. Your seed data and ERD should show which fields are live references and which are immutable snapshots." />


## Before you continue

<OpenQuestion id="01M34MR8Y051MZECMPYFMXFB6E" />

## Next

**Sub-chapter 5.13:** CartItem and Order models — refs plus embedded order line snapshots.

Product catalogue schema done — cart and orders next.
