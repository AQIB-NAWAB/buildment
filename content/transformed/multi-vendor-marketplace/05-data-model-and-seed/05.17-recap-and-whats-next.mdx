---
title: "Recap and what's next"
module: 5
lesson: 17
summary: "You entered Chapter 5 with an empty MongoDB and a draft ERD. You leave with **six Mongoose models**, **seed data visible in Compass**, and a **final ERD** that matches your code."
---
You entered Chapter 5 with an empty MongoDB and a draft ERD. You leave with **six Mongoose models**, **seed data visible in Compass**, and a **final ERD** that matches your code.

<BigWordAlert term="Populated database" plainEnglish="MongoDB with models registered and seed data loaded — ready for API handlers to query." whyItMatters="Chapter 6 adds auth routes that create and look up User documents — models must exist first." />

## What you built — narrative recap

**User + Profile:** Identity split — auth fields on User, display on Profile. Unique email, role enum, verification flag ready for Chapter 6.

**Store:** Vendor shop with unique `userId` and `slug` — ownership anchor for products.

**Product:** Grocery domain fields — unit, stockQty, isPerishable, category, isPublished. References store, not vendor directly.

**CartItem:** Customer cart lines with productId and storeId for multi-vendor checkout split.

**Order:** Per-vendor order documents with embedded snapshot lines — price immutability prepared for Chapter 18.

**Seed script:** Two vendors, two stores, realistic products, two customers — stable test emails for isolation tests.

**docs/erd.md:** Final data model documentation.

## Repo tree now

```
your-marketplace/
├── docs/
│   ├── erd-draft.md
│   └── erd.md                    ← NEW
├── server/
│   ├── package.json              ← + seed script
│   ├── scripts/
│   │   └── seed.js               ← NEW
│   └── src/modules/
│       ├── users/
│       │   ├── user.model.js
│       │   └── profile.model.js
│       ├── stores/store.model.js
│       ├── products/product.model.js
│       ├── cart/cartItem.model.js
│       └── orders/order.model.js
└── client/                       ← unchanged
```

## Week 1 phase — complete

| Demo target | Status |
|---|---|
| Both apps run | ✓ |
| Client hits `/health` | ✓ |
| MongoDB connected | ✓ |
| Seed visible in Compass | ✓ **done** |
| ERD finalized | ✓ **docs/erd.md** |

**Week 1 foundations gate cleared** after 5.18 checklist.

## Decisions you can defend in a viva

1. **Why User/Profile split?** — Auth rotation vs display data separation.  
2. **Why storeId on Product not userId?** — One store per vendor; products belong to shop entity.  
3. **Why storeId on CartItem?** — Checkout grouping without extra Product lookup.  
4. **Why embed orderItems?** — Snapshots immutable; no separate orderlines collection.  
5. **Why not vendorName on Product?** — Denormalisation trap — populate instead.  
6. **Why empty cart/orders in seed?** — Created by feature chapters; models exist first.

## What's explicitly not built yet

| Not yet | Chapter |
|---|---|
| Auth routes / JWT | 6 |
| Login UI | 7 |
| Role middleware | 8 |
| Store API | 9 |
| Product CRUD API | 11 |
| Cart / checkout | 16–18 |

Health still only route — intentional.

<InterestingRead title="Why orders store prices, not live product lookups" hook="A receipt is a photograph, not a live feed." readMinutes={4}>

If an order stored only a product ID, a vendor price change would rewrite history. FreshMarket copies name, unit price, and weight into order items at checkout so customers and vendors always see what was agreed at purchase time.

</InterestingRead>


## Before you continue

<OpenQuestion id="01M34MR8ZY587088GANTS230B7" />

## Next chapter — Authentication API (Chapter 6)

Chapter 6 adds **server-side auth only**:

- `POST /api/auth/register` — hash password, create User + Profile  
- `POST /api/auth/login` — bcrypt compare, issue JWT  
- `POST /api/auth/refresh` — refresh token rotation  
- `POST /api/auth/verify-otp` — block unverified login  
- `requireAuth` middleware — 401 on bad/missing token  

**Reuses from Chapter 5:**

- User and Profile models  
- Seed users get real password hashes if not already  
- Test emails from seed for curl flows  

**New env vars (preview):** `JWT_SECRET`, `JWT_ACCESS_EXPIRY`, `JWT_REFRESH_EXPIRY`

## Callback before Chapter 6

- Complete **5.18** checklist and learning log  
- Confirm seed re-runnable  
- Document test password in README (not in Git as secret — test password OK for local)  
- Glance ownership chain — Chapter 8 builds on same refs  

## Suggested commit history through Chapter 5

```
feat(server): add User and Profile mongoose models
feat(server): add Store mongoose model
feat(server): add Product model with grocery domain fields
feat(server): add CartItem and Order models with embedded snapshots
feat(server): add database seed script with test vendors and products
docs: add final ERD matching mongoose schemas
docs: complete chapter 5 learning log
```

## Key milestones unlocked

- [x] MongoDB connected (Ch 4)  
- [x] All schemas exist (Ch 5)  
- [x] Seed in Compass (Ch 5)  
- [x] ERD final (Ch 5)  
- [ ] Auth API (Ch 6)  
- [ ] Vendor shop (Week 2)  

## Continue to gate

**Sub-chapter 5.18:** Definition of Done checklist + learning log.

**Do not open Chapter 6 until 5.18 is fully ticked.**

Your marketplace has data shape and test groceries — next it learns who is knocking on the door.
<RealWorldEvent title="Amazon's item-level order snapshots" when="Marketplace era" summary="Marketplace order systems persist item details at purchase time so disputes, refunds, and tax reporting reflect the transaction as it happened." lesson="Model for auditability early. Your seed data and ERD should show which fields are live references and which are immutable snapshots." />
