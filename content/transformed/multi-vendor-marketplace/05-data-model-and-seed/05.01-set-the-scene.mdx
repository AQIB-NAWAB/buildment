---
title: "Set the scene"
module: 5
lesson: 1
summary: "**Where the project is now:** Chapter 4 gave you a connected MongoDB — `shared/config.js`, `shared/db.js`, boot order correct, `GET /health` reports `db: connected`. Compass shows an empty database. `docs/erd-draft.md` sketches collections but no Mongoose schemas exist yet."
---
**Where the project is now:** Chapter 4 gave you a connected MongoDB — `shared/config.js`, `shared/db.js`, boot order correct, `GET /health` reports `db: connected`. Compass shows an empty database. `docs/erd-draft.md` sketches collections but no Mongoose schemas exist yet.

**Where this chapter goes:** You implement **every collection** your marketplace needs — User, Profile, Store, Product, CartItem, Order — with grocery-specific fields, a seed script that populates realistic test data, and a final **`docs/erd.md`** aligned with what you actually built.

This is a **build chapter**. The gate is **runnable proof**: seed runs, Compass shows documents, you can explain the ownership chain aloud.

## Bridge from last chapter

Chapter 4 connected MongoDB — `shared/config.js`, `shared/db.js`, boot order correct, `GET /health` reporting `db: connected`. Your database is **reachable but empty** (or nearly so). Chapter 3's ERD describes vendors, stores, products, carts, and orders; Chapter 2's module folders wait for files.

What carries forward: connection helpers (seed will reuse `connectDB()`), your ERD draft, and empty `modules/*` placeholders. What you add now is **enforceable structure** — Mongoose schemas for every collection, grocery-specific product fields, embedded order line snapshots, and seed data with **two vendors** so isolation tests in Chapter 8 have something to attack.

The hat you wear now is **domain modeller**. You translate paper design into schemas that every future route depends on. Wrong refs break vendor isolation; missing snapshot fields break order history at checkout. Auth routes come in Chapter 6 — but they need User and Profile documents to exist first. That is why schemas precede login API work.

## In plain English

| Term | Meaning |
|---|---|
| **Mongoose schema** | Blueprint for one collection — field types, required flags, enums |
| **Model** | The compiled schema you call to create and query documents |
| **`ObjectId`** | MongoDB's default id type — used for references between documents |
| **`ref`** | Field pointing to another collection's document — enables `populate()` |
| **Seed script** | A one-off program that inserts test data — two vendors, sample products |
| **Ownership chain** | User → Store → Product — who may touch which document |
| **Snapshot fields** | Copied price/name/unit on order lines — frozen at purchase time |
| **Denormalisation** | Copying data for speed — correct on orders, wrong on live products |

## Think of it like…

**Stocking the market stalls with real labels and inventory sheets.** The hall has power (Chapter 4) and a floor plan (Chapter 3). Now each vendor gets a named stall (Store), product tags with units and perishability (Product), empty shopping baskets waiting for customers (CartItem schema), and receipt books that freeze prices at sale time (embedded orderItems). Seed data is sample merchandise so you can walk the aisles in Compass before customers arrive.

## Common beginner questions

<FaqGroup>

<FaqItem question="Why models before auth — shouldn't login come first?">

Login **creates and reads User documents**. Register needs fields (`email`, `passwordHash`, `role`, `isVerified`) defined in a schema. Building auth against imaginary users leads to rework. Data shape first; HTTP auth layer second.

</FaqItem>

<FaqItem question="Why separate User and Profile?">

Login identity (email, password, role) changes rarely; display info (name, avatar) updates often. Splitting collections keeps auth lean and matches how Chapter 6 register flow creates both records.

</FaqItem>

<FaqItem question="Can I skip seed and add data manually in Compass?">

Seed is **required for the gate** — two vendors, two stores, products with grocery fields. Chapter 8 isolation tests assume predictable test emails like `vendor-a@test.local`.

</FaqItem>

<FaqItem question="What's wrong with putting `vendorName` on every product?">

When the vendor renames their shop, every product copy goes stale. Reference `storeId` and look up the name when displaying — except on **orders**, where copied names are intentional receipts.

</FaqItem>

<FaqItem question="Do I build API routes this chapter?">

No — models and seed only. Routes arrive from Chapter 6 onward. If you add routes now, you blur the course's step-by-step arc.

</FaqItem>

</FaqGroup>


## The gap between "connected" and "useful"

Right now MongoDB answers: *Can the server reach the database?*

It cannot answer:

- What does a vendor look like in a document?
- How does a product know which store it belongs to?
- What gets copied into an order at checkout time?

Those are **schema decisions** — the most consequential choices in a marketplace. Wrong refs break isolation in Chapter 8. Missing snapshot fields break order history in Chapter 18. Chapter 5 is where design from Chapter 3 becomes **enforceable structure**.

## Why this chapter sits after config

Chapter 4 established **one connection path** — `connectDB()` from `shared/db.js`. Chapter 5's seed script reuses that same path. If you skipped Chapter 4's gate, stop here — seed will fail or write to the wrong database.

Chapter 5 assumes:

- `MONGODB_URI` in `server/.env` works
- `docs/erd-draft.md` committed from Chapter 3
- Module folders exist under `server/src/modules/` (empty placeholders from Chapter 2)

## What "data model & seed" means in production terms

| Concern | This chapter | Why it matters |
|---|---|---|
| Identity vs profile | User + Profile schemas | Auth fields separate from display data |
| Ownership chain | User → Store → Product | Vendor isolation in Chapter 8 |
| Grocery attributes | unit, stockQty, isPerishable | Domain realism — not generic e-commerce |
| Order snapshots | Embedded orderItems | Price immutability at checkout (Ch 18) |
| Seed data | 2 vendors, 2 stores, products | Isolation tests need two owners |
| ERD | `docs/erd.md` final | Portfolio artifact + viva reference |

## Continuity from Chapter 4 — files you already have

```
server/src/
  app.js              ← health route with db status
  server.js           ← connect then listen
  shared/
    config.js         ← MONGODB_URI validation
    db.js             ← connectDB / disconnectDB
  modules/
    auth/             ← empty until Chapter 6
    stores/           ← model lands in 5.11
    products/         ← model lands in 5.12
    cart/             ← model lands in 5.13
    orders/           ← model lands in 5.13
    users/            ← models land in 5.10
```

No routes yet — models only. Auth API comes in Chapter 6.

## The Week 1 demo completes here

| Week 1 target | After Ch 4 | After Ch 5 |
|---|---|---|
| Both apps run | ✓ | ✓ |
| Client hits `/health` | ✓ | ✓ |
| MongoDB connected | ✓ | ✓ |
| Seed visible in Compass | | **this chapter** |
| ERD finalized | draft | **`docs/erd.md`** |

Phase demo language: *"Seed data visible in Compass"* — Chapter 5 completes Week 1 foundations.

## What this chapter is not

- **Not** auth routes or JWT — Chapter 6
- **Not** login screens — Chapter 7
- **Not** vendor product CRUD API — Chapter 11
- **Not** checkout logic — Chapter 18
- **Not** optional — empty schemas block every feature chapter

## Reading pace

| Sub-chapter | Cluster | Focus |
|---|---|---|
| 5.1–5.3 | Open | Schema stakes, denormalisation trap, deliverables |
| 5.4–5.6 | Learn | Ownership chain, grocery fields, Mongoose refs |
| 5.7 | Check | Quiz reserved |
| 5.8–5.16 | Build | Models → seed → Compass → ERD |
| 5.17–5.18 | Wrap | Recap + gate |

Estimated time: **1–2 days** — more files than Chapter 4, but each model is one focused step.

## Self-check before continuing

- [ ] Chapter 4 gate cleared — health shows `db: connected`
- [ ] `docs/erd-draft.md` open beside you
- [ ] Compass connected to same URI as `server/.env`
- [ ] Module folders exist under `server/src/modules/`


## Before you continue

<OpenQuestion id="01KZ5FW3NGEFGXPQHE42ST6P96" />

## Next

**Sub-chapter 5.2:** The denormalisation trap — why copying `vendorName` onto every product feels fast and fails at scale.

Your database is connected. Next you decide what shape your grocery data takes.
