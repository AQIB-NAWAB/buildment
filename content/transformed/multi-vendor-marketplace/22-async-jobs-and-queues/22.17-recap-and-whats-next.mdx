---
title: "Recap and what's next"
module: 22
lesson: 17
summary: "Chapter 22 moved **notifications and optional cache bust** off the HTTP hot path — BullMQ on the same Redis as Chapter 21, separate worker process, commerce responses stay fast."
---
Chapter 22 moved **notifications and optional cache bust** off the HTTP hot path — BullMQ on the same Redis as Chapter 21, separate worker process, commerce responses stay fast.

## What you built

**queue.js** — BullMQ Queue connected via `REDIS_URL`.

**worker.js** — standalone consumer; `npm run worker`.

**order.created** — enqueue after checkout; processor mocks vendor + customer notify.

**order.statusUpdated** — enqueue after vendor PATCH; processor mocks customer status email.

**Retries + jobId** — transient failures retry; duplicate enqueue suppressed.

**product.updated** (optional) — async catalogue invalidation tied to Ch 21.

## Architecture you can explain

```
API commits commerce state synchronously
        ↓
API enqueues side-effect jobs (milliseconds)
        ↓
API returns 201/200
        ↓
Worker processes jobs independently
```

Week 4 demo: confirmation page **before** worker logs — that is correct behaviour.

## What you did not build (by design)

- Real SendGrid/Mailgun integration
- Kafka or separate microservice
- Inventory reservation queues
- Bull Board UI (optional stretch)

Swap `console.log` for email provider in portfolio — same queue shape.

## Week 4 progress

| Target | Status |
|---|---|
| Cached catalogue (Ch 21) | ✓ |
| Async notifications | ✓ |
| Two-process dev setup | ✓ |
| Live HTTPS deploy | Chapter 23 |


## Before you continue

<OpenQuestion id="01KZ5P6CHXB8RJ647VT6W1XXAG" />

## Next — Chapter 23: Deploy

- Production env checklist — include `REDIS_URL` and **worker process**
- Build client with `VITE_API_URL`
- Deploy API + client on HTTPS
- Run worker as second PM2 process or platform worker service
- Smoke tests + restart survival

**Production note:** API alone is not enough — orders notify only if worker runs in prod. Document in Ch 23 learning log.

Localhost commerce + cache + async jobs is complete. Next ship it.

## Continue to gate

**Sub-chapter 22.18:** Checklist + learning log.
