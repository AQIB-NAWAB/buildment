---
title: "Set the scene"
module: 22
lesson: 1
summary: "**Where the project is now:** Week 4 polish underway — catalogue cached in Redis (Chapter 21), commerce flow complete in browser (Chapters 18–19). Checkout creates orders, decrements stock, clears cart. **No notifications** run after checkout or status changes."
---
**Where the project is now:** Week 4 polish underway — catalogue cached in Redis (Chapter 21), commerce flow complete in browser (Chapters 18–19). Checkout creates orders, decrements stock, clears cart. **No notifications** run after checkout or status changes.

**Where this chapter goes:** **Async job queue** with **BullMQ + Redis** — checkout and status PATCH return fast; **workers** send vendor and customer notifications in the background (mock email via `console.log` for gate).

Same Redis instance from Chapter 21 — different keys than cache. Separate **worker process** (`npm run worker`).

## Bridge from last chapter

Chapter 21 made browsing **fast**. Chapter 22 makes checkout **reliable under side effects** — the emails, SMS, and push notifications every real marketplace sends **after** an order exists.

Right now your checkout handler does the right **synchronous** work:

1. Validate cart  
2. Create orders + decrement stock  
3. Clear cart  
4. Return 201  

That is correct. The trap is step 5 that teams add later: *"Oh, let's also send confirmation emails before we return."* Chapter 22 teaches you to **stop at step 4** and hand step 5 to a **worker** that runs in a separate process.

| Chapter 21 solved | Chapter 22 solves |
|---|---|
| Repeated catalogue reads are slow | Checkout blocked by slow/unreliable notifications |
| Stale prices from cache | 500 errors when email provider times out **after** order already created |
| Search without index | Customer waits 2 seconds for SMTP while staring at a spinner |

**Why after checkout works, not during:** Order creation and stock must be **immediate and consistent** — the customer needs a 201 with order IDs **now**. Email can arrive three seconds later. If email fails, you **retry in the worker** without undoing a valid order.

## In plain English

| Term | Simple meaning |
|---|---|
| **Async (asynchronous)** | "Do this later" — the API responds first, background work follows |
| **Job queue** | A to-do list stored in Redis — jobs wait in line until a worker picks them up |
| **Enqueue** | Add a task to the to-do list — fast, milliseconds |
| **Worker** | A separate Node process that reads the to-do list and does the slow work |
| **BullMQ** | A library that manages the to-do list — retries, scheduling, job metadata |
| **Processor** | The function the worker runs for each job — e.g. "send mock vendor email" |
| **Side effect** | Something that happens because of an action but is not the main result — email after order is a side effect; the order itself is the main result |
| **Sync trap** | Putting slow side effects **inside** the checkout handler so the customer waits or the whole request fails |

## Think of it like a restaurant kitchen

**Synchronous (must happen before the waiter returns):** Take the order, charge the card, print the ticket, deduct ingredients from inventory. The customer cannot leave without knowing whether the order was accepted.

**Asynchronous (can happen after):** Send a text when the food is ready, email a receipt, notify the supplier. If the text fails, the meal still happened — you retry the text later.

Your checkout API is the **waiter**. The worker is the **back office** that sends texts. Chapter 22 wires the ticket to the back office **after** the waiter tells the customer "Order confirmed."

## The sync side-effect problem

```
POST /checkout
  → validate cart
  → create orders
  → decrement stock
  → clear cart
  → send email to customer        ← blocks response
  → send email to vendor A        ← blocks response
  → send email to vendor B        ← blocks response
  → return 201 (800ms later)
```

Customer waits for SMTP. One slow vendor notification fails the whole checkout. Chapter 22 moves notifications **off the request path**.

## Common beginner questions

<FaqGroup>

<FaqItem question="What is a queue? I thought Redis was for cache.">

Redis can hold many data types. Chapter 21 used it for **cached strings**. Chapter 22 uses it for **BullMQ job lists** — same Redis server, different key prefixes. Think of cache as a photograph; the queue is a **to-do inbox**.

</FaqItem>

<FaqItem question="Why a separate worker process? Why not just `setTimeout` in the API?">

`setTimeout` dies when you restart the API, cannot retry reliably, and shares memory with HTTP traffic. A worker process **survives independently**, can retry failed jobs, and scales separately in production (Ch 23).

</FaqItem>

<FaqItem question="What if the worker is not running?">

Checkout still returns 201 — orders exist. Jobs sit in Redis until you start the worker. That is **eventual** notification delivery — acceptable for email, unacceptable for stock decrement (which stays synchronous).

</FaqItem>

<FaqItem question="Do I need real SendGrid/Mailgun?">

No. `console.log` with a `[NOTIFY]` prefix satisfies the gate. Swap for a real provider in portfolio — same queue shape.

</FaqItem>

<FaqItem question="Should checkout wait until the worker finishes?">

**No.** Gate proof: 201 returns **before** worker logs appear. If you await the worker in the handler, you rebuilt the sync trap.

</FaqItem>

<FaqItem question="What jobs will I build?">

`order.created` after checkout (notify each vendor + customer), `order.statusUpdated` after vendor PATCH (notify customer of new status). Optional: `product.updated` for async cache bust (21.16).

</FaqItem>

<FaqItem question="Two terminals again?">

Yes. Terminal 1: `npm run dev` (API). Terminal 2: `npm run worker`. Same habit as client + server from Chapter 2 — now three processes if you count the React dev server.

</FaqItem>

</FaqGroup>


## What this chapter is not

- **Not** real SendGrid/Mailgun integration — mock in worker is enough for gate
- **Not** inventory reservation queues — out of scope
- **Not** Kafka or RabbitMQ — BullMQ on Redis only
- **Not** microservices split — API and worker share one repo, one MongoDB
- **Not** deploy — Chapter 23

## Jobs you will implement

| Event | Trigger | Worker action |
|---|---|---|
| `order.created` | `POST /checkout` success | Notify each vendor + customer (console mock) |
| `order.statusUpdated` | `PATCH /vendor/orders/:id` success | Notify customer of new status |
| `product.updated` (optional) | Product POST/PATCH/DELETE | Async catalogue cache invalidation (Ch 21 tie-in) |

## Reading pace

| Sub-chapter | Cluster | Focus |
|---|---|---|
| 21.1–21.3 | Open | Sync trap, blueprint |
| 21.4–21.6 | Learn | Sync vs async, BullMQ, docs read |
| 21.7 | Check | Quiz reserved |
| 21.8–21.16 | Build | queue → worker → jobs → verify |
| 21.17–21.18 | Wrap | Recap + gate |

Estimated time: **two days** — worker process wiring deserves care.

## Week 4 demo note

When you demo checkout live: **201 returns before notification logs appear** in the worker terminal. That proves async — mention it in the demo script.

## Self-check before continuing

- [ ] Chapter 21 gate cleared — `REDIS_URL` works for cache
- [ ] `POST /checkout` creates split orders (Chapter 18)
- [ ] `PATCH /vendor/orders/:orderId` updates status (Chapter 20)
- [ ] Redis running locally or via cloud URL


## Before you continue

<OpenQuestion id="01KZ63P5NEJGS8FQBW62G5YDQ3" />

## Next

**Sub-chapter 22.2:** The sync trap — why notify-in-handler breaks checkout.
