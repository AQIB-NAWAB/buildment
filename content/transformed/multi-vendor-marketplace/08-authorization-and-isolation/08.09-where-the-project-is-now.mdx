---
title: "Where the project is now"
module: 8
lesson: 9
summary: "**Where the project is now:** JWT auth from Ch 6–7; `requireAuth` exists; `docs/AUTHORIZATION.md` from 8.8; no `requireRole` or ownership helper; no store routes."
---
**Where the project is now:** JWT auth from Ch 6–7; `requireAuth` exists; `docs/AUTHORIZATION.md` from 8.8; no `requireRole` or ownership helper; no store routes.

**After this step:** Continuity recap — middleware implementation begins 8.10.

**Why this step matters:** Build chapters go wrong when you start coding against a mental picture that doesn't match disk. This sub-chapter is a **pause before middleware** — confirm auth works, confirm the matrix is committed, confirm you have two vendor tokens. If any box below is unchecked, fix it before 8.10. Adding `requireRole` on top of a broken `requireAuth` wastes an afternoon.

## Server auth state

```
modules/auth/
  auth.middleware.js    ← requireAuth only
modules/stores/
  store.model.js        ← exists from Ch 5
  stores.routes.js      ← missing or empty — 8.12
shared/
  ownership.js          ← missing — 8.11
```

## Test tokens ready

Login both seed vendors — save tokens and store ids from Compass:

| Account | Purpose |
|---|---|
| vendor-a@test.local | Legitimate owner |
| vendor-b@test.local | IDOR attacker |

Document store _ids in local test notes — not committed.

## Baseline curl

```bash
# Vendor A token
curl -s http://localhost:5000/api/auth/me -H "Authorization: Bearer $TOKEN_A"
```

Both vendors return role vendor.

## Client state (unchanged this chapter)

```
client/src/
  hooks/useAuth.js          ← login, token in memory
  pages/Login.jsx           ← working
  api/client.js             ← Bearer header attached
```

No React changes in Chapter 8 — authorization is **server-side only**. The vendor stub page from Chapter 7 may exist, but the real proof is curl returning 403.

## Common mistakes at this point

| Mistake | Symptom | Fix |
|---|---|---|
| Matrix not written | Each route invents its own rules | Finish 8.8 first |
| One vendor token only | IDOR test passes trivially | Login vendor-a **and** vendor-b |
| Store ids unknown | curl PATCH uses wrong id | Open Compass, copy `_id` for each vendor's store |
| Role field mismatch | 403 never fires | JWT payload `role` must match `requireRole` check |

## Build sequence

| Step | Deliverable |
|---|---|
| 8.10 | requireRole |
| 8.11 | assertStoreOwner |
| 8.12 | PATCH /stores/:id stub |
| 8.13 | IDOR curl test |

## ✅ Do / ❌ Don't

**✅ Do:** Re-read AUTHORIZATION.md before coding.

**❌ Don't:** Add product routes yet — Ch 11.

## Verify checklist

- [ ] AUTHORIZATION.md committed
- [ ] Two vendor tokens obtainable
- [ ] Store ids known for both vendors


## Before you continue

<OpenQuestion id="01KZAX76P66Q2NTX5593JMD0J4" />

## Next

**Sub-chapter 8.10:** requireRole middleware.

Recap done — RBAC middleware next.
