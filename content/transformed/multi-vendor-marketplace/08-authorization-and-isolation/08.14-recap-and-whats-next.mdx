---
title: "Recap and what's next"
module: 8
lesson: 14
summary: "Chapter 8 closes the security gap between **authenticated** and **authorized**. Vendor B cannot mutate Vendor A's store — proven with curl and documented in `docs/AUTHORIZATION.md`."
---
Chapter 8 closes the security gap between **authenticated** and **authorized**. Vendor B cannot mutate Vendor A's store — proven with curl and documented in `docs/AUTHORIZATION.md`.

<BigWordAlert term="Isolated tenant data" plainEnglish="Each vendor's stores and products are accessible only to that vendor — multi-tenant isolation." whyItMatters="Chapter 9 builds store API routes assuming ownership guards are already in place." />

## What you built

Five pieces, and each one earns its place because the *next* chapter reuses it unchanged. If you can hold one sentence per item in your head, you can answer any viva question about this chapter.

**docs/AUTHORIZATION.md:** Matrix of action × role × check type; IDOR checklist; 401 vs 403 rules.

**requireRole:** Function-level RBAC — vendor vs customer routes.

**assertStoreOwner:** Object-level ownership via Store.userId.

**PATCH /api/stores/:id:** Full middleware stack + minimal update — pattern for all vendor mutations.

**IDOR test:** Vendor B → 403 on Vendor A store; data unchanged.

## Authorization stack (reference)

```
Request
  → requireAuth (401)
  → requireRole('vendor') (403 wrong role)
  → assertStoreOwner (403 wrong owner)
  → handler (400 validation / 200 success)
```

Reuse this diagram in viva.

## Repo additions

```
docs/AUTHORIZATION.md
server/src/shared/ownership.js
server/src/modules/stores/stores.routes.js
server/src/modules/stores/stores.service.js   ← minimal
auth.middleware.js                            ← + requireRole
```

<InterestingRead title="IDOR: the bug where changing a number steals data" hook="If `/api/products/42` works, will `/api/products/43`?" readMinutes={5}>

Insecure Direct Object Reference bugs happen when the server checks login but not ownership. FreshMarket's guards compare the authenticated user to the document owner on every mutating route — not just on the dashboard UI.

</InterestingRead>

## What's next — Chapter 9 Vendor store API

With authorization in place, the store feature can ship without redesigning security later. Chapter 9 builds the full store lifecycle on top of the exact middleware you just wrote.

Full store lifecycle:

- POST /api/stores — create shop, one per vendor  
- GET /api/stores/me — vendor's store  
- PATCH /api/stores/:id — extend stub from Ch 8  
- curl vendor lifecycle  

Ownership helper and requireRole **reuse unchanged**.

## Chapter 10 preview

Vendor onboarding UI — storeless vendor forced to open shop before dashboard.

## Week 2 progress

| Milestone | Status |
|---|---|
| Auth API | Ch 6 ✓ |
| Login UI | Ch 7 ✓ |
| Isolation | Ch 8 ✓ |
| Store API | Ch 9 |
| Vendor UI | Ch 10 |

## Continue to gate

**Sub-chapter 8.15:** Checklist + learning log.

**Do not open Chapter 9 until 8.15 ticked.**

Logged in and allowed are different — store features can ship safely now.
<RealWorldEvent title="Facebook Graph API permission scandals" when="2010s" summary="Platforms exposed user data when apps could request broader permissions than users understood — driving industry focus on least-privilege access." lesson="Centralize authorization in middleware and guards. Ad-hoc checks in individual routes drift and get missed during code review." />
## Before you continue

<OpenQuestion id="01M34MR9N7MN8W93RAX9BME8Y6" />
