---
title: "Authorization matrix"
module: 8
lesson: 8
summary: "Writing middleware before you know the rules produces inconsistent guards — one route checks role, another forgets ownership. The matrix is your **spec**: every action, every role, every check type, committed before code. Chapters 9–18 will add rows; Chapter 8 sets the pattern."
---
**Why this step matters:** Writing middleware before you know the rules produces inconsistent guards — one route checks role, another forgets ownership. The matrix is your **spec**: every action, every role, every check type, committed before code. Chapters 9–18 will add rows; Chapter 8 sets the pattern.

<BigWordAlert term="Authorization matrix" plainEnglish="A table mapping roles × resources × allowed actions — design doc before coding guards." whyItMatters="FreshMarket's matrix drives which routes get `requireRole` vs ownership checks." />

**Where the project is now:** Auth works; no `docs/AUTHORIZATION.md`; no role/ownership middleware.

**After this step:** **`docs/AUTHORIZATION.md`** committed — action × role × check type matrix driving all later chapter guards.

## Deliverable file

```
docs/AUTHORIZATION.md
```

Write **before** implementing middleware (8.10–8.11) — spec first.

## Required sections

### 1. Purpose

One paragraph: authn (JWT) vs authz (role + ownership). Link to ownership chain in erd.md.

### 2. Roles

| Role | Capabilities summary |
|---|---|
| vendor | Own store, products, vendor orders |
| customer | Cart, checkout, own orders |
| anonymous | Public catalogue only (Ch 14) |

### 3. Master matrix table

Minimum rows — extend as course progresses:

| Action | Route (current/planned) | Authn | Role | Ownership / scope | Deny status |
|---|---|---|---|---|---|
| Health | GET /health | none | — | — | — |
| Register | POST /api/auth/register | none | — | — | — |
| Me | GET /api/auth/me | JWT | any | self | 401 |
| Patch store | PATCH /api/stores/:id | JWT | vendor | store.userId = req.user.id | 403 |
| Create store | POST /api/stores | JWT | vendor | one store per user (Ch 9) | 403 |
| Create product | POST /api/products | JWT | vendor | product.storeId = own store | 403 |
| Patch product | PATCH /api/products/:id | JWT | vendor | via store chain | 403 |
| Public catalogue | GET /api/products | optional | — | published only | — |
| Get cart | GET /api/cart | JWT | customer | cart.userId | 403 |
| Checkout | POST /api/checkout | JWT | customer | own cart | 403 |
| Vendor orders | GET /api/vendor/orders | JWT | vendor | order.storeId → own store | 403 |
| Customer orders | GET /api/orders/my | JWT | customer | order.customerId | 403 |

Mark rows "planned" for future chapters — honest status column OK.

### 4. Check types glossary

| Type | Meaning |
|---|---|
| none | Public |
| JWT | requireAuth |
| role | requireRole(...) |
| ownership | DB lookup chain |
| scope | query filter &#123; userId: req.user.id &#125; |

### 5. IDOR test checklist

```markdown
- [ ] Vendor B PATCH Vendor A store → 403
- [ ] Vendor B PATCH Vendor A product → 403 (Ch 11)
- [ ] Customer A GET Customer B cart → 403 (Ch 16)
```

### 6. HTTP status rules

Copy 401 vs 403 rules from 8.4.

## Cross-check with erd.md

Field names in matrix must match schemas — userId, storeId, customerId.

## Commit

```bash
git add docs/AUTHORIZATION.md
git commit -m "docs: add authorization matrix for roles and ownership"
```

## ✅ Do / ❌ Don't

**✅ Do:** Include planned routes — matrix is living doc updated in Ch 9–18.

**✅ Do:** Reference matrix in code review for new routes.

**❌ Don't:** Implement PATCH store before matrix row exists.

## Verify checklist

- [ ] docs/AUTHORIZATION.md committed
- [ ] Matrix covers store PATCH stub
- [ ] 401 vs 403 documented
- [ ] IDOR test list present
- [ ] Roles match JWT payload enum

<RealWorldEvent title="Facebook Graph API permission scandals" when="2010s" summary="Platforms exposed user data when apps could request broader permissions than users understood — driving industry focus on least-privilege access." lesson="Centralize authorization in middleware and guards. Ad-hoc checks in individual routes drift and get missed during code review." />


## Before you continue

<OpenQuestion id="01M34MR9JVF5Z4Y2Z5ZTF1AJQD" />

## Next

**Sub-chapter 8.9:** Where the project is now — continuity before middleware.

Matrix written — implement guards next.
