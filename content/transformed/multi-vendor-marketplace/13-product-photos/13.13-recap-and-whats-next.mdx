---
title: "Recap and what's next"
module: 13
lesson: 13
summary: "You entered Chapter 13 with text-only products. You leave with **Cloudinary-backed photos**, **URL-only MongoDB documents**, and **vendor dashboard thumbnails** — the last piece of the Week 2 vendor experience."
---
You entered Chapter 13 with text-only products. You leave with **Cloudinary-backed photos**, **URL-only MongoDB documents**, and **vendor dashboard thumbnails** — the last piece of the Week 2 vendor experience.

## What you built — narrative recap

**Object storage discipline:** Images in Cloudinary; MongoDB stores `imageUrl` (+ `imagePublicId` for lifecycle) — no base64 trap.

**Server config:** Cloudinary credentials in `server/.env`; validated at boot; never in client.

**Upload pipeline:** multer parses multipart → ownership check → Cloudinary upload → product update.

**Replace flow:** New upload removes previous Cloudinary asset — storage does not grow unbounded on edits.

**Vendor UI:** File input on edit (or chosen flow); thumbnails in product list.

## Your repo tree now

```
server/src/
  shared/
    config.js           ← + Cloudinary vars
    cloudinary.js       ← upload/delete helpers
  modules/products/
    products.routes.js  ← + POST :id/image
    products.service.js ← image replace logic
    upload.middleware.js

client/src/
  components/ImageUpload.jsx
  pages/vendor/
    ProductList.jsx     ← thumbnails
    EditProduct.jsx     ← upload
```

Compare to **13.7** — new shared helper, multer middleware, one route, client component.

## Week 2 demo — complete

| Week 2 target | Status |
|---|---|
| Vendor registers | ✓ (Ch 6–7) |
| Opens a store | ✓ (Ch 9–10) |
| Adds a product | ✓ (Ch 11–12) |
| Product has photo | ✓ **Ch 13** |
| Sees it in dashboard | ✓ **Ch 13** |

**Demo script for mentor:**

*"I'll log in as a vendor, show my store, create a product, upload a photo, and show the thumbnail in my product table. The image is served from Cloudinary; MongoDB only has the URL."*

Record or rehearse before starting Week 3 buyer chapters.

## Decisions you can defend in a viva

1. **Why Cloudinary instead of MongoDB for bytes?** — Document size, query speed, CDN.  
2. **Why server upload vs client direct?** — Secret protection; ownership enforcement.  
3. **Why separate POST for image vs PATCH JSON?** — multer + JSON don't mix cleanly.  
4. **What is `imagePublicId` for?** — Delete/replace without parsing URLs.  
5. **What if Cloudinary is down?** — Upload fails 502; existing URLs still serve from CDN until expiry.

## What's explicitly not built yet

| Not yet | Coming in |
|---|---|
| Public product catalogue API | Chapter 14 |
| Customer shop UI | Chapter 15 |
| Cart | Chapter 16–17 |
| Multiple photos per product | Out of scope |
| Image transforms in URLs | Optional stretch |


## Before you continue

<OpenQuestion id="01KZ5P6AYH02MCQBHXWHERFFS4" />

## Next chapter — Browse catalogue API (Chapter 14)

Week 3 begins on the **buyer side** — but backend first:

- Public **`GET /products`** — published, in-stock  
- Filters: `category`, `storeId`  
- Pagination: `page`, `limit`  
- **Populate** store name — fix N+1 before it ships  

Customer shop UI (Chapter 15) consumes this API — do not skip N+1 verification in 14.12.

## Callback to authorization (Chapter 8)

Upload route uses same ownership chain as PATCH product. Public catalogue in Chapter 14 is **unauthenticated read** — different threat model; still no vendor-only fields leaked.

## Suggested commit history through Chapter 13

```
feat(server): add cloudinary config and upload helper
feat(products): add multipart image upload route
feat(products): remove previous cloudinary asset on image replace
feat(client): vendor product image upload and list thumbnails
docs: document product image upload verification steps
docs: complete chapter 13 learning log
```

## Key milestones unlocked

- [x] Vendor product CRUD in UI (Ch 12)  
- [x] Images in object storage (Ch 13)  
- [x] Week 2 vendor demo complete (Ch 13)  
- [ ] Public catalogue API (Ch 14)  
- [ ] Customer browse UI (Ch 15)

## Continue in this order — do not skip

1. **Sub-chapter 13.14 — Bridge to Week 3 (mandatory)**  
   Week 2 ends here; Week 3 is a **role change** (seller → shopper). Read 13.14 even if the demo feels done — it explains why Chapter 14 builds a public API before Chapter 15 builds customer UI, and what to verify before leaving vendor work behind.

2. **Sub-chapter 13.15 — Checklist and learning log (gate)**  
   Tick every box honestly. Learning log entry for Chapter 13. **Do not open Chapter 14 until 13.15 is fully complete.**

If the jump to customer experience feels abrupt, that is exactly why 13.14 exists — go there next, not to Chapter 14.

Your vendor shop has a window display. Next: read the bridge (13.14), pass the gate (13.15), then customers get to browse the market (Chapter 14).
