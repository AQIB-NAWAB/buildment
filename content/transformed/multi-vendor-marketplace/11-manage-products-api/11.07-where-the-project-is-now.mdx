---
title: "Where the project is now"
module: 11
lesson: 7
summary: "Chapter 11 adds four HTTP verbs on a new resource. This snapshot confirms store routes work and the Product model has `storeId` — every product handler inherits that foundation. Five minutes here prevents an hour of opaque 500 errors on `POST /products`."
---
**Why this step matters:** Chapter 11 adds four HTTP verbs on a new resource. This snapshot confirms store routes work and the Product model has `storeId` — every product handler inherits that foundation. Five minutes here prevents an hour of opaque 500 errors on `POST /products`.

**Build cluster starts.** Confirm Chapters 9–10 gates. Snapshot before product routes.

## Why this snapshot matters

Chapter 11 adds the busiest vendor API in Week 2 — four HTTP verbs on a new resource. If store routes are shaky or the Product model is missing `storeId`, every product handler inherits the bug. This sub-chapter is a **pre-flight check**: same pattern as 9.7 and 10.5. Five minutes here prevents an hour of "why does POST /products return 500?"

## Server tree

```
modules/
  auth/          ← working
  stores/        ← POST, GET me, PATCH
  products/
    products.model.js   ← Ch 5 only
shared/
  ownership.js   ← store owner assert
```

## Product model sanity check

- [ ] `storeId` ref Store — required  
- [ ] `name`, `price`, `unit`, `stockQty`, grocery fields per ERD  
- [ ] `isPublished` or `status` for draft/publish — Ch 12 UI  
- [ ] Timestamps  

## Test vendors

| Vendor | Store | Use |
|---|---|---|
| A | yes | Create/list/PATCH/DELETE |
| B | yes | Cross-vendor 403 |
| Optional C | no store | POST product should fail |

## Client

Vendor UI has shop — **no product pages** required yet. Chapter 11 is API-only like Chapter 9.

```
client/src/pages/vendor/
  OpenShop.jsx          ← Ch 10
  StoreSettings.jsx     ← Ch 10
  (no ProductList yet)  ← Ch 12
```

## Authorization matrix — rows to add

Open `docs/AUTHORIZATION.md` and confirm you will add rows for:

| Action | Role | Check |
|---|---|---|
| Create product | vendor | resolve store from JWT; reject if no store |
| List my products | vendor | filter by vendor's storeId |
| Update product | vendor | assertProductOwner (or store chain) |
| Delete product | vendor | same ownership assert |

Write the rows in the matrix **before** implementing handlers — same discipline as Chapter 8.

## Verify before 11.8

- [ ] GET /stores/me → 200 for test vendor A  
- [ ] Product collection empty or seed products noted  
- [ ] AUTHORIZATION.md open for updates  

<RealWorldEvent title="DoorDash menu sync failures" when="2020s" summary="Delivery platforms struggled when restaurant menus changed faster than sync pipelines, showing unavailable items to hungry customers." lesson="Product APIs should reflect live stock and publish state — stale catalogue data directly loses sales." />


## Before you continue

<OpenQuestion id="01KZP7KPYDKAB1WA9Q3TKGF54N" />

## Next

**Sub-chapter 11.8:** Scaffold products module.
