---
title: "Set the scene"
module: 11
lesson: 1
summary: "**Where the project is now:** Vendors have shops — API and UI from Chapters 9–10. A store document exists with `vendorId`. The Product model from Chapter 5 waits in `modules/products/` with grocery fields, but there is no **vendor product API** — no way to stock the shelves progr"
---
**Where the project is now:** Vendors have shops — API and UI from Chapters 9–10. A store document exists with `vendorId`. The Product model from Chapter 5 waits in `modules/products/` with grocery fields, but there is no **vendor product API** — no way to stock the shelves programmatically.

**Where this chapter goes:** Vendors **CRUD their own products** through protected routes — create, list mine, update, delete — with **store ownership** enforced and **cross-vendor PATCH → 403**.

This is a **build chapter**. Sub-chapters 11.7 onward add routes and service logic. The gate is curl-proof vendor product lifecycle plus isolation test.

## Bridge from last chapter

Chapters 9–10 gave vendors a **shop** — API and browser forms, with an onboarding gate blocking dashboard routes until a store exists. The Product model from Chapter 5 sits in MongoDB, but there is no way to **stock the shelves** yet. Chapter 11 adds vendor product CRUD on the server, reusing Chapter 8's ownership pattern: Vendor B must get **403** on Vendor A's kale. Same isolation discipline, new resource type.

## In plain English

| Term | Simple meaning |
|---|---|
| **Vendor inventory** | Products a seller manages — drafts, prices, stock |
| **Public catalogue** | Products shoppers browse — built in Chapter 14, not here |
| **`POST /products`** | Create a product linked to the vendor's store |
| **`GET /vendor/products`** | List only **my** products — not everyone's |
| **`storeId`** | Field tying each product to one store |
| **Ownership chain** | User → Store → Product — the path your guards follow |
| **`isPublished`** | Draft vs live flag — customers won't see drafts in Week 3 |

## Think of it like stocking a stall

The vendor signed the lease (Ch 10) and has an empty stall with a nameplate. Chapter 11 is **filling the shelves via the stockroom door** — API only, no shop window yet. Each crate (product) is tagged with which stall it belongs to. Another vendor cannot relabel your crates because the warehouse checks the ownership chain on every move.

## Common beginner questions

<FaqGroup>

<FaqItem question="Why not let customers see products in this chapter?">

Two different audiences, two different routes. Vendor inventory is authenticated and scoped. Public catalogue is unauthenticated read — Chapter 14. Mixing them early leaks drafts.

</FaqItem>

<FaqItem question="Can the client send `storeId` in the POST body?">

**No.** Resolve the store from the JWT. Client-supplied `storeId` is an IDOR vector — Vendor B could pass Vendor A's store id.

</FaqItem>

<FaqItem question="What if a vendor has no store and tries to create a product?">

Reject with 404 or 403 — pick one, document it in `AUTHORIZATION.md`. No store means no shelves.

</FaqItem>

<FaqItem question="Is delete a hard delete?">

Yes in this course. Soft delete is a valid production choice — note the tradeoff in your learning log.

</FaqItem>

<FaqItem question="Do I need the dashboard UI to test this chapter?">

No. curl is enough — same as Chapter 9. Chapter 12 builds the screens.

</FaqItem>

</FaqGroup>


## The gap between "empty shop" and "stocked shop"

| Works today | Missing |
|---|---|
| Store exists | Products linked to store |
| Vendor gate in UI | Product routes on API |
| Product schema in MongoDB | HTTP CRUD for vendor inventory |
| Authorization patterns from Ch 8 | Applied to product `:id` |

Customers cannot browse yet — Chapter 14.public catalogue is separate. This chapter is **vendor inventory only**.

## Why after store chapters

Ownership chain:

```
User (vendor) → Store → Product
```

`POST /products` must resolve **vendor's store** from JWT — reject if no store (404 or 403 — document choice). Cannot create product without shop from Ch 10.

## Two views preview (11.2)

| View | Audience | Chapter |
|---|---|---|
| Vendor inventory | Authenticated vendor | **11** |
| Public catalogue | Everyone | **14** |

Same Product collection — different routes and filters. Do not expose draft products publicly in Ch 14.

## Continuity

```
server/src/modules/products/
  products.model.js     ← Ch 5
  products.routes.js    ← NEW
  products.service.js   ← NEW
  products.schema.js    ← NEW
```

```
shared/ownership.js     ← extend: assertProductOwner or store chain
```

## Week 2 progress

| Target | After Ch 10 | After Ch 11 |
|---|---|---|
| Shop open | ✓ | ✓ |
| Product API | | **this chapter** |
| Product UI | | Chapter 12 |

## What this chapter is not

- **Not** vendor dashboard UI — Chapter 12  
- **Not** `GET /products` public — Chapter 14  
- **Not** image upload — Chapter 13  
- **Not** cart or checkout — Week 3  

## Reading pace

| Cluster | Sub-chapters |
|---|---|
| Open | 11.1–11.3 |
| Learn | 11.4–11.5 |
| Check | 11.6 quiz |
| Build | 11.7–11.13 |
| Wrap | 11.14–11.15 |

Estimated time: **1–1.5 days** — similar scope to Chapter 9 plus delete verb.

## Self-check

- [ ] Ch 10 gate cleared — vendor has store in UI  
- [ ] Product model reviewed — `storeId`, grocery fields, `isPublished` or status  
- [ ] Two vendor accounts with stores for 403 test  


## Before you continue

<OpenQuestion id="01KZ63P5AV3WX9Y792N1D214RA" />

## Next

**Sub-chapter 11.2:** Two views of products — inventory vs catalogue.
