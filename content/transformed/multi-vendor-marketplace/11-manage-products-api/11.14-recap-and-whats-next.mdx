---
title: "Recap and what's next"
module: 11
lesson: 14
summary: "Chapter 11 completes **vendor product CRUD on the API** — create on own store, scoped list, update, delete, with cross-vendor mutation blocked at 403. The shop has shelves; the browser dashboard comes next."
---
Chapter 11 completes **vendor product CRUD on the API** — create on own store, scoped list, update, delete, with cross-vendor mutation blocked at 403. The shop has shelves; the browser dashboard comes next.

<BigWordAlert term="Catalogue backend" plainEnglish="Vendors can manage products via API; public browse routes come in Chapter 14." whyItMatters="Chapter 12–13 add dashboard UI and photo uploads on top of these routes." />

## Narrative recap

Each line below is a *decision* you made, not just a file you created — if you can explain the reasoning behind any of them, the chapter has done its job.

**Products module:** routes, service, schema wired to existing model.

**POST /products:** storeId from vendor's store; grocery fields validated.

**GET /vendor/products:** inventory scoped to JWT — drafts included.

**PATCH /products/:id:** partial update including isPublished.

**DELETE /products/:id:** hard delete with ownership.

**Isolation:** Vendor B cannot PATCH/DELETE Vendor A's product.

## Repo tree

```
server/src/modules/products/
  products.model.js
  products.routes.js
  products.service.js
  products.schema.js
```

Client unchanged — Chapter 12.

## Viva prep

Four questions, four one-breath answers. If you can answer these without opening your editor, you understand the chapter's core idea: the API serves two different audiences with one model and two entry points.

1. **Two views of products?** — Vendor inventory (Ch 11) vs public catalogue (Ch 14).

2. **Why GET /vendor/products?** — Token-scoped; no client storeId.

3. **Ownership chain?** — product.storeId → store.vendorId === userId.

4. **Default isPublished?** — false — drafts until vendor publishes in Ch 12.

## Week 2 progress

| Target | Status |
|---|---|
| Shop open | ✓ |
| Product API | ✓ **Ch 11** |
| Product UI | Chapter 12 |
| Photos | Chapter 13 |

<InterestingRead title="Grocery SKUs vs marketplace listings" hook="The same barcode can mean different things to different shops." readMinutes={4}>

Central warehouses use global SKUs; local vendors may sell loose produce by weight with no barcode at all. FreshMarket models unit, weight, and stock per store so APIs stay honest about what is actually for sale.

</InterestingRead>


## Before you continue

<OpenQuestion id="01KZP7KPZPBNWZDYW4FWKMCJBQ" />

## Next — Vendor dashboard (Chapter 12)

The API now has shelves with products on them; Chapter 12 builds the storefront window the vendor looks through. Every UI screen is a thin layer over an endpoint you just proved with curl — the same "verify with curl before blaming the browser" rhythm from Chapter 7.

- Product table from GET /vendor/products  
- Create form — grocery fields  
- Edit form — PATCH  
- Publish toggle — PATCH isPublished  
- Full flow: draft → edit → publish in browser  

Pair chapter — API done; UI consumes it.

## Not built

Three things deferred on purpose so Chapter 11 stays a pure-API chapter — each one gets its own focused chapter later.

- Public GET /products — Ch 14  
- Image upload — Ch 13  
- Cart — Week 3  

## Continue to gate

**Sub-chapter 11.15:** Checklist + learning log.

**Do not open Chapter 12 until 11.15 ticked.**
<RealWorldEvent title="DoorDash menu sync failures" when="2020s" summary="Delivery platforms struggled when restaurant menus changed faster than sync pipelines, showing unavailable items to hungry customers." lesson="Product APIs should reflect live stock and publish state — stale catalogue data directly loses sales." />
