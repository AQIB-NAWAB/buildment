---
title: "Scaffold products module"
module: 11
lesson: 8
summary: "Same scaffold pattern as stores and auth: routes, service, schema files mounted before handlers. Product CRUD spans four sub-chapters — structure first keeps each step focused on one verb."
---
## Why this step matters

Same scaffold pattern as stores and auth: routes, service, schema files mounted before handlers. Product CRUD spans four sub-chapters — structure first keeps each step focused on one verb.

**After this step:** `products.routes.js`, `products.service.js`, `products.schema.js` exist; router mounted; handlers stubbed for 11.9–11.12.

## Module layout

```
server/src/modules/products/
  products.model.js
  products.routes.js    ← NEW
  products.service.js   ← NEW
  products.schema.js    ← NEW
```

## Route registration order (spec)

| Order | Method | Path |
|---|---|---|
| 1 | GET | `/vendor/products` |
| 2 | POST | `/` |
| 3 | PATCH | `/:id` |
| 4 | DELETE | `/:id` |

`/vendor/products` on router mounted at `/api/products` **or** separate mount at `/api/vendor/products` — pick one:

| Mount | GET list path |
|---|---|
| `app.use('/api/products', router)` | `GET /api/products/vendor/products` — awkward |
| `app.use('/api', router)` with paths `/products`, `/vendor/products` | cleaner |

**Recommended:** Router defines `GET /vendor/products` and mount at `/api` OR split list to `vendorProducts.routes.js` mounted at `/api/vendor`.

Simplest course path: mount products router at `/api` with routes:

- `POST /products`  
- `GET /vendor/products`  
- `PATCH /products/:id`  
- `DELETE /products/:id`  

Document exact paths in README.

## Service exports (spec)

| Function | Purpose |
|---|---|
| `createProduct(vendorId, data)` | Resolve store; insert |
| `listVendorProducts(vendorId)` | By storeId |
| `updateProduct(productId, vendorId, data)` | Ownership + patch |
| `deleteProduct(productId, vendorId)` | Ownership + remove |

## Schema exports

| Validator | For |
|---|---|
| `validateCreateProduct` | POST body |
| `validateUpdateProduct` | PATCH body |

Include grocery fields validation rules — positive price, non-negative stock, enum unit if defined.

## Ownership helper

Add to `shared/ownership.js`:

- `assertProductOwner(productId, vendorId)`  

Or inline store-chain check in service — prefer shared helper.

## Commit

```bash
git add server/src/modules/products/ server/src/shared/ownership.js server/src/app.js
git commit -m "feat(products): scaffold products module routes service validation"
```

## ✅ Do / ❌ Don't

**✅ Do:** Register `/vendor/products` before `/:id` routes.

**✅ Do:** Keep POST/PATCH validation symmetric with model.

**❌ Don't:** Add public GET in scaffold step.

## Verify

- [ ] Server starts  
- [ ] POST with vendor token not 404 route missing  


## Before you continue

<OpenQuestion id="01KZ63P5BAS8S5KNY0S4PK9BGC" />

## Next

**Sub-chapter 11.9:** Create product route.
