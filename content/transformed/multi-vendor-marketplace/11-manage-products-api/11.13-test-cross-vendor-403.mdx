---
title: "Test cross-vendor 403"
module: 11
lesson: 13
summary: "Chapter 8 proved store isolation; this step proves **product** isolation — Vendor B cannot PATCH Vendor A's kale. Same curl template, new resource. The Chapter 11 gate depends on this negative test, not just happy-path create."
---
**Why this step matters:** Chapter 8 proved store isolation; this step proves **product** isolation — Vendor B cannot PATCH Vendor A's kale. Same curl template, new resource. The Chapter 11 gate depends on this negative test, not just happy-path create.

**Where the project is now:** Product CRUD works for the owning vendor (11.8–11.12); nothing yet proves Vendor B cannot touch Vendor A's products.

**After this step:** Full **vendor product lifecycle** and **isolation suite** recorded for learning log — the Chapter 11 proof.

## Prerequisites

- Vendor A and B — each with store and token  
- At least one product owned by A (`PRODUCT_ID_A`)  

## Test matrix (execute all)

### Lifecycle — Vendor A

| Step | Action | Expect |
|---|---|---|
| 1 | POST product as A | 201 |
| 2 | GET /vendor/products as A | includes product |
| 3 | PATCH product as A | 200 |
| 4 | GET list — verify PATCH | updated fields |

### Isolation — Vendor B on A's product

| Step | Action | Expect |
|---|---|---|
| 5 | PATCH PRODUCT_ID_A as B | **403** |
| 6 | DELETE PRODUCT_ID_A as B | **403** |

### Cleanup — Vendor A

| Step | Action | Expect |
|---|---|---|
| 7 | DELETE PRODUCT_ID_A as A | 204 |
| 8 | GET list as A | product gone |

### Auth boundaries

| Step | Action | Expect |
|---|---|---|
| 9 | POST product no token | 401 |
| 10 | POST product as customer | 403 |
| 11 | GET /vendor/products as customer | 403 |

### No-store vendor (optional)

| Step | Action | Expect |
|---|---|---|
| 12 | POST product vendor without store | 404 |

## Critical gate rows

Steps **5 and 6** are non-negotiable — same IDOR class as Chapter 8 stores.

If 200 on step 5 — **stop** — fix ownership before 11.15.

## Compass cross-check

After step 1:

- `storeId` = Vendor A store `_id`  
- No product with A's name under B's storeId  

## AUTHORIZATION.md

Final update — all product actions with check types.

## README

Document four endpoints + 403 isolation note.

## Commit

```bash
git add docs/AUTHORIZATION.md README.md
git commit -m "docs: document vendor product API and cross-vendor tests"
```

## ✅ Do / ❌ Don't

**✅ Do:** Save PRODUCT_ID_A from create response — don't guess ids.

**✅ Do:** Re-run after any ownership refactor.

**❌ Don't:** Commit tokens in test notes.

## Verify checklist

- [ ] Steps 5–6 return 403  
- [ ] Full lifecycle 1–8 passes  
- [ ] Results in learning log notes  


## Before you continue

<OpenQuestion id="01KZAX7767RF6EDEVS9CP7CCFF" />

## Next

**Sub-chapter 11.14:** Recap — API ready for Chapter 12 dashboard.
