---
title: "Cache catalogue middleware"
module: 21
lesson: 9
summary: "**Why this step matters:** This is the core cache-aside pattern — the shop window stops hitting MongoDB on every repeat glance. HIT/MISS logs here are your first proof the performance layer works."
---
**Why this step matters:** This is the core cache-aside pattern — the shop window stops hitting MongoDB on every repeat glance. HIT/MISS logs here are your first proof the performance layer works.

<BigWordAlert term="Cache middleware" plainEnglish="Express middleware checking Redis before running catalogue handler — return cached JSON if hit." whyItMatters="FreshMarket `/products` gets cache-aside middleware — transparent to client." />

**Where the project is now:** `redis.js` connects from `REDIS_URL` (21.8) — but catalogue routes still read MongoDB on every request.

**After this step:** `GET /products` checks Redis before MongoDB; stores JSON response on miss.

## Cache-aside flow

```
GET /products?...
  1. Build cache key from path + query
  2. GET key from Redis
  3. HIT → return cached JSON (same status 200)
  4. MISS → run existing catalogue query
  5. SET key with TTL (seconds)
  6. Return response
```

Apply only to **GET** public products list — not single product route unless you extend (optional).

## Key generation contract

Deterministic from sorted query params:

```
catalogue:page=1&limit=20&category=Dairy
```

Include `search` param when present — different keys for different searches.

## Response body

Cache **final JSON string** sent to client — includes populated store names from Ch 14.

Set `Content-Type: application/json` as today.

## Dev observability

Log `CACHE HIT` / `CACHE MISS` with key — remove or reduce in production.

## Middleware placement

Either:

- Express middleware before products router for `GET /products` only, or
- Inside `products.service.listPublic()` — service-layer cache

Pick one — document in README.

## ✅ Do / ❌ Don't

**✅ Do:** TTL 300–900s — state value in README.

**✅ Do:** Only cache 200 responses.

**❌ Don't:** Cache vendor-only routes.

**❌ Don't:** Cache authenticated personalised responses on public route.

## Verify

- [ ] First GET → MISS log
- [ ] Second identical GET → HIT log
- [ ] Response body identical

<RealWorldEvent title="Facebook Memcached outage lessons" when="2010" summary="Major sites learned that caching layers need fallbacks — when cache fails, databases must survive the thundering herd." lesson="Always code a cache miss path. Redis is an accelerator, not the system of record." />


## Before you continue

<OpenQuestion id="01KZP7KQV091RF1FF3SFZJH82T" />

## Next

**Sub-chapter 21.10:** Measure before/after — the middleware runs; the last step is proving it made things faster, not just different.
