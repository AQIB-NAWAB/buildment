---
title: "Recap and what's next"
module: 21
lesson: 15
summary: "Chapter 21 added **performance and discoverability** — Redis cache-aside on catalogue, write invalidation, text search index, Redis-down fallback."
---
Chapter 21 added **performance and discoverability** — Redis cache-aside on catalogue, write invalidation, text search index, Redis-down fallback.

<BigWordAlert term="Cached catalogue" plainEnglish="Shop API fast with Redis; search indexed — ready for async jobs in Chapter 22." whyItMatters="Background jobs handle email and receipts — cache keeps read path lean." />

## What you built

**redis.js** — connection with graceful degradation.

**Cache layer** — GET /products HIT/MISS with TTL.

**Invalidation** — product writes bust catalogue keys.

**Text index** — `$text` search on name + category.

**Measurements** — before/after in README.

**Fallback** — MongoDB direct when Redis unavailable.

## Week 4 progress

| Target | Status |
|---|---|
| Cached catalogue | ✓ |
| Search works | ✓ |
| Invalidation verified | ✓ |
| Async jobs after checkout | Chapter 22 |
| Live HTTPS | Chapter 23 |

<InterestingRead title="The stale cache trap" hook="Fast wrong answers are worse than slow right ones." readMinutes={4}>

Teams enable caching, celebrate response times, then ship outdated stock levels. FreshMarket defines what is cacheable (public catalogue pages) and what never is (cart, checkout, auth).

</InterestingRead>


## Before you continue

<OpenQuestion id="01M34MRBPHSVRYABRY0ZTQTW66" />

## Next — Chapter 22: Async jobs & queues

- BullMQ on same Redis as cache
- Worker process separate from API
- Checkout enqueues vendor + customer notifications
- Status PATCH enqueues customer updates
- API returns before worker logs complete

Performance layer done. Next move side effects off the hot path.

## After that — Chapter 23: Deploy

## Continue to gate

**Sub-chapter 21.16:** Checklist + learning log.
<RealWorldEvent title="Facebook Memcached outage lessons" when="2010" summary="Major sites learned that caching layers need fallbacks — when cache fails, databases must survive the thundering herd." lesson="Always code a cache miss path. Redis is an accelerator, not the system of record." />
