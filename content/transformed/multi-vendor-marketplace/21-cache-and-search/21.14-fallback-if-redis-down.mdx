---
title: "Fallback if Redis down"
module: 21
lesson: 14
summary: "**Why this step matters:** Production Redis restarts, misconfigured URLs, and free-tier limits happen. An app that crashes when cache is unavailable is not shippable — degraded speed is acceptable; downtime is not."
---
**Why this step matters:** Production Redis restarts, misconfigured URLs, and free-tier limits happen. An app that crashes when cache is unavailable is not shippable — degraded speed is acceptable; downtime is not.

<BigWordAlert term="Cache degradation" plainEnglish="Serving from MongoDB when Redis unavailable — slower but still functional." whyItMatters="FreshMarket shop must not 500 if Redis restarts — graceful fallback to uncached queries." />

**Where the project is now:** Cache, invalidation, and search index work (21.8–21.13) — kill Redis and the catalogue breaks; nothing falls back to MongoDB.

**After this step:** API serves catalogue from MongoDB when Redis unavailable — **no crash**, degraded performance only.

## Fallback contract

```
getFromCache(key):
  if redis client null or error → return null (miss)

setCache(key, value):
  if redis unavailable → no-op, log debug

catalogue handler:
  always can fall through to MongoDB query
```

`GET /products` returns 200 with correct data — slower without cache.

## Test script

1. Stop Redis: `sudo systemctl stop redis` or stop Docker container.
2. Restart API — boot succeeds with warning log.
3. Catalogue still loads from MongoDB:

<ApiRequestPanel title="Redis down — graceful fallback">

<ApiRequest
  label="GET /products with Redis unavailable (slower, must still 200)"
  method="GET"
  url="http://localhost:5000/api/products?page=1&limit=20"
  responseStatus={200}
  responseBody='{"items":[{"_id":"...","name":"Organic Kale"}],"total":42,"page":1,"limit":20}'
/>

</ApiRequestPanel>

4. Start Redis — HIT/MISS logging resumes.

## Boot behaviour

| Redis down at boot | Acceptable |
|---|---|
| API starts, warns | ✓ Required for gate |
| API crash loop | ✗ Fix |

## Health endpoint — optional

Do not mark `redis: connected` in `/health` unless you add it — not required for gate. MongoDB `db` status remains primary.

## ✅ Do / ❌ Don't

**✅ Do:** Try/catch around Redis GET/SET — never throw to client.

**✅ Do:** Document fallback in README ops section.

**❌ Don't:** Require Redis for checkout or orders.

## Verify

- [ ] Redis stopped → products still load
- [ ] Redis restored → cache works again

<RealWorldEvent title="Facebook Memcached outage lessons" when="2010" summary="Major sites learned that caching layers need fallbacks — when cache fails, databases must survive the thundering herd." lesson="Always code a cache miss path. Redis is an accelerator, not the system of record." />


## Before you continue

<OpenQuestion id="01M34MRBP7CGJJV70KEMH9PVVJ" />

## Next

**Sub-chapter 21.15:** Recap and what's next — caching survives a Redis outage; close the chapter with the gate.
