---
title: "Recap and what's next"
module: 17
lesson: 11
summary: "**Why this step matters:** Chapter 18 checkout reads the **cart document** your UI just filled — recap confirms grouped display matches stored `storeId` before server-side order split."
---
**Why this step matters:** Chapter 18 checkout reads the **cart document** your UI just filled — recap confirms grouped display matches stored `storeId` before server-side order split.

You entered Chapter 17 with a cart that lived only in API responses. You leave with a **customer-facing cart** — grouped by store, mutable quantities, header badge, and persistence across reload.

## What you built — narrative recap

**Cart page:** `/cart` protected route; items grouped by `storeId`; per-store subtotals and grand total estimate.

**Interactions:** PATCH for quantity; DELETE for remove; refetch or sync state after mutations.

**Add to cart:** Product detail posts to Chapter 16 API; auth required.

**Header badge:** Live count on customer pages; updates after cart changes.

**Verification:** Two-vendor cart survives refresh and re-login; user isolation confirmed.

## Your client tree now

```
client/src/
  pages/
    Cart.jsx                 ← NEW
    ProductDetail.jsx        ← add wired
  components/
    CartIcon.jsx             ← NEW
    StoreCartGroup.jsx       ← NEW
    CartItemRow.jsx          ← NEW
  hooks/
    useCart.js               ← optional
  api/client.js              ← + cart helpers
```

**Server unchanged** — Chapter 16 API is the backend for this chapter.

## Decisions you can defend in a viva

1. **Why group by store in UI?** — Same boundary as multi-vendor checkout split (Ch 18).
2. **Why server cart not localStorage?** — Persistence across devices; auth scoping; source of truth.
3. **Why refetch after PATCH?** — Avoid drift; server enforces stock rules.
4. **Cart prices vs order prices?** — Cart shows live catalogue prices; orders snapshot at checkout.

## Week 3 phase progress

| Demo target | Status |
|---|---|
| Browse marketplace | ✓ |
| Multi-vendor cart (UI) | ✓ **this chapter** |
| Checkout → split orders | Chapter 18–19 |
| Both sides see orders | Chapter 20 |


## Before you continue

<OpenQuestion id="01KZ5FW4Y61X0S2XSA2165RM54" />

## Next chapter — Checkout & orders API (Chapter 18)

Chapter 18 implements what the cart previewed:

- `POST /checkout` — group cart lines by `storeId`
- One **Order** per store from single checkout
- **Snapshot** fields on embedded order lines — `priceAtPurchase`, `nameAtPurchase`, `unitAtPurchase`
- Atomic **stock decrement** — fail entire checkout if any line OOS
- Clear cart on success
- `GET /orders/my` and `GET /vendor/orders`

**Do not skip Chapter 18** because the cart looks done — Week 3 demo requires real orders.

## Next UI chapter — Track orders (Chapter 20)

Pairs with Chapter 18:

- Checkout review page → POST /checkout
- Order confirmation with split orders
- Customer order history
- Vendor order list + status updates
- **Week 3 full demo**

## Continue to gate

**Sub-chapter 17.12:** Definition of Done checklist + learning log.

**Do not open Chapter 18 until 17.12 is fully ticked.**

The customer sees the cart. Next the server turns it into orders.
