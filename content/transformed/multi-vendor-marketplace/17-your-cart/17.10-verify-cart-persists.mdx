---
title: "Verify cart persists"
module: 17
lesson: 10
summary: "**Why this step matters:** Persistence is the whole point of Chapter 16 — reload, new tab, or re-login must show the same cart. If this fails, checkout in Chapter 18 has nothing reliable to read."
---
**Why this step matters:** Persistence is the whole point of Chapter 16 — reload, new tab, or re-login must show the same cart. If this fails, checkout in Chapter 18 has nothing reliable to read.

**Where the project is now:** Cart UI complete in a single session.

**After this step:** You have **screenshot-worthy proof** that cart survives refresh, re-login, and matches server state.

## Test script — run in browser

**Setup:** Log in as seed customer. Empty cart first if needed.

1. Add product from **Store A** — quantity 2.
2. Add product from **Store B** — quantity 1.
3. Open `/cart` — confirm two store groups.
4. **Hard refresh** (Ctrl+Shift+R) — cart unchanged.
5. Change quantity on one line — refresh — change persisted.
6. Remove one line — refresh — removal persisted.
7. Log out → log in as **same customer** — cart restored.
8. Log in as **different customer** — cart empty or different — not User A's items.

## Server cross-check

After browser tests, curl as same customer:

```bash
curl -s http://localhost:5000/api/cart \\
  -H "Authorization: Bearer <customer-token>"
```

Browser display must match JSON — no phantom lines in UI.

## Screenshot checklist for portfolio

Capture:

- Cart page with **two store sections**
- Header badge with correct count
- Product detail with add confirmation

Store in `docs/screenshots/` or learning log — optional but useful for Chapter 99.

## Common failures

| Symptom | Likely cause |
|---|---|
| Cart empty after refresh | Client-only state; POST never ran |
| Wrong user's cart | Token mix-up; scoping bug in Ch 16 |
| Count wrong | Badge rule ≠ actual lines |
| Store name missing | Populate not on GET /cart |

## README — demo steps

Add **Week 3 partial demo** section:

1. Login as customer
2. Add from two vendors
3. Open cart — grouped view
4. Refresh — still there

Full checkout demo comes Chapter 20.

## Commit

```bash
git add README.md docs/screenshots/  # if added
git commit -m "docs: verify cart persistence and update demo steps"
```

## ✅ Do / ❌ Don't

**✅ Do:** Run persistence test as two different users — isolation proof.

**✅ Do:** Note product IDs and store names in learning log.

**❌ Don't:** Mark gate complete without reload test.

**❌ Don't:** Use incognito without login — test real auth persistence.

## Verify checklist

- [ ] All eight test steps pass
- [ ] curl matches browser
- [ ] Two-customer isolation confirmed
- [ ] README demo steps updated


## Before you continue

<OpenQuestion id="01KZ63P5H68D3PJV42FEM2DCPT" />

## Next

**Sub-chapter 17.11:** Recap and what's next — checkout preview.
