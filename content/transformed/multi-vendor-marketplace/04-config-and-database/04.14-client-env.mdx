---
title: "Client env"
module: 4
lesson: 14
summary: "**Why this step matters:** Re-verifying `VITE_API_URL` while adding server secrets reinforces the client/server boundary — the browser still never sees `MONGODB_URI`, but it must display the new health field from your API."
---
**Why this step matters:** Re-verifying `VITE_API_URL` while adding server secrets reinforces the client/server boundary — the browser still never sees `MONGODB_URI`, but it must display the new health field from your API.

**Where the project is now:** Server health returns `db: connected`. Client Home page from Chapter 2 likely shows only `status`.

**After this step:** Client env verified; Home page displays **database status** from live API response; no MongoDB secrets on client.

## Step 1 — Verify client environment files

**`client/.env`** (local):

```env
VITE_API_URL=http://localhost:5000
```

**`client/.env.example`** (committed):

```env
VITE_API_URL=http://localhost:5000
```

If server uses non-default `PORT` from `server/.env`, update client URL to match.

Restart Vite after any `.env` change:

```bash
cd client
npm run dev
```

Console check (temporary):

```javascript
console.log(import.meta.env.VITE_API_URL)
```

**Expected:** Full base URL — not `undefined`.

## Step 2 — Confirm `api/client.js` unchanged in contract

`getHealth()` (or `apiGet('/health')`) should already:

- GET `{VITE_API_URL}/health`  
- Parse JSON  
- Return object to caller  

**No change required** if helper returns full parsed body — Home will read new `db` field.

If helper strips fields, update to pass through full response object.

**Do not** add MongoDB URI to client env or helper.

## Step 3 — Update Home page UI

**Requirements — you implement JSX and styling:**

Display at minimum:

| UI element | Data source |
|---|---|
| API status | `data.status` from health response |
| Database status | `data.db` from health response |

Example structure (not copy-paste solution):

```
FreshMarket — System status
API: ok
Database: connected
```

**States to handle (from Chapter 2 patterns):**

| State | UI behaviour |
|---|---|
| Loading | Show loading indicator while fetch in flight |
| Success | Show `status` and `db` |
| Network error / server down | Show error message — not fake success |
| JSON missing `db` | Show sensible fallback — e.g. "unknown" — and note in learning log |

When server returns `db: disconnected` (4.15 test), Home should show that — not hide behind generic ok.

## Step 4 — Browser verification

Both terminals running:

1. Server — MongoDB connected  
2. Client — Vite dev server  

Open Home in browser. Network tab:

| Check | Expected |
|---|---|
| Request URL | `http://localhost:5000/health` |
| Status | 200 |
| Response body | includes `db: "connected"` |
| UI | Shows matching database status |

Screenshot-worthy — same Week 1 demo habit as 2.16, now richer.

## Step 5 — Failure mode quick test

Stop server (Ctrl+C in server terminal).

Reload Home.

**Expected:** Error UI — same pattern as Chapter 2 — not blank page.

Restart server with MongoDB up — UI recovers.

## README client section

Confirm root README includes:

```markdown
cp client/.env.example client/.env
npm run dev   # in client/
```

Note: client only needs API URL — never database credentials.

## Commit

```bash
git add client/src/pages/Home.jsx client/.env.example
git commit -m "feat(client): display database status from health endpoint"
```

Do not commit `client/.env` if gitignored.

## ✅ Do / ❌ Don't

**✅ Do:** Read `db` from API response — don't hardcode "connected" in JSX.

**✅ Do:** Keep fetch logic in `api/client.js` — Home calls helper only.

**❌ Don't:** Add `VITE_MONGODB_URI` — architectural violation.

**❌ Don't:** Create a second fetch in Home bypassing helper — breaks Chapter 7 auth header plan.

## Optional polish (not gate)

- Color badge: green connected / red disconnected  
- Show last checked timestamp  
- Link to README troubleshooting  

## Troubleshooting

| Problem | Fix |
|---|---|
| CORS error | Server cors origin must include Vite URL |
| UI shows old status only | Home not reading `db`; cache — hard refresh |
| `undefined` API URL | `VITE_` prefix; restart Vite |
| 404 on health | Wrong base URL path — no double `/health/health` |

## Verify checklist

- [ ] `VITE_API_URL` correct in client `.env`  
- [ ] Home shows `status` and `db` when server up  
- [ ] Home shows error when server down  
- [ ] No secrets added to client  
- [ ] Committed  


## Before you continue

<OpenQuestion id="01KZ5P693XQM0MY6716X7HGHB2" />

## Next

**Sub-chapter 4.15:** Verify connection — deliberate failure tests (config missing, MongoDB down).

Happy path works; engineers prove the unhappy paths.
