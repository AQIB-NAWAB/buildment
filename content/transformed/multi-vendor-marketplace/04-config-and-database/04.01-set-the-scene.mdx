---
title: "Set the scene"
module: 4
lesson: 1
summary: "**Where the project is now:** Chapter 2 gave you a running skeleton — Express listens, React loads, `GET /health` returns `{ \"status\": \"ok\" }`. Chapter 3 gave you a **design** — collections, embed vs reference, `docs/erd-draft.md` committed. Nothing persists yet. Restart the serv"
---
**Where the project is now:** Chapter 2 gave you a running skeleton — Express listens, React loads, `GET /health` returns `{ "status": "ok" }`. Chapter 3 gave you a **design** — collections, embed vs reference, `docs/erd-draft.md` committed. Nothing persists yet. Restart the server and every vendor, product, and cart item is still imaginary.

**Where this chapter goes:** Your API gets a **real database connection** — configuration from environment variables, Mongoose wired on boot, and a health check that tells the truth about whether MongoDB is reachable.

This is a **build chapter**. Every sub-chapter from 4.8 onward adds files to your repo. The gate is **runnable proof**: MongoDB connected, secrets not in Git, client still shows health — now including database status.

## Bridge from last chapter

Chapter 3 gave you a **blueprint** — collections, embed-vs-reference decisions, grocery fields on products, snapshot lines on orders — committed as `docs/erd-draft.md`. Chapter 2 gave you a **running skeleton** that forgets everything on restart. You can demo "the apps talk" but not "the apps remember."

What carries forward: module folders (still empty of models), your ERD, and the ownership chain you can explain aloud. What you add now is **memory with discipline** — MongoDB connected through environment variables, configuration validated at boot, secrets kept out of Git, and a health check that tells the truth about database status.

The hat you wear now is **platform engineer**. You are wiring infrastructure every production Node API needs before features touch user data: where secrets live, how the process starts, what happens when configuration is missing. Chapter 5 will create Mongoose models; this chapter only connects the wire so those models have somewhere to persist.

## In plain English

| Term | Meaning |
|---|---|
| **Environment variable** | A key-value setting outside your code — e.g. `MONGODB_URI` in `.env` |
| **`.env` file** | Local file holding secrets and config — never committed to Git |
| **`.env.example`** | A committed template listing variable **names** with fake placeholders |
| **Config module** | One file (`shared/config.js`) that reads and validates all env vars |
| **Boot order** | The sequence on startup: load config → connect DB → start listening |
| **Fail-fast** | Crash immediately with a clear error if required config is missing |
| **Mongoose** | Node library that connects to MongoDB — models come in Chapter 5 |
| **Connection string (URI)** | The address and credentials MongoDB expects — treat as a secret |

## Think of it like…

**Hooking up electricity to the market hall.** Chapter 2 built the structure; Chapter 3 drew the floor plan. This chapter runs the main cable from the grid (MongoDB) into the building through a breaker box (`config.js` + `db.js`) that refuses to power lights if the wiring is wrong. You still have empty stalls — no stock, no vendors — but the building can now support refrigerators (schemas) in the next chapter.

## Common beginner questions

<FaqGroup>

<FaqItem question="Why config before schemas — isn't the data model the point?">

Models need an **active connection**. More importantly, teams establish **secret-handling habits** before real user data exists. Leaking a connection string in Git is painful; learning `.env` discipline on an empty database is cheap.

</FaqItem>

<FaqItem question="Local MongoDB or Atlas — which should I pick?">

Either works for the gate. Local is fine offline; Atlas matches many deploy guides. Sub-chapter 4.5 helps you decide; the course does not mandate cloud.

</FaqItem>

<FaqItem question="Will the React app talk to MongoDB directly?">

**Never.** Only the server reads `MONGODB_URI`. The browser talks HTTP to Express; Express talks to MongoDB. Chapter 2 established that boundary — this chapter implements it on the server side.

</FaqItem>

<FaqItem question="What if the server starts but MongoDB is down?">

You will design honest behaviour — fail at boot and/or report `db: disconnected` on health. The app must not pretend everything is fine when the database is unreachable.

</FaqItem>

<FaqItem question="Do I need to re-debate SQL vs MongoDB here?">

No. Chapter 3 closed that decision. If the ERD gate is incomplete, finish the draft first — you can connect MongoDB, but implementing models without a blueprint invites rework.

</FaqItem>

</FaqGroup>


## The gap between "runs" and "remembers"

Right now your marketplace can answer one question: *Is the Node process alive?*

It cannot answer:

- Is MongoDB reachable from this server?
- What connection string does this environment use?
- Should the API start if the database is down?

Those are **configuration and boot-order** problems — not schema problems. Chapter 5 writes Mongoose models; **Chapter 4 connects the wire**. Order matters: connect → then implement schemas → then seed.

## Why this chapter sits after "Why MongoDB"

Chapter 3 was deliberate: you decided **document model + collections** before installing drivers. Jumping straight to `mongoose.connect()` without that design produces copy-paste schemas that break at checkout.

Chapter 4 assumes:

- You committed **`docs/erd-draft.md`**
- You know **User, Store, Product, CartItem, Order** are coming in Chapter 5
- You are **not** re-debating SQL vs MongoDB here

If Chapter 3 gate is incomplete, finish the ERD draft first — you can still connect MongoDB, but you'll implement models without a blueprint.

## What "config & database" means in production terms

Professional Node APIs treat configuration as **inputs to the process**, not constants in source code:

| Concern | Where it lives | Why |
|---|---|---|
| MongoDB connection string | `server/.env` → read by `shared/config.js` | Different per machine (local, staging, production) |
| API port | `server/.env` or host default | Deploy platforms set `PORT` dynamically |
| Client API base URL | `client/.env` as `VITE_API_URL` | Already started in Chapter 2 |
| Secrets | Never in Git | `.gitignore` + `.env.example` without values |

**Fail-fast:** If `MONGODB_URI` is missing or invalid, the server should **not** silently run pretending everything is fine. Chapter 4 teaches that discipline before you have user data to lose.

## Continuity from Chapter 2 — files you already have

```
server/src/
  app.js          ← Express app; health route lives here (extended in 4.13)
  server.js       ← listen(); will call db connect before listen (4.12)
  shared/         ← empty or minimal — config.js and db.js land here
  modules/        ← still empty placeholders until Chapter 5
```

```
client/src/
  api/client.js   ← getHealth() — will display new db field from API (4.14–4.15)
  pages/Home.jsx  ← shows health JSON — update when contract changes
```

Your **module folders do not change** in this chapter. Only `shared/` gains cross-cutting infrastructure.

## The Week 1 demo moves forward

| Week 1 target | After Ch 3 | After Ch 4 |
|---|---|---|
| Both apps run | ✓ | ✓ |
| Client hits `/health` | ✓ | ✓ + shows `db` status |
| ERD in repo | ✓ draft | ✓ draft |
| MongoDB connected | | **this chapter** |
| Seed in Compass | | Chapter 5 |

Phase demo language: *"Both apps run; client hits GET /health; MongoDB connected"* — Chapter 4 completes the third item.

## What this chapter is not

- **Not** writing User/Product schemas — Chapter 5  
- **Not** seed data — Chapter 5  
- **Not** Redis or Cloudinary — later phases  
- **Not** a MongoDB modelling tutorial — Chapter 3 covered that  
- **Not** optional — skipping env discipline causes real incidents (next sub-chapter)

## Reading pace

| Sub-chapter | Cluster | Focus |
|---|---|---|
| 4.1–4.3 | Open | Problem, secrets, deliverables |
| 4.4–4.6 | Learn | 12-factor config, local vs Atlas, Mongoose docs |
| 4.7 | Check | Quiz reserved |
| 4.8–4.15 | Build | env → config → db → boot → health → verify |
| 4.16–4.17 | Wrap | Recap + gate |

Estimated time: **one full day** — less conceptual than Chapter 3, more terminal work than Chapter 2's client half.

## Self-check before continuing

- [ ] Chapter 3 gate cleared (`docs/erd-draft.md` committed, learning log done)  
- [ ] Chapter 2 gate still true — `curl /health` returns 200  
- [ ] You have **MongoDB available** — local install or Atlas cluster (decide in 4.5)  
- [ ] Root `.gitignore` includes `.env` (from Chapter 2 — re-verify in 4.9)


## Before you continue

<OpenQuestion id="01KZ5P68YJDW6P2C6ATV5AYJYE" />

## Next

**Sub-chapter 4.2:** Why secrets matter — the cost of leaking connection strings, and why `.env` discipline is non-negotiable before you paste a `MONGODB_URI`.

Your skeleton is about to get a memory — configure it safely first.
