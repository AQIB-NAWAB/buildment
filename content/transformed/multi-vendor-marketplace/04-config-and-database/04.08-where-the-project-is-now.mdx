---
title: "Where the project is now"
module: 4
lesson: 8
summary: "**Why this step matters:** The build cluster assumes Chapter 2's skeleton and Chapter 3's ERD still hold. This recap prevents wiring MongoDB into a repo that never passed the health demo or skipped the design gate — fixing foundation bugs now is cheaper than debugging `MONGODB_UR"
---
**Why this step matters:** The build cluster assumes Chapter 2's skeleton and Chapter 3's ERD still hold. This recap prevents wiring MongoDB into a repo that never passed the health demo or skipped the design gate — fixing foundation bugs now is cheaper than debugging `MONGODB_URI` on a broken tree.

**Build cluster starts here.** Before creating `server/.env` or `shared/config.js`, confirm your repo matches what Chapters 2 and 3 left behind — and that gates are cleared.

## Prerequisites gate

| Prior gate | Verify |
|---|---|
| Chapter 2 | `curl -i http://localhost:5000/health` → `200`, `"status":"ok"` |
| Chapter 2 | Client Home shows live health; error when server stopped |
| Chapter 3 | `docs/erd-draft.md` committed |
| Chapter 3 | Chapter 3 gate cleared on the platform |
| Chapter 4 prep | Working `MONGODB_URI` from 4.5 (mongosh or Compass tested) |

If health is broken, fix Chapter 2 before adding MongoDB — you'll blame Mongoose for Express bugs otherwise.

## Server tree — as of Chapter 3 end

```
server/
├── package.json
├── package-lock.json
└── src/
    ├── app.js                 ← Express + GET /health + CORS + json()
    ├── server.js              ← imports app, listen(PORT)
    ├── shared/                ← exists; likely empty or minimal
    └── modules/
        ├── auth/              ← empty placeholder
        ├── stores/
        ├── products/
        ├── cart/
        └── orders/
```

**What changes in Chapter 4:**

```
server/
├── .env                       ← NEW local only
├── .env.example               ← NEW committed
└── src/
    ├── app.js                 ← health adds db field
    ├── server.js              ← await connectDB() before listen
    └── shared/
        ├── config.js          ← NEW
        └── db.js              ← NEW
```

## Client tree — as of Chapter 2 end

```
client/
├── .env                       ← local — VITE_API_URL (may exist)
├── .env.example
└── src/
    ├── App.jsx
    ├── main.jsx
    ├── pages/Home.jsx         ← fetches health
    ├── api/client.js          ← getHealth()
    ├── components/
    └── hooks/
```

**Chapter 4 client changes:** verify env; display `db` from health JSON — no new folders.

## Root repo

```
your-marketplace/
├── README.md
├── .gitignore
├── docs/
│   └── erd-draft.md
│   ├── 01-introduction.md
│   ├── 02-project-skeleton.md
│   └── 03-why-mongodb.md
├── server/
└── client/
```

## Current `/health` contract (Chapter 2)

| Field | Value |
|---|---|
| Method | GET |
| Path | `/health` |
| Auth | None |
| Success | 200 + `{ "status": "ok" }` |

**Chapter 4 extends** with database status — does not remove `status`.

## Runtime today vs after Chapter 4

| Step | Today (Ch 2–3) | After Ch 4 |
|---|---|---|
| Start server | `npm run dev` → listen | load env → validate config → connect Mongo → listen |
| `/health` | process alive | process alive + DB state |
| Data persistence | none | connection ready; still no collections until Ch 5 |
| Compass | empty or no DB | database exists; empty collections until seed |

Connecting MongoDB **does not** create User/Product documents — Chapter 5 schemas + seed do that.

## Dependencies today

`server/package.json` includes at minimum:

- `express`  
- likely `cors`  

**Chapter 4 adds:**

- `mongoose`  
- optionally `dotenv` (if not using Node `--env-file`)

Run from `server/` when installing — lockfile commits with package.json.

## Two terminals — your daily habit

| Terminal | Directory | Command |
|---|---|---|
| 1 | `server/` | `npm run dev` |
| 2 | `client/` | `npm run dev` |

After Chapter 4, Terminal 1 log should show **successful MongoDB connection** before or alongside listen message — exact log text is your choice; clarity matters.

## Git hygiene checkpoint

```bash
git status
```

**Expected:** No `server/.env` or `client/.env` in staged files.

```bash
git check-ignore -v server/.env
```

If not ignored, fix `.gitignore` in **4.9** before creating `.env`.

## What you will not touch this chapter

- `server/src/modules/*` — no models yet  
- Auth routes — Chapter 6  
- `docs/erd-draft.md` — refine in Chapter 5 to `erd.md`  
- Redis, Cloudinary — later phases  

## Build sequence reminder

| Sub-chapter | Action |
|---|---|
| 4.9 | `.env`, `.env.example`, gitignore |
| 4.10 | `config.js` |
| 4.11 | `db.js` + mongoose install |
| 4.12 | `server.js` boot order |
| 4.13 | health + `db` |
| 4.14 | client display |
| 4.15 | failure-mode tests |

Do not skip order — config before db, db before listen, listen before client demo.

## ✅ Do / ❌ Don't

**✅ Do:** Commit after each sub-chapter milestone (suggested messages in each file).

**❌ Don't:** Create `users.model.js` early because you're excited — Chapter 5.

**❌ Don't:** Merge config and db into one file — separation keeps seed script imports clean.

## Verify before 4.9

- [ ] Health works without MongoDB wired  
- [ ] `shared/` folder exists  
- [ ] MongoDB instance reachable via mongosh/Compass  
- [ ] Chapter 3 learning log committed  


## Before you continue

<OpenQuestion id="01KZAX75R9H1PSXT258X889P1J" />

## Next

**Sub-chapter 4.9:** Create `server/.env`, `server/.env.example`, and verify `.gitignore`.

The build cluster begins — one file at a time.
