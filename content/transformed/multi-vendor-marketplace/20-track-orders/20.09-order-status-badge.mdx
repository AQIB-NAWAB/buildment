---
title: "Order status badge"
module: 20
lesson: 9
summary: "**Why this step matters:** One shared badge component keeps status labels consistent across confirmation, customer history, and vendor inbox — without it, the same order shows \"Pending\" in one place and \"pending\" in another."
---
**Why this step matters:** One shared badge component keeps status labels consistent across confirmation, customer history, and vendor inbox — without it, the same order shows "Pending" in one place and "pending" in another.

<BigWordAlert term="Order status lifecycle" plainEnglish="States like pending, paid, preparing, shipped, delivered — tracked on order document." whyItMatters="FreshMarket badges color-code status — customers and vendors share same vocabulary." />

**Where the project is now:** Confirmation (20.6), customer history (20.7), and vendor inbox (20.8) render status strings — each page formats them differently.

**After this step:** Orders display **status** consistently — `pending` → `packed` → `dispatched` (or your enum).

## Component contract

`OrderStatusBadge.jsx` — prop `status: string`.

| Status | Visual |
|---|---|
| `pending` | Neutral — awaiting fulfilment |
| `packed` | In progress |
| `dispatched` | Complete / shipped |

Map unknown status to raw string — don't crash.

## Schema alignment

Use exact enum values from Order model (Ch 5). Document in README:

```
pending | packed | dispatched
```

Optional: `cancelled` — out of scope unless implemented.

## Where used

- Order confirmation
- My orders list
- Vendor orders list

Single component — one mapping table.

## Accessibility

Badge text readable — not color-only. `aria-label` includes status name.

## ✅ Do / ❌ Don't

**✅ Do:** Centralize status → label/color map.

**✅ Do:** Default new orders to `pending` from API.

**❌ Don't:** Hardcode different labels per page.

## Verify

- [ ] New checkout orders show `pending`
- [ ] Badge consistent on customer and vendor pages

<RealWorldEvent title="Domino's Pizza Tracker effect" when="2008–present" summary="Transparent order tracking reduced support calls and increased trust — customers tolerate wait when they see progress." lesson="Expose meaningful statuses, not internal jargon. 'Preparing your order' beats 'STATE_PICKING'." />


## Before you continue

<OpenQuestion id="01KZP7KQRXMZ9AVAGR7AER7TRZ" />

## Next

**Sub-chapter 20.10:** Vendor update status — the badge only means something once vendors can actually move an order through pending → packed → delivered.
