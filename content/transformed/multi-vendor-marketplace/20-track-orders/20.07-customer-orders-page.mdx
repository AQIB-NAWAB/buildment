---
title: "Customer orders page"
module: 20
lesson: 7
summary: "**Why this step matters:** Confirmation is a one-time screen — order history is where customers return days later. This page proves snapshot prices stay correct when vendors change the catalogue."
---
**Why this step matters:** Confirmation is a one-time screen — order history is where customers return days later. This page proves snapshot prices stay correct when vendors change the catalogue.

**Where the project is now:** Confirmation page shows orders after checkout (19.6). No persistent **order history** route — refresh or return later has no dedicated home.

**After this step:** `/orders` lists customer history from `GET /orders/my` — split orders as separate cards with snapshot line prices.

History is where the **price snapshot trap** (Ch 18) shows in UI. Every line reads `orderItems` embedded fields — never live catalog prices.

## Prerequisites

| Gate | Verify |
|---|---|
| Ch 18.15 | `GET /orders/my` works via curl |
| 19.6 | `OrderLineRow` snapshot display exists |
| 19.9 | `OrderStatusBadge` wired (can stub `pending` first) |

## Target files

| File | Purpose |
|---|---|
| `client/src/pages/MyOrders.jsx` | Customer history list |
| `client/src/components/OrderCard.jsx` | Reusable summary (confirmation + history) |
| `client/src/components/OrderLineRow.jsx` | Snapshot line display |
| `client/src/api/client.js` | Add `getMyOrders()` |
| `client/src/App.jsx` | Register `/orders` protected customer route |
| Customer nav | Add **Orders** link beside cart |

## Step 1 — API contract

```
GET /api/orders/my
Authorization: Bearer <customer-token>
```

**Middleware:** `requireAuth`, `requireRole('customer')` — Ch 18.15.

| Field | Use in UI |
|---|---|
| `_id` | Card key |
| `status` | `OrderStatusBadge` |
| `total` | Formatted currency |
| `createdAt` | Date/time |
| `storeId` / populated store | Store name heading |
| `orderItems[]` | `nameAtPurchase`, `priceAtPurchase`, `unitAtPurchase`, `quantity` |

Response: array or `{ orders: [...] }` — match vendor list envelope. Sort `{ createdAt: -1 }` on server.

## Step 2 — Page load contract

1. On mount → `getMyOrders()`.
2. Loading → spinner (not empty state).
3. Zero orders → empty state + link to `/shop`.
4. With orders → one `OrderCard` per order, newest first.
5. `401` → redirect login; 5xx → message + retry.

## Step 3 — OrderCard contract

| Data | Display |
|---|---|
| Store name | Card title |
| `createdAt` | Human-readable date |
| `status` | Badge |
| `total` | Prominent total |
| `orderItems` | `OrderLineRow` list or expandable section |

**Split orders:** Two orders from one checkout → **two cards** — do not merge. Match confirmation formatting (19.6).

Add **Orders** nav link; confirmation "View all orders" → `/orders`. Customer role only.

## Commit

```bash
git add client/src/pages/MyOrders.jsx client/src/components/OrderCard.jsx client/src/api/client.js
git commit -m "feat(client): add customer order history page"
```

Document `/orders` in README — prices are purchase-time snapshots.

## ✅ Do / ❌ Don't

**✅ Do:** Separate cards per order; reuse `OrderLineRow` from confirmation.

**✅ Do:** Show `pending` badge on new orders.

**❌ Don't:** Fetch live product prices or populate product for display.

**❌ Don't:** Merge split orders into one receipt card.

## Verify in browser

| Step | Expected |
|---|---|
| After two-store checkout | **Two cards** on `/orders` |
| Line prices | Match confirmation + curl |
| Customer B login | Does not see Customer A's orders |
| Vendor changed catalog price | History prices **unchanged** |

```bash
curl -s http://localhost:5000/api/orders/my \
  -H "Authorization: Bearer <customer-token>"
```

## Troubleshooting

| Observation | Likely cause | Action |
|---|---|---|
| One card after two-store checkout | UI merging by date | One card per `order._id` |
| Wrong line prices | Used product populate | Read snapshot fields |
| Empty after checkout | Wrong logged-in user | Match checkout session |
| 403 | Vendor token on customer route | Separate sessions |
| Flash of empty state | No loading flag | Spinner until response |

## Verify checklist

- [ ] `/orders` in nav; protected route works
- [ ] Two-store checkout → two cards with snapshot prices
- [ ] `OrderStatusBadge` shows `pending` for new orders
- [ ] Empty state for customer with no orders
- [ ] Customer B isolation confirmed


## Before you continue

<OpenQuestion id="01KZ63P5KW2WFVQV61SRVRXYZW" />

## Next

**Sub-chapter 20.8:** Vendor orders page — fulfilment queue scoped to vendor's store.
