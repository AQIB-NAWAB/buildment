---
title: "Course outline"
module: 1
lesson: 6
summary: "This is your map for four weeks. Every chapter below is a **folder** of sub-chapters you complete **in order**. Each row tells you what milestone you reach when the checklist at the end of that chapter is fully ticked."
---
This is your map for four weeks. Every chapter below is a **folder** of sub-chapters you complete **in order**. Each row tells you what milestone you reach when the checklist at the end of that chapter is fully ticked.

<BigWordAlert term="Module arc" plainEnglish="The ordered journey from skeleton app to deployed marketplace, grouped into themed chapters." whyItMatters="Knowing the arc helps you see why MongoDB comes before auth and why payments come after checkout logic." />

Do not treat this as a table of contents to skim once. Return here when you finish a week and ask: **"Can I demo what this week promised?"**

## How to read this outline

| Column | Meaning |
|---|---|
| **#** | Chapter number — matches folder prefix (`06-authentication-api/`) |
| **Chapter** | Feature area name — what you'll say you built |
| **Week** | Phase grouping — demo target at week end |
| **Done when…** | One-line gate summary — full checklist lives in the chapter |
| **Pair** | Backend chapter → frontend chapter that follows immediately |

**Sub-chapter count:** Most feature chapters have **20–25 sub-chapters**. Introduction and concept chapters run shorter. Use the sidebar to see every lesson in order.

## Week 1 — Foundations

**Week demo target:** Both apps run. Client displays health check response. MongoDB connected. Seed data visible in Compass. ERD in repo.

| # | Chapter | Done when… |
|---|---|---|
| 02 | **Project skeleton** | `server/` + `client/` run; client calls `GET /health` and shows JSON |
| 03 | **Why MongoDB** | Can explain why MongoDB fits this project; ERD draft in `docs/` |
| 04 | **Config & database** | Config from env; MongoDB connects; `.env` not in Git; health shows DB status |
| 05 | **Data model & seed** | All Mongoose models; seed script; ownership chain explainable |

### What Week 1 builds in your repo (cumulative)

After Chapter 02:

```text
your-marketplace/
├── server/src/          ← Express app, health route
├── client/src/          ← Vite React, Home page, api helper
└── README.md
```

After Chapter 05:

```text
your-marketplace/
├── server/src/modules/  ← users, stores, products, cart, orders
├── server/scripts/seed.js
└── docs/erd.md
```

**End of Week 1 question:** Can you open Compass, see two vendors' products, and open the browser to a page that proves the client talks to the server? If yes, Week 2.

## Week 2 — Vendor experience

**Week demo target:** Vendor registers, verifies email, opens shop, adds product with photo, sees it in dashboard. Vendor B cannot touch Vendor A's data.

| # | Chapter | Pair | Done when… |
|---|---|---|---|
| 06 | **Authentication API** | → 07 | Register/login; bcrypt; JWT + refresh; OTP; auth middleware |
| 07 | **Login & registration UI** | ← 06 | Login/register/verify in browser; refresh on 401 |
| 08 | **Authorization & isolation** | — | Role + ownership middleware; IDOR test → 403; matrix in docs |
| 09 | **Vendor store API** | → 10 | Vendor CRUDs own store; one store enforced |
| 10 | **Open your shop** | ← 09 | Store setup UI; storeless vendor gated to onboarding |
| 11 | **Manage products API** | → 12 | Vendor product CRUD; cross-vendor PATCH → 403 |
| 12 | **Vendor dashboard** | ← 11 | Product table; create/edit/publish in UI |
| 13 | **Product photos** | — | Cloudinary upload; URL only in MongoDB |

### Backend → frontend pairing this week

The course deliberately places **UI chapters immediately after API chapters** for the same feature:

```text
06 Auth API   ──→  07 Login screens
09 Store API  ──→  10 Open your shop
11 Products API ──→  12 Vendor dashboard
```

Chapter 08 (authorization) and 13 (photos) span both layers in one chapter arc — API first, then UI component in the same week.

**End of Week 2 question:** Can you record thirty seconds of a vendor adding "Organic Honey" with a photo and seeing it in their table? If yes, Week 3.

## Week 3 — Buyer experience

**Week demo target:** Customer browses two vendors, carts from both, checks out once, sees two orders. Snapshot prices survive a vendor price change.

| # | Chapter | Pair | Done when… |
|---|---|---|---|
| 14 | **Browse catalogue API** | → 15 | Public products; filters; pagination; no N+1 |
| 15 | **Shop the marketplace** | ← 14 | Product grid, filters, detail page in browser |
| 16 | **Cart API** | → 17 | Persisted cart; multi-vendor items; user-scoped |
| 17 | **Your cart** | ← 16 | Cart UI grouped by store |
| 18 | **Checkout & orders API** | → 19 | Split checkout; snapshots; atomic stock (pre-payment) |
| 19 | **Payment processing** | → 20 | Stripe Payment Intents; webhook; checkout gated on paid intent |
| 20 | **Track orders** | ← 19 | Checkout UI with Payment Element; customer history; vendor orders |

### The commerce arc — one continuous build

Week 3 is where the **marketplace identity** of the product appears. Chapters 14–20 should feel like one story:

```text
Catalogue (see products)
  → Cart (hold choices)
    → Checkout API (split + snapshot)
      → Payment (Stripe gate)
        → Orders UI (both sides see truth)
```

**End of Week 3 question:** Cart with items from two stores → checkout → two orders in DB → change a price → old order unchanged in UI? If yes, Week 4.

## Week 4 — Polish & ship

**Week demo target:** Cached catalogue invalidates on product update. Search uses index. Live HTTPS URL.

| # | Chapter | Done when… |
|---|---|---|
| 21 | **Cache & search** | Redis cache; bust on save; text index; fallback if Redis down |
| 22 | **Async jobs & queues** | BullMQ on Redis; checkout enqueues notify jobs; worker process |
| 23 | **Deploy** | Public HTTPS; client + API + worker; secrets in env; survives restart |
| 99 | **Closing** | Docs, demo video, case study, bar-raiser |

## Full chapter list — quick reference

| # | Folder name | Week |
|---|---|---|
| 01 | `01-introduction/` | Mindset |
| 02 | `02-project-skeleton/` | 1 |
| 03 | `03-why-mongodb/` | 1 |
| 04 | `04-config-and-database/` | 1 |
| 05 | `05-data-model-and-seed/` | 1 |
| 06 | `06-authentication-api/` | 2 |
| 07 | `07-login-and-registration/` | 2 |
| 08 | `08-authorization-and-isolation/` | 2 |
| 09 | `09-vendor-store-api/` | 2 |
| 10 | `10-open-your-shop/` | 2 |
| 11 | `11-manage-products-api/` | 2 |
| 12 | `12-vendor-dashboard/` | 2 |
| 13 | `13-product-photos/` | 2 |
| 14 | `14-browse-catalogue-api/` | 3 |
| 15 | `15-shop-the-marketplace/` | 3 |
| 16 | `16-cart-api/` | 3 |
| 17 | `17-your-cart/` | 3 |
| 18 | `18-checkout-and-orders/` | 3 |
| 19 | `19-payment-processing/` | 3 |
| 20 | `20-track-orders/` | 3 |
| 21 | `21-cache-and-search/` | 4 |
| 22 | `22-async-jobs-and-queues/` | 4 |
| 23 | `23-deploy/` | 4 |
| 99 | `99-closing/` | Ship |

## Complexity ladder — what gets harder when

The course climbs deliberately (Rule 20). You don't meet Redis in Week 1 or deployment in Week 2.

| Rung | Topics | Chapters |
|---|---|---|
| **Stand project on disk** | Repo shape, client/server, first HTTP | 02 |
| **Config & persistence** | Env, MongoDB, models, seed | 03–05 |
| **Identity** | Hash, JWT, refresh, OTP | 06–07 |
| **Authorization** | Role, ownership, IDOR | 08 |
| **Vendor CRUD** | Store, products, images, UI | 09–13 |
| **Catalogue performance** | Pagination, populate, N+1 | 14–15 |
| **Commerce** | Cart, split checkout, snapshots, Stripe payment | 16–20 |
| **Scale & ship** | Cache, search, async jobs, HTTPS | 21–23 |

Skipping a rung to jump ahead produces chapters that reference files that don't exist. **Work in order.**

## Concept chapters vs build chapters

Most chapters **ship a feature**. A few teach a **decision** with shorter build sections but still touch your repo:

| Chapter | Type | Applied work (not essay-only) |
|---|---|---|
| 03 Why MongoDB | Concept | ERD draft; collection list in docs |
| 08 Authorization | Concept + build | `docs/AUTHORIZATION.md` matrix + middleware |

Everything else ends in a **Definition of Done** you can demo.

## Sub-chapter arc — what every feature chapter repeats

When you open Chapter 06 (or any feature chapter), expect this rhythm:

1. **02.01–03** — Set scene, why it matters, goals  
2. **02.04–07** — Learn approaches, decide, deep-dive, reads  
3. **02.08** — Quiz placeholder (skip)  
4. **02.09–16** — Build: recap → scaffold → implement steps → verify  
5. **02.17–19** — Practice (optional), recap, **checklist gate**

The build cluster is the **longest** block. If a chapter feels like reading without typing, you're in the wrong file — jump forward to the "where the project is now" sub-chapter or check you didn't skip a build step.

## Course folder layout

Chapters are numbered folders at the course root — no phase subfolders. Week groupings (Week 1–4) are labels in this outline, not directory names.

```
multi-vendor-marketplace/
  01-introduction/
  02-project-skeleton/
  03-why-mongodb/
  04-config-and-database/
  05-data-model-and-seed/
  06-authentication-api/
  ...
  21-cache-and-search/
  22-async-jobs-and-queues/
  23-deploy/
  99-closing/
```

Your **application code** lives in a **separate repo** you create in Chapter 2 — not inside the course content folder. The course folder is the textbook; your repo is the project.

## Competencies — what you'll prove you can do

By Chapter 23, you should be able to **build and explain** each of these (tested in viva):

1. Model vendors, stores, and products with correct ownership  
2. Hash passwords; issue role-aware JWTs; explain the threat model  
3. Enforce vendor data isolation — demonstrate IDOR failure  
4. Build multi-vendor cart and split checkout with price snapshots  
5. Integrate Stripe Payment Intents and verify paid checkout gate  
6. Avoid N+1 on catalogue; explain populate vs looping queries  
7. Store images in object storage, not MongoDB documents  
8. Cache catalogue reads; invalidate when products change  
9. Enqueue async jobs after paid checkout; separate worker from API  
10. Deploy with secrets in environment variables, not Git  

Each maps to specific chapters — see the course outline in the sidebar.

## Timeboxing — four weeks at ~60 hours

| Week | Chapters | Rough hour budget | If you're behind |
|---|---|---|---|
| 1 | 02–05 | ~60h | Do not start Week 2 without Week 1 demo |
| 2 | 06–13 | ~60h | Auth + isolation are non-negotiable before products |
| 3 | 14–20 | ~60h | Checkout (18–19) + payment UI (20) are core — don't rush catalogue at expense of snapshots |
| 4 | 21–23 + close | ~60h | Async jobs before deploy; ship URL before bar-raiser |

Scope is by **chapters finished with gates cleared**, not hours. Some chapters take two days; some take four. The demo targets define "week done," not the calendar.

## What to open next

| If you… | Open… |
|---|---|
| Just finished this outline | `01.07-your-working-rhythm.md` |
| Cleared Chapter 1 checklist | `02-project-skeleton/02.01-set-the-scene.md` |
| Need sub-chapter detail | Expand modules in the course sidebar |
| Forgot a chapter gate | This file — Done when column |

<RealWorldEvent title="Instacart scales local grocery delivery" when="2012–present" summary="Instacart connected independent grocery stores to shoppers through a single app, handling inventory sync, delivery logistics, and multi-store baskets at national scale." lesson="Your FreshMarket build starts smaller, but the same pattern applies: one customer experience, many vendor backends, and careful data ownership." />

## Key ideas from this page

- **22 build chapters** + intro + closing — work in numeric order using the sidebar  
- **Four week demos** — health+seed → vendor+photo → Stripe checkout+snapshots → HTTPS  
- **Backend/UI paired** in Weeks 2–3 — API chapter then UI chapter  
- **Concept chapters** (03, parts of 08) still touch your repo  
- Course folder = textbook; **your repo** starts Chapter 2  

Next: working rhythm — habits that carry you through all 22 chapters.
## Before you continue

<OpenQuestion id="01KZP7KNQEM3X5F2W30W6BCWPG" />
