---
title: "Before you start"
module: 1
lesson: 5
summary: "This table is the **floor** the course stands on — not a gauntlet, not a reading list of everything you'll learn anyway. It's the few things you should already be comfortable with so Chapter 2 doesn't stop on hour one to explain what `async/await` means."
---
This table is the **floor** the course stands on — not a gauntlet, not a reading list of everything you'll learn anyway. It's the few things you should already be comfortable with so Chapter 2 doesn't stop on hour one to explain what `async/await` means.

**Two rules govern this list:**

1. **Pitch it to the level.** This is a **medium** course — it assumes you can already build CRUD. It does not assume you've built a multi-vendor system, deployed with Redis, or enforced row-level ownership.
2. **Never list what the course teaches.** If an early chapter covers it, it's not a prerequisite — it's the climb.

If a row says **must have**, you're expected to start with it. **Revise if rusty** means skim for an hour if you haven't used it in months — don't take a three-day detour. **Helpful, taught anyway** means don't panic if it's new; the course will touch it when needed.


## If you feel closer to beginner than "medium"

This course is labeled **medium** because it assumes you can already build CRUD. That does **not** mean you must know JWT, Redis, or multi-vendor architecture on day one — those are taught here.

**You are in a good place if you can:**
- Write a simple Express route that returns JSON
- Build a React component that loads data with `useEffect`
- Use Git to commit without putting secrets in the repo

**Take a short detour first if:**
- JavaScript `async/await` is completely unfamiliar
- You have never run a server and a React app at the same time
- HTTP status codes (200, 404, 401) mean nothing to you yet

The course is written so most chapters include **Bridge from last chapter**, **In plain English** jargon tables, and **Think of it like…** analogies — use them whenever a sub-chapter feels dense. Introduction lessons include **Common beginner questions** where helpful.


## Prerequisites table

Everything you should already be comfortable with — grouped by what you need it for, and how much it matters:

| You should be comfortable with | Why you need it | Status |
|---|---|---|
| **JavaScript (ES6+)** — arrow functions, destructuring, modules (`import`/`export`) | Every file in `server/` and `client/` | Must have |
| **async / await** — calling async functions, basic error handling with try/catch | Every database call and API handler | Must have |
| **Building a basic REST API** — routes, JSON request/response, status codes (200, 201, 400, 401, 404, 403) | The course starts from CRUD and climbs | Must have |
| **HTTP fundamentals** — methods (GET, POST, PATCH, DELETE), headers, query strings | All API work; curl testing | Must have |
| **React basics** — components, props, state, `useEffect`, conditional render | Customer shop and vendor dashboard | Must have |
| **Git everyday use** — commit, push, branch, `.gitignore` | Daily commits; secrets must never enter history | Must have |
| **npm or yarn** — install dependencies, run scripts | Both `server/` and `client/` packages | Must have |
| **MongoDB concepts** — document, collection, `_id`, basic query | Data model week; revise if you've only used SQL | Revise if rusty |
| **JSON** — read and write nested objects | API bodies and MongoDB documents | Must have |
| **Terminal / command line** — cd, run scripts, env vars | Dev workflow throughout | Must have |


## Helpful but not required — the course will touch these

| Topic | Note |
|---|---|
| **Mongoose** | Taught in Chapters 4–5; SQL experience transfers |
| **JWT concept** | Skim jwt.io decoder before Chapter 6; Chapter 6 teaches properly |
| **Redis** | Introduced in Chapter 21; install when you get there |
| **Stripe** | Chapter 19; free account + test keys before payment chapter |
| **Cloudinary** | Chapter 13; free tier signup takes ten minutes |
| **Vite** | Client scaffold in Chapter 2; Create React App experience transfers |
| **Docker** | Optional for local MongoDB; Atlas is fine |
| **TypeScript** | This course uses **JavaScript**; TS experience still helps |

Don't delay starting because Redis is unfamiliar. You'll meet it in Week 4 with a chapter dedicated to it.


## What this course does NOT require

Clarifying negatives saves impostor syndrome:

- **Not required:** prior marketplace or multi-tenant experience — that's what you're here to build
- **Not required:** production DevOps career — deploy chapter walks the path
- **Not required:** advanced CSS or design systems — clean and functional beats pretty
- **Not required:** knowing normalization theory upfront — taught when your schema and checkout need it
- **Not required:** memorizing MERN — you'll know it by building it


## Environment setup — what to install before Chapter 2

You don't need every tool on day zero, but before **Chapter 2 (Project skeleton)** you should have:

### Required on your machine

These four are the day-one minimum — nothing else gets installed until a later chapter needs it:

| Tool | Minimum version | Verify with |
|---|---|---|
| **Node.js** | 18 LTS or 20 LTS | `node --version` |
| **npm** (or pnpm/yarn) | Comes with Node | `npm --version` |
| **Git** | Any recent | `git --version` |
| **A code editor** | VS Code, Cursor, etc. | — |

### Required before Chapter 4 (database)

| Tool | Options | Verify with |
|---|---|---|
| **MongoDB** | [MongoDB Atlas](https://www.mongodb.com/cloud/atlas) free tier **or** local install | Connect via Compass or shell |

Atlas is recommended if local install fights your OS. The course cares that you can connect — not how MongoDB arrived on your machine.

### Required before Chapter 13 (images)

| Tool | Note |
|---|---|
| **Cloudinary account** | Free tier; signup when Chapter 13 opens |

### Required before Chapter 19 (payments)

| Tool | Note |
|---|---|
| **Stripe account** | Free signup; test mode keys (`pk_test_`, `sk_test_`) |
| **Stripe CLI** | Recommended for local webhook forwarding in Ch 19 |

Env vars: `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET` (server); `VITE_STRIPE_PUBLISHABLE_KEY` (client).

### Required before Chapter 21 (cache)

| Tool | Note |
|---|---|
| **Redis** | Local install or cloud free tier when Chapter 21 opens |

### Required before Chapter 22 (async jobs)

| Tool | Note |
|---|---|
| **Redis** | Same instance as Chapter 21 — BullMQ uses it as queue backend |
| **BullMQ** | Installed in `server/` when Chapter 22 opens — worker runs as second process |

### Required before Chapter 23 (deploy)

| Tool | Options |
|---|---|
| **Hosting** | Render, Railway, DigitalOcean, Vercel (client), etc. — decided in that chapter |

**Do not spend day one configuring Redis and Cloudinary.** Chapter 2 only needs Node, Git, and an editor.


## Knowledge self-check — honest questions

Before Chapter 2, you should answer **yes** without googling the syntax:

1. Can you create an Express route that returns JSON and a 404 for a missing path?
2. Can you write a React component that fetches data in `useEffect` and displays it?
3. Can you explain the difference between `401 Unauthorized` and `403 Forbidden`?
4. Can you make a Git commit and confirm a file is listed in `.gitignore`?

If **1 or 2** is no, pause this course and build one small CRUD app (todo or notes) first. Medium assumes that baseline.

If **3** is no, search "HTTP 401 vs 403" — ten minutes — then continue.

If **4** is no, search "gitignore tutorial" — fifteen minutes — then continue. Secrets in Git is a one-way mistake.


## Recommended refresh — if rusty

If it's been months since you touched MongoDB or Express, one afternoon of refresh beats a week of friction:

**Express (1–2 hours)** — official "Hello world" + one CRUD route with in-memory array. Remind yourself: `app.get`, `app.post`, `req.body`, middleware.

**React (1–2 hours)** — Vite default template + fetch from a public API (jsonplaceholder.typicode.com). Remind yourself: state, effect, map over array.

**MongoDB (1 hour)** — MongoDB University free "MongoDB Basics" first module **or** create one collection in Atlas, insert three documents in Compass, run one find query.

Stop when comfortable — not when certified.


## Topics the course will teach — do not pre-study deeply

Studying these for days before starting slows you down without context:

| Topic | When the course teaches it |
|---|---|
| Password hashing & JWT | Chapter 6 |
| Role vs ownership authorization | Chapter 8 |
| Mongoose schemas & refs | Chapter 5 |
| N+1 queries & populate | Chapter 14 |
| Price snapshots | Chapter 18 |
| Stripe Payment Intents | Chapter 19 |
| Redis caching & invalidation | Chapter 21 |
| Deployment & HTTPS | Chapter 23 |

Skim JWT before Chapter 6 if curious. Otherwise trust the chapter order.


## Reading pointers — optional before start

Not prerequisites — optional depth if you want momentum:

- **REST API design** — search "REST API best practices MDN" for status code conventions
- **Multi-tenancy intro** — search "multi-tenant SaaS data isolation" for motivation before Week 2
- **Marketplace order flow** — search "marketplace split order multi-vendor checkout" for Week 3 intuition

Never invent URLs. Use official docs or search by topic name.


## Accessibility & workflow notes

**OS:** Linux, macOS, and Windows all work. Commands in the course use bash-style examples; translate `export FOO=bar` to Windows `set` or PowerShell as needed.

**Monorepo:** One Git repo with `server/` and `client/`. One terminal tab each when developing.

**AI tools:** Allowed. If you use Cursor or Copilot, still **read** what you commit — the viva is on you.

**Time:** ~60 hours/week × 4 weeks is the intended pace. Scope is by **chapters finished**, not hours logged. Slow is fine; skipping gates is not.


## If you're under-prepared — two paths

**Path A — Shorter gap (1–3 days):** Build a minimal **single-user todo API + React list** — Express, MongoDB or in-memory, five routes, one page. Then start Chapter 2.

**Path B — Larger gap (1–2 weeks):** Complete one structured Node + React CRUD tutorial end-to-end, then return here. A medium marketplace on weak CRUD foundations frustrates more than it teaches.


## If you're over-prepared — one warning

Senior developers sometimes skip chapters because "they know auth." **Don't skip the gates.** This course's auth chapter isn't only login — it's OTP, refresh token hashing, role in JWT, and the isolation tests that follow. Skim the learn-it sections if bored, but **complete the build steps and tick the checklist.** The viva asks about *this* project's choices, not auth in general.


## Prerequisites summary card

Use the checklist below on this page — tick each item when it is true for you.

✓ JavaScript + async/await
✓ Basic REST API (Express or similar)
✓ React components + useEffect
✓ Git + .gitignore discipline
✓ Node 18+ installed
○ MongoDB concepts (revise if rusty)
○ MongoDB running before Ch 4
○ Cloudinary before Ch 13
○ Stripe account before Ch 19
○ Redis before Ch 21
○ Hosting before Ch 23

When every **✓** item is ticked and MongoDB is ready before Chapter 4, you are standing on the floor this course expects.


## Key ideas from this page

- Prerequisites are the **minimum floor** — easier than the course itself
- **Medium** assumes CRUD + React already; not marketplace experience
- Install **Node + Git** before Chapter 2; **MongoDB** before Chapter 4
- Don't pre-study JWT, Redis, or normalization for days — chapters teach them in context
- If CRUD is shaky, build a tiny app first — don't skip the foundation

Next: the full course outline — every chapter in order with demo targets.
## Before you continue

<OpenQuestion id="01KZAX756B5119R4QFKF3V0CMQ" />
