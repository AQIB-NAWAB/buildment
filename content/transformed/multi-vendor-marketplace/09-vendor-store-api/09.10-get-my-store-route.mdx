---
title: "Get my store route"
module: 9
lesson: 10
summary: "Vendors should not hunt MongoDB ids in Compass. `GET /stores/me` resolves the store from the JWT — and returns **404** when no shop exists yet. That 404 is not an error; Chapter 10 turns it into the onboarding redirect signal."
---
**Why this step matters:** Vendors should not hunt MongoDB ids in Compass. `GET /stores/me` resolves the store from the JWT — and returns **404** when no shop exists yet. That 404 is not an error; Chapter 10 turns it into the onboarding redirect signal.

<BigWordAlert term="GET /stores/me" plainEnglish="Returns the current vendor's store without exposing an ID in the URL — convenience and security." whyItMatters="FreshMarket client calls `/stores/me` on dashboard load — no guessing store IDs." />

**Where the project is now:** Vendor can create a store via POST.

**After this step:** `GET /api/stores/me` returns the authenticated vendor's store — **200** with document, or **404** when they have not opened a shop yet. Chapter 10 uses this contract for the onboarding gate.

## Why `/me` exists

| Without `/me` | With `/me` |
|---|---|
| Client must store `_id` after POST | Client asks "do I have a shop?" anytime |
| Onboarding state unclear after refresh | Single source of truth from API |
| Risk client sends wrong id | Server scopes by JWT only |

## Handler flow (spec)

```
GET /api/stores/me
  1. requireAuth
  2. requireRole('vendor')
  3. getStoreByVendorId(userId)
  4. if null → 404
  5. else → 200 + store JSON
```

No `:id` param — no IDOR surface on this route.

## Service: `getStoreByVendorId(vendorId)`

Single query:

- `Store.findOne({ vendorId })`  
- Return document or null — do not throw for missing  

Route maps null → 404.

## 404 contract (important for Ch 10)

When vendor has **no** store:

| Choice | Status | Body |
|---|---|---|
| **Recommended** | 404 | `{ "message": "Store not found" }` |
| Alternative | 200 | `{ "store": null }` |

**Pick recommended 404** unless you update Chapter 10 client handling accordingly. Document choice in README.

## Response 200

Full store document — same shape as POST 201 response. Client can populate settings form from this payload.

## Error matrix

| Condition | Status |
|---|---|
| No token | 401 |
| Customer role | 403 |
| Vendor, no store | 404 |
| Vendor, has store | 200 |

## curl verify

**Vendor with store** (after 9.9):

<ApiRequestPanel title="Get my store — vendor GET /me">

<ApiRequest
  label="Vendor with store (after 9.9)"
  method="GET"
  url="http://localhost:5000/api/stores/me"
  bearer="TOKEN"
  responseStatus={200}
  responseBody='{"_id":"...","name":"Test Shop","vendorId":"...","description":"Local greens","address":"1 Market St"}'
/>

</ApiRequestPanel>

Expect 200; `_id` matches POST response.

**Vendor without store** (fresh account, no POST):

Expect 404.

**Customer token:**

Expect 403.

## Route ordering reminder

`GET /me` must be registered **before** any `GET /:id` you add later. Express matches in registration order.

## Client preview (Chapter 10)

Onboarding gate pseudologic:

```
on vendor layout load:
  GET /stores/me
  if 404 → redirect /vendor/open-shop
  if 200 → allow dashboard routes
```

You do not build this yet — ensure API contract is stable.

## Update AUTHORIZATION.md

Confirm row for GET /stores/me — auth + vendor role + query scoped by token.

## Commit

```bash
git add server/src/modules/stores/
git commit -m "feat(stores): add GET /stores/me for vendor own store"
```

## ✅ Do / ❌ Don't

**✅ Do:** Query by `vendorId` from JWT only.

**✅ Do:** Return 404 for no store — enables clean onboarding branch.

**❌ Don't:** Return empty 200 with null without documenting — confuses fetch error handling.

**❌ Don't:** Expose other vendors' stores on this route — ever.

## Verify checklist

- [ ] Vendor with store → 200  
- [ ] Fresh vendor → 404  
- [ ] Customer → 403  
- [ ] Unauthenticated → 401  
- [ ] Response matches POST document shape  

<RealWorldEvent title="Etsy shop URLs and seller identity" when="2005–present" summary="Marketplace sellers receive branded storefront URLs while the platform enforces global uniqueness and policy compliance behind the scenes." lesson="Store creation is more than a form — it is the moment a user becomes a vendor with isolated data and public presence." />


## Before you continue

<OpenQuestion id="01M34MR9SANMVV0JX4PE8NX425" />

## Next

**Sub-chapter 9.11:** `PATCH /stores/:id` — partial update with ownership check.
