---
title: "Test with curl"
module: 9
lesson: 12
summary: "The store API must work without React before you build forms. curl proves create → fetch me → update → cross-vendor 403 in one session. If this step fails, Chapter 10 will hide the bugs behind loading spinners."
---
**Why this step matters:** The store API must work without React before you build forms. curl proves create → fetch me → update → cross-vendor 403 in one session. If this step fails, Chapter 10 will hide the bugs behind loading spinners.

**Where the project is now:** POST, GET me, and PATCH implemented.

**After this step:** You have run the **full vendor store lifecycle** twice — happy path and isolation path — and recorded results for the learning log.

## Prerequisites

| Item | Ready |
|---|---|
| Server running with MongoDB | |
| Vendor A credentials or seed | |
| Vendor B credentials or seed | |
| JWT obtain flow from Ch 6 | login → access token |

Use separate terminals: one for server logs, one for curl.

## Test script (spec — you execute)

### Phase 1 — Vendor A creates and reads store

| Step | Request | Expect |
|---|---|---|
| 1 | Login vendor A → `TOKEN_A` | 200 + token |
| 2 | `GET /stores/me` with A (if no store yet) | 404 |
| 3 | `POST /stores` with A — valid body | 201; note `STORE_ID_A` |
| 4 | `GET /stores/me` with A | 200; same `_id` |
| 5 | `POST /stores` again with A | 409 |

### Phase 2 — Vendor A updates store

| Step | Request | Expect |
|---|---|---|
| 6 | `PATCH /stores/STORE_ID_A` with A — change `name` | 200 |
| 7 | `GET /stores/me` with A | 200; new `name` |

### Phase 3 — Isolation

| Step | Request | Expect |
|---|---|---|
| 8 | Login vendor B → `TOKEN_B` | 200 |
| 9 | `PATCH /stores/STORE_ID_A` with B | **403** |
| 10 | `GET /stores/me` with B | 404 or 200 (if B has own seed store) |

### Phase 4 — Auth boundaries

| Step | Request | Expect |
|---|---|---|
| 11 | `POST /stores` no header | 401 |
| 12 | Customer token `POST /stores` | 403 |

## What to capture for learning log

- Redacted token prefix only — never full JWT in Git  
- Status codes for steps 3, 5, 9 — the critical three  
- One sentence if anything unexpected — and fix before 9.14  

Optional: save as `docs/curl-notes/stores-ch9.txt` locally — **do not commit tokens**.

## Common failures

| Symptom | Likely cause |
|---|---|
| POST 500 on duplicate | E11000 not mapped to 409 |
| GET me 200 with wrong store | Query not filtering by vendorId |
| PATCH 200 for vendor B | Ownership check missing or wrong id field |
| 404 on PATCH for owner | Wrong STORE_ID or invalid ObjectId |
| 401 always | Bearer prefix missing; token expired |

## Compass verification

After Phase 2:

- One store document for vendor A with updated `name`  
- `vendorId` still vendor A's user id  

No duplicate stores for vendor A after step 5.

## Regression check

Quick sanity — auth still works:

- `GET /health` → ok  
- Customer can still login — not broken by store routes  

## README update

Document the three store endpoints with:

- Required role  
- 409 on duplicate create  
- 404 on GET me when no store  

## Commit

```bash
git add README.md docs/
git commit -m "docs: document store API lifecycle and curl test notes"
```

Code should already be committed in 9.8–9.11.

## ✅ Do / ❌ Don't

**✅ Do:** Run cross-vendor PATCH before declaring chapter done.

**✅ Do:** Test fresh vendor 404 on GET me before POST.

**❌ Don't:** Paste live tokens into committed files.

**❌ Don't:** Skip 409 test — Chapter 10 double-submit may hit it.

## Verify checklist

- [ ] All 12 steps match expected status codes  
- [ ] 403 on vendor B PATCH vendor A store  
- [ ] 409 on second POST vendor A  
- [ ] Results noted for learning log 9.14  


## Before you continue

<OpenQuestion id="01KZAX76WPBPF2W164HWZDJE5R" />

## Next

**Sub-chapter 9.13:** Recap and what's next — API complete; Chapter 10 builds onboarding UI.
