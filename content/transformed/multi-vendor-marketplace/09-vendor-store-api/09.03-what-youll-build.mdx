---
title: "What you'll build"
module: 9
lesson: 3
summary: "By the end of Chapter 9, a **vendor-authenticated** client (curl for now, browser in Chapter 10) can complete the full store lifecycle on the API. Customers and unauthenticated users cannot create stores. Vendor B cannot update Vendor A's store."
---
# 9.3 — What you'll build

> **[Read]** · Chapter 09 — Vendor store API · *Create + update store API*

By the end of Chapter 9, a **vendor-authenticated** client (curl for now, browser in Chapter 10) can complete the full store lifecycle on the API. Customers and unauthenticated users cannot create stores. Vendor B cannot update Vendor A's store.

## Deliverables checklist (preview)

| Deliverable | Proof |
|---|---|
| Stores module scaffolded | `stores.routes.js`, `stores.service.js`, `stores.schema.js` |
| `POST /api/stores` | Vendor creates store; 201; `vendorId` from token |
| One-store enforcement | Second `POST` → 409 |
| `GET /api/stores/me` | Returns own store; 404 if none |
| `PATCH /api/stores/:id` | Owner updates fields; 403 for wrong vendor |
| Mounted on app | `app.use('/api/stores', storesRoutes)` |
| curl lifecycle test | Documented in learning log |
| Prior gates intact | Auth, role, IDOR from Ch 8 still pass |

## API surface (this chapter)

Base path assumes `/api` prefix from earlier chapters — adjust to match your `app.js`.

| Method | Path | Auth | Role | Purpose |
|---|---|---|---|---|
| `POST` | `/stores` | required | vendor | Create store (once) |
| `GET` | `/stores/me` | required | vendor | Fetch authenticated vendor's store |
| `PATCH` | `/stores/:id` | required | vendor | Update own store |

**Not in Chapter 9:**

- `GET /stores` public list — Chapter 14 catalogue context  
- `GET /stores/:id` public detail — later  
- `DELETE /stores/:id` — out of scope  
- Product routes — Chapter 11  

## Files you will create or extend

```
server/src/modules/stores/
  stores.model.js      ← exists from Ch 5 — verify fields
  stores.routes.js     ← NEW or extend Ch 8 stub
  stores.service.js    ← NEW — business logic
  stores.schema.js     ← NEW — request validation shapes
```

```
server/src/app.js      ← mount stores router (if not already)
```

```
docs/AUTHORIZATION.md  ← add rows for POST/GET me/PATCH stores
```

No client files in Chapter 9 — API-only pair before Chapter 10 UI.

## Request/response contracts (spec level)

### `POST /stores`

**Body (example shape — you define exact validation):**

| Field | Required | Notes |
|---|---|---|
| `name` | yes | Display name — "Maya's Market" |
| `description` | optional | Short editorial blurb |
| `address` | yes or optional per your ERD | Pickup/delivery context |
| `logoUrl` | optional | String URL; upload in later chapter |

**Response 201:** Store document including `_id`, `vendorId`, timestamps.

**Must not accept:** `vendorId` in body — set from `req.user.id` (or your JWT claim name).

### `GET /stores/me`

**Response 200:** Store document owned by caller.

**Response 404:** No store for this vendor yet.

### `PATCH /stores/:id`

**Body:** Partial update — any subset of mutable store fields.

**Response 200:** Updated document.

**Response 403:** Store exists but `vendorId` ≠ caller (via `assertStoreOwner`).

**Response 404:** Store id not found.

## Middleware stack per route

| Route | Middleware order |
|---|---|
| All store routes | `requireAuth` |
| POST, GET me, PATCH | `requireRole('vendor')` |
| PATCH `/:id` | + ownership check on `storeId` param |

Reuse Chapter 8 helpers — do not duplicate ownership logic inline in every handler.

## Ownership chain (reminder)

```
JWT userId (vendor)
    └── Store.vendorId
            └── Product.storeId (Chapter 11)
```

Store creation sets `vendorId` once at insert. Products inherit `storeId` from the vendor's single store.

## Testing plan (9.12)

You will prove with curl:

1. Register/login vendor A → token A  
2. `POST /stores` with token A → 201  
3. `GET /stores/me` with token A → 200, same `_id`  
4. `POST /stores` again with token A → 409  
5. Register/login vendor B → token B  
6. `PATCH /stores/:idA` with token B → 403  
7. `PATCH /stores/:idA` with token A → 200  

Save redacted curl transcript or notes in learning log — not necessarily committed as a script.

## Chapter gate (9.14)

**Done when:**

- Vendor CRUDs own store via API  
- Second store rejected (409)  
- Cross-vendor PATCH → 403  
- `GET /stores/me` works for onboarding detection in Ch 10  

## Reading map

| If you are… | Focus on |
|---|---|
| Ready to code | Skip to 9.7 after skimming 9.4–9.5 |
| Unsure on store fields | Read 9.4 carefully against your ERD |
| Want URL design context | Read 9.5 before PATCH paths |

## ✅ Do / ❌ Don't

**✅ Do:** Keep routes thin — validation in schema module, logic in service.

**✅ Do:** Update `AUTHORIZATION.md` as you add each route.

**❌ Don't:** Add public store listing — scope creep into Chapter 14.

**❌ Don't:** Skip 409 duplicate test — Chapter 10 may call POST twice on double-submit.

## Next

**Sub-chapter 9.4:** Store fields — name, description, address, logoUrl, and what belongs on the document vs derived later.
