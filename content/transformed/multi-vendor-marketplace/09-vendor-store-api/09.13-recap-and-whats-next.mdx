---
title: "Recap and what's next"
module: 9
lesson: 13
summary: "You entered Chapter 9 with authenticated vendors and isolation middleware but no way to open a shop. You leave with a **complete vendor store API** — create once, fetch mine, update with ownership — tested with curl and ready for the browser in Chapter 10."
---
You entered Chapter 9 with authenticated vendors and isolation middleware but no way to open a shop. You leave with a **complete vendor store API** — create once, fetch mine, update with ownership — tested with curl and ready for the browser in Chapter 10.

## What you built — narrative recap

**Stores module:** `stores.routes.js`, `stores.service.js`, `stores.schema.js` alongside existing model.

**POST /api/stores:** Vendor creates store; `vendorId` from JWT; duplicate → 409.

**GET /api/stores/me:** Vendor fetches own store; 404 when storeless — onboarding signal.

**PATCH /api/stores/:id:** Partial update; `assertStoreOwner` → 403 for cross-vendor.

**Authorization matrix updated:** Store actions documented in `docs/AUTHORIZATION.md`.

**curl lifecycle:** Create → read → update → isolation verified.

## Your repo tree now

```
server/src/modules/stores/
  stores.model.js
  stores.routes.js      ← NEW
  stores.service.js     ← NEW
  stores.schema.js      ← NEW
```

```
docs/AUTHORIZATION.md   ← store rows added
```

Client **unchanged** — intentional until Chapter 10.

## Decisions you can defend in a viva

1. **Why one store per vendor?** — Simplifies cart split, ownership, onboarding.

2. **Why 409 on second POST?** — Valid auth, business rule violation — not 403.

3. **Why GET /stores/me?** — Token-scoped; no client-supplied vendor id.

4. **Why 403 not 404 on cross-vendor PATCH?** — Consistent with Chapter 8 IDOR tests.

5. **Why vendorId not in request body?** — Prevents privilege escalation.

## Week 2 phase progress

| Demo target | Status |
|---|---|
| Vendor registers & logs in | ✓ |
| Vendor creates store via API | ✓ **this chapter** |
| Vendor opens shop in UI | Chapter 10 |
| Vendor adds product | Chapter 11–12 |
| Product photo | Chapter 13 |


## Before you continue

<OpenQuestion id="01KZ63P59ZKGW7RENNV5KM3KS9" />

## Next chapter — Open your shop (Chapter 10)

Chapter 10 is the **frontend pair** to Chapter 9:

- `layouts/VendorLayout.jsx` and `pages/vendor/`  
- Store setup form → `POST /stores`  
- Store settings → `PATCH /stores/:id`  
- Onboarding redirect: no store → `/vendor/open-shop`  
- Storeless vendor **cannot** reach product dashboard routes  

**API contract you must honour:**

- `GET /stores/me` → 404 means show open-shop flow  
- `POST /stores` → 409 show friendly "you already have a store"  
- Use same field names as 9.4 validation  

## What's explicitly not built yet

| Not yet | Chapter |
|---|---|
| Vendor product CRUD API | 11 |
| Vendor dashboard UI | 12 |
| Public store catalogue | 14 |
| Store logo upload | optional / later |

## Suggested commit history through Chapter 9

```
feat(stores): scaffold stores module routes service and validation
feat(stores): add POST create store with one-store enforcement
feat(stores): add GET /stores/me for vendor own store
feat(stores): add PATCH update store with ownership check
docs: document store API lifecycle and curl test notes
docs: complete chapter 9 learning log
```

## Key milestones unlocked

- [x] Auth + role + ownership (Ch 6–8)  
- [x] Vendor store API (Ch 9)  
- [ ] Vendor onboarding UI (Ch 10)  
- [ ] Vendor products API (Ch 11)  
- [ ] Vendor dashboard UI (Ch 12)  

## Continue to gate

**Sub-chapter 9.14:** Definition of Done checklist + learning log.

**Do not open Chapter 10 until 9.14 is fully ticked.**

Your vendors can open a shop via API — Chapter 10 gives them a door.
