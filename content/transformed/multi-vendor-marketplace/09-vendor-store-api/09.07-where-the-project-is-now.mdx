---
title: "Where the project is now"
module: 9
lesson: 7
summary: "Chapter 9 is the first feature built on top of Chapter 8's guards. This snapshot confirms isolation middleware exists and two vendor tokens are ready — so your first real `POST /stores` does not ship on a broken foundation."
---
**Why this step matters:** Chapter 9 is the first feature built on top of Chapter 8's guards. This snapshot confirms isolation middleware exists and two vendor tokens are ready — so your first real `POST /stores` does not ship on a broken foundation.

**Build cluster starts here.** Confirm Chapter 8 gate before creating store routes. This sub-chapter is a **continuity snapshot** — what exists on disk, what Chapter 9 adds, and what stays untouched.

## Repo tree entering Chapter 9

```
your-marketplace/
├── docs/
│   ├── erd.md (or erd-draft.md)
│   └── AUTHORIZATION.md          ← Ch 8 matrix
│   └── 08-authorization-and-isolation.md
├── server/
│   └── src/
│       ├── app.js                ← auth mounted; stores stub optional
│       ├── modules/
│       │   ├── auth/             ← requireAuth, requireRole
│       │   ├── stores/
│       │   │   └── stores.model.js
│       │   ├── products/         ← model only
│       │   └── ...
│       └── shared/
│           ├── config.js
│           ├── db.js
│           └── ownership.js      ← assertStoreOwner
└── client/
    └── src/
        ├── hooks/useAuth.js      ← Ch 7
        └── pages/Login.jsx       ← no vendor shop UI yet
```

## What already works

| Capability | Verified how |
|---|---|
| Vendor register + login | UI or curl from Ch 6–7 |
| JWT on protected routes | 401 without token |
| `requireRole('vendor')` | Customer gets 403 on vendor routes |
| Store Mongoose model | Compass or seed — document with `vendorId` |
| IDOR stub | Vendor B → 403 PATCH Vendor A store (Ch 8) |

## What is missing (Chapter 9 fills)

| Gap | Sub-chapter |
|---|---|
| `stores.routes.js` full implementation | 9.8 |
| `stores.service.js` create/get/update | 9.9–9.11 |
| `stores.schema.js` validation | 9.8 |
| `POST /stores` one-store rule | 9.9 |
| `GET /stores/me` | 9.10 |
| Complete `PATCH /stores/:id` | 9.11 |
| curl lifecycle proof | 9.12 |

## Store model sanity check

Open `stores.model.js` and confirm:

- [ ] `vendorId` refs User — required, indexed  
- [ ] Unique index on `vendorId` (or equivalent one-store enforcement)  
- [ ] Fields: `name`, `description`, `address`, `logoUrl` per 9.4  
- [ ] `timestamps: true` or manual dates  

If unique index missing, add in 9.8 before duplicate-create tests.

## Seed vs fresh vendor

| Test account | Use for |
|---|---|
| Seed vendor with existing store | GET me 200; PATCH; duplicate POST → 409 |
| Fresh registered vendor | Full create flow POST → 201 |

Both should exist before 9.12.

## `app.js` mount point

Verify stores router mount path matches client expectations later:

```
/api/stores   ← typical
```

Auth routes likely `/api/auth` — keep prefix consistent.

## Client intentionally unchanged

Chapter 9 is **API-only**. Home page still shows health — no store UI. Chapter 10 consumes `GET /stores/me` and `POST /stores`.

## ✅ Do / ❌ Don't

**✅ Do:** Re-run Chapter 8 IDOR test before modifying PATCH — baseline must pass.

**✅ Do:** Commit any uncommitted Ch 7–8 work before 9.8.

**❌ Don't:** Start client pages in this chapter — breaks backend/frontend pair order.

**❌ Don't:** Change User or Product schemas unless blocking — note in log.

## Verify before 9.8

- [ ] Server starts; MongoDB connected  
- [ ] Can obtain vendor JWT via login  
- [ ] `docs/AUTHORIZATION.md` open for updates  
- [ ] Store model reviewed  

<RealWorldEvent title="Etsy shop URLs and seller identity" when="2005–present" summary="Marketplace sellers receive branded storefront URLs while the platform enforces global uniqueness and policy compliance behind the scenes." lesson="Store creation is more than a form — it is the moment a user becomes a vendor with isolated data and public presence." />


## Before you continue

<OpenQuestion id="01KZP7KPSDK7FVCEZNZYTPNJMH" />

## Next

**Sub-chapter 9.8:** Scaffold the stores module — routes, service, schema files and router mount.
