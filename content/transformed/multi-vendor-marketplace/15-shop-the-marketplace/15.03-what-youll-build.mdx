---
title: "What you'll build"
module: 15
lesson: 3
summary: "By chapter end, customers open **Shop**, filter and paginate the catalogue, open **product detail**, and see items from multiple vendors — all driven by Chapter 14 API."
---
By chapter end, customers open **Shop**, filter and paginate the catalogue, open **product detail**, and see items from multiple vendors — all driven by Chapter 14 API.

Blueprint for 15.4–15.10. Specs only.

## The customer journey this chapter completes

```
Home / nav  →  /shop (grid + filters)  →  /shop/:id (detail + stub button)
                     ↑                           ↑
              GET /products                 GET /products/:id
```

No new server routes. Every screen **consumes** Chapter 14. If the API returns wrong data, fix the API — do not patch around it in React with hardcoded arrays.

**Multi-vendor proof for gate:** Grid must show products from **at least two different `store.name` values** at once — that is what makes FreshMarket a marketplace, not a single-shop app.

## Deliverables checklist

| Artifact | Path | Purpose |
|---|---|---|
| Shop page | `client/src/pages/Shop.jsx` | Grid + filters + pagination |
| Detail page | `client/src/pages/ProductDetail.jsx` | Single product + add-to-cart stub |
| Product card | `client/src/components/ProductCard.jsx` | Reusable card |
| Filter bar | `client/src/components/CatalogueFilters.jsx` (or inline) | category + storeId |
| API helpers | `client/src/api/products.js` | `getProducts(params)`, `getProduct(id)` |
| Routes | `App.jsx` | `/shop`, `/shop/:productId` |
| Nav link | Header/layout | Link to shop from home |
| Learning log 
**Not in chapter:** Cart API calls (stub button only), checkout.

## Routes contract

| Path | Component | Auth |
|---|---|---|
| `/shop` | Shop.jsx | Public (optional login) |
| `/shop/:productId` | ProductDetail.jsx | Public |

Use `productId` param name consistently with API `:id`.

## API client functions

### `getProducts(params)`

| Param | Maps to query |
|---|---|
| `page` | `page` |
| `limit` | `limit` |
| `category` | `category` |
| `storeId` | `storeId` |

**Returns:** `{ items, page, limit, total, totalPages }` — typed or JSDoc your choice.

### `getProduct(id)`

**Returns:** single product object — same shape as list item + description if present.

Errors: 404 → user message on detail page.

## Shop page behaviour spec

| Feature | Spec |
|---|---|
| Initial load | `GET /products?page=1&limit=20` |
| Filter change | Reset to page 1; refetch with params |
| Pagination | Page buttons or Prev/Next using `totalPages` |
| URL sync | **Stretch:** `?category=` in searchParams |
| Loading | Show loading state per fetch |

## Filter bar spec

| Control | Options source |
|---|---|
| Category | Hardcoded list matching seed OR derive from first full fetch (stretch) |
| Store | Fetch stores list **stretch** OR hardcode seed store ids + names for MVP |

**MVP acceptable:** categories from fixed array in constants file matching seed.

Changing filter updates query and refetches — no client-side filter of full catalogue (API paginates).

## Product detail spec

| Element | Source |
|---|---|
| Large image | imageUrl |
| Name, price, unit, description | API |
| Store name | store.name |
| Add to cart button | Visible; onClick stub — `console.log` or toast "Coming in Ch 17" |

Chapter 17 wires POST /cart.

## Add-to-cart stub contract

```javascript
// Pseudocode — 15.9
function handleAddToCart() {
  // TODO Ch 17: POST /cart with productId, storeId, quantity
  alert('Cart wiring in Chapter 17')
}
```

Button must exist for layout — gate does not require working cart yet.

## Verification (15.10)

- Shop loads without login  
- Grid shows photos from Cloudinary URLs  
- Filter by category reduces grid  
- Pagination changes products  
- Detail page for product from Store A and Store B  
- Two different store names visible on shop grid

## ✅ Do / ❌ Don't

**✅ Do:** Pass `store.id` from detail to future cart payload — needed Ch 16.

**✅ Do:** Use same ProductCard field names as API DTO.

**❌ Don't:** Duplicate publish/stock filter in client — trust API.

**❌ Don't:** Block shop behind customer login unless product requirement says so — public browse OK.

## Build map

| Step | Sub-chapter |
|---|---|
| Recap | 15.4 |
| Scaffold pages + routes | 15.5 |
| ProductCard | 15.6 |
| Filters | 15.7 |
| Pagination on shop | 15.8 |
| Detail + stub | 15.9 |
| Verify two vendors | 15.10 |


## Before you continue

<OpenQuestion id="01KZ5FW4QKS4KEBS5X0BDK77BX" />

## Next

**Sub-chapter 15.4:** Where the project is now — client tree before shop pages.

Build cluster starts.
