---
title: "Verify browse UI"
module: 15
lesson: 10
summary: "**Why this step matters:** End-to-end browser proof — **two vendors on one grid** — is the Week 3 browse milestone. This step catches broken filters, missing photos, and wrong API wiring before cart work begins."
---
**Why this step matters:** End-to-end browser proof — **two vendors on one grid** — is the Week 3 browse milestone. This step catches broken filters, missing photos, and wrong API wiring before cart work begins.

**Where the project is now:** Shop, filters, pagination, and detail implemented separately.

**After this step:** Full customer browse path demonstrated — **two vendors' products** visible with filters working.

## End-to-end test script

| # | Action | Expected |
|---|---|---|
| 1 | Open `/shop` without login | Grid loads |
| 2 | Count distinct store names on page 1 | ≥2 if seed supports |
| 3 | Filter by Store A | Only Store A products |
| 4 | Clear → filter by category | Subset correct |
| 5 | Go to page 2 if exists | Different products |
| 6 | Click product from Store B | Detail shows Store B name |
| 7 | Add to cart stub | Console/log shows productId + storeId |
| 8 | Cloudinary images | Network img requests to res.cloudinary.com |

Screenshot shop grid with two store names visible for learning log.

## Multi-vendor acceptance

**Gate requirement:** Customer can browse products from **at least two vendors** in one session.

If only one store in seed — re-run seed or publish products from second vendor before gate.

## Regression checks

| Area | Check |
|---|---|
| Vendor dashboard | Still works — unrelated routes |
| Public API | Still no auth on GET /products |
| Vendor login | Can still open /shop as vendor user |
| Mobile width | Grid readable — 1-2 columns |

## Performance sanity

- Shop page: one GET /products per page/filter change — not one GET per card  
- Images load from CDN — not through API proxy  

## README — customer browse

Add **Browse as customer**:

1. Start server + client  
2. Visit `/shop`  
3. Use filters and pagination  
4. Open product detail  

Note add-to-cart completes in Chapter 17.

## Commit

```bash
git add README.md
git commit -m "docs: document customer shop browse verification"
```

Fix any UI bugs in prior commits.

## ✅ Do / ❌ Don't

**✅ Do:** Test without customer login if browse is public.

**✅ Do:** Document hardcoded store ids in constants if used for filter dropdown.

**❌ Don't:** Pass gate with single-vendor seed only.

**❌ Don't:** Skip detail page test — store name must appear there too.

## Verify checklist

- [ ] Two store names on shop grid  
- [ ] Filters refetch API correctly  
- [ ] Pagination works  
- [ ] Detail + stub button  
- [ ] README updated


## Before you continue

<OpenQuestion id="01KZ63P5FJHG788EDTTZ88BCPK" />

## Next

**Sub-chapter 15.11:** Recap and what's next — Cart API Chapter 16.

Browse UI done; persistence comes next on backend.
