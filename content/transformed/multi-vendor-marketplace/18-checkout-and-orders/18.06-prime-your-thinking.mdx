---
title: "Prime your thinking"
module: 18
lesson: 6
summary: "Before scaffolding the orders module, read how production commerce systems avoid **double charge** and **duplicate orders**. Chapter 18 builds checkout **without** payment; Chapter 19 adds Stripe — but both share the same **at-most-once** instinct."
---
Before scaffolding the orders module, read how production commerce systems avoid **double charge** and **duplicate orders**. Chapter 18 builds checkout **without** payment; Chapter 19 adds Stripe — but both share the same **at-most-once** instinct.

## Extra FAQ before the build cluster

**Q: I'm scared of checkout — where do I start?**  
Read 18.3 blueprint. Build in order: scaffold module (18.9) → checkout skeleton (18.10) → snapshots (18.11) → stock (18.12) → clear cart (18.13) → list routes (18.14–15) → snapshot test (18.16). One sub-chapter per sitting.

**Q: Do I need payment before orders?**  
Not in Chapter 18. Order = "customer committed to buy" on the server — `paymentStatus: 'pending'`. Chapter 19 adds the Stripe gate; your Ch 18 gate is correct order documents + stock + empty cart.

**Q: What's idempotency in one sentence?**  
Sending the same checkout request twice should not create two order sets — production uses idempotency keys; MVP documents double-click risk.

**Q: Transactions vs no transactions?**  
Transactions bundle "decrement stock + insert orders + clear cart" into all-or-nothing. If your local MongoDB lacks them, document compensating steps in learning log — do not silently skip fail-all semantics.

## Mandatory read topics

| Topic | Search terms | Why |
|---|---|---|
| Idempotency keys | Stripe idempotency key docs | Same POST twice → one charge |
| MongoDB transactions | Mongoose `withTransaction` | Multi-order + stock + cart clear |
| Optimistic concurrency | `findOneAndUpdate` filter patterns | Stock decrement you build in 18.12 |

**Time box:** 45 minutes reading. Notes in learning log optional.

## Idempotency — concept for your README

**Problem:** Customer double-clicks "Place order". Two `POST /checkout` → two order sets.

**Production fix:** Client sends `Idempotency-Key: uuid`; server stores key → response mapping; replay returns same response.

**Course MVP:** Document as known limitation. Optional stretch: accept `Idempotency-Key` header and reject duplicate within 24h.

## Questions to answer after reading

1. What is the difference between **idempotent** and **safe** HTTP methods?
2. Why is checkout POST neither safe nor idempotent by default?
3. Where would you store idempotency keys in MongoDB?
4. How does a transaction help when inserting two orders and clearing cart?

## Connection to atomic stock (18.5)

Idempotency prevents duplicate orders from **retries**. Conditional `$inc` prevents oversell from **concurrency**. Both matter in production; Chapter 18 gate requires **stock atomicity** — idempotency is stretch.

## ✅ Do / ❌ Don't

**✅ Do:** Mention double-submit risk in README under checkout.

**✅ Do:** Use transactions if Atlas/replica set available.

**❌ Don't:** Block gate on idempotency implementation.

**❌ Don't:** Skip reading transactions because "local Mongo is simple".

## Self-check

- [ ] I read at least one idempotency article
- [ ] I read Mongoose transaction docs
- [ ] I can explain double-click risk in one sentence


## Before you continue

<OpenQuestion id="01KZ63P5HP7PGE9TB8913C2CM7" />

## Next

**Sub-chapter 18.7:** Quiz — reserved.
