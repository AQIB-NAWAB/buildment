---
title: "Test snapshot immutability"
module: 18
lesson: 16
summary: "**Why this step matters:** This is the **proof** snapshots work — change product price after checkout, reload orders, old price must remain. Do not skip; it catches populate-on-read mistakes that pass every other test."
---
**Why this step matters:** This is the **proof** snapshots work — change product price after checkout, reload orders, old price must remain. Do not skip; it catches populate-on-read mistakes that pass every other test.

<BigWordAlert term="Snapshot immutability test" plainEnglish="Change product price after order — verify order line price unchanged in database." whyItMatters="FreshMarket gate proves receipts are trustworthy — snapshot not live product reference." />

**Where the project is now:** Checkout, lists, stock, and cart clear implemented.

**After this step:** You have **proof** that changing a product price does not rewrite historical order lines.

## Test script — run in order

<ApiRequestPanel title="Snapshot immutability proof">

<ApiRequest
  label="3. GET /orders/my — record priceAtPurchase after checkout"
  method="GET"
  url="http://localhost:5000/api/orders/my"
  bearer="$CUSTOMER_TOKEN"
  responseStatus={200}
  responseBody='[{"orderItems":[{"nameAtPurchase":"Organic Kale","priceAtPurchase":2.5,"quantity":1}]}]'
/>

<ApiRequest
  label="4. Vendor PATCH product price (P1 → P2)"
  method="PATCH"
  url="http://localhost:5000/api/products/PRODUCT_ID"
  bearer="$VENDOR_TOKEN"
  body='{"price":4.99}'
  responseStatus={200}
  responseBody='{"_id":"PRODUCT_ID","price":4.99}'
/>

<ApiRequest
  label="5. GET /orders/my again — priceAtPurchase unchanged (still P1)"
  method="GET"
  url="http://localhost:5000/api/orders/my"
  bearer="$CUSTOMER_TOKEN"
  responseStatus={200}
  responseBody='[{"orderItems":[{"nameAtPurchase":"Organic Kale","priceAtPurchase":2.5,"quantity":1}]}]'
/>

<ApiRequest
  label="6. GET /vendor/orders — vendor view also shows P1"
  method="GET"
  url="http://localhost:5000/api/vendor/orders"
  bearer="$VENDOR_TOKEN"
  responseStatus={200}
  responseBody='[{"orderItems":[{"nameAtPurchase":"Organic Kale","priceAtPurchase":2.5}]}]'
/>

</ApiRequestPanel>

1. Note product **P** current price `P1` from shop or Compass.
2. As customer, add **P** to cart; checkout (single-store cart is fine).
3. `GET /orders/my` — record `priceAtPurchase` on line → must equal `P1`.
4. As vendor owner of **P**, `PATCH /products/:id` price to `P2` (different value).
5. `GET /orders/my` again — **same order**, `priceAtPurchase` still `P1`.
6. `GET /vendor/orders` — vendor view also shows `P1`.
7. Shop browse shows new price `P2` for new customers.

## Compass verification

Open Order document in Compass. Inspect embedded `orderItems` — numeric fields unchanged after product PATCH.

## What failure looks like

| Symptom | Bug |
|---|---|
| Order line shows P2 after PATCH | Populate live product on read, or no snapshot at write |
| `total` changed on order header | Recomputed from live data |
| lineTotal ≠ priceAtPurchase × quantity | Snapshot math wrong at checkout |

## Document in README or learning log

Section **Order price immutability** — three-step test summary. Mentors run this in viva.

## Commit

```bash
git add README.md
git commit -m "docs: document order snapshot immutability test"
```

## ✅ Do / ❌ Don't

**✅ Do:** Run test before Chapter 20 UI — API must be correct first.

**✅ Do:** Screenshot or paste redacted JSON in learning log.

**❌ Don't:** Mark gate complete if populate overwrites display price.

**❌ Don't:** Edit snapshot fields manually in Compass to "fix" UI — fix code.

## Verify checklist

- [ ] Price change test passes
- [ ] Shop shows new price for new adds
- [ ] Old order unchanged in Compass

<RealWorldEvent title="Double-charge incidents during flash sales" when="Various" summary="E-commerce outages and retries caused duplicate charges until teams adopted idempotent payment APIs and clear order states." lesson="Model order status explicitly — pending, paid, failed — and never create a second order for the same checkout attempt." />


## Before you continue

<OpenQuestion id="01M34MRB6NK03EK8RMBG1KYTZQ" />

## Next

**Sub-chapter 18.17:** Recap and what's next — with snapshots proven immutable, close the chapter and check the gate.
