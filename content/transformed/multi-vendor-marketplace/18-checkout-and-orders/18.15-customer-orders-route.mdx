---
title: "Customer orders route"
module: 18
lesson: 15
summary: "**Why this step matters:** Customers need **order history** without re-running checkout — `GET /orders/my` returns embedded snapshot lines only, training the read path Chapter 20 UI will use."
---
**Why this step matters:** Customers need **order history** without re-running checkout — `GET /orders/my` returns embedded snapshot lines only, training the read path Chapter 20 UI will use.

**Where the project is now:** Vendor list route works; customer needs history without re-running checkout.

**After this step:** `GET /orders/my` returns all orders for authenticated customer, split orders visible as separate entries.

## Route contract

```
GET /api/orders/my
Authorization: Bearer <customer-token>
```

**Middleware:** `requireAuth`, `requireRole('customer')`.

## listOrdersForCustomer(userId) — spec

```
Order.find({ customerId: userId }).sort({ createdAt: -1 })
```

Populate `storeId` with store **name** for display — optional but helps Chapter 20 UI.

**Never populate product price** — display from `orderItems` snapshots only.

## Split orders appearance

One checkout creating two orders → customer history shows **two rows** — same `createdAt` roughly, different `storeId` / store name. UI may group "checkout session" later — not required for gate.

## Response contract

Array or `{ orders: [...] }` — match vendor route envelope style for client helper consistency.

Each order includes:

- `_id`, `status`, `total`, `createdAt`
- `store` or populated `storeId`
- `orderItems` with snapshot fields

## Isolation test

Customer A orders → Customer B `GET /orders/my` does not include A's orders.

## curl verify

```bash
curl -s http://localhost:5000/api/orders/my \\
  -H "Authorization: Bearer <customer-token>"
```

After two-store checkout, expect **two orders** in array.

## ✅ Do / ❌ Don't

**✅ Do:** Sort descending by `createdAt`.

**✅ Do:** Use snapshot fields in JSON — mentor can verify without Compass.

**❌ Don't:** Merge split orders into one API response object.

**❌ Don't:** Allow vendor token on customer route.

## Verify checklist

- [ ] Customer sees both orders from multi-store checkout
- [ ] Snapshot prices in response
- [ ] Customer B isolation confirmed


## Before you continue

<OpenQuestion id="01KZ5FW51449N28SVGZG29GQF3" />

## Next

**Sub-chapter 18.16:** Test snapshot immutability.
