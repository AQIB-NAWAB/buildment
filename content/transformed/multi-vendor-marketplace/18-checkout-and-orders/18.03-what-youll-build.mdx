---
title: "What you'll build"
module: 18
lesson: 3
summary: "Blueprint for the **orders module** and checkout flow. Read once; verify against repo after 18.16."
---
Blueprint for the **orders module** and checkout flow. Read once; verify against repo after 18.16.

## The three endpoints in plain English

| Route | Who calls it | What it does |
|---|---|---|
| `POST /checkout` | Logged-in customer | Cart → N orders (one per store), stock down, cart cleared |
| `GET /orders/my` | Customer | "Show my past orders" — snapshot prices only |
| `GET /vendor/orders` | Vendor | "Show orders for **my** store only" |

Checkout is the **big bang** — everything else is read-only lists. Do not build list routes before checkout works; there is nothing to list yet.

**Success picture:** Cart with items from Store A and Store B → one POST → response with **two orders**, each with embedded snapshot lines, cart empty, stock reduced.

## Server tree at end of Chapter 18

```
server/src/modules/orders/
  orders.routes.js       ← POST /checkout, GET /orders/my, GET /vendor/orders
  orders.service.js      ← split, snapshot, stock, clear cart
  orders.schema.js       ← validation for checkout body if needed
```

Reuse Order model from Chapter 5 — extend only if snapshot fields missing.

```
server/src/modules/cart/
  (existing)             ← read cart in checkout; clear on success
```

## Endpoints contract

| Method | Path | Auth | Purpose |
|---|---|---|---|
| POST | `/checkout` | Customer | Cart → N orders; stock dec; clear cart |
| GET | `/orders/my` | Customer | Own orders, newest first |
| GET | `/vendor/orders` | Vendor | Orders for vendor's store only |

Mount under `/api` per your app convention.

## POST /checkout — behaviour contract

**Input:** Authenticated customer. Body empty or `{ note }` optional — cart is source of truth. (`paymentIntentId` required starting in Chapter 19.)

**Process (spec — you implement):**

1. Load user's cart with products; reject empty cart → 400.
2. Validate every line: product exists, published, stock ≥ quantity.
3. Group lines by `storeId`.
4. For each group, build Order document:
   - `customerId` = auth user
   - `storeId` = group key
   - `status` = `pending` (or your enum default)
   - `paymentStatus` = `pending` (Chapter 19 sets `paid` after Stripe)
   - `stripePaymentIntentId` — added in Chapter 19 at gated checkout
   - `orderItems[]` = embedded snapshots per line
   - `total` = sum of snapshot line totals
5. Decrement stock **atomically** per product — fail entire checkout if any decrement fails.
6. Insert all orders — prefer transaction if replica set available; document if single-node.
7. Delete all cart items for user.
8. Return `{ orders: [...] }` — array length = number of stores in cart.

**Idempotency:** Out of scope for MVP in Ch 18 — double-click may double order; note in README. Chapter 18.6 covers idempotency reads; Chapter 19 applies them to Payment Intents and checkout.

## Order line snapshot shape

```javascript
// orderItems[] element — illustrative
{
  productId: ObjectId,      // reference optional for reorder
  nameAtPurchase: String,
  priceAtPurchase: Number,
  unitAtPurchase: String,
  quantity: Number,
  lineTotal: Number         // priceAtPurchase * quantity
}
```

**You implement** — field names must match Order schema from Ch 5.

## GET /orders/my response contract

Array of orders for `customerId === auth.userId`. Each order includes embedded `orderItems` — no live product price. Populate `storeId` name optional for display.

## GET /vendor/orders response contract

Vendor role only. Orders where `storeId` belongs to auth vendor's store. Populate `customerId` with safe fields (name or email) — not password hash.

Cross-vendor: Vendor B must not see Vendor A's orders — 403 or empty, enforced by store ownership lookup.

## Definition of Done — preview

Full checklist in 18.18. Headlines:

- [ ] Two-store cart → two orders on checkout
- [ ] Snapshots verified after price change
- [ ] Stock decremented
- [ ] Cart empty after success
- [ ] Vendor and customer list routes scoped

## Pace through build steps

| Sub-chapter | You'll build |
|---|---|
| 18.8 | Continuity recap |
| 18.9 | Scaffold orders module |
| 18.10 | POST /checkout grouping |
| 18.11 | Snapshot fields on lines |
| 18.12 | Atomic stock decrement |
| 18.13 | Clear cart |
| 18.14 | Vendor orders route |
| 18.15 | Customer orders route |
| 18.16 | Snapshot immutability test |


## Before you continue

<OpenQuestion id="01KZ5P6BKKM3E7P6H63Q9STX4D" />

## Next

**Sub-chapter 18.4:** Normalization payoff — why embedded snapshots are the right denormalization here.
