---
title: "Checklist and learning log"
module: 16
lesson: 15
summary: "Gate for Chapter 16 — Cart API. Tick every box and answer the learning log on this page before continuing."
---
# 16.15 — Checklist and learning log
> **[Gate]** · Chapter 16 — Cart API

This is the **gate** for Chapter 16. Tick every box honestly and answer the learning log below before opening Chapter 17.

## Chapter 16 — Cart API checklist (gate)

Do **not** open Chapter 17 until every box is ticked and learning log answers are written below.

### Module & auth

- [ ] `cart.routes.js`, `cart.service.js`, `cart.schema.js` exist
- [ ] Router mounted on app with API prefix consistent with project
- [ ] **All** cart routes require authentication — 401 without token
- [ ] userId taken from JWT only — never from request body

### POST /cart

- [ ] Accepts `productId`, `storeId`, `quantity`
- [ ] Validates product exists and is published / in stock
- [ ] Validates `product.storeId` matches request `storeId` — mismatch → 400
- [ ] Rejects quantity > stockQty and stockQty === 0
- [ ] Creates CartItem with userId, productId, storeId, quantity

### GET /cart

- [ ] Returns only current user's items
- [ ] Populates product (name, price, unit, imageUrl) and store (name)
- [ ] Empty cart → 200 with `items: []`
- [ ] No N+1 populate loop

### PATCH /cart/:itemId

- [ ] Updates quantity for own item only
- [ ] Other user's itemId → 404 or 403
- [ ] Stock re-validated on increase
- [ ] quantity &lt; 1 rejected — use DELETE

### DELETE /cart/:itemId

- [ ] Removes own item only
- [ ] Cross-user delete fails
- [ ] Item removed from subsequent GET

### Multi-vendor & isolation (16.13)

- [ ] One customer cart holds items from **≥2 storeIds** (tested)
- [ ] User A cannot GET User B's cart contents via itemId manipulation
- [ ] User B PATCH/DELETE on A's itemId fails
- [ ] Compass shows correct userId on all cart documents

### Stock

- [ ] Shared stock validation on POST and PATCH
- [ ] Stock **not** decremented on add to cart

### Docs

- [ ] README documents cart endpoints with curl examples
- [ ] Login required for cart noted

### Prior gates

- [ ] Chapter 15 shop UI complete
- [ ] Chapter 14 catalogue API complete
- [ ] Auth API complete
- [ ] Explain storeId on cart line vs lookup at checkout
- [ ] Explain user scoping query pattern
- [ ] Explain stock check timing vs checkout decrement
- [ ] Explain IDOR risk on cart itemId

### Learning log — write now

Answer **in your own words** in the fields below:

1. **storeId:** Why is storeId stored on the cart line? Walk through validation on POST when client sends wrong storeId.

2. **CRUD flow:** Describe curl sequence: add two products from different stores, GET cart, PATCH one, DELETE one.

3. **Isolation:** How did you test User B accessing User A's item? What status code and why?

4. **Populate:** How does GET /cart avoid N+1? Which fields populated on product vs store?

5. **Stock:** What happens adding quantity 5 when stockQty is 3? What happens at checkout if stock drops after add?

6. **Upsert:** If same product added twice, one line or two? How is quantity computed?

7. **Multi-vendor JSON:** Paste redacted GET /cart showing two storeIds.

8. **Chapter 17 preview:** How will cart page group items by store for display?


## All boxes ticked?

Mark this chapter complete in the platform, then continue to **Your cart** (Chapter 17).

## Troubleshooting

| Blocker | Fix |
|---|---|
| 401 always | Token; requireAuth |
| Store mismatch always | storeId from product.store in client |
| Empty GET after POST | userId mismatch JWT vs saved |
| B can PATCH A's item | Missing userId in findOneAndUpdate |
| populate undefined | Wrong ref path names |
| Stock always fails | seed stockQty 0 |

---

## Phase progress — Week 3 (partial)

| Week 3 demo target | Status |
|---|---|
| Browse marketplace | ✓ Ch 15 |
| Cart API multi-vendor | ✓ Ch 16 |
| Cart UI two stores | Chapter 17 |
| Checkout split orders | Chapter 18 |

---

## Final gate statement

*"Cart API complete: POST/GET/PATCH/DELETE scoped to authenticated user, storeId on lines validated against product, stock checks on write, multi-vendor cart proven, User B cannot access User A cart. Ready for Chapter 17 cart UI."*

**All boxes ticked? Open Chapter 17.**
