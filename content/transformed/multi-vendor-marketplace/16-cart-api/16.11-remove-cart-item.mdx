---
title: "Remove cart item"
module: 16
lesson: 11
summary: "**Why this step matters:** DELETE completes CRUD — customers must remove mistakes before checkout. Same ownership rule as PATCH: only the cart owner can delete their line."
---
**Why this step matters:** DELETE completes CRUD — customers must remove mistakes before checkout. Same ownership rule as PATCH: only the cart owner can delete their line.

**Where the project is now:** Cart lines can be added and quantity updated; no delete.

**After this step:** `DELETE /cart/:itemId` removes one line from current user's cart.

## DELETE /cart/:itemId contract

| Field | Value |
|---|---|
| Auth | Required |
| Param | itemId — CartItem id |
| Success | 200 with message or 204 No Content |

## Handler sequence

1. `findOneAndDelete({ _id: itemId, userId: currentUser })`  
2. If null → 404  
3. Else → 204 or 200 `{ message: "Removed" }`  

Same user scoping as PATCH — never delete by `_id` alone.

## Response options

| Style | When |
|---|---|
| 204 No Content | Minimal REST |
| 200 + remaining cart | UX friendly for SPA — optional |

Pick one; document in README. Chapter 17 UI may refetch GET /cart after delete either way.

## curl tests

```bash
curl -i -X DELETE http://localhost:5000/api/cart/ITEM_ID \
  -H "Authorization: Bearer CUSTOMER_A_TOKEN"

curl -i -X DELETE http://localhost:5000/api/cart/A_ITEM_ID \
  -H "Authorization: Bearer CUSTOMER_B_TOKEN"
```

After delete — GET cart no longer includes item.

## Commit

```bash
git commit -m "feat(cart): add DELETE /cart/:itemId"
```

## ✅ Do / ❌ Don't

**✅ Do:** Use same 404 strategy as PATCH for wrong user.

**✅ Do:** Idempotent delete optional — second delete 404 OK.

**❌ Don't:** DELETE by productId — use itemId unless you add alternate route.

**❌ Don't:** Soft-delete cart lines for MVP — hard delete OK.

## Verify checklist

- [ ] Delete own item → gone from GET /cart  
- [ ] Delete other user's item → 404/403  
- [ ] Delete random id → 404


## Before you continue

<OpenQuestion id="01KZ5P6BDDH36Y6TJVBGJT52DJ" />

## Next

**Sub-chapter 16.12:** Stock check on add — consolidate validation.

Ensure POST and PATCH share stock rules.
