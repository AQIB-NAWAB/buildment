---
title: "Update cart item"
module: 16
lesson: 10
summary: "**Why this step matters:** Quantity changes are the most common cart mutation — PATCH must stay **user-scoped** and re-check stock so customers cannot PATCH another user's itemId or order more than exists."
---
**Why this step matters:** Quantity changes are the most common cart mutation — PATCH must stay **user-scoped** and re-check stock so customers cannot PATCH another user's itemId or order more than exists.

<BigWordAlert term="Quantity update" plainEnglish="PATCH endpoint changing line item quantity — revalidates stock and recalculates subtotals." whyItMatters="Shoppers change mind on quantities — must not allow ordering more than available stock." />

**Where the project is now:** GET and POST work; quantity fixed after add unless re-POST.

**After this step:** `PATCH /cart/:itemId` updates quantity for **own** line only — stock validated.

## PATCH /cart/:itemId contract

| Field | Value |
|---|---|
| Auth | Required |
| Body | `{ "quantity": number }` |
| Param | `itemId` — CartItem `_id` |

## Handler sequence

1. Validate quantity integer ≥ 1  
2. Find CartItem `{ _id: itemId, userId: currentUser }` — 404 if not found (includes other user's item)  
3. Load product for stock check  
4. If quantity > product.stockQty → 400  
5. Update quantity; save  
6. Return updated item or full cart  

**Quantity 0:** reject with 400 — use DELETE to remove.

## User scoping — critical

Query must include **both** `_id` and `userId`:

```javascript
// findOneAndUpdate(
//   { _id: itemId, userId },
//   { quantity },
//   { new: true }
// )
```

If not found → 404 — do not reveal whether item exists for another user.

## Response

**200** — updated line DTO or full cart — match POST/GET convention.

## curl tests

<ApiRequestPanel title="Update cart item — PATCH /cart/:itemId">

<ApiRequest
  label="Update own item quantity"
  method="PATCH"
  url="http://localhost:5000/api/cart/ITEM_ID"
  bearer="CUSTOMER_A_TOKEN"
  body='{"quantity":3}'
  responseStatus={200}
  responseBody='{"id":"ITEM_ID","productId":"...","storeId":"...","quantity":3}'
/>

<ApiRequest
  label="Customer B tries A's item id — must fail"
  method="PATCH"
  url="http://localhost:5000/api/cart/A_ITEM_ID"
  bearer="CUSTOMER_B_TOKEN"
  body='{"quantity":1}'
  responseStatus={404}
  responseBody='{"message":"Cart item not found"}'
/>

</ApiRequestPanel>

Second expects 404 or 403 — not success.

## Commit

```bash
git commit -m "feat(cart): add PATCH /cart/:itemId for quantity updates"
```

## ✅ Do / ❌ Don't

**✅ Do:** Re-validate stock on every PATCH increase.

**✅ Do:** Return 404 for cross-user itemId — same as not found.

**❌ Don't:** Allow PATCH to change productId or storeId — delete + re-add instead.

**❌ Don't:** Accept quantity as string without parsing.

## Verify checklist

- [ ] Own item quantity updates  
- [ ] quantity > stock → 400  
- [ ] Other user's itemId → 404/403  
- [ ] quantity 0 → 400

<RealWorldEvent title="Target cart persistence across channels" when="2010s–present" summary="Retailers invested in unified carts so web, app, and in-store pickup saw the same basket — reducing abandoned purchases." lesson="Treat cart mutations as concurrent — two tabs updating quantity should not corrupt totals." />


## Before you continue

<OpenQuestion id="01M34MRAV44HY35JTAKXVN3R3B" />

## Next

**Sub-chapter 16.11:** DELETE /cart/:itemId.

Updates done; removal next.
