---
title: "What you'll build"
module: 10
lesson: 3
summary: "By the end of Chapter 10, a vendor can **complete shop onboarding entirely in the browser** and edit shop details later. Storeless vendors are redirected to open-shop; vendors with a store reach the vendor shell (product pages stub OK until Ch 12)."
---
By the end of Chapter 10, a vendor can **complete shop onboarding entirely in the browser** and edit shop details later. Storeless vendors are redirected to open-shop; vendors with a store reach the vendor shell (product pages stub OK until Ch 12).

<BigWordAlert term="Vendor portal UI" plainEnglish="Layout, setup wizard, and settings pages for vendors to manage their FreshMarket presence." whyItMatters="This is the seller half of the marketplace — distinct from the customer shop experience." />

## UI previews (build targets)

| Screen | Preview |
|---|---|
| Open your shop | ![Open shop](/showcase/multi-vendor-marketplace/vendor-open-shop.png) |
| Store settings | ![Store settings](/showcase/multi-vendor-marketplace/store-settings-page.png) |

## Deliverables checklist (preview)

| Deliverable | Proof |
|---|---|
| `VendorLayout` | Wraps vendor routes; runs store gate |
| `pages/vendor/OpenShop.jsx` | POST /stores form |
| `pages/vendor/StoreSettings.jsx` | GET me + PATCH form |
| Store API helpers in `api/` | getMyStore, createStore, updateStore |
| Onboarding redirect | 404 on GET me → `/vendor/open-shop` |
| Verify flow | New vendor forced through setup |
| Product routes blocked when storeless | Manual URL test |

## Route map (spec)

Adjust paths to match your router — stay consistent in learning log.

| Path | Component | Gate |
|---|---|---|
| `/vendor/open-shop` | OpenShop | Public within vendor auth — no store required |
| `/vendor/settings` | StoreSettings | Store required |
| `/vendor` or `/vendor/dashboard` | Placeholder or redirect | Store required |
| `/vendor/products` | Stub OK until Ch 12 | Store required |

All except open-shop nested under `VendorLayout` with shared nav shell.

## Files you will create

```
client/src/
  layouts/
    VendorLayout.jsx       ← NEW
  pages/vendor/
    OpenShop.jsx           ← NEW
    StoreSettings.jsx      ← NEW
    VendorHome.jsx         ← optional placeholder
  api/
    stores.js              ← NEW — or extend client.js
```

Router file — `App.jsx` or `routes.jsx` — updated with vendor routes.

## API helpers (spec)

| Function | HTTP | Used by |
|---|---|---|
| `getMyStore()` | GET /stores/me | Layout gate, settings load |
| `createStore(payload)` | POST /stores | OpenShop submit |
| `updateStore(id, payload)` | PATCH /stores/:id | StoreSettings submit |

Use authenticated client from Ch 7 — Bearer header automatic.

## Form fields (match Ch 9)

| Field | OpenShop | StoreSettings |
|---|---|---|
| name | required | editable |
| description | optional | editable |
| address | per ERD | editable |
| logoUrl | optional | editable |

Submit buttons: "Open shop" vs "Save changes". Loading and error states required.

## Error handling (preview Ch 12 depth)

| Status | UI |
|---|---|
| 400 | Inline field errors from API |
| 409 on POST | Message + link to settings/dashboard |
| 403 | Rare — role bug |
| 401 | Redirect login |
| Network error | Retry message |

## Chapter gate (10.12)

**Done when:**

- Vendor opens shop in UI  
- Storeless vendor cannot reach product pages (gate)  
- Settings PATCH persists after reload  

<MandatoryReadCard
  title="React Router — Routing"
  href="https://reactrouter.com/start/framework/routing"
  source="React Router"
  summary="Official guide to nested routes and navigation. Applies directly to vendor onboarding and protected route layouts."
  readMinutes={20}
/>

<RealWorldEvent title="Stripe Connect seller onboarding" when="2010s–present" summary="Payment platforms built multi-step seller verification flows because compliance and payout details cannot be optional after money moves." lesson="Collect required vendor data before they reach the dashboard — fixing incomplete profiles later is expensive support work." />

## Out of scope

- Product list implementation — stub "Coming in Ch 12" acceptable  
- Image upload widget — text URL only  
- Multi-step wizard — single form OK (see 10.4 optional)  
- Public store profile page  

## ✅ Do / ❌ Don't

**✅ Do:** Reuse same input components for open-shop and settings where possible.

**✅ Do:** After POST success, refetch GET me before navigation.

**❌ Don't:** Hardcode store id in client — read from GET me.

**❌ Don't:** Skip gate because seed vendor always has store — test fresh vendor.


## Before you continue

<OpenQuestion id="01KZP7KPV7E0QV3ERMNAHKFVX5" />

## Next

**Sub-chapter 10.4:** Prime your thinking — optional UX reads for onboarding forms.
<InterestingRead title="Empty states that convert" hook="A blank dashboard is where vendors quit." readMinutes={4}>

Good onboarding explains the next single step — name your shop, add hours, upload a logo — instead of dropping users into a grid of zeros. FreshMarket's open-your-shop flow is the product moment where supply enters the marketplace.

</InterestingRead>
