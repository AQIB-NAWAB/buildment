---
title: "Verify onboarding flow"
module: 10
lesson: 10
summary: "Individual pages can work while the journey breaks — gate bypass, wrong redirect, stale cache after create. Run the full script with a **fresh vendor account** in the browser. This is the Chapter 10 gate before product APIs assume every vendor has a store."
---
**Why this step matters:** Individual pages can work while the journey breaks — gate bypass, wrong redirect, stale cache after create. Run the full script with a **fresh vendor account** in the browser. This is the Chapter 10 gate before product APIs assume every vendor has a store.

**Where the project is now:** Layout gate, open-shop, and settings implemented.

**After this step:** You have run the **full browser onboarding script** with a fresh vendor account and documented results for the learning log.

## Test script (browser — you execute)

### Scenario A — New vendor opens shop

| Step | Action | Expect |
|---|---|---|
| 1 | Register new vendor + verify OTP | Account ready |
| 2 | Login as vendor | Enters vendor shell |
| 3 | Observe URL | `/vendor/open-shop` or redirect there |
| 4 | Try navigate to `/vendor/products` | Redirected to open-shop |
| 5 | Fill open-shop form — valid data | |
| 6 | Submit | Loading state → success |
| 7 | Land on vendor home/dashboard | Not open-shop |
| 8 | `GET /stores/me` in Network tab | 200 + store JSON |
| 9 | Navigate Settings | Fields pre-filled |
| 10 | Change description → Save | Success message |
| 11 | Hard refresh settings page | New description persists |

### Scenario B — Existing vendor (seed)

| Step | Action | Expect |
|---|---|---|
| 1 | Login seed vendor | No open-shop redirect |
| 2 | Settings loads | 200 GET me |
| 3 | PATCH still works | |

### Scenario C — 409 double create (optional)

| Step | Action | Expect |
|---|---|---|
| 1 | Craft duplicate POST via devtools or double-submit | 409 message handled |

## Network tab checklist

Confirm requests:

| Request | When | Status |
|---|---|---|
| GET /stores/me | Layout mount | 404 then 200 after create |
| POST /stores | Open shop submit | 201 |
| GET /stores/me | After create | 200 |
| PATCH /stores/:id | Settings save | 200 |

Authorization header present on all.

## Screenshot-worthy demo

Capture for portfolio / Week 2 demo:

1. Open-shop empty form  
2. Vendor dashboard after create (stub OK)  
3. Settings with saved data  

## Common failures

| Symptom | Fix |
|---|---|
| Stuck on open-shop after POST | Gate not refetching; navigate before 201 completes |
| Flash of products page | Gate async — add loading state |
| 401 on POST | Token not attached — check api helper |
| CORS error | Server CORS config |
| Form fields empty on settings | GET me response mapping wrong |

## Regression

- [ ] Customer login still works — not sent to vendor open-shop  
- [ ] Logout clears vendor state  
- [ ] Chapter 9 curl tests still pass — server unchanged  

## README update

Document vendor onboarding:

- Fresh vendor path  
- Gate behaviour  
- Routes table  

## Commit

```bash
git add README.md
git commit -m "docs: document vendor onboarding flow verification"
```

## ✅ Do / ❌ Don't

**✅ Do:** Use genuinely new vendor — not seed with store.

**✅ Do:** Record Network status codes in learning log.

**❌ Don't:** Mark chapter done without Scenario A steps 3–4 (gate).

**❌ Don't:** Skip settings persistence test — step 11.

## Verify checklist

- [ ] Scenario A complete  
- [ ] Scenario B complete  
- [ ] Product route blocked when storeless  
- [ ] Screenshots or notes for learning log  


## Before you continue

<OpenQuestion id="01KZAX77106SNVDH4M6XS7J8AV" />

## Next

**Sub-chapter 10.11:** Recap and what's next — Chapter 11 product API.
