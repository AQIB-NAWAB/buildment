---
title: "Where the project is now"
module: 10
lesson: 5
summary: "Chapter 10 is client-only — it consumes Chapter 9's API. This snapshot confirms store endpoints work via curl before you touch React routing. Building forms against a broken API wastes time on UI bugs that are really 401/404 contract mismatches."
---
## Why this step matters

Chapter 10 is client-only — it consumes Chapter 9's API. This snapshot confirms store endpoints work via curl before you touch React routing. Building forms against a broken API wastes time on UI bugs that are really 401/404 contract mismatches.

**Where the project is now:** Chapter 9 store API is complete and curl-tested. Client has auth pages and a generic home — **no vendor shell, no store forms, no onboarding gate**.

**After this chapter:** Vendor logs in → hits store gate → opens shop or lands dashboard → edits settings in browser.

**Build cluster starts here.** Confirm Chapter 9 gate before any client work.

## Server — unchanged this chapter

```
server/src/modules/stores/
  stores.routes.js    ← POST, GET me, PATCH — working from Ch 9
```

No new server files. UI consumes existing contracts.

### Store API contracts (reference)

| Method | Path | Role | Success | Key failure |
|---|---|---|---|---|
| `POST` | `/api/stores` | vendor | 201 | 409 duplicate, 403 customer |
| `GET` | `/api/stores/me` | vendor | 200 | 404 no store yet |
| `PATCH` | `/api/stores/:id` | vendor | 200 | 403 wrong owner |

**Body rule:** Never send `vendorId` — server sets from JWT.

### Pre-build curl gate

```bash
curl -s http://localhost:5000/api/stores/me -H "Authorization: Bearer <vendor-token>"
curl -s -X POST http://localhost:5000/api/stores \
  -H "Authorization: Bearer <vendor-token>" \
  -H "Content-Type: application/json" \
  -d '{"name":"Test Market","address":"1 High St"}'
```

- [ ] GET /stores/me → 404 for fresh vendor
- [ ] POST /stores → 201; second POST → 409
- [ ] PATCH /stores/:id → 200 for owner

## Client entering Chapter 10

```
client/src/
  App.jsx, api/client.js, hooks/useAuth.js
  pages/Login.jsx, Register.jsx, VerifyOtp.jsx, Home.jsx
```

**Missing:**

| Deliverable | Sub-chapter |
|---|---|
| `layouts/VendorLayout.jsx` | 10.6 |
| `pages/vendor/OpenShop.jsx`, `StoreSettings.jsx` | 10.7–10.8 |
| `api/stores.js` | 10.6–10.8 |
| Store gate + onboarding redirect | 10.9 |

## Login redirect check

| Current (likely) | Target after Ch 10 |
|---|---|
| Generic home `/` | `/vendor` — layout runs store gate |
| Same path for all roles | Vendor → vendor shell; customer → shop |

**Spec:** vendors enter the **vendor shell**, not customer shop. Adjust login handler in 10.6 or 10.9.

## Environment

- [ ] `VITE_API_URL` points to running API
- [ ] CORS allows client origin
- [ ] Auth interceptor attaches Bearer token
- [ ] Ch 7 refresh loop still works

## Test accounts

| Account | Purpose |
|---|---|
| Fresh vendor (no store) | Full onboarding in 10.10 |
| Seed vendor (has store) | Gate passes; settings edit |
| Customer | Must not access `/vendor/*` |

Add `/vendor/products` stub ("Chapter 12") to test gate blocks access — no product API calls yet.

## Troubleshooting

| Symptom | Likely cause |
|---|---|
| curl GET me → 401 | Token missing; check interceptor |
| POST stores → 403 | Customer token — register as vendor |
| Client fetch fails | Wrong `VITE_API_URL` or CORS |
| Vendor lands on customer home | Login handler not role-aware |

## ✅ Do / ❌ Don't

**✅ Do:** Confirm Ch 9 learning log committed; keep server running; know RR v6 `<Outlet />`.

**❌ Don't:** Mock store API; build product forms; change server routes.

## Verify before 10.6

- [ ] Store API curl tests pass
- [ ] Vendor can log in via UI
- [ ] Fresh vendor account ready for onboarding
- [ ] `api/client.js` pattern understood for `stores.js`


## Before you continue

<OpenQuestion id="01KZ5FW49AQK7WS5HQS5PCKJTG" />

## Next

**Sub-chapter 10.6:** Scaffold vendor layout and `pages/vendor/` folder.
