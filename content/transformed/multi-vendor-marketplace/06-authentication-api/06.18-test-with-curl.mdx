---
title: "Test with curl"
module: 6
lesson: 18
summary: "**Why this step matters:** Browser bugs hide API bugs and vice versa. A full curl script — register, verify, login, me, refresh — is your Chapter 6 gate evidence and the contract Chapter 7 must not break."
---
**Why this step matters:** Browser bugs hide API bugs and vice versa. A full curl script — register, verify, login, me, refresh — is your Chapter 6 gate evidence and the contract Chapter 7 must not break.

**Where the project is now:** All auth endpoints implemented individually.

**After this step:** You have run the **complete auth flow** via curl and documented results — proof for gate and Chapter 7 client integration.

## Full flow script

Run in order; replace values from your responses.

```bash
BASE=http://localhost:5000

# --- 1. Register ---
curl -s -X POST $BASE/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "curltest@test.local",
    "password": "Password123!",
    "role": "customer",
    "displayName": "Curl Tester"
  }'
# Save OTP from response or server log

# --- 2. Verify OTP ---
curl -s -X POST $BASE/api/auth/verify-otp \
  -H "Content-Type: application/json" \
  -d '{"email":"curltest@test.local","otp":"REPLACE_OTP"}'

# --- 3. Login ---
LOGIN=$(curl -s -X POST $BASE/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"curltest@test.local","password":"Password123!"}')
echo $LOGIN
# Extract accessToken and refreshToken — use jq if available:
# ACCESS=$(echo $LOGIN | jq -r .accessToken)
# REFRESH=$(echo $LOGIN | jq -r .refreshToken)

# --- 4. Protected route ---
curl -s $BASE/api/auth/me \
  -H "Authorization: Bearer REPLACE_ACCESS"

# --- 5. Refresh ---
curl -s -X POST $BASE/api/auth/refresh \
  -H "Content-Type: application/json" \
  -d '{"refreshToken":"REPLACE_REFRESH"}'

# --- 6. Seed vendor login ---
curl -s -X POST $BASE/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"vendor-a@test.local","password":"Password123!"}'
```

## Negative tests checklist

| # | Test | Expected |
|---|---|---|
| 1 | Login unverified new user (skip step 2) | 403 |
| 2 | Login wrong password | 401 |
| 3 | /me without token | 401 |
| 4 | /me with garbage token | 401 |
| 5 | Register duplicate email | 409 |
| 6 | Refresh with invalid token | 401 |
| 7 | Register role admin | 400 |

Document pass/fail in learning log.

## Compass verification

After register — before verify:

- [ ] User exists, isVerified false  
- [ ] passwordHash bcrypt  
- [ ] Profile linked  

After verify:

- [ ] isVerified true  

After login:

- [ ] refreshTokenHash populated  

## Optional — save as script

`server/scripts/test-auth.sh` — executable, not committed with real secrets — optional dev helper.

## README — test credentials

Document seed accounts and curltest flow pointer.

## Screenshot / paste for learning log

Paste redacted /me JSON and note token exp from jwt.io — no full tokens in Git.

## ✅ Do / ❌ Don't

**✅ Do:** Test both vendor and customer role logins.

**✅ Do:** Run negative tests — not just happy path.

**❌ Don't:** Commit curl script with real JWT_SECRET.

**❌ Don't:** Skip refresh test — Ch 7 depends on it.

## Verify checklist

- [ ] Full happy path completes
- [ ] All seven negative tests pass expected status
- [ ] Seed vendor-a login works
- [ ] Results noted in learning log prep
- [ ] No plain passwords in Compass


## Before you continue

<OpenQuestion id="01KZ5FW3XRZN3GXNB9C5DA3MAF" />

## Next

**Sub-chapter 6.19:** Recap and what's next — Chapter 7 login UI preview.

Auth API proven — wrap chapter next.
