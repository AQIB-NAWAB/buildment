---
title: "Login handler"
module: 6
lesson: 13
summary: "**Why this step matters:** Login proves bcrypt compare works and gates unverified users — the daily path vendors and customers repeat. Tokens issued here feed every protected route and Chapter 7's browser session."
---
# 6.13 — Login handler

> **[Build]** · Chapter 06 — Authentication API · *`POST /auth/login` — compare hash; issue tokens*

**Why this step matters:** Login proves bcrypt compare works and gates unverified users — the daily path vendors and customers repeat. Tokens issued here feed every protected route and Chapter 7's browser session.

**Where the project is now:** Register works; login endpoint stub or incomplete.

**After this step:** `POST /api/auth/login` validates credentials with bcrypt, blocks unverified users, returns access and refresh tokens on success.

## Request contract

**POST** `/api/auth/login`

**Body:**

| Field | Required |
|---|---|
| email | yes |
| password | yes |

## Success response — 200

```json
{
  "accessToken": "eyJhbG...",
  "refreshToken": "eyJhbG...",
  "user": {
    "id": "664a...",
    "email": "vendor-a@test.local",
    "role": "vendor",
    "displayName": "Vendor A"
  }
}
```

**user object:** No passwordHash. Include profile displayName from Profile lookup.

Token structure refined in 6.14–6.15 — login calls `issueTokens(user)` service helper.

## Error responses

| Status | Condition |
|---|---|
| 400 | missing email/password |
| 401 | wrong email or password |
| 403 | isVerified false |
| 500 | server error |

Use generic "Invalid credentials" for wrong email/password if preventing enumeration.

## Service behaviour

1. Find User by email — include passwordHash (select +passwordHash if schema hides)  
2. If !user → 401  
3. `bcrypt.compare(password, user.passwordHash)` — if false → 401  
4. If !user.isVerified → 403 `{ message: "Email not verified" }`  
5. Load Profile for displayName  
6. Call token issuance (6.14 + 6.15)  
7. Return tokens + safe user object  

## curl verify — seed user

After seed passwords bcrypt-compatible:

```bash
curl -s -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "vendor-a@test.local",
    "password": "Password123!"
  }'
```

Save accessToken for 6.17 /me test.

## Unverified user test

Register new user **without** verify-otp → login → expect **403**.

## Wrong password test

```bash
curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"vendor-a@test.local","password":"wrong"}'
```

Expect **401**.

## Commit

```bash
git add server/src/modules/auth/
git commit -m "feat(auth): implement login with bcrypt verify and token issuance"
```

## ✅ Do / ❌ Don't

**✅ Do:** Consistent JSON error shape for Ch 7 UI mapping.

**✅ Do:** Test seed vendor and customer roles both login.

**❌ Don't:** Skip isVerified check — security requirement.

**❌ Don't:** Put tokens only in cookies yet if curl tests need JSON — support body for Ch 6.

## Verify checklist

- [ ] Verified seed user login 200 with tokens
- [ ] Unverified user login 403
- [ ] Wrong password 401
- [ ] Response user object has no passwordHash
- [ ] Both vendor and customer roles work

## Next

**Sub-chapter 6.14:** Access token — short-lived JWT with userId + role.

Login returns tokens — access token spec next.
