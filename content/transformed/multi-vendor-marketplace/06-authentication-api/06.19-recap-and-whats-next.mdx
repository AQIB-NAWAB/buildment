---
title: "Recap and what's next"
module: 6
lesson: 19
summary: "Chapter 6 turned an anonymous API into an **authenticated** one. Every protected route from here forward builds on `requireAuth` and the token contracts you tested with curl."
---
Chapter 6 turned an anonymous API into an **authenticated** one. Every protected route from here forward builds on `requireAuth` and the token contracts you tested with curl.

## What you built

**Register:** bcrypt password hash, User + Profile creation, OTP generation, duplicate email rejection.

**Verify OTP:** isVerified gate before login.

**Login:** Credential check, 403 for unverified, access + refresh tokens issued.

**Access JWT:** Short-lived, sub + role, Bearer header.

**Refresh:** Hashed in DB, rotation endpoint, revocation on logout.

**requireAuth:** 401 on bad/missing/expired token; req.user for downstream handlers.

**GET /me:** Protected smoke test returning user + profile without secrets.

## Repo additions

```
server/src/modules/auth/
├── auth.routes.js
├── auth.service.js
├── auth.middleware.js
└── auth.schema.js
server/.env.example     ← JWT_*
shared/config.js        ← JWT validation
```

## Security properties achieved

- No plain passwords in MongoDB  
- Access token short-lived  
- Refresh revocable via hash clear  
- Unverified users cannot login  
- Role in JWT for Chapter 8  

## What's not built yet

| Item | Chapter |
|---|---|
| Login/register UI | 7 |
| Access token in memory + refresh cookie | 7 |
| 401 refresh loop in client | 7 |
| requireRole / ownership | 8 |
| Store API | 9 |

## Handoff to Chapter 7 — keep contracts stable

Chapter 7 will call these exact shapes from React. Do **not** rename fields after the gate.

| Your curl response field | Chapter 7 usage |
|---|---|
| `accessToken` | Stored in useAuth memory; sent as Bearer |
| `refreshToken` | Moved to httpOnly cookie in 7.x (body OK for 6.x curl) |
| `user.id` | Auth context identity |
| `user.role` | Post-login redirect (vendor vs customer) |
| `user.displayName` | Navbar / profile stub |
| 401 on bad token | Triggers client refresh loop |
| 403 on unverified | VerifyOtp page redirect from Register |


## Before you continue

<OpenQuestion id="01KZAX76B12VNV23FNCBYDTCQ8" />

## Next chapter — Login & registration (Chapter 7)

Humans need screens:

- `pages/Login.jsx`, `Register.jsx`, `VerifyOtp.jsx`  
- `hooks/useAuth.js` or AuthContext  
- API client attaches Bearer header  
- 401 → refresh → retry once  
- ProtectedRoute wrapper  

**Reuse:** Same `/api/auth/*` contracts — client must not invent new shapes.

## Callback before Chapter 7

- Complete **6.20** checklist  
- Document test credentials in README  
- Confirm CORS allows client origin  

## Continue to gate

**Sub-chapter 6.20:** Definition of Done + learning log.

**Do not open Chapter 7 until 6.20 is fully ticked.**

API knows who is calling — next you teach the browser to prove it.
