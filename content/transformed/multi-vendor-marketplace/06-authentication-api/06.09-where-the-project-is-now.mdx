---
title: "Where the project is now"
module: 6
lesson: 9
summary: "**Why this step matters:** Auth builds on seeded User documents with real fields — not imaginary users. This recap confirms Week 1 gate, test emails, and empty `modules/auth/` before you add routes that mutate identity data."
---
**Why this step matters:** Auth builds on seeded User documents with real fields — not imaginary users. This recap confirms Week 1 gate, test emails, and empty `modules/auth/` before you add routes that mutate identity data.

**Where the project is now:** Chapter 5 complete — six models, seed data in Compass, `docs/erd.md` committed. Auth module folder may exist empty. No `/api/auth` routes. All requests anonymous except future work.

**After this step:** Continuity recap only — build starts at 6.10.

## Repo tree entering Chapter 6

```
server/src/
├── app.js                 ← mounts routes — auth router added in 6.11
├── server.js
├── shared/
│   ├── config.js          ← extend with JWT_* vars
│   └── db.js
└── modules/
    ├── auth/              ← empty — 6.10
    ├── users/
    │   ├── user.model.js
    │   └── profile.model.js
    ├── stores/ ...
    ├── products/ ...
    ├── cart/ ...
    └── orders/ ...
```

## Baseline verify

<ApiRequestPanel title="Pre-auth baseline">

<ApiRequest
  label="GET /health"
  method="GET"
  url="http://localhost:5000/health"
  responseStatus={200}
  responseBody='{"status":"ok","db":"connected"}'
/>

</ApiRequestPanel>

```bash
npm run seed   # still works — may need bcrypt hashes updated after 6.12
```

Seed users should exist for login tests — update hashes when register/login implemented.

## Config extensions needed

| Variable | In config.js by |
|---|---|
| JWT_SECRET | 6.10 or 6.11 |
| JWT_ACCESS_EXPIRY | 6.14 |
| JWT_REFRESH_EXPIRY | 6.15 |

Fail startup if JWT_SECRET missing — same discipline as MONGODB_URI.

## Dependencies to install

From `server/`:

```bash
npm install bcrypt jsonwebtoken
```

Optional later: `cookie-parser` for refresh cookie in Ch 7.

## Build cluster map

| Step | File / action |
|---|---|
| 6.10 | auth module scaffold |
| 6.11 | mount `/api/auth` |
| 6.12 | register |
| 6.13 | login |
| 6.14 | access JWT |
| 6.15 | refresh |
| 6.16 | verify-otp |
| 6.17 | requireAuth |
| 6.18 | curl full flow |

## Client unchanged

React app still shows health only — no auth UI until Chapter 7.

## ✅ Do / ❌ Don't

**✅ Do:** Update `.env.example` with JWT placeholders before first commit with auth.

**✅ Do:** Keep Chapter 5 models — extend User if OTP fields needed.

**❌ Don't:** Mount auth on `/auth` without `/api` prefix — course uses `/api/auth`.

## Verify checklist

- [ ] Chapter 5 gate complete
- [ ] Seed users visible in Compass
- [ ] health still 200
- [ ] auth/ folder exists or will be created in 6.10

<RealWorldEvent title="Auth0 breach response reshapes token hygiene" when="2023" summary="High-profile auth incidents pushed teams to shorten token lifetimes, rotate secrets, and treat refresh flows as first-class security surfaces." lesson="Build OTP verification and refresh rotation now so auth is production-shaped before real customer data exists." />


## Before you continue

<OpenQuestion id="01KZP7KPDH31J5N030HP7CVGQQ" />

## Next

**Sub-chapter 6.10:** Scaffold auth module — routes, service, middleware, schema files.

Baseline set — auth files next.
