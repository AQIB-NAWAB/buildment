---
id: multi-vendor-marketplace-15-04
title: "15.4 — Where the project is now"
module: 15
lesson: 4
stepType: Build
phase: "Module 15"
summary: "**Why this step matters:** Week 3's first **visible** customer feature starts here. Confirming the Chapter 14 API works via curl before React saves hours debugging empty grids caused by backend issues, not CSS."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 15.4 — Where the project is now

> **[Build]** · Chapter 15 — Shop the marketplace · *Catalogue API ready*

**Why this step matters:** Week 3's first **visible** customer feature starts here. Confirming the Chapter 14 API works via curl before React saves hours debugging empty grids caused by backend issues, not CSS.

**Build cluster starts.** Confirm Chapter 14 API before adding React pages.

## Prerequisites gate

| Prior gate | Verify |
|---|---|
| Chapter 14 | `curl GET /products` returns paginated JSON with store.name |
| Chapter 14 | Filters and detail route work |
| Chapter 13 | Products have imageUrl in API responses |
| Chapter 7 | API client + optional auth interceptor |
| Client runs | Vite dev server; CORS OK for GET |

## Client tree — before Chapter 15

```
client/src/
  App.jsx              ← routes: home, auth, vendor/*
  pages/
    Home.jsx
    Login.jsx, Register.jsx
    vendor/            ← vendor dashboard pages
  api/
    client.js
  components/
  layouts/
```

**After Chapter 15:**

```
client/src/
  pages/
    Shop.jsx                 ← NEW
    ProductDetail.jsx        ← NEW
  components/
    ProductCard.jsx          ← NEW
    CatalogueFilters.jsx     ← NEW (optional name)
  api/
    products.js              ← NEW public catalogue helpers
```

## Routing today

Vendor routes under `/vendor/*`. Customer shop **new namespace** `/shop` — avoids collision with vendor product edit routes.

Update nav/header with **Shop** link visible to all users (or customer role).

## API — consume only

| Endpoint | Used by |
|---|---|
| GET /products | Shop.jsx |
| GET /products/:id | ProductDetail.jsx |

No new server files this chapter unless bugfix from Ch 14.

## Design tokens

Reuse CSS approach from vendor dashboard — same font stack, button styles, spacing variables if you have them.

## Build sequence

| Sub-chapter | Action |
|---|---|
| 15.5 | Routes + empty pages |
| 15.6 | ProductCard |
| 15.7 | Filters |
| 15.8 | Pagination wired |
| 15.9 | Detail + stub button |
| 15.10 | Two-vendor verify |

## ✅ Do / ❌ Don't

**✅ Do:** Test API from browser network tab before styling grid.

**❌ Don't:** Mock product data — use live API.

**❌ Don't:** Start cart API (Ch 16) in same commits — keep chapter scope.

## Verify before 15.5

- [ ] curl catalogue returns items with imageUrl and store.name  
- [ ] Chapter 14 learning log committed  
- [ ] App.jsx routing pattern understood

## Next

**Sub-chapter 15.5:** Scaffold customer pages and routes.

Empty shells first; fill with data next.
