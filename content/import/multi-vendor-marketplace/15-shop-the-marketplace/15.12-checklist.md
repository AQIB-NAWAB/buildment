---
id: multi-vendor-marketplace-15-12
title: "15.12 — Checklist and learning log"
module: 15
lesson: 12
stepType: Gate
phase: "Module 15"
summary: "Gate for Chapter 15 — Shop the marketplace. Tick every box and answer the learning log on this page before continuing."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 15.12 — Checklist and learning log
> **[Gate]** · Chapter 15 — Shop the marketplace

Work through each section in order. Tick a box only after you tested it in the browser.

<BigWordAlert term="Client-side filter state" plainEnglish="React state (or URL sync) tracking category, search, sort — drives API query params." whyItMatters="FreshMarket filter bar refetches `/products?…` when shopper changes filters." />

<Callout type="info" title="How to use this gate">
If the shop is empty, test `curl GET /products` first — 90% of "broken shop" issues are API or seed problems from Chapter 14, not React bugs.
</Callout>

## Chapter 15 — Shop the marketplace checklist (gate)

Do **not** open Chapter 16 until every box is ticked and learning log answers are written below.

### Routes & navigation

- [ ] `/shop` renders Shop page  
  *Verify:* Visit URL directly — no blank screen.
- [ ] `/shop/:productId` renders ProductDetail  
  *Verify:* Click a card from grid.
- [ ] Nav link to shop from home or global header
- [ ] No collision with vendor `/vendor/*` routes

### Shop page

- [ ] Fetches `GET /products` with page, limit, optional category, storeId  
  *Verify:* Network tab shows correct query params.
- [ ] Renders grid of ProductCard components
- [ ] Loading and error states handled
- [ ] Empty state when no products match filters
- [ ] Pagination controls using `totalPages`  
  *Verify:* Page 2 shows different products than page 1.
- [ ] Filter change resets to page 1

### ProductCard

- [ ] Shows imageUrl or placeholder
- [ ] Shows name, price/unit, store.name
- [ ] Links to product detail

### Filters

- [ ] Category filter refetches API with `category` param
- [ ] Store filter refetches API with `storeId` param
- [ ] Clear filters restores full catalogue

### Product detail

- [ ] Fetches `GET /products/:id`
- [ ] Shows image, name, price, unit, store, description if available
- [ ] 404 handling for missing product
- [ ] Add to cart button present — stub logs productId + storeId

### Multi-vendor browse (required)

- [ ] Shop grid shows products from **≥2 stores** at once  
  *Verify:* Two different store names visible on same screen — screenshot it.
- [ ] Verified in browser — not API-only

### API discipline

- [ ] No duplicate publish/stock filtering in client
- [ ] No per-card detail API calls on grid
- [ ] Uses `client/src/api/products.js` helpers

### Prior gates

- [ ] Chapter 14 public catalogue API complete
- [ ] Chapter 13 images display from Cloudinary URLs
- [ ] I can explain why shop consumes API instead of embedding catalogue logic
- [ ] I can explain why page resets when filter changes
- [ ] I can explain why storeId is needed on future cart line (Ch 16)
- [ ] I can explain difference: customer shop vs vendor product list UI

### Learning log — write now

Answer **in your own words** in the fields below:

1. **Data flow:** Trace one shop page load from React to MongoDB and back — include query params.

2. **Card design:** What fields from API appear on ProductCard? What did you exclude?

3. **Filters:** How do category and store filters map to API params? Where do option lists come from?

4. **Pagination:** How does UI use `total`, `page`, `totalPages`? What happens on last page Next click?

5. **Two vendors:** Name the two stores visible in your browse demo. How did you verify?

6. **Detail page:** Paste stub add-to-cart payload shape you will send in Ch 17.

7. **Errors:** What does UI show for 404 product and network failure?

8. **Chapter 16 preview:** What must cart API store per line besides productId?

## All boxes ticked?

Mark this chapter complete in the platform, then continue to **Cart API** (Chapter 16).

## Troubleshooting

| Blocker | Fix |
|---|---|
| Empty shop | Ch 14 — publish + stock; seed |
| CORS | Server cors |
| Filters no effect | Params not sent; empty string not stripped |
| Same page on Next | page state not updating |
| No store name | Ch 14 populate |
| Images broken | imageUrl; Cloudinary |

---

## Final gate statement

*"Customer shop at /shop browses paginated catalogue with filters, shows products from two vendors with photos, detail page works with add-to-cart stub. Ready for Chapter 16 cart API."*

**All boxes ticked? Open Chapter 16.**
<InterestingRead title="Skeleton screens vs spinners" hook="Empty grids feel broken; skeletons feel fast." readMinutes={3}>

While catalogue fetches run, placeholder cards communicate progress better than a lone spinner. FreshMarket's shop UI should feel responsive even on slow networks.

</InterestingRead>

<RealWorldEvent title="Instacart's shop-by-aisle experience" when="2020s" summary="Grocery apps invested heavily in browse UX — filters, dietary tags, and store context — because search alone misses discovery." lesson="Customer-facing catalogue UI is where backend data models become revenue — treat loading and empty states as first-class design." />
