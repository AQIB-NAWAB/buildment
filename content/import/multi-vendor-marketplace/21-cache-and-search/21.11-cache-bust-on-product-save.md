---
id: multi-vendor-marketplace-21-11
title: "21.11 — Cache bust on product save"
module: 21
lesson: 11
stepType: Build
phase: "Module 21"
summary: "**Why this step matters:** Without invalidation, your fast cache serves wrong prices — the stale trap from 21.2 becomes a real bug. This step is what makes caching **safe** for a marketplace, not just fast."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 21.11 — Cache bust on product save

> **[Build]** · Chapter 21 — Cache & search · *Invalidate on POST/PATCH/DELETE product*

**Why this step matters:** Without invalidation, your fast cache serves wrong prices — the stale trap from 21.2 becomes a real bug. This step is what makes caching **safe** for a marketplace, not just fast.

<BigWordAlert term="Cache invalidation" plainEnglish="Deleting or updating Redis keys when vendor saves product — fresh data on next request." whyItMatters="Wrong prices in cache hurt trust — tie invalidation to product updates and TTLs." />

**Where the project is now:** Catalogue reads are cached (21.9) — product saves never touch the cache, so stale prices serve until expiry.

**After this step:** Product POST/PATCH/DELETE invalidates catalogue cache keys — next GET reflects changes.

## invalidateCatalogueCache() — spec

| Trigger | Action |
|---|---|
| `createProduct` success | Delete keys matching `catalogue:*` |
| `updateProduct` success | Same |
| `deleteProduct` success | Same |

Implement in `products.service.js` after DB write succeeds — before return.

Keep invalidation beside the successful mutation, not only in HTTP routes. Service-level placement covers every caller that changes products and prevents a future script or admin route from bypassing cache cleanup.

## Key deletion strategies

| Approach | Dev OK | Production note |
|---|---|---|
| `SCAN` + `DEL` matching prefix | ✓ | Preferred |
| Track keys in a Set on write | ✓ | Explicit |
| `KEYS catalogue:*` | Dev only | Blocks Redis — document avoid |

## Test script

1. Request an unfiltered page and a category-filtered page containing the product; repeat both to prove they are warm.
2. Change the product price through the authenticated vendor `PATCH` endpoint.
3. Request both URLs again. Each must miss and contain the new price.
4. Repeat each URL. Both should now hit with the updated payload.
5. Repeat the same pattern for publish/unpublish or delete so a removed public product cannot linger.

## Stale cache failure

If step 3 shows old price — invalidation not wired or wrong key pattern.

If the write returns success but invalidation fails, emit a structured error with the product id and operation. TTL limits the stale period, but it does not make the failure harmless. Your README should state whether this implementation returns a write error or accepts the write and relies on monitoring plus TTL.

## ✅ Do / ❌ Don't

**✅ Do:** Test price change specifically — 21.2 trap.

**✅ Do:** Invalidate even if only stock changed — list may filter in-stock.

**❌ Don't:** Rely on TTL alone for gate.

## Verify

- [ ] PATCH product → catalogue shows update immediately
- [ ] POST, publish-state changes, and DELETE clear every catalogue variant
- [ ] Failed database writes do not invalidate or report success
- [ ] Invalidation failure policy is documented
- [ ] Evidence is recorded in the learning log

## Next

**Sub-chapter 21.12:** Text index — writes invalidate the cache; search is the next read path that needs the same treatment.
