---
id: multi-vendor-marketplace-02-11
title: "2.11 — The health route"
module: 2
lesson: 11
stepType: Build
phase: "Module 2"
summary: "**Why this step matters:** `GET /health` is your first **proof the API works** — independent of React, MongoDB, or auth. Load balancers and Chapter 4's extended health both build on this route; Chapter 2's gate requires curl to return honest JSON."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 2.11 — The health route

> **[Build]** · Chapter 02 — Project skeleton · *`GET /health` → `{ status: "ok" }`; curl → 200*

**Why this step matters:** `GET /health` is your first **proof the API works** — independent of React, MongoDB, or auth. Load balancers and Chapter 4's extended health both build on this route; Chapter 2's gate requires curl to return honest JSON.

<BigWordAlert term="Health check route" plainEnglish="A tiny API endpoint that returns OK so you can prove the server is alive before building real features." whyItMatters="Your first end-to-end win is the React app calling `/health` — if that works, networking and CORS are probably configured correctly." />

**Where the project is now:** Express boots and listens. No defined routes yet — or only a catch-all 404.

**After this step:** `GET /health` returns **200** with JSON `{ "status": "ok" }` (minimum). Verified with curl.

## The contract

| Field | Value |
|---|---|
| Method | `GET` |
| Path | `/health` |
| Authentication | None — public |
| Success status | `200 OK` |
| Content-Type | `application/json` |
| Body (minimum) | `{ "status": "ok" }` |

**Optional fields** (your choice):

```json
{
  "status": "ok",
  "service": "GreenBasket API",
  "uptime": 123.45
}
```

Chapter 4 extends health with database status — don't add `db: connected` until MongoDB exists.

## What you must implement

1. Register a **GET** handler for `/health` on the Express app in `app.js` (or a tiny `shared/health.routes.js` you mount — either is fine at this scale).

2. Handler behaviour:
   - Set status code **200**
   - Respond with JSON object containing **`status: "ok"`** string
   - Do not throw for normal operation

3. **You write the handler body.** The course specifies behaviour, not implementation.

## Suggested structure (not the solution)

Many teams mount health at app root:

```
app.get('/health', handler)
```

Others prefix API routes:

```
app.use('/api', apiRouter)
// health at /api/health
```

**This course uses `/health` at root** unless you document a different path consistently everywhere (client env in 2.14 must match).

If you choose `/api/health`, update `VITE_API_URL` usage in 2.15 to fetch `/api/health`.

## Wire CORS (optional here, required by 2.15)

If `cors` is installed:

- Import `cors` package
- `app.use(cors({ origin: 'http://localhost:5173' }))` — adjust when client port differs

Browser demo in 2.15 fails without CORS. Installing here saves a debug step later.

## Verify with curl

Server running (`npm run dev` in `server/`):

<ApiRequestPanel title="Health route">

<ApiRequest
  label="GET /health — success"
  method="GET"
  url="http://localhost:5000/health"
  responseStatus={200}
  responseBody='{"status":"ok"}'
/>

<ApiRequest
  label="Unknown path — routing check"
  method="GET"
  url="http://localhost:5000/does-not-exist"
  responseStatus={404}
  responseBody='{"message":"Not found"}'
/>

</ApiRequestPanel>

**Expected:** `200` with JSON `{ "status": "ok" }` for health; `404` for wrong path — proves routing works selectively.

## Verify with browser

Open `http://localhost:5000/health` in browser address bar.

**Expected:** JSON visible or download — not HTML error page from connection refused.

## Update root README

In repository root `README.md`, under Run locally → API, note:

```
Health check: http://localhost:5000/health
```

## Commit

```bash
git add server/src/
git commit -m "feat(server): add GET /health endpoint"
```

## ✅ Do / ❌ Don't

**✅ Do:** Return JSON via `res.json()` or equivalent — not plain text `ok`.

**✅ Do:** Use GET only — health checks should not mutate state.

**❌ Don't:** Put authentication on `/health` — load balancers and Chapter 2 client demo need public access.

**❌ Don't:** Return `200` with `{ "status": "error" }` body when DB is down — Chapter 4 will define DB-aware health properly.

## Hint

If curl returns connection refused, server isn't listening — fix 2.10 first. If curl returns 404, route not mounted on correct path or typo in URL.

## Learning connection

Health endpoints are how deploy platforms (Chapter 23) know your API process is alive. Minimal now; richer later.

## Verify checklist

- [ ] `curl -i http://localhost:5000/health` → `200`  
- [ ] Response JSON includes `"status":"ok"`  
- [ ] Committed to Git  

<RealWorldEvent title="Shopify's modular commerce platform" when="2006–present" summary="Shopify grew from a single-product store builder into a platform with separate admin, storefront, and API surfaces — all coordinated through clear project boundaries." lesson="Starting with a deliberate folder skeleton prevents painful renames later when vendor, cart, and payment code all need room to grow." />

## Next

**Sub-chapter 2.12:** Scaffold Vite React app in `client/` with folder convention.

Server side of Week 1 demo half-done — client next.