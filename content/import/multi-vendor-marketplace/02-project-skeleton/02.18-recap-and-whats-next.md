---
id: multi-vendor-marketplace-02-18
title: "2.18 — Recap and what's next"
module: 2
lesson: 18
stepType: Learn
phase: "Module 2"
summary: "You started Chapter 2 with an empty directory (or only a learning log). You finish with a **full-stack skeleton** — the foundation every later chapter bolts onto."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 2.18 — Recap and what's next

> **[Wrap]** · Chapter 02 — Project skeleton · *Tree diagram of repo as it stands now*

You started Chapter 2 with an empty directory (or only a learning log). You finish with a **full-stack skeleton** — the foundation every later chapter bolts onto.

## What you built — narrative recap

**Repository & habits:** Git initialized; `.gitignore` protects secrets; platform learning log habit established

**Structural decision:** Module-based server layout mandated — `modules/auth`, `stores`, `products`, `cart`, `orders` plus `shared/` for cross-cutting code.

**Server:** Express app separated into `app.js` (configure) and `server.js` (listen). `GET /health` returns JSON `{ "status": "ok" }`. CORS enables browser access from Vite dev server.

**Client:** Vite React with `pages/`, `components/`, `api/`, `hooks/`. React Router serves Home at `/`. `api/client.js` centralizes calls to `VITE_API_URL`. Home fetches and displays real health status.

**Proof:** curl and browser both confirm the API; stopping server surfaces error UI — not a mock.

## Your repo tree now

```
your-marketplace/
├── README.md
├── .gitignore
├── docs/
├── server/
│   ├── package.json
│   └── src/
│       ├── app.js
│       ├── server.js
│       ├── shared/
│       └── modules/
│           ├── auth/
│           ├── stores/
│           ├── products/
│           ├── cart/
│           └── orders/
└── client/
    ├── package.json
    ├── .env.example
    └── src/
        ├── App.jsx
        ├── main.jsx
        ├── pages/Home.jsx
        ├── api/client.js
        ├── components/
        └── hooks/
```

Compare to **2.3** — they should match.

## Decisions you can defend in a viva

Prepare short answers:

1. **Why module-based folders?** — Cohesion per domain; marketplace has many domains; course paths align.

2. **Why two entry files on server?** — Separate app wiring from listen for testability.

3. **Why CORS?** — Browser same-origin policy; client and API on different ports.

4. **Why `api/client.js`?** — Single place for base URL and future auth headers.

5. **Why health route first?** — Proves HTTP pipeline before MongoDB complexity.

## What's explicitly not built yet

| Not yet | Coming in |
|---|---|
| MongoDB connection | Chapter 4 |
| User / Product models | Chapter 5 |
| Auth | Chapter 6–7 |
| Vendor UI | Chapter 10–12 |
| Shop / cart / checkout | Chapters 14–19 |

Empty module folders are **intentional placeholders** — not missing work from Chapter 2.

## Next chapter — Why MongoDB (Chapter 3)

Before connecting a database in Chapter 4, Chapter 3 explains **why MongoDB fits this marketplace** — document model, embedding vs referencing, trade-offs vs SQL — and has you **draft an ERD** applied to your project.

Chapter 3 is shorter on build steps but still **touches your repo** (`docs/erd-draft.md`). It's not a detour from the product — it prepares the schema you implement in Chapter 5.

**Do not skip Chapter 3** to jump into Mongoose because you're eager — the decision chapter prevents schema mistakes that hurt through checkout.

## Next chapter after that — Config & database (Chapter 4)

You'll add:

- `server/.env` with `MONGODB_URI`
- `shared/config.js` validating env at boot
- `shared/db.js` connecting Mongoose
- Extended `/health` including database connection status

The skeleton you built **receives** those files in known locations — no restructure.

## Callback to working rhythm (1.7)

Before opening Chapter 3:

- Commit today's work if anything uncommitted  
- Glance `.gitignore` — no `.env` staged  
- Update your learning log answers on the platform

## Commit hygiene through Chapter 2

Suggested commit messages you might have after this chapter:

```
chore: initialize repository with gitignore and learning log
chore(server): scaffold module folders and entry stubs
feat(server): add express and dev scripts
feat(server): add GET /health endpoint
chore(client): scaffold vite react app with folder convention
feat(client): add router and Home page shell
feat(client): add api client helper and env example
feat(client): fetch and display API health on Home
docs: complete chapter 2 learning log
```

If your history looks like one commit `initial`, consider whether you can still explain each step — the viva may ask *when* health appeared relative to client wiring.

## Key milestones unlocked

- [x] "I can run server and client together"  
- [x] "I know where auth/products/orders code will live"  
- [x] "I've demo'd browser → API HTTP"  
- [ ] "MongoDB connected" — Chapter 4  
- [ ] "Vendor can list products" — Week 2  

## Continue to gate

**Sub-chapter 2.19:** Definition of Done checklist + learning log questions.

**Do not open Chapter 3 until 2.19 is fully ticked.**
