---
id: multi-vendor-marketplace-02-16
title: "2.16 — Verify end-to-end"
module: 2
lesson: 16
stepType: Build
phase: "Module 2"
summary: "**Why this step matters:** Informal \"it works on my machine\" is not a gate. This verification pass documents curl output, browser screenshot, and folder tree — evidence that Week 1's first demo brick is real before you open the database chapters."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 2.16 — Verify end-to-end

> **[Build]** · Chapter 02 — Project skeleton · *Both terminals running; screenshot-worthy first demo*

**Why this step matters:** Informal "it works on my machine" is not a gate. This verification pass documents curl output, browser screenshot, and folder tree — evidence that Week 1's first demo brick is real before you open the database chapters.

**Where the project is now:** Home fetches `/health`; server and client both run. This sub-chapter is a **formal verification pass** — treat it like a mini QA checklist before you claim Chapter 2 done.

**After this step:** Chapter 2 is proven with evidence — curl output, browser demo, folder tree, and ports all verified consistent — so Chapters 4–5 build MongoDB on a skeleton you trust.

## Full demo script

Perform these steps **in order** while recording optional screen capture for your portfolio:

### 1. Clean start

```bash
# Stop any running dev servers
# From repo root
cd server && npm run dev
# New terminal
cd client && npm run dev
```

### 2. API alone

```bash
curl -s http://localhost:5000/health | jq .
```

If `jq` not installed, use raw curl:

```bash
curl -i http://localhost:5000/health
```

**Pass criteria:** HTTP 200; body contains `"status":"ok"`.

### 3. Client alone

Open `http://localhost:5173`.

**Pass criteria:**

- Home loads without white screen  
- No uncaught errors in browser console (warnings OK)  
- API status area shows success with server running  

### 4. Failure mode

Stop server terminal. Refresh browser.

**Pass criteria:** Home shows **error state** — not blank, not fake ok.

Restart server. Refresh.

**Pass criteria:** Success returns.

### 5. Git hygiene

```bash
git status
```

**Pass criteria:** No `.env` staged; no `node_modules`; working tree clean or only intentional changes.

```bash
git log --oneline -5
```

**Pass criteria:** Multiple commits through Chapter 2 — not one giant commit.

## Repository tree audit

From repo root, run `find` or tree if installed:

```bash
# Linux/macOS
find . -type f -not -path '*/node_modules/*' -not -path '*/.git/*' | head -60
```

Compare mentally to target in **2.3**. Required presence:

| Path | Required |
|---|---|
| `server/src/app.js` | ✓ |
| `server/src/server.js` | ✓ |
| `server/src/modules/auth/` | ✓ |
| `server/src/shared/` | ✓ |
| `client/src/pages/Home.jsx` | ✓ |
| `client/src/api/client.js` | ✓ |
| `.gitignore` | ✓ |

## Ports and env audit

| Setting | Expected value |
|---|---|
| Server `PORT` | 5000 unless you changed consistently |
| `VITE_API_URL` | Matches server origin exactly |
| CORS origin | Matches Vite URL (5173 default) |

If you changed any port, **grep** repo for old port references:

```bash
grep -r "5000" --include="*.md" --include="*.env*" --include="*.js" --include="*.jsx" .
```

Consistency prevents mysterious failures in Chapter 7.

## Screenshot-worthy moment

Capture one image showing:

- Browser: Home with API status ok  
- Optional: terminal with server log + curl output side by side  

Store in `docs/screenshots/` if you want a portfolio folder — optional, not gate.

## Common failures at this stage

| Failure | Diagnosis |
|---|---|
| CORS error | Server cors origin mismatch |
| `status ok` in UI but curl fails | You hardcoded UI — fix 2.15 |
| curl works, UI errors | Wrong `VITE_API_URL`; restart Vite |
| Both fail | Server not running or wrong port |
| `import` errors in server | ESM path missing `.js` extension |

Fix before proceeding — Chapter 4 adds MongoDB on top of this skeleton.

## Performance note

Health check should respond in milliseconds locally. If curl hangs, server not listening or firewall — not MongoDB (not connected yet).

## ✅ Do / ❌ Don't

**✅ Do:** Run this verification checklist every time you clone repo on a new machine.

**✅ Do:** Save screenshot — first full-stack win matters for motivation.

**❌ Don't:** Skip failure-mode test — error UI is part of Done.

**❌ Don't:** Proceed to Chapter 3 with server-only working — client must show health.

## Week 1 progress marker

Chapter 2 completes **first brick** of Week 1 demo:

| Week 1 target | After Ch 2 | After Ch 4–5 |
|---|---|---|
| Both apps run | ✓ | ✓ |
| Health in browser | ✓ | ✓ |
| MongoDB + seed | | ✓ |

You're one third of Week 1 foundations on **running code** — half on **data** after next chapters.

## Verify

- [ ] Demo script steps 1–5 passed  
- [ ] Tree audit matches module convention  
- [ ] Ports/env consistent  
- [ ] Optional screenshot captured  

## Next

**Sub-chapter 2.17:** Optional stretch — `/api/version` route + UI display.

Then recap and chapter gate.
