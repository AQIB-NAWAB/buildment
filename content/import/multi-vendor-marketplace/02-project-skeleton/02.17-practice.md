---
id: multi-vendor-marketplace-02-17
title: "2.17 — Practice (optional stretch)"
module: 2
lesson: 17
stepType: Learn
phase: "Module 2"
summary: "This sub-chapter is **optional**. Clear the Chapter 2 gate in **2.19** without it. Complete it if you want extra practice wiring **server route → client display** before Chapter 3 adds concepts without new UI."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 2.17 — Practice (optional stretch)

> **[Stretch]** · Chapter 02 — Project skeleton · *Optional: add a `/api/version` route and show it in UI*

This sub-chapter is **optional**. Clear the Chapter 2 gate in **2.19** without it. Complete it if you want extra practice wiring **server route → client display** before Chapter 3 adds concepts without new UI.

## Stretch goal

Add a second public endpoint on the API and surface it on Home (or a footnote section).

## Server contract to implement

| Method | Path | Auth | Success |
|---|---|---|---|
| GET | `/version` | None | `200` JSON |

Response body **minimum:**

```json
{
  "version": "0.1.0",
  "name": "GreenBasket API"
}
```

Use your product name and a semver string you choose. **You implement the route** — same pattern as `/health`.

Mount at root `/version` or under `/api/version` — if you prefix, update client helper path to match.

## Client task

1. Add `getVersion()` to `api/client.js` (or extend generic GET helper)
2. On Home, fetch version after health succeeds — or in parallel with `Promise.all`
3. Display version string under API status

**Handle errors** — version fetch failing shouldn't crash page if health succeeded (decouple state).

## Verify

```bash
curl -s http://localhost:5000/version
```

Browser shows version string matching API.

## Why this exercise helps

Repeats the Chapter 2 loop without new concepts:

```
define contract → implement route → add client helper → render in UI → curl + browser verify
```

Chapter 6 adds ten routes — muscle memory from this stretch reduces friction.

## Commit if completed

```bash
git commit -m "feat: add version endpoint and display on Home"
```

## Skip if

You're behind on time or confident — move to **2.18-recap-and-whats-next.md**.

## Alternative stretches (pick one)

If `/version` feels boring:

**A. Health timestamp** — Add ISO timestamp to `/health` response; display "last checked" on Home.

**B. Server name banner** — Read `SERVICE_NAME` from server env (non-secret); return in health JSON; show on Home.

**C. Loading skeleton** — Replace text "Loading…" with simple CSS pulse skeleton on Home.

Each repeats: contract → implement → verify → commit.

## Reflection question (for learning log optional section)

*"What broke when you added the second endpoint — if anything? How did you debug it?"*

Write two sentences if you complete stretch — good viva material.

## Key ideas

- **Optional** — gate does not require this  
- **Same loop** as health: route + client + UI  
- **Commit** if you do it; skip if time-constrained  

## Timebox

If you choose this stretch, cap at **45 minutes**. If not done, skip — Chapter 3 is reading-heavy and benefits from fresh attention.

## Verification checklist for stretch

- [ ] curl returns 200 for new endpoint  
- [ ] Home displays new field without breaking health display  
- [ ] Committed with clear message  

No gate item requires this stretch.
