---
id: multi-vendor-marketplace-14-01
title: "14.1 — Set the scene"
module: 14
lesson: 1
stepType: Read
phase: "Module 14"
summary: "**Where the project is now:** Week 2 vendor experience is complete — stores, products, photos. All product reads so far are **vendor-scoped** (`GET /vendor/products`). Customers have accounts (Chapters 6–7) but no way to browse the market."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 14.1 — Set the scene

> **[Read]** · Chapter 14 — Browse catalogue API · *Customers need to see all vendors' products*

**Where the project is now:** Week 2 vendor experience is complete — stores, products, photos. All product reads so far are **vendor-scoped** (`GET /vendor/products`). Customers have accounts (Chapters 6–7) but no way to browse the market.

**Where this chapter goes:** A **public catalogue API** — `GET /products` returns published, in-stock items from all vendors, with filters, pagination, and store name populated **without N+1 queries**.

This is a **build chapter**. Sub-chapters 14.8 onward add routes and query logic. The gate is **runnable proof**: curl returns paginated catalogue; query count stays flat when listing many products.

## Bridge from last chapter

You finished **Week 2 — the vendor experience** (Chapters 9–13). Vendors can register, open a shop, add products with photos, and see them in the dashboard. Sub-chapter **[13.14 — Bridge to Week 3](../13-product-photos/13.14-bridge-to-week-3-customer-experience.md)** spelled out the biggest **role change** in the course: you stop thinking like a shop owner and start thinking like a shopper.

**Read 13.14 if Week 3 feels sudden.** It explains why customer features come after vendor work, why new API routes sit **beside** vendor routes (not instead of them), and why a multi-vendor cart shows up later.

What you carry into Chapter 14:

| From Week 2 | Why it matters now |
|---|---|
| Products in MongoDB with `isPublished`, `stockQty`, `imageUrl`, `storeId` | Public catalogue reads the same documents vendors created |
| `GET /vendor/products` — vendor JWT, own store only | Stays unchanged; Chapter 14 **adds** public `GET /products` |
| Seed with **≥2 stores** and published products | Empty shops make browse demos look broken when the problem is Week 2 |
| Chapter 13 gate — photo URLs on products | Shop cards in Chapter 15 need `imageUrl` in API responses |

If the Week 2 demo is not true in your browser yet — vendor logs in, adds a product with a photo, sees it in the dashboard — **finish that before Chapter 14**. Week 3 builds on inventory vendors already published.

## In plain English

| Term | Simple meaning |
|---|---|
| **Public catalogue** | The shop window — products any visitor can browse without being a vendor |
| **Vendor-scoped route** | API that only returns **your** inventory when **you** are logged in as vendor |
| **N+1 query problem** | Loading a list, then running one extra database query **per row** — fast with 5 items, slow with 500 |
| **Populate** | Mongoose fetching related documents (e.g. store name) in a batched way instead of a loop |
| **Pagination** | Returning products in pages (e.g. 20 at a time) instead of the whole catalogue at once |
| **DTO** | The shape of JSON the API sends — public browse hides draft products and vendor secrets |

## Think of it like a farmers' market hall

Week 2 you built **individual stalls** — each vendor's store and shelves. Chapter 14 builds the **central aisle directory**: a board listing every stall's published goods, with stall name on each card, filters for "vegetables only" or "this stall only," and page numbers so the board does not list ten thousand tomatoes at once.

Customers walk the aisle (Chapter 15 UI). Vendors still manage their stall from the back (vendor dashboard unchanged).

## Common beginner questions

**Q: Do I delete vendor routes or the vendor dashboard?**  
No. You **add** public read routes. Vendors keep `GET /vendor/products` for their own inventory.

**Q: Why API before the shop UI (Chapter 15)?**  
Same pairing as Chapters 6→7 and 11→12: **backend contract first**, React second. Building the grid before `GET /products` exists forces fake hardcoded products you throw away.

**Q: Can customers browse without logging in?**  
Yes for this chapter's gate — public `GET /products` needs no JWT. Cart and checkout (Chapters 16–18) require login.

**Q: What's the difference between `/vendor/products` and `/products`?**  
`/vendor/products` = "show **my** products" (vendor token). `/products` = "show **everyone's published, in-stock** products" (no token).

**Q: I only have products from one store in seed — is that OK?**  
Finish Week 2 seed with **two stores** before the gate. Multi-vendor browse is the whole point of a marketplace course.

**Q: What is N+1 and why should I care before I have users?**  
It is a pattern bug: 20 products → 21 database trips instead of 2. Dev feels fine; production does not. Chapter 14 teaches you to **measure** query count before shipping the shop UI.

## The gap between "vendor inventory" and "marketplace"

Right now your API can answer:

- What products does **this vendor** sell?
- Can Vendor A edit Vendor B's product?

It cannot answer:

- What can **any customer** browse without logging in (or with customer login)?
- How do we filter by category or store?
- How do we page through 200 tomatoes without loading all at once?

Those are **public read model** problems — different from vendor CRUD in Chapter 11.

## Why this chapter starts Week 3 on the backend

Chapter 15 builds the shop UI. UI without a stable API contract produces mock data and rework. **Backend first** — same pairing as 06→07, 11→12, 14→15.

Chapter 14 assumes:

- Product model has `isPublished`, `stockQty`, `category`, `storeId`, `imageUrl`
- Seed data includes products from **at least two stores**
- Vendor routes remain unchanged — public route is **additive**

## Public vs vendor routes — mental split

| Audience | Route pattern | Auth | Data scope |
|---|---|---|---|
| Vendor | `GET /vendor/products` | Required vendor JWT | Own store only |
| Customer/public | `GET /products` | Optional none for browse | All published, in-stock |
| Vendor write | `POST/PATCH/DELETE /products` | Vendor + ownership | Own only |

Never expose draft products or internal fields on public DTO.

## Week 3 demo preview

| Week 3 target | After Ch 14 | After Ch 15 |
|---|---|---|
| Customer browses products | API only | UI |
| Filters work | API | UI |
| Two vendors visible | API | UI |

Phase demo language includes browse → cart → checkout — catalogue API is the first brick.

## What this chapter is not

- **Not** shop React pages — Chapter 15  
- **Not** cart — Chapter 16  
- **Not** full-text search — Chapter 21 (basic filter only here)  
- **Not** Redis cache — Chapter 21  
- **Not** changing vendor CRUD behaviour

## Reading pace

| Sub-chapter | Cluster | Focus |
|---|---|---|
| 14.1–14.3 | Open | Public catalogue, N+1, deliverables |
| 14.4–14.6 | Learn | populate, pagination, cursor vs offset |
| 14.7 | Check | Quiz reserved |
| 14.8–14.14 | Build | route → filters → pagination → populate → test |
| 14.15–14.16 | Wrap | Recap + gate |

Estimated time: **one to two days** — query tuning and N+1 verification take thought.

## Self-check before continuing

- [ ] Chapter 13 gate cleared — products have `imageUrl` in seed/dashboard  
- [ ] Seed has ≥2 stores with published products  
- [ ] You understand difference between `/vendor/products` and `/products`  
- [ ] Mongoose populate docs skimmed (14.4)

## Next

**Sub-chapter 14.2:** The N+1 trap — one query per product for store name.

Understand the cost before you write the list handler.
