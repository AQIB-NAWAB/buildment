---
id: multi-vendor-marketplace-22-06
title: "22.6 — Prime your thinking"
module: 22
lesson: 6
stepType: Learn
phase: "Module 22"
summary: "Before you write `shared/queue.js` and `worker.js`, read how **BullMQ** expects Queues and Workers to connect to Redis. This sub-chapter is a **mandatory reading assignment**."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 22.6 — Prime your thinking

> **[Learn]** · Chapter 22 — Async jobs & queues · *Mandatory read: BullMQ docs*

Before you write `shared/queue.js` and `worker.js`, read how **BullMQ** expects Queues and Workers to connect to Redis. This sub-chapter is a **mandatory reading assignment**.

<BigWordAlert term="At-least-once delivery" plainEnglish="Jobs may run more than once on failure/retry — handlers must be idempotent." whyItMatters="FreshMarket order email job retry must not send duplicate receipts to shopper." />

Chapter 21 taught cache-aside. This read covers **producer/consumer job flow** — so your checkout enqueue in 22.11 matches official patterns.


```predict
prompt: "Checkout commits an order, but the notification worker is temporarily offline. What should the API do?"
options:
  - id: enqueue-return
    label: "Persist the job in Redis and return checkout success"
  - id: wait-worker
    label: "Hold the HTTP request open until the worker sends the notification"
correctOptionId: enqueue-return
explanation: "The queue decouples commerce from notification delivery. A durable job can wait for the worker and retry later without turning a successful order into an HTTP failure."
allowRetry: true
```


## Mandatory read

**Primary:** BullMQ — Quick Start  
https://docs.bullmq.io/guide/quick-start

**Focus on these sections:**

1. **Adding jobs to the queue** — `queue.add(name, data, opts)`
2. **Workers** — `new Worker(queueName, processor, { connection })`
3. **Connections** — `ioredis` / `REDIS_URL` reuse
4. **Job options** — `attempts`, `backoff`, `jobId` (you use in 22.14)
5. **Separate processes** — producer and consumer in different Node processes

**Secondary (skim):** Retrying failing jobs  
https://docs.bullmq.io/guide/retrying-failing-jobs

Enough to configure `attempts: 3` and understand exponential backoff — implement in 22.14.

## What to extract for your implementation

While reading, note answers — you'll implement in 22.9–22.12:

| Question | Why it matters |
|---|---|
| Does `queue.add()` return a Promise? | Await in handler — fast, only enqueues |
| Must Worker use same queue name as Queue? | Typo = jobs never processed |
| What happens if worker is not running? | Jobs sit in Redis — API still 201 |
| How to pass `jobId` for deduplication? | Prevent duplicate notifications |
| Graceful worker shutdown? | Optional — `worker.close()` on SIGTERM for Ch 23 |

**Do not copy-paste** the entire quick-start into one file — split `queue.js` vs `worker.js` per 22.3 blueprint.

## BullMQ vs raw Redis commands

| Concept | BullMQ handles |
|---|---|
| Job persistence | Serialized payload in Redis |
| At-least-once delivery | Worker ack/nack |
| Retry scheduling | Delayed zset |
| Concurrency | Worker `concurrency` option |

You implement **processor functions** — not LPUSH/BRPOP manually.

## Common beginner mistakes (read before coding)

1. **Worker not started** — jobs enqueue; nothing logs; mentee thinks checkout broken.  
2. **Queue name mismatch** — `notifications` vs `notification`.  
3. **Enqueue before DB commit** — worker runs before order exists.  
4. **Throwing in processor without retry config** — job lost or stuck.  
5. **Same Redis `FLUSHALL` during dev** — wipes cache from Ch 21 mid-demo.  

Build order in this chapter: queue module → worker boots → checkout enqueue → processor logs.

<InterestingRead title="Why checkout should not send email inline" hook="SMTP is slow and flaky compared to HTTP." readMinutes={4}>

Sending vendor notification emails inside the payment request adds failure modes where money succeeded but the user sees an error. Queue the email; return success when the order is saved and payment confirmed.

</InterestingRead>

## Reflect — write in notebook or learning log draft

After reading, answer without opening the docs:

1. What is the difference between a **Queue** and a **Worker**?  
2. Why must the worker run as a **separate process** for this course?  
3. What happens to jobs if Redis is up but worker is down?  
4. How does `jobId` help with duplicate `order.created` enqueue?

If you can't answer #1, re-read Quick Start "Producers" and "Consumers".

## How this connects to Chapter 23

Production runs **two processes** from one repo: API + worker. PM2 ecosystem file or platform second service — preview only; implement in deploy chapter.

Worker needs same env: `REDIS_URL`, `MONGODB_URI` (if processor loads User/Order), `NODE_ENV`.

<RealWorldEvent title="Shopify flash sale queue architecture" when="2010s–present" summary="Commerce platforms moved heavy work to background jobs during traffic spikes so checkout stayed available." lesson="Design jobs to be idempotent — retries will happen, and duplicate emails beat lost orders." />

## Time box

**Minimum:** 35 minutes focused reading + reflect questions.  
**If rushed:** Quick Start + Retrying jobs — skip advanced flows and sandbox.

## ✅ Do / ❌ Don't

**✅ Do:** Read official BullMQ docs — version matches `npm install bullmq`.

**✅ Do:** Keep docs tab open while writing `worker.js` in 22.10.

**❌ Don't:** Use deprecated `bull` v3 tutorials — API differs from BullMQ.

**❌ Don't:** Skip reading because "Redis is just a list" — BullMQ semantics matter.

## Next

**Sub-chapter 22.7:** Queue reliability knowledge check — complete it before **22.8** starts the build.

Reading done → hands on keyboard in the build cluster.
