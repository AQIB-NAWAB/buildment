---
id: multi-vendor-marketplace-22-13
title: "22.13 — Order status job"
module: 22
lesson: 13
stepType: Build
phase: "Module 22"
summary: "**Why this step matters:** Status changes are the second hot path for side effects — vendors PATCH fast, customers learn later. Queuing here keeps fulfilment UI snappy and mirrors how real marketplaces notify on dispatch."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 22.13 — Order status job

> **[Build]** · Chapter 22 — Async jobs & queues · *Enqueue from vendor order status PATCH*

**Why this step matters:** Status changes are the second hot path for side effects — vendors PATCH fast, customers learn later. Queuing here keeps fulfilment UI snappy and mirrors how real marketplaces notify on dispatch.

<BigWordAlert term="Status change notification" plainEnglish="Background job notifying customer when vendor updates order to shipped or delivered." whyItMatters="FreshMarket shoppers get email on status change without blocking vendor's PATCH request." />

**Where the project is now:** `order.created` flows queue → worker → processor (22.11–22.12); status changes are not queued at all.

**After this step:** `PATCH /vendor/orders/:orderId` enqueues `order.statusUpdated` after status saves — customer notification runs in worker.

## Enqueue call site — spec

**File:** `orders.service.js` — inside `updateOrderStatus()` (or equivalent from Ch 20) **after** successful `save()`.

**Order of operations:**

```
1. Ownership + transition validation  (sync)
2. Persist new status  (sync)
3. enqueueOrderStatusUpdated({ orderId, customerId, previousStatus, newStatus, storeId })
4. return updated order JSON
```

## API contract (unchanged externally)

```
PATCH /api/vendor/orders/:orderId
Authorization: Bearer <vendor-token>
{ "status": "packed" }
```

**Response 200:** Order document with new `status` — same as Chapter 20.

## enqueueOrderStatusUpdated() — contract

| Field | Source |
|---|---|
| `orderId` | Route param |
| `customerId` | From order document |
| `previousStatus` | Value before update |
| `newStatus` | Request body status |
| `storeId` | Order's store — for log context |

**Job name:** `JOB_NAMES.ORDER_STATUS_UPDATED`

## Processor — behaviour contract

**File:** `orderStatusUpdated.job.js`

**Log mock customer notification:**

```
[NOTIFY][customer][status] orderId=<id> pending→packed store=<storeName>
```

**You implement** — fetch store name optional; ids sufficient for gate.

Log the transition as structured fields (`previousStatus`, `newStatus`, `orderId`) rather than interpolating a full order document. This gives the retry lesson enough context without leaking customer data.

## Valid transitions reminder

Only enqueue **after** valid transition accepted — not on 400/403 responses.

| Response | Enqueue? |
|---|---|
| 200 status saved | ✓ |
| 400 invalid transition | ✗ |
| 403 wrong vendor | ✗ |
| 404 order missing | ✗ |

## jobId — spec

```javascript
// illustrative — dedupe rapid double PATCH
jobId: `order-status:${orderId}:${newStatus}`
```

Same status enqueued twice → BullMQ ignores duplicate `jobId` — see 22.14.

## curl verification

<ApiRequestPanel title="Status update enqueue — PATCH returns before worker log">

<ApiRequest
  label="Vendor PATCH pending → packed (worker running in other terminal)"
  method="PATCH"
  url="http://localhost:5000/api/vendor/orders/<orderId>"
  bearer="<vendor-token>"
  body='{"status":"packed"}'
  responseStatus={200}
  responseBody='{"_id":"<orderId>","status":"packed","storeId":"...","total":305}'
/>

</ApiRequestPanel>

**Expected:** 200 JSON immediately. Worker logs `[NOTIFY][customer][status]` shortly after.

## Customer UI note

Chapter 20 UI refetches orders — status visible without waiting for notification job. Queue is parallel concern.

## ✅ Do / ❌ Don't

**✅ Do:** Pass `previousStatus` for meaningful log message.

**✅ Do:** Keep PATCH handler fast — no inline customer email.

**❌ Don't:** Enqueue on rejected transitions.

**❌ Don't:** Notify vendor on status change — customer only for this job.

## Verify checklist

- [ ] Valid PATCH returns 200 before worker log
- [ ] Worker shows status notification log
- [ ] Invalid PATCH does not enqueue
- [ ] Vendor B cannot trigger jobs for Vendor A's orders
- [ ] Enqueue and worker logs share the job ID and order ID


## Next

**Sub-chapter 22.14:** Retries and idempotency — the job updates status; make it safe to retry without double effects.
