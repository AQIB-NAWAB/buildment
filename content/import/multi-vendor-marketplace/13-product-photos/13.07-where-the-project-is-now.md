---
id: multi-vendor-marketplace-13-07
title: "13.7 — Where the project is now"
module: 13
lesson: 7
stepType: Build
phase: "Module 13"
summary: "Upload infrastructure sits on top of working product CRUD. If the dashboard or API is broken, multipart errors will mislead you. Confirm Chapters 11–12 gates and `imageUrl` on the model before touching Cloudinary or multer."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 13.7 — Where the project is now

> **[Build]** · Chapter 13 — Product photos · *Products without images*

**Why this step matters:** Upload infrastructure sits on top of working product CRUD. If the dashboard or API is broken, multipart errors will mislead you. Confirm Chapters 11–12 gates and `imageUrl` on the model before touching Cloudinary or multer.

**Build cluster starts here.** Before adding Cloudinary env vars or multer, confirm your repo matches what Chapters 11–12 left behind — and that gates are cleared.

## Prerequisites gate

| Prior gate | Verify |
|---|---|
| Chapter 11 | Vendor product CRUD via API; cross-vendor PATCH → 403 |
| Chapter 12 | Product list, create, edit, publish in browser |
| Chapter 8 | `requireAuth`, `requireRole('vendor')`, ownership helpers |
| Chapter 4 | `.env` gitignored; `.env.example` committed |
| Product model | `imageUrl` field exists (optional string) |

If vendor dashboard is broken, fix Chapter 12 before debugging upload — multipart errors mask routing bugs.

## Server tree — as of Chapter 12 end

```
server/src/modules/products/
  products.routes.js      ← GET/POST/PATCH/DELETE vendor routes
  products.service.js
  products.schema.js      ← Product model with storeId, grocery fields

server/src/shared/
  config.js
  db.js

server/src/modules/auth/
  auth.middleware.js      ← requireAuth
```

**What changes in Chapter 13:**

```
server/
├── .env.example              ← + CLOUDINARY_* vars
└── src/
    ├── shared/
    │   ├── config.js         ← validate Cloudinary vars
    │   └── cloudinary.js     ← NEW
    └── modules/products/
        ├── products.routes.js    ← + POST /:id/image
        ├── products.service.js   ← + image helpers
        └── upload.middleware.js  ← NEW multer
```

## Client tree — as of Chapter 12 end

```
client/src/pages/vendor/
  ProductList.jsx
  CreateProduct.jsx
  EditProduct.jsx

client/src/api/
  client.js               ← Bearer interceptor from Ch 7
  products.js             ← optional API helpers
```

**Chapter 13 client changes:** ImageUpload component; thumbnails in list; file input on edit (or post-create upload step).

## Current product document shape (expected)

| Field | Type | Chapter 13 change |
|---|---|---|
| `name`, `price`, `unit`, etc. | various | unchanged |
| `storeId` | ObjectId ref | unchanged |
| `isPublished` | Boolean | unchanged |
| `imageUrl` | String | set by upload route |
| `imagePublicId` | String | optional add for delete |

Compass check: existing products may have empty or missing `imageUrl` — valid.

## Routes today vs after Chapter 13

| Route | Today | After Ch 13 |
|---|---|---|
| `POST /products` | Create product JSON | unchanged — image optional separate step |
| `PATCH /products/:id` | Update fields | unchanged — or exclude image from JSON patch |
| `POST /products/:id/image` | — | **NEW** multipart upload |

Keep image upload **separate** from JSON PATCH — cleaner multer wiring.

## Dependencies today

`server/package.json` includes express, mongoose, auth deps from prior chapters.

**Chapter 13 adds:**

- `cloudinary`  
- `multer`

## Two terminals — habit unchanged

| Terminal | Directory | Command |
|---|---|---|
| 1 | `server/` | `npm run dev` |
| 2 | `client/` | `npm run dev` |

After 13.8, server should **fail fast** if Cloudinary env vars missing — same discipline as `MONGODB_URI`.

## Git hygiene checkpoint

```bash
git check-ignore -v server/.env
```

Cloudinary API secret goes in `.env` only — update `.env.example` with placeholders in 13.8.

## What you will not touch this chapter

- Public `GET /products` — Chapter 14  
- Shop UI — Chapter 15  
- Cart — Chapter 16  
- Multiple images per product — out of scope

## Build sequence reminder

| Sub-chapter | Action |
|---|---|
| 13.8 | Cloudinary config + upload helper |
| 13.9 | multer + POST `/products/:id/image` |
| 13.10 | Replace — delete old Cloudinary asset |
| 13.11 | Client file input + FormData |
| 13.12 | End-to-end verify + Week 2 demo prep |

## ✅ Do / ❌ Don't

**✅ Do:** Verify vendor JWT still works on existing routes before adding upload.

**❌ Don't:** Add Cloudinary secret to `VITE_*` client env.

**❌ Don't:** Start Chapter 14 public catalogue until 13.14 gate cleared — Week 2 demo includes photos.

## Verify before 13.8

- [ ] Vendor can create and publish product without image  
- [ ] `imageUrl` field on schema  
- [ ] Cloudinary account credentials ready  
- [ ] Chapter 12 learning log committed

## Next

**Sub-chapter 13.8:** Cloudinary config — env vars and server-side upload helper.

The build cluster begins.
