---
id: multi-vendor-marketplace-13-01
title: "13.1 — Set the scene"
module: 13
lesson: 1
stepType: Read
phase: "Module 13"
summary: "**Where the project is now:** Chapters 11–12 gave vendors a product CRUD API and dashboard. Products have names, prices, units, stock — but no photos. A grocery marketplace without images feels like a spreadsheet. Customers need to see what they're buying; vendors need their shel"
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 13.1 — Set the scene

> **[Read]** · Chapter 13 — Product photos · *Grocery needs photos; MongoDB is not a photo album*

**Where the project is now:** Chapters 11–12 gave vendors a product CRUD API and dashboard. Products have names, prices, units, stock — but no photos. A grocery marketplace without images feels like a spreadsheet. Customers need to see what they're buying; vendors need their shelves to look real.

**Where this chapter goes:** Product images live in **Cloudinary** (object storage). MongoDB stores only the **URL string** on the product document — never binary data, never base64 blobs.

This is a **build chapter**. Every sub-chapter from 13.8 onward adds upload infrastructure to your repo. The gate is **runnable proof**: vendor uploads a photo, URL persists in MongoDB, image renders in the dashboard list.

**Chapter arc note:** Sub-chapter **13.14** is the deliberate **Week 2 → Week 3 bridge** — read it after the recap (13.13) and **before** the gate checklist (13.15). It explains the shift from vendor mindset to customer mindset. Do not skip straight to 13.15.

## Bridge from last chapter

Chapter 12 gave vendors a **product dashboard** — table, create form, edit form, publish toggle — all working in the browser. Products have names, prices, and stock, but shelves still look like a spreadsheet without images. Chapter 13 adds the **photo pipeline**: bytes in Cloudinary, URL string in MongoDB, thumbnail in the product list. Week 2's vendor demo finishes here — then 13.14 prepares you for buyer-side chapters.

## In plain English

| Term | Simple meaning |
|---|---|
| **Object storage** | Service that holds files (images) — Cloudinary, S3, etc. |
| **`imageUrl`** | Short string on the product document pointing to the hosted image |
| **Multipart upload** | HTTP request carrying a file — not JSON |
| **multer** | Express middleware that parses file uploads |
| **`imagePublicId`** | Cloudinary's id for delete/replace — not the same as MongoDB `_id` |
| **CDN** | Content delivery network — serves images fast worldwide |
| **Base64 trap** | Storing image data inside MongoDB documents — avoid this |

## Think of it like a market photographer

Chapter 12 listed what's on each shelf in text. Chapter 13 hires a **market photographer** — photos hang on the public display board (Cloudinary CDN), and the stall ledger (MongoDB) only records **where to find the picture**, not the picture itself. Replacing a photo means removing the old print from the wall, not stapling a new one on top.

## Common beginner questions

**Q: Why not store images in MongoDB?**  
Documents balloon, queries slow, backups hurt. Store a URL string; let Cloudinary hold bytes. Sub-chapter 13.2 explains the cost in detail.

**Q: Why upload through the server instead of directly from the browser?**  
Your Cloudinary **secret** must never ship in the React bundle. Server upload also reuses ownership checks from Chapter 8.

**Q: Can I skip photos and still pass Week 2?**  
Photos are part of the Week 2 demo — *"vendor adds a product with a photo"*. Plan for Cloudinary setup in 13.8.

**Q: One image enough?**  
Yes for MVP. Multiple images per product is out of scope.

**Q: What comes after the checklist?**  
**13.14** bridges to Week 3 (customer experience). **13.15** is the gate. Chapter 14 builds the public catalogue API — buyer side, not vendor.

## The gap between "text catalogue" and "shop window"

Right now your vendor dashboard can answer:

- What products does this store sell?
- What is the price per kilogram?

It cannot answer:

- What does the produce look like?
- Where do image bytes live without bloating MongoDB?
- What happens when a vendor replaces a photo?

Those are **object storage and upload pipeline** problems — not CRUD problems. Chapter 12 built forms; **Chapter 13 wires the camera**.

## Why this chapter sits after the vendor dashboard

Chapter 12 proved the vendor can create and publish products in the browser. Adding file upload to an unfinished product flow would mix two failure modes — form validation bugs vs upload bugs.

Chapter 13 assumes:

- **`POST /products`** and **`PATCH /products/:id`** work with ownership checks
- Product schema has an **`imageUrl`** field (string, optional until upload)
- Vendor dashboard create/edit pages exist

If Chapter 12 gate is incomplete, finish product CRUD in UI first — upload attaches to existing products.

## What "product photos" means in production terms

Professional marketplaces treat images as **assets in object storage**, referenced by URL in the database:

| Concern | Where it lives | Why |
|---|---|---|
| Image bytes | Cloudinary (or S3, etc.) | CDN delivery; resize/transform; not your DB |
| Public URL | `product.imageUrl` in MongoDB | Small string; queryable; cacheable |
| Upload credentials | `server/.env` — Cloudinary API secret | Server-only; never in React bundle |
| Multipart file | Request body → multer → stream to Cloudinary | Browser sends file; server proxies upload |

**Never store images in MongoDB documents** — next sub-chapter explains the cost.

## Continuity from Chapter 12 — files you already have

```
server/src/modules/products/
  products.routes.js     ← vendor CRUD routes
  products.service.js
  products.schema.js     ← imageUrl field (optional string)

client/src/pages/vendor/
  ProductList.jsx        ← table without thumbnails yet
  CreateProduct.jsx
  EditProduct.jsx        ← upload UI lands here (13.11)
```

Your **auth and ownership middleware** from Chapters 8 and 11 still apply — upload is a vendor-only action on **own** products.

## The Week 2 demo milestone

| Week 2 target | After Ch 12 | After Ch 13 |
|---|---|---|
| Vendor registers and opens store | ✓ | ✓ |
| Vendor adds products | ✓ | ✓ |
| Product has photo | | **this chapter** |
| Photo visible in dashboard | | **this chapter** |

Phase demo language: *"Vendor registers, opens a store, adds a product with a photo, sees it in their dashboard"* — Chapter 13 completes the photo items.

## What this chapter is not

- **Not** public catalogue browsing — Chapter 14–15  
- **Not** image optimization theory — Cloudinary handles transforms  
- **Not** multiple images per product — one `imageUrl` for MVP  
- **Not** client-side direct upload to Cloudinary unsigned — server holds secret  
- **Not** optional for Week 2 gate — photos are part of the vendor demo

## Reading pace

| Sub-chapter | Cluster | Focus |
|---|---|---|
| 13.1–13.3 | Open | Problem, base64 trap, deliverables |
| 13.4–13.5 | Learn | Object storage pattern, Cloudinary docs |
| 13.6 | Check | Quiz reserved |
| 13.7–13.12 | Build | config → upload route → UI → verify |
| 13.13–13.15 | Wrap | Recap → Week 2→3 bridge (13.14) → gate (13.15) |

Estimated time: **one full day** — Cloudinary account setup plus multer wiring.

## Self-check before continuing

- [ ] Chapter 12 gate cleared — vendor creates/edits/publishes in UI  
- [ ] Product model has `imageUrl` (string, optional)  
- [ ] Cloudinary free account ready (or account created in 13.8)  
- [ ] Chapter 4 secrets discipline still true — no `.env` in Git

## Next

**Sub-chapter 13.2:** The base64 trap — why storing image data in MongoDB breaks at scale.

Your shelves have names and prices. Next they get a face — stored in the right place.
