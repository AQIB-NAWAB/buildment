---
id: multi-vendor-marketplace-13-15
title: "13.14 — Checklist and learning log"
module: 13
lesson: 15
stepType: Gate
phase: "Module 13"
summary: "Gate for Chapter 13 — Product photos. Tick every box and answer the learning log on this page before continuing."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 13.14 — Checklist and learning log
> **[Gate]** · Chapter 13 — Product photos

This is the **gate** for Chapter 13. Tick every box honestly and answer the learning log below before opening Chapter 14.

## Chapter 13 — Product photos checklist (gate)

Do **not** open Chapter 14 until every box is ticked and learning log answers are written below.

### Cloudinary & secrets

- [ ] `CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET` in `server/.env.example` (placeholders only)
- [ ] Real credentials in local `server/.env` — **not** committed
- [ ] `shared/config.js` validates Cloudinary vars at startup
- [ ] No Cloudinary API secret in client code or `VITE_*` env vars
- [ ] `cloudinary` and `multer` in `server/package.json`

### Server upload pipeline

- [ ] `shared/cloudinary.js` (or equivalent) exports upload + delete helpers
- [ ] Multer middleware limits size (≤5 MB) and image MIME types
- [ ] `POST /products/:id/image` requires auth + vendor role
- [ ] Ownership check — vendor cannot upload to another vendor's product (403)
- [ ] Success updates `imageUrl` on product document
- [ ] `imagePublicId` stored if delete-on-replace implemented
- [ ] Replace flow removes previous Cloudinary asset when applicable
- [ ] Errors return consistent JSON — no secret leakage in messages

### MongoDB contract

- [ ] Product documents contain **URL string only** — no embedded image bytes or base64
- [ ] Compass inspection confirms small document size for product with image
- [ ] `imageUrl` loads in browser when pasted directly

### Client

- [ ] Vendor can upload image through UI (not curl-only)
- [ ] Product list displays thumbnail when `imageUrl` present
- [ ] Placeholder when no image
- [ ] FormData upload works with Bearer auth interceptor
- [ ] Client-side validation mirrors server limits (UX)

### Verification (13.12)

- [ ] Full flow: upload → DB URL → list thumbnail → page refresh persists
- [ ] Replace image updates URL and removes old asset (or documented exception)
- [ ] Cross-vendor upload test returns 403
- [ ] Product list API response size remains reasonable (no embedded images)

### Prior gates still true

- [ ] Chapter 12 vendor dashboard CRUD still works
- [ ] Chapter 11 product API ownership still enforced
- [ ] Week 2 demo path rehearsed: register → shop → product → photo → dashboard
- [ ] I can explain object storage vs storing images in MongoDB
- [ ] I can trace upload flow: browser → multer → Cloudinary → MongoDB URL
- [ ] I can explain why upload is a separate route from JSON PATCH
- [ ] I can explain what happens on image replace
- [ ] I know public shop will use `imageUrl` in `<img>` without API proxy

### Learning log — write now

Answer **in your own words** in the fields below:

1. **Storage choice:** Why does FreshMarket store `imageUrl` in MongoDB instead of image bytes? What problems does base64 in a document cause for `GET /vendor/products`?

2. **Upload flow:** Walk through your upload pipeline from file selection to Cloudinary to database update. Which middleware runs in what order?

3. **Secrets:** Which Cloudinary values are safe in public URLs vs which must stay in `server/.env`?

4. **Replace lifecycle:** What happens when a vendor uploads a second photo for the same product? How do you prevent orphaned assets?

5. **Authorization:** How is upload ownership enforced? What status code when Vendor B targets Vendor A's product?

6. **Verification:** Paste a redacted Compass document for a product with an image. Confirm fields present and absent.

7. **Week 2 demo:** Describe your demo script in four steps. What would break the demo if Cloudinary credentials were wrong?

8. **Chapter 14 preview:** Will public `GET /products` return `imageUrl` to customers? Any fields you will exclude from public DTO?


## All boxes ticked?

Mark this chapter complete in the platform, then continue to **Browse catalogue API** (Chapter 14).

## Phase progress — Week 2

| Week 2 demo target | After Ch 13 gate |
|---|---|
| Vendor registers | ✓ |
| Opens store | ✓ |
| Adds product with photo | ✓ |
| Sees it in dashboard | ✓ |

**Week 2 vendor phase complete.** Week 3 buyer experience starts with catalogue API.

---

## Troubleshooting before you leave Chapter 13

| Blocker | Where to look |
|---|---|
| 401 on upload | Ch 7 interceptor; multipart + Bearer |
| 403 unexpected | Ch 11 ownership; storeId chain |
| Multer "Unexpected field" | Field name must be `image` both sides |
| Empty imageUrl after 200 | Service save bug; check await |
| CORS on POST | Server CORS config for Vite origin |
| Huge list response | Embedded image data — fix schema/API |
| Cloudinary 401 | Wrong API secret in `.env` |

---

## Final gate statement

Optional confirmation statement:

*"I confirm product images upload via multer to Cloudinary, MongoDB stores URL only, vendors see thumbnails in dashboard, replace removes old assets, cross-vendor upload returns 403, and Week 2 vendor demo is complete. I am ready for Chapter 14."*

**All boxes ticked? Open Chapter 14 — Browse catalogue API.**

Customers are next — build the read path before the shop UI.
