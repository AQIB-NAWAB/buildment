---
id: multi-vendor-marketplace-13-12
title: "13.12 — Verify image flow"
module: 13
lesson: 12
stepType: Build
phase: "Module 13"
summary: "Upload bugs hide in three places — multer, Cloudinary, and React file state. End-to-end proof: pick image → see thumbnail → confirm MongoDB has URL only → replace photo → old asset gone. This is the technical gate before recap and the Week 2→3 bridge."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 13.12 — Verify image flow

> **[Build]** · Chapter 13 — Product photos · *Upload → URL in MongoDB → shows in list*

**Why this step matters:** Upload bugs hide in three places — multer, Cloudinary, and React file state. End-to-end proof: pick image → see thumbnail → confirm MongoDB has URL only → replace photo → old asset gone. This is the technical gate before recap and the Week 2→3 bridge.

**Where the project is now:** Upload API and UI exist; individual pieces tested in isolation.

**After this step:** Full vendor photo flow demonstrated — upload, MongoDB URL only, thumbnail in dashboard, replace works. Ready for Week 2 demo statement in 13.13.

## End-to-end test script

Run with two terminals (server + client). Use **your** product name in notes.

| # | Action | Expected |
|---|---|---|
| 1 | Login as vendor | Dashboard loads |
| 2 | Open product without image | Placeholder in list |
| 3 | Edit product → upload JPEG | 200; preview shows Cloudinary URL |
| 4 | Refresh page | Image persists — URL from API |
| 5 | Open Compass → product doc | `imageUrl` string present; **no** binary field |
| 6 | Upload second image same product | URL changes; list shows new thumb |
| 7 | Open old URL in browser tab | Gone or 404 (if destroy works) |
| 8 | Login as other vendor → POST image to first vendor's product id | 403 |

Screenshot or learning log notes for steps 3, 4, 6.

## MongoDB document audit

In Compass, inspect one product with image:

**Must have:**

- `imageUrl`: `https://res.cloudinary.com/...`  
- `imagePublicId`: non-empty string (if you implemented delete)

**Must not have:**

- `imageData`, `base64`, `Buffer`, large string fields  
- Document size >> few KB for metadata fields

Paste redacted doc shape into learning log — not API secrets.

## curl regression

Re-run upload curl from 13.9 after UI works — proves client is not doing something special.

Verify `Content-Type` of response is `application/json` — not multipart.

## Performance sanity

Vendor product list with 10 products with images:

- API response should be **JSON kilobytes** — not megabytes  
- Network tab: list request does not download image bytes — separate img requests to Cloudinary  

If list response is huge, you embedded images wrong — fix before gate.

## README demo steps

Add **Week 2 partial demo** subsection:

1. Register/login vendor  
2. Open shop (Ch 10)  
3. Create and publish product (Ch 12)  
4. Upload photo (Ch 13)  
5. Show thumbnail in dashboard  

Full Week 2 demo completed in 13.13 recap.

## Commit

```bash
git add README.md
git commit -m "docs: document product image upload verification steps"
```

Include any fixes discovered during verification in prior commits.

## ✅ Do / ❌ Don't

**✅ Do:** Test replace flow — common mentor question.

**✅ Do:** Confirm CORS allows multipart POST from Vite origin.

**❌ Don't:** Mark gate done if images only work via curl — UI must work.

**❌ Don't:** Skip Compass audit — "URL only" is a gate requirement.

## Verify checklist

- [ ] UI upload → Compass URL → list thumbnail — full chain  
- [ ] Replace deletes old asset (or documented orphan if delete failed once)  
- [ ] Cross-vendor upload blocked  
- [ ] API list payload stays small  
- [ ] No secrets in client bundle or Git

## Next

**Sub-chapter 13.13:** Recap and Week 2 demo milestone — verification done; wrap the chapter and prepare the demo.

Verification done; wrap the chapter and phase checkpoint.
