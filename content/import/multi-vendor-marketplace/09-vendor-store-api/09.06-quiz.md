---
id: multi-vendor-marketplace-09-06
title: "9.6 — Quiz"
module: 9
lesson: 6
stepType: Check
phase: "Module 9"
summary: "**Status:** Reserved — excluded from this course pass."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 9.6 — Quiz

> **[Check]** · Chapter 09 — Vendor store API · *Reserved*

**Status:** Reserved — excluded from this course pass.

<BigWordAlert term="RESTful resource" plainEnglish="An API organized around nouns (stores, products) with HTTP verbs (GET, POST, PATCH, DELETE) describing actions." whyItMatters="Vendor store routes follow predictable patterns so the client and future mobile apps can integrate without surprises." />

No quiz content here. Continue to **Where The Project Is Now** in the sidebar to start the build cluster.

When quizzes are added later, this file may contain self-check questions on one-store enforcement, 409 vs 403, `GET /stores/me` contract, and route ordering for `/me` vs `/:id`.

## Mini self-check (optional, not graded)

1. What HTTP status when a vendor creates a second store?  
2. Why must `vendorId` never come from the request body on POST?  
3. What does `GET /stores/me` return when the vendor has no store?  
4. Which middleware runs before `PATCH /stores/:id` handler logic?  
5. Why register `/stores/me` before `/stores/:id` routes?

Answers from 9.2–9.5. Re-read if unsure before 9.7.

## Next

**Sub-chapter 9.7:** Where the project is now — continuity recap before scaffolding the stores module.
<InterestingRead title="Slug collisions in multi-vendor marketplaces" hook="Two shops cannot both be `/store/fresh-produce`." readMinutes={3}>

Human-readable URLs help SEO and sharing, but slugs must be unique per platform. FreshMarket generates or validates slugs on create so vendors get friendly links without breaking routing.

</InterestingRead>
