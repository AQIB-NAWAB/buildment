---
id: multi-vendor-marketplace-11-12
title: "11.12 — Delete product route"
module: 11
lesson: 12
stepType: Build
phase: "Module 11"
summary: "Vendors discontinue items — hard delete in this course. `DELETE /products/:id` completes the CRUD set and reuses the same ownership chain. Optional in the UI (Ch 12) but required in the API for a complete inventory lifecycle."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 11.12 — Delete product route

> **[Build]** · Chapter 11 — Manage products API · *`DELETE /products/:id` — ownership*

**Why this step matters:** Vendors discontinue items — hard delete in this course. `DELETE /products/:id` completes the CRUD set and reuses the same ownership chain. Optional in the UI (Ch 12) but required in the API for a complete inventory lifecycle.

<BigWordAlert term="Soft delete vs hard delete" plainEnglish="Soft delete marks a record inactive; hard delete removes it permanently from the database." whyItMatters="Products referenced by past orders may need to be unpublished rather than destroyed." />

**Where the project is now:** Create, list, and update work (11.9–11.11); no delete path — discontinued items linger on the shelf.

**After this step:** `DELETE /api/products/:id` removes own product → **204** or **200**. Cross-vendor → **403**.

## Handler flow

```
DELETE /api/products/:id
  1. requireAuth
  2. requireRole('vendor')
  3. validateObjectId
  4. assertProductOwner(id, userId)
  5. deleteProduct(id, userId)
  6. 204 No Content (or 200 { deleted: true })
```

Pick **204** for REST convention — document in README.

## Service: deleteProduct

Hard delete `Product.findByIdAndDelete` — course default.

**Stretch:** soft delete `isDeleted` flag — note in learning log if chosen instead.

## Cart / order implications

If product in active carts — MVP may still delete — orphan cart handling later. Document known limitation in learning log.

Orders use snapshots — historical orders unaffected.

## Error matrix

| Condition | Status |
|---|---|
| Owner delete | 204 |
| Vendor B | 403 |
| Unknown id | 404 |

## curl verify

<ApiRequestPanel title="DELETE product — ownership">

<ApiRequest
  label="Owner DELETE own product"
  method="DELETE"
  url="http://localhost:5000/api/products/PRODUCT_ID_A"
  bearer="TOKEN_A"
  responseStatus={204}
  responseBody='...'
/>

<ApiRequest
  label="Vendor B DELETE Vendor A product — must 403"
  method="DELETE"
  url="http://localhost:5000/api/products/PRODUCT_ID_A"
  bearer="TOKEN_B"
  responseStatus={403}
  responseBody='{"message":"Forbidden"}'
/>

</ApiRequestPanel>

Create product → DELETE with owner → subsequent GET list excludes it.

## Commit

```bash
git commit -m "feat(products): add DELETE product with ownership check"
```

## ✅ Do / ❌ Don't

**✅ Do:** Return 404 if already deleted — idempotent debate: 404 OK.

**✅ Do:** Test DELETE after 11.11 PATCH tests — same ownership helper.

**❌ Don't:** Expose DELETE on public routes.

## Verify

- [ ] Owner delete removes from Compass  
- [ ] Cross-vendor 403  
- [ ] List no longer includes deleted id  

<RealWorldEvent title="DoorDash menu sync failures" when="2020s" summary="Delivery platforms struggled when restaurant menus changed faster than sync pipelines, showing unavailable items to hungry customers." lesson="Product APIs should reflect live stock and publish state — stale catalogue data directly loses sales." />

## Next

**Sub-chapter 11.13:** Cross-vendor 403 test suite — CRUD is complete; prove the isolation rules hold with real two-vendor tests.