---
id: multi-vendor-marketplace-05-02
title: "5.2 — The denormalisation trap"
module: 5
lesson: 2
stepType: Read
phase: "Module 5"
summary: "Every junior marketplace tutorial tempts you to **copy display names onto child documents** so queries feel simple. *\"Just put `vendorName` and `storeName` on every product — one query, no joins.\"*"
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 5.2 — The denormalisation trap

> **[Read]** · Chapter 05 — Data model & seed · *Copied `vendorName` on every product — explain, don't build wrong*

Every junior marketplace tutorial tempts you to **copy display names onto child documents** so queries feel simple. *"Just put `vendorName` and `storeName` on every product — one query, no joins."*

<BigWordAlert term="Denormalisation" plainEnglish="Storing copied data in multiple places so reads are faster, even though updates become slightly harder." whyItMatters="Order line items snapshot product price at purchase time — that is intentional denormalisation for correct receipts." />

That shortcut creates a maintenance nightmare. This sub-chapter explains the trap so you **reference** stores instead of duplicating names — and understand when denormalisation is actually justified (order snapshots in Chapter 18).

## Why this matters for absolute beginners

Denormalisation is a real technique — not always wrong. Beginners apply it in the **wrong place**: copying store names onto products "to avoid populate." That feels like optimization; it is **stale data waiting to happen**. The rule of thumb: **live catalogue references; historical receipts copy.** Learn that distinction now and Chapter 18 checkout will feel obvious instead of contradictory.

## Think of it like…

**A mall directory vs a printed receipt.** The directory (catalogue) points to "Green Valley Grocer — moved to Level 2" in one place. The receipt in your wallet still says what you paid yesterday — it does not auto-update when the shop renames.

## Common beginner questions

**Q: Isn't populate slow — why not copy store name?**  
A: One batched populate per page is fast enough, and it's far cheaper than the alternative. When you `populate('storeId')`, Mongoose gathers the referenced ids and fetches all the matching store documents in a single second query — not one query per product. Copying `vendorName` onto N products means every rename needs N updates, and any miss leaves stale data silently behind. Chapter 14 optimises the catalogue read further. For a beginner, the rule is simple: correctness and maintainability beat a micro-optimisation that quietly corrupts data over time.

**Q: What is denormalisation in plain English?**  
A: Denormalisation means deliberately copying the same data into more than one place to make some reads faster or simpler. A normalised design stores each fact once — the product knows its `storeId`, and the store's name lives only in the stores collection. A denormalised design also copies "Green Valley Grocer" onto every product so a catalogue query needs no join. It feels convenient, but every copy is a lie waiting to happen when the source changes. The rule of thumb this course teaches: reference live, mutable data; copy only data that must stay frozen, like order receipts.

**Q: Should CartItem embed the product or reference it?**  
A: Reference it — a cart is a live, changeable thing, so it should point at the product rather than copy it. CartItem stores `productId` (and `storeId`) as references, so if the price changes before checkout, the cart reflects the current price or you re-fetch and warn the customer. If you embedded the whole product, every price change would leave stale lines duplicated across every customer's cart. Only at checkout do you switch to copying — the Order embeds `priceAtPurchase`, `nameAtPurchase`, and `unitAtPurchase` because an order is history, not a live state.

**Q: Why is copying price onto an Order right when copying storeName onto Product is wrong?**  
A: Because an order records what happened in the past, and a live product must show what's true right now. You paid £2.49 for bananas yesterday; when the shop raises the price today, your receipt should not change — that's why order lines carry `nameAtPurchase`, `priceAtPurchase`, and `unitAtPurchase`. But a catalogue product must always show the current store name, so it references `storeId` and lets the store be the one source of truth. Same act, opposite intent: copy for history, reference for live data. That's the mall directory vs printed receipt distinction.

**Q: How does populate actually find the store name?**  
A: Populate is Mongoose's built-in join: it looks up the documents your ref points to and merges them into the result. A product stores `storeId` as an ObjectId. When you call `Product.find().populate('storeId')`, Mongoose sees `ref: 'Store'`, collects the ObjectIds, and fetches those store documents in a second query — then attaches them so you can read `product.storeId.name` without ever copying the name yourself. It fails silently if the ref string doesn't match the registered model name exactly (`'Store'`, not `'store'`), so keep the two identical or you'll get null.

<RealWorldEvent title="Amazon's item-level order snapshots" when="Marketplace era" summary="Marketplace order systems persist item details at purchase time so disputes, refunds, and tax reporting reflect the transaction as it happened." lesson="Model for auditability early. Your seed data and ERD should show which fields are live references and which are immutable snapshots." />

## The tempting pattern

Imagine a product document like this:

```json
{
  "name": "Organic Bananas",
  "price": 2.49,
  "vendorName": "Green Valley Grocer",
  "storeName": "Green Valley — Main St",
  "storeAddress": "42 Market Lane"
}
```

**Feels good because:**

- Catalogue page reads one collection — no populate
- Product card shows store name immediately
- Tutorial completes in ten minutes

**Fails because:**

| Event | Consequence |
|---|---|
| Vendor renames store | Every product document stale until bulk update |
| Vendor changes address | Same — N products to patch |
| Typo in copied name | Two spellings of same store in catalogue |
| Vendor deleted | Orphan strings with no link back to owner |

In a multi-vendor marketplace, **store identity is authoritative in one place** — the `stores` collection.

## The correct pattern for live catalogue data

```
products.storeId  →  ObjectId ref to stores._id
```

**Read path (Chapter 14):**

- `Product.find({ isPublished: true }).populate('storeId', 'name slug')`
- One query with populate — not N+1 if done correctly
- Store rename updates **one** document; catalogue reflects immediately

**Write path (Chapter 11):**

- Vendor creates product with their own `storeId` only
- Ownership check: product.storeId belongs to vendor's store

## When denormalisation IS correct — order snapshots

Checkout is different from catalogue. At purchase time you **must copy** price, name, and unit into the order — because the product document will change tomorrow.

```json
{
  "orderItems": [
    {
      "productId": "...",
      "nameAtPurchase": "Organic Bananas",
      "priceAtPurchase": 2.49,
      "unitAtPurchase": "bunch",
      "quantity": 2
    }
  ]
}
```

| Context | Reference or copy? | Why |
|---|---|---|
| Live product catalogue | **Reference** storeId | Source of truth stays current |
| Order history | **Copy (embed snapshot)** | Customer paid 2.49 forever — not today's price |
| Cart (pre-checkout) | **Reference** productId | Price may update before checkout — re-fetch or warn |

Chapter 5 embeds snapshot **field names** on Order schema. Checkout logic fills them in Chapter 18.

## Real-world cost story

A food marketplace shipped with denormalised vendor names on 40,000 products. Rebrand campaign required a **batch migration script** and two days of stale search results. Referenced design: one `stores` update, populate handles the rest.

You won't hit 40,000 products in this course — but the **pattern** is the same at 40 products.

## Red flags in schema review

If your ERD or model has any of these on **Product**, push back:

- `vendorName` string (use storeId ref + populate)
- `vendorEmail` on product (PII leak + stale data)
- `storeName` duplicated (use populate unless order snapshot context)

If your **Order** lacks snapshot fields, push back:

- Order line only stores `productId` with no price copy
- "We'll look up price later" — breaks immutability

## Decision for FreshMarket (this course)

| Document | Relationship to Store | Denormalise? |
|---|---|---|
| Product | `storeId` ref | **No** — populate for display |
| CartItem | `productId` + `storeId` refs | **No** — live prices until checkout |
| Order | `storeId` ref + embedded items | **Yes** — snapshot fields on embed |

Document this table in your learning log when models are done.

## ✅ Do / ❌ Don't

**✅ Do:** Use `storeId: { type: ObjectId, ref: 'Store' }` on Product.

**✅ Do:** Plan `nameAtPurchase`, `priceAtPurchase`, `unitAtPurchase` on embedded order lines.

**❌ Don't:** Add `vendorName` to Product to "save a populate" — you'll unpick it in code review.

**❌ Don't:** Embed full product documents inside CartItem — cart is mutable pre-checkout.

<InterestingRead title="Why orders store prices, not live product lookups" hook="A receipt is a photograph, not a live feed." readMinutes={4}>

If an order stored only a product ID, a vendor price change would rewrite history. FreshMarket copies name, unit price, and weight into order items at checkout so customers and vendors always see what was agreed at purchase time.

</InterestingRead>

## Reflect

Answer briefly (learning log preview):

1. Why is copying store name onto Product wrong but copying price onto Order right?
2. What Mongoose method loads store name for a product list without duplicating data?

## Next

**Sub-chapter 5.3:** What you'll build — full model list, seed script, and final ERD deliverables.

The trap is named — next you see the full blueprint.