---
id: multi-vendor-marketplace-05-15
title: "5.15 — Run seed and inspect"
module: 5
lesson: 15
stepType: Build
phase: "Module 5"
summary: "**Why this step matters:** Running seed and opening Compass is the Week 1 **\"data is real\"** moment. Inspecting every collection catches ref typos and missing grocery fields before you build auth against broken documents."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 5.15 — Run seed and inspect

> **[Build]** · Chapter 05 — Data model & seed · *Run seed; open Compass; read every document*

**Why this step matters:** Running seed and opening Compass is the Week 1 **"data is real"** moment. Inspecting every collection catches ref typos and missing grocery fields before you build auth against broken documents.

<BigWordAlert term="MongoDB Compass" plainEnglish="A GUI to browse collections and documents — inspect seed output without writing queries." whyItMatters="After seeding, you verify vendors, stores, and products exist before building API routes." />

**Where the project is now:** Seed script committed; may not have been run yet.

**After this step:** Database populated; you have **read every collection** in Compass and confirmed fields match schema spec.

## Run seed

```bash
cd server
npm run seed
```

**Expected terminal output (example):**

```
MongoDB connected
Cleared collections (if using clear strategy)
Inserted 4 users, 4 profiles, 2 stores, 16 products
Seed complete
Disconnected
```

Adjust to your log messages — must show success and counts.

## Compass inspection checklist

Connect to same database as `MONGODB_URI`. Open each collection:

### users

- [ ] 4 documents
- [ ] Emails match vendor-a/b, customer-a/b@test.local
- [ ] roles correct — 2 vendor, 2 customer
- [ ] passwordHash present — not plain `password` field
- [ ] isVerified true for seed users (Ch 6 login tests)

### profiles

- [ ] 4 documents
- [ ] Each userId matches a user _id
- [ ] displayName populated

### stores

- [ ] 2 documents
- [ ] Each userId points to vendor user only
- [ ] Slugs unique
- [ ] No customer userId on any store

### products

- [ ] 10–20 total across both stores
- [ ] Every product has storeId pointing to valid store
- [ ] unit enum values valid
- [ ] stockQty > 0 on published items
- [ ] Mix of isPublished true/false
- [ ] isPerishable mix
- [ ] **No** vendorName or storeName duplicated on product

### cartitems

- [ ] Empty — expected

### orders

- [ ] Empty — expected

## Cross-document ownership audit

Manual trace in Compass:

1. Pick Product P → note storeId → open Store S → note userId → open User U  
2. Confirm U.role is `vendor`  
3. Repeat for product from other store — different vendor user  

**Failure:** Product.storeId points to store owned by wrong vendor — fix seed logic.

## Re-run idempotency test

Run `npm run seed` **twice**.

| Strategy | Expected second run |
|---|---|
| Clear + insert | Same counts — no duplicate emails |
| Upsert | Same counts — no duplicate slugs |

Document result in learning log.

## Optional — mongosh spot check

```bash
mongosh "$MONGODB_URI" --eval 'db.products.countDocuments()'
mongosh "$MONGODB_URI" --eval 'db.stores.find({}, {name:1, userId:1})'
```

Replace URI with your connection or use Compass shell.

## Screenshot for portfolio

Capture Compass showing:

- stores collection with 2 docs
- products with grocery fields visible
- Relationship visible in document JSON (storeId, userId)

Store in `docs/screenshots/` optional — not gate required.

## Troubleshooting

| Symptom | Fix |
|---|---|
| E11000 duplicate key | Idempotency bug — clear or upsert |
| ValidationError | Seed missing required field — check model vs seed |
| 0 products | storeId not set after store insert |
| Wrong database | seed URI ≠ server URI — unify config |
| Connection refused | MongoDB not running |

## ✅ Do / ❌ Don't

**✅ Do:** Read raw JSON — don't trust counts alone.

**✅ Do:** Note collection names (users vs user) — Mongoose pluralization.

**❌ Don't:** Manually edit Compass docs without updating seed — drift breaks teammates.

**❌ Don't:** Seed production Atlas with clear-all strategy without confirmation.

## Verify checklist

- [ ] Seed runs successfully
- [ ] All four collections with data inspected
- [ ] Ownership chain verified for sample products
- [ ] cartitems and orders empty
- [ ] Second seed run behaves as documented
- [ ] Test credentials documented for Ch 6

<RealWorldEvent title="Amazon's item-level order snapshots" when="Marketplace era" summary="Marketplace order systems persist item details at purchase time so disputes, refunds, and tax reporting reflect the transaction as it happened." lesson="Model for auditability early. Your seed data and ERD should show which fields are live references and which are immutable snapshots." />

## Next

**Sub-chapter 5.16:** Draw final ERD — commit `docs/erd.md` matching implemented schemas.

Data is real — document it formally next.