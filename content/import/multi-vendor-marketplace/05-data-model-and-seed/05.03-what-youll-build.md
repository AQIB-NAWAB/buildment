---
id: multi-vendor-marketplace-05-03
title: "5.3 — What you'll build"
module: 5
lesson: 3
stepType: Read
phase: "Module 5"
summary: "By the end of Chapter 5, your repo contains **six Mongoose models**, a **runnable seed script**, and a **final ERD** committed to `docs/erd.md`. No API routes yet — only schemas and test data."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 5.3 — What you'll build

> **[Read]** · Chapter 05 — Data model & seed · *All Mongoose models + seed script + ERD*

By the end of Chapter 5, your repo contains **six Mongoose models**, a **runnable seed script**, and a **final ERD** committed to `docs/erd.md`. No API routes yet — only schemas and test data.

## Why this matters for absolute beginners

Chapter 5 is where FreshMarket becomes **real in the database** — not health checks, not empty folders, but vendors, stores, and products you can click in Compass. The deliverables table is your gate checklist. Weak models here mean every auth, cart, and checkout chapter fights the same schema mistakes. Strong models mean you mostly **wire routes** to shapes that already exist.

## Common beginner questions

**Q: Why six models before any routes?**  
A: Routes without schemas guess field names. Register needs User+Profile; checkout needs Order embed shape — define once, reuse everywhere.

**Q: Must seed create exactly `vendor-a@test.local`?**  
A: Use deterministic test emails documented in README — Chapter 8 isolation tests assume two distinct vendors.

**Q: What's idempotent seed?**  
A: Running `npm run seed` twice does not duplicate stores — clear-then-insert or upsert; document your approach.

**Q: Keep `erd-draft.md`?**  
A: Yes — add `erd.md` as final; diff them in learning log to show design evolution.

## Deliverables checklist

| Deliverable | Location | Gate proof |
|---|---|---|
| User model | `server/src/modules/users/user.model.js` | Seed creates vendor + customer users |
| Profile model | `server/src/modules/users/profile.model.js` | Linked to User via userId |
| Store model | `server/src/modules/stores/store.model.js` | One store per vendor in seed |
| Product model | `server/src/modules/products/product.model.js` | Grocery fields present |
| CartItem model | `server/src/modules/cart/cartItem.model.js` | Schema ready for Ch 16 |
| Order model | `server/src/modules/orders/order.model.js` | Embedded orderItems with snapshot fields |
| Seed script | `server/scripts/seed.js` | `npm run seed` populates DB |
| Final ERD | `docs/erd.md` | Matches implemented schemas |
| npm script | `server/package.json` `"seed"` | Documented in README |

## Target folder tree after Chapter 5

```
server/
├── package.json              ← + seed script
├── scripts/
│   └── seed.js               ← NEW
└── src/
    ├── shared/
    │   ├── config.js
    │   └── db.js
    └── modules/
        ├── users/
        │   ├── user.model.js       ← NEW
        │   └── profile.model.js    ← NEW
        ├── stores/
        │   └── store.model.js      ← NEW
        ├── products/
        │   └── product.model.js    ← NEW
        ├── cart/
        │   └── cartItem.model.js   ← NEW
        ├── orders/
        │   └── order.model.js      ← NEW
        ├── auth/                     ← still empty — Ch 6
        └── ...
docs/
├── erd-draft.md              ← Ch 3 — keep for history
└── erd.md                    ← NEW — final
```

## Seed data specification

The seed script must create **deterministic, named test entities** for isolation tests in Chapter 8:

| Entity | Count | Notes |
|---|---|---|
| Vendor users | 2 | `vendor-a@test.local`, `vendor-b@test.local` — passwords hashed in Ch 6; plain for seed OK if documented |
| Customer users | 2 | `customer-a@test.local`, `customer-b@test.local` |
| Profiles | 4 | One per user |
| Stores | 2 | Store A → Vendor A; Store B → Vendor B |
| Products | 5–10 per store | Mix of perishable/non-perishable; varied units |
| CartItems | 0 | Created in later chapters |
| Orders | 0 | Created at checkout demo |

**Idempotent seed:** Running seed twice should not duplicate stores — use upsert or clear-then-insert pattern. Document your choice in README.

## Model design principles (course mandate)

1. **Module colocation** — each model lives in its feature module folder
2. **Refs over embed** — except order line snapshots
3. **Grocery fields** — not generic `{ title, price }` only
4. **Indexes noted** — unique email on User; compound indexes where queries demand (document in ERD)
5. **Timestamps** — `createdAt` / `updatedAt` on all collections
6. **No route handlers** — models and seed only

## API preview (not built this chapter)

These routes **depend** on models you build now:

| Route | Chapter | Uses model |
|---|---|---|
| `POST /auth/register` | 6 | User, Profile |
| `POST /stores` | 9 | Store |
| `POST /products` | 11 | Product |
| `GET /cart` | 16 | CartItem |
| `POST /checkout` | 18 | Order, CartItem, Product |

Weak schemas here = rework everywhere.

## ERD evolution

| File | Purpose |
|---|---|
| `docs/erd-draft.md` | Chapter 3 sketch — keep, don't delete |
| `docs/erd.md` | Chapter 5 final — field names match Mongoose schemas |

Diff draft vs final in learning log. Explain any change (e.g. added `isPublished` on Product).

## What you will NOT build

- Auth routes, middleware, JWT — Chapter 6
- Validation schemas (Zod/Joi) for HTTP — Chapter 6+
- Product images / Cloudinary — Chapter 13
- Any client changes — health page unchanged

## Estimated build sequence

| Step | Sub-chapter | Time |
|---|---|---|
| Recap continuity | 5.8 | 10 min |
| Scaffold folders | 5.9 | 15 min |
| User + Profile | 5.10 | 45 min |
| Store | 5.11 | 30 min |
| Product | 5.12 | 45 min |
| Cart + Order | 5.13 | 60 min |
| Seed script | 5.14–5.15 | 90 min |
| Final ERD | 5.16 | 45 min |

## ✅ Do / ❌ Don't

**✅ Do:** Open `docs/erd-draft.md` before writing first model.

**✅ Do:** Run seed and inspect every collection in Compass before 5.16 ERD.

**❌ Don't:** Add Express routes in model files — separation of concerns.

**❌ Don't:** Skip Profile split — auth chapter expects it.

## Next

**Sub-chapter 5.4:** Ownership chain — User → Store → Product → CartItem → Order as a security and data model story.

Blueprint is set — next you learn why the chain order matters.
