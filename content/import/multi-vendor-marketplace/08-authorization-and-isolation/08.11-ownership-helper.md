---
id: multi-vendor-marketplace-08-11
title: "8.11 — Ownership helper"
module: 8
lesson: 11
stepType: Build
phase: "Module 8"
summary: "Role checks are not enough — two vendors share the same role. Ownership helpers load the **object** and compare its owner field to `req.user.id`. This is the core IDOR defense. You'll clone the pattern for products in Chapter 11."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 8.11 — Ownership helper

> **[Build]** · Chapter 08 — Authorization & isolation · *`assertStoreOwner(storeId, userId)` in shared*

## Why this step matters

Role checks are not enough — two vendors share the same role. Ownership helpers load the **object** and compare its owner field to `req.user.id`. This is the core IDOR defense. You'll clone the pattern for products in Chapter 11.

**Where the project is now:** requireRole blocks wrong roles; no reusable store ownership check.

**After this step:** `assertStoreOwner(storeId, userId)` in shared module — loads Store, compares userId, throws or returns error for 403. Optional Express wrapper middleware for `:id` param routes.

## Why ownership is separate from role

Both Vendor A and Vendor B pass `requireRole('vendor')`. Role answers *"Are you a seller?"* Ownership answers *"Is this **your** store?"*

Real marketplace incidents often look like:

| Attack | What failed | This course's fix |
|---|---|---|
| Change `:id` in URL to another vendor's store | No object-level check | `assertStoreOwner` |
| Send someone else's `storeId` in POST body | Trusted client input | Resolve store from JWT in Ch 9 |
| Guess sequential MongoDB ids | Predictable ids | Still blocked — owner field must match |

**Production note:** Never trust ids from the client body for authorization. The `:id` in the URL or a resource derived from the authenticated user is the check surface.

## Checkpoint — before you code

1. Which field on Store links to the owning User? (from Chapter 5)
2. Why compare with `.toString()` on ObjectIds?
3. What HTTP status when store exists but wrong owner — 403 or 404?

If unsure, re-read **8.2** (IDOR) and your Store model in Compass.

## Core helper contract

**File:** `server/src/shared/ownership.js` (or `stores/stores.authorization.js`)

```
assertStoreOwner(storeId, userId) → Promise<Store>
```

**Behaviour:**

1. Validate storeId is valid ObjectId format — 400 if malformed optional  
2. `store = await Store.findById(storeId)`  
3. If !store → throw NotFoundError (404) or ForbiddenError (403) — document choice  
4. If store.userId.toString() !== userId.toString() → throw ForbiddenError  
5. Return store for handler use — avoids double fetch  

## Express middleware wrapper (optional)

```
assertStoreOwnerFromParams(req, res, next)
  → reads req.params.id
  → calls assertStoreOwner(req.params.id, req.user.id)
  → attaches req.store = store
  → next() or 403
```

Reuse on all `/stores/:id` mutating routes.

## Product ownership preview (Ch 11)

Add later:

```
assertProductOwner(productId, userId)
  → load Product → assertStoreOwner(product.storeId, userId)
```

Chain reuse — don't duplicate store.userId compare in product handlers.

## Error mapping

| Error | HTTP |
|---|---|
| ForbiddenError | 403 |
| NotFoundError | 404 |

Central error handler or try/catch in route — consistent JSON shape.

## Unit test mentally

| store.userId | userId arg | Result |
|---|---|---|
| A | A | pass |
| A | B | 403 |
| invalid id | A | 404 |

## Commit

```bash
git add server/src/shared/ownership.js
git commit -m "feat(server): add assertStoreOwner ownership helper"
```

## ✅ Do / ❌ Don't

**✅ Do:** Accept store document return — handler updates without re-query.

**✅ Do:** Use same helper in Ch 9 PATCH.

**❌ Don't:** Compare userId without toString on ObjectIds.

**❌ Don't:** Trust storeId from body — use params or derived from product.

## Verify checklist

- [ ] Helper exported and importable from stores routes
- [ ] Wrong owner throws 403 path
- [ ] Missing store returns 404 (if chosen)
- [ ] AUTHORIZATION.md references helper

## Next

**Sub-chapter 8.12:** Apply to stub PATCH /stores/:id route.

Helper ready — wire to HTTP next.
