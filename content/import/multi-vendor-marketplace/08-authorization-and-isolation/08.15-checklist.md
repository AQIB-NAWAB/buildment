---
id: multi-vendor-marketplace-08-15
title: "8.15 — Checklist and learning log"
module: 8
lesson: 15
stepType: Gate
phase: "Module 8"
summary: "Gate for Chapter 8 — Authorization and isolation. Tick every box and answer the learning log on this page before continuing."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 8.15 — Checklist and learning log
> **[Gate]** · Chapter 08 — Authorization and isolation

This is the **gate** for Chapter 8. Tick every box honestly and answer the learning log below before opening Chapter 9.

## Chapter 8 — Authorization and isolation checklist (gate)

Do **not** open Chapter 9 until every box is ticked and learning log answers are written below.

### Documentation

- [ ] `docs/AUTHORIZATION.md` committed with matrix, roles, 401/403 rules, IDOR checklist
- [ ] Matrix includes PATCH /stores/:id row with ownership check type
- [ ] IDOR store test marked done with date in doc or learning log

### Middleware & helpers

- [ ] `requireRole(...roles)` returns 403 for wrong role, 401 if no req.user
- [ ] `assertStoreOwner(storeId, userId)` enforces store.userId match
- [ ] requireAuth runs before requireRole on protected routes

### Store route stub

- [ ] PATCH `/api/stores/:id` mounted at `/api/stores`
- [ ] Stack: requireAuth → requireRole('vendor') → ownership → handler
- [ ] Vendor A updates own store — 200
- [ ] Customer token — 403 at role check

### IDOR verification (8.13)

- [ ] Vendor B PATCH Vendor A store — **403**
- [ ] Vendor A store document unchanged after attack attempt
- [ ] Vendor A PATCH Vendor B store — 403
- [ ] Unauthenticated PATCH — 401

### Prior gates

- [ ] Ch 6–7 auth still work
- [ ] Models and seed intact
- [ ] No product isolation required yet — noted as Ch 11 in matrix
- [ ] Explain IDOR / BOLA in marketplace context
- [ ] Explain 401 vs 403 with examples
- [ ] Explain why requireRole alone is insufficient
- [ ] Trace ownership chain for product (preview Ch 11)
- [ ] Explain why client-side hiding is not security

### Learning log — write now

Answer **in your own words** in the fields below:

1. **IDOR test:** Paste curl status codes for five control tests (no tokens). What happened in Compass?

2. **Matrix:** Pick one planned row (e.g. GET /cart) — what checks will apply in Ch 16?

3. **Middleware order:** Why must requireAuth precede requireRole?

4. **404 vs 403:** If store id not found, what status do you return and why?

5. **Helper design:** What does assertStoreOwner return and how does it avoid double DB fetch?

6. **Real world:** Name one IDOR consequence if PATCH store had no ownership check.

7. **Chapter 9 preview:** What does POST /stores add that PATCH stub doesn't?

8. **Regression:** How will you reuse IDOR test pattern for products in Ch 11?


## All boxes ticked?

Mark this chapter complete in the platform, then continue to **Vendor store API** (Chapter 9).

## Final gate statement

*"requireRole and assertStoreOwner exist; PATCH store enforces ownership; Vendor B gets 403 on Vendor A store; AUTHORIZATION.md matrix committed. Ready for Chapter 9 vendor store API."*

**All boxes ticked? Open Chapter 9.**

---

## Week 2 note

Auth + authorization foundation complete. Next chapters add **vendor-facing features** on top of this stack — reuse middleware, don't reinvent checks.
