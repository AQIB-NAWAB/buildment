---
id: multi-vendor-marketplace-07-09
title: "7.9 — Scaffold auth pages"
module: 7
lesson: 9
stepType: Build
phase: "Module 7"
summary: "**Why this step matters:** Empty page components and router entries give Register, Login, and Verify OTP **named homes** before you wire forms. Adding routes now prevents a single mega-component auth file later."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 7.9 — Scaffold auth pages

> **[Build]** · Chapter 07 — Login & registration · *`pages/Login.jsx`, `Register.jsx`, `VerifyOtp.jsx`*

**Why this step matters:** Empty page components and router entries give Register, Login, and Verify OTP **named homes** before you wire forms. Adding routes now prevents a single mega-component auth file later.

<BigWordAlert term="Auth route group" plainEnglish="Public pages (/register, /login, /verify-otp) outside the protected layout — no token required." whyItMatters="FreshMarket keeps auth pages separate from shop and vendor dashboard routes." />

**Where the project is now:** No auth pages in client.

**After this step:** `Login.jsx`, `Register.jsx`, `VerifyOtp.jsx` exist with placeholder forms; React Router paths registered; navigation links from Home optional.

## Target UI previews

These are the screens you will flesh out in 7.11–7.13:

| Page | Preview |
|---|---|
| Register | ![Register](/showcase/multi-vendor-marketplace/register-page.png) |
| Login | ![Login](/showcase/multi-vendor-marketplace/login-page.png) |
| Verify OTP | ![Verify OTP](/showcase/multi-vendor-marketplace/verify-otp-page.png) |

## Files to create

```
client/src/pages/Login.jsx
client/src/pages/Register.jsx
client/src/pages/VerifyOtp.jsx
```

## Route table

| Path | Component | Public |
|---|---|---|
| `/login` | Login | yes |
| `/register` | Register | yes |
| `/verify-otp` | VerifyOtp | yes |

Add to `App.jsx` or router config.

## Placeholder UI spec

Each page minimum:

- `<h1>` title  
- Form with labeled inputs (no submit logic yet OK)  
- Link to sibling pages ("Already have account? Login")  
- Basic layout — match Home editorial style  

**Register fields:** email, password, confirm password (client-side), role select vendor/customer, displayName optional

**Login fields:** email, password

**VerifyOtp fields:** email (pre-filled from state), otp 6 digits

## Navigation stubs

Home page optional links:

- Login  
- Register  

Vendor/customer dashboard links come after ProtectedRoute (7.15).

## Verify in browser

```bash
cd client && npm run dev
```

Visit `/login`, `/register`, `/verify-otp` — no 404.

## Commit

```bash
git add client/src/pages/ client/src/App.jsx
git commit -m "feat(client): scaffold auth pages and routes"
```

## ✅ Do / ❌ Don't

**✅ Do:** Use React Router v6 patterns from Ch 2.

**✅ Do:** Consistent form class names for later styling.

**❌ Don't:** Wire API yet — 7.11–7.13.

## Verify checklist

- [ ] Three routes load in browser
- [ ] Cross-links between login/register work
- [ ] No console router errors

<RealWorldEvent title="GitHub's session and token evolution" when="2010s–present" summary="Developer platforms refined PATs, OAuth apps, and session handling as users demanded security without constant re-authentication." lesson="Treat the client auth layer as part of security design, not a UI afterthought — refresh logic belongs in one place, not scattered across pages." />

## Next

**Sub-chapter 7.10:** Auth context or useAuth hook.

Pages exist — state management next.