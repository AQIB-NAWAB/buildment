---
id: multi-vendor-marketplace-07-12
title: "7.12 — Verify OTP page"
module: 7
lesson: 12
stepType: Build
phase: "Module 7"
summary: "**Why this step matters:** Without this screen, `isVerified` stays false and login always fails — the registration story feels broken. This page closes the loop between register API and usable accounts."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 7.12 — Verify OTP page

> **[Build]** · Chapter 07 — Login & registration · *OTP input → verify endpoint*

**Why this step matters:** Without this screen, `isVerified` stays false and login always fails — the registration story feels broken. This page closes the loop between register API and usable accounts.

**Where the project is now:** Register navigates here with email; verify not wired.

**After this step:** VerifyOtp submits to `POST /api/auth/verify-otp`, success navigates to `/login` with success message optional.

## Form contract

| Field | Source |
|---|---|
| email | router location.state.email or input fallback |
| otp | 6-digit input — text or number |

If email missing from state, show email field editable.

## Submit flow

```
1. await verifyOtp(email, otp)
2. On 200: navigate('/login', { state: { message: 'Verified — please login' } })
3. On 400: show invalid/expired OTP
```

## UX notes

- Auto-focus OTP input  
- Login page displays state.message if present  
- Link back to register if wrong email  

## Verify in browser

Full path: register → verify with OTP from console/response → login page → login succeeds.

## Commit

```bash
git add client/src/pages/VerifyOtp.jsx client/src/pages/Login.jsx
git commit -m "feat(client): wire verify OTP page"
```

## ✅ Do / ❌ Don't

**✅ Do:** Handle missing router state gracefully.

**✅ Do:** Strip whitespace from OTP input.

**❌ Don't:** Skip verify in demo — show full flow.

## Verify checklist

- [ ] Verify success → login works
- [ ] Wrong OTP shows error
- [ ] Email pre-filled from register

## Next

**Sub-chapter 7.13:** Login page — tokens, redirect by role.

Verify done — login next.
