---
id: multi-vendor-marketplace-18-18
title: "18.18 — Checklist and learning log"
module: 18
lesson: 18
stepType: Gate
phase: "Module 18"
summary: "Gate for Chapter 18 — Checkout and orders. Tick every box and answer the learning log on this page before continuing."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 18.18 — Checklist and learning log
> **[Gate]** · Chapter 18 — Checkout and orders

This is the **gate** for Chapter 18. Tick every box honestly and answer the learning log below before opening Chapter 19.

## Chapter 18 — Checkout and orders checklist (gate)

Do **not** open Chapter 19 until every box is ticked and learning log answers are written below.

### Checkout

- [ ] `POST /checkout` requires customer auth
- [ ] Empty cart returns 400
- [ ] Two-store cart creates **two** Order documents
- [ ] Each order has single `storeId` — no mixed-store orders
- [ ] `orderItems` embedded with `nameAtPurchase`, `priceAtPurchase`, `unitAtPurchase`, `quantity`, `lineTotal`
- [ ] Order `total` equals sum of line totals
- [ ] Stock decremented atomically with `$gte` guard
- [ ] Insufficient stock fails entire checkout — no partial orders
- [ ] Cart empty after successful checkout
- [ ] Cart unchanged after failed checkout

### List routes

- [ ] `GET /orders/my` returns customer's orders only
- [ ] `GET /vendor/orders` returns vendor store's orders only
- [ ] Cross-vendor `GET /vendor/orders` does not leak other stores' orders
- [ ] List responses use snapshot prices — not populated live product price

### Immutability (18.16)

- [ ] Product price PATCH after order does not change `priceAtPurchase` on existing order
- [ ] Test documented in README or learning log

### Prior gates

- [ ] Chapter 17 cart UI works
- [ ] Chapter 16 cart API works
- [ ] Order model snapshot fields from Ch 5
- [ ] Explain one checkout → many orders
- [ ] Explain snapshot trap and fix
- [ ] Explain atomic stock decrement
- [ ] Explain fail-all checkout semantics

### Learning log — write now

Answer **in your own words** in the fields below:

1. **Split:** Walk through checkout for two-store cart — how many orders, what `storeId` on each?

2. **Snapshots:** Paste redacted `orderItems[0]` from Compass after checkout. Which fields would change if you only stored `productId`?

3. **Immutability test:** Describe 18.16 test with before/after prices.

4. **Stock:** How does your code prevent oversell? Do you use transactions?

5. **Failure:** What happens if second product in cart is OOS mid-checkout?

6. **Vendor list:** How do you scope `GET /vendor/orders` to one store?

7. **Chapter 19 preview:** What will checkout UI show after POST returns two orders?


## All boxes ticked?

Mark this chapter complete in the platform, then continue to **Payment processing** (Chapter 19).

## Phase progress — Week 3

| Target | After Ch 18 |
|---|---|
| Split orders API | ✓ |
| End-to-end browser demo | Chapter 19 |

---

## Final gate statement

*"I confirm POST /checkout splits multi-vendor cart into per-store orders with price snapshots, atomic stock decrement, cart cleared on success, and scoped list routes. Snapshot immutability tested. Ready for Chapter 19."*
