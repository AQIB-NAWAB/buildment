---
id: multi-vendor-marketplace-18-10
title: "18.10 — Checkout route"
module: 18
lesson: 10
stepType: Build
phase: "Module 18"
summary: "Implement POST /checkout: load the cart, group lines by storeId, create one Order per vendor. This is the spine of multi-vendor checkout."
estimatedMinutes: 35
prerequisites: []
resources: []
media: []
---
# 18.10 — Checkout route

> **[Build]** · Chapter 18 — Checkout & orders API · *POST /checkout — group by storeId*

This is the most important handler in Week 3. One customer checkout can create **multiple orders** — one per vendor. Your cart UI already showed this split in Chapter 17; now the server enforces it.

<BigWordAlert term="Checkout session" plainEnglish="The short-lived flow where cart items become orders and payment is authorized." whyItMatters="FreshMarket checkout validates stock, creates order documents, and hands off to Stripe without double-charging." />

## What happens at checkout

![Checkout split flow](/showcase/multi-vendor-marketplace/order-confirmation-page.png)

---

## Before you start

| Check | How |
|---|---|
| Cart has multi-vendor items | curl GET /cart — two storeIds |
| Order model exists | Chapter 5 schema |
| Orders module scaffolded | 18.9 routes mounted |
| Customer auth works | Bearer token on POST |

---

## Step 1 — Implement checkout(userId) — phase 1

In `orders.service.js`, build the pipeline in this order (snapshots and stock come in 18.11–18.12):

1. **Load cart** — all items for `userId`, product populated
2. **Empty check** — if no items → `400 { message: 'Cart is empty' }`
3. **Group by storeId** — `Map<storeId, lines[]>`
4. **For each store group**, build one Order:
   - `customerId` = userId
   - `storeId` = group key
   - `status` = `'pending'`
   - `orderItems` = copy from cart lines (refine in 18.11)
   - `total` = sum of line totals from current product price
5. **Insert** — `Order.insertMany(orders)` or loop inside transaction
6. **Return** — `{ orders: createdOrders }`

---

## Step 2 — Validate before insert

| Check | If fails |
|---|---|
| Product missing | 400 |
| Product unpublished | 400 |
| quantity < 1 | 400 |
| line.storeId ≠ product.storeId | 400 — data integrity |

Never trust grouping from the client body — read the cart from the database.

---

## Step 3 — Wire the route

```
POST /api/checkout
Middleware: requireAuth → requireRole('customer')
Body: {} (optional deliveryNote later)
```

Vendor tokens must get **403** on this route.

---

## Step 4 — Test with curl

Cart with items from two stores:

<ApiRequestPanel title="Checkout — POST /checkout">

<ApiRequest
  label="Multi-vendor checkout — two stores in cart"
  method="POST"
  url="http://localhost:5000/api/checkout"
  bearer="$CUSTOMER_TOKEN"
  body="{}"
  responseStatus={201}
  responseBody='{"orders":[{"_id":"...","storeId":"STORE_A","total":305,"orderItems":[...]},{"_id":"...","storeId":"STORE_B","total":40,"orderItems":[...]}]}'
/>

</ApiRequestPanel>

**Expected:** JSON with **2 orders** — different `storeId` on each.

Open Compass — two Order documents, not one mixed order.

---

## Response shape

```json
{
  "orders": [
    { "_id": "...", "storeId": "...", "total": 305, "orderItems": [...] },
    { "_id": "...", "storeId": "...", "total": 40, "orderItems": [...] }
  ]
}
```

Array length = number of distinct stores in cart.

---

## Common mistakes

| Mistake | Result | Fix |
|---|---|---|
| One order with mixed stores | Vendors can't fulfil | groupBy storeId |
| Client sends store groups | Tamperable | Server reads cart |
| Skip empty cart check | Ghost orders | 400 early |
| Vendor can checkout | Wrong role | requireRole('customer') |

---

## Verify checklist

- [ ] Two-store cart → two orders in response and Compass
- [ ] Single-store cart → one order
- [ ] Empty cart → 400
- [ ] Vendor token → 403

<RealWorldEvent title="Double-charge incidents during flash sales" when="Various" summary="E-commerce outages and retries caused duplicate charges until teams adopted idempotent payment APIs and clear order states." lesson="Model order status explicitly — pending, paid, failed — and never create a second order for the same checkout attempt." />

## Next

**Sub-chapter 18.11:** Snapshot order lines — freeze name, price, unit at purchase time.

Orders exist. Next, make prices immutable on order history.