---
id: multi-vendor-marketplace-23-09
title: "23.9 — Build client for production"
module: 23
lesson: 9
stepType: Build
phase: "Module 23"
summary: "**Why this step matters:** A live client that still calls `localhost:5000` is the most common deploy failure — Vite bakes the API URL at build time, so this step locks the browser to your production API forever (until you rebuild)."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 23.9 — Build client for production

> **[Build]** · Chapter 23 — Deploy · *`npm run build`; set `VITE_API_URL`*

**Why this step matters:** A live client that still calls `localhost:5000` is the most common deploy failure — Vite bakes the API URL at build time, so this step locks the browser to your production API forever (until you rebuild).

**Where the project is now:** Production env inventories are complete. You may know a custom API domain already, but a platform-generated URL may not exist until 23.10.

**After this step:** the build contract has been tested. If the final API origin already exists, `client/dist/` is ready for 23.12. Otherwise, repeat the same command after 23.11 with the real origin.

Vite replaces `import.meta.env.VITE_API_URL` at **compile time** — frozen in minified JS. Host env changes after upload do nothing. URL change → rebuild and redeploy. Worker never reads `VITE_*` — worker proof is 23.13 logs.

## Build contract — spec

| Input | Requirement |
|---|---|
| `VITE_API_URL` | Full HTTPS origin — `https://api.yourdomain.com` |
| Scheme | `https://` when client is HTTPS |
| Trailing slash | Match `api.js` convention |
| API origin | Use only a final custom domain or an already assigned platform URL |

```bash
cd client
VITE_API_URL=https://api.yourdomain.com npm run build
```

Exit code `0`. `api.js` must use `import.meta.env.VITE_API_URL` — not hardcoded localhost.

## Verify API URL in the artifact

```bash
rg -n "localhost|api\.yourdomain\.com" dist
```

| Result | Meaning |
|---|---|
| Production HTTPS URL found | Pass |
| `localhost:5000` found | Fail — rebuild |
| No production match | Inspect the Network tab in `npm run preview` before deploy |

Run `npm run preview` and use the Network tab to confirm the request origin. Grepping a minified bundle is a useful guard, but an actual request proves the URL composition code is correct.

## SPA routing — static host requirement

`BrowserRouter` needs fallback to `index.html` on unknown paths.

| Host | Config — you implement |
|---|---|
| Netlify | `_redirects` or `netlify.toml` |
| Vercel | `vercel.json` rewrites |
| Nginx on VPS | `try_files $uri /index.html` |

Without fallback, `/shop` refresh → 404. Include config in 23.12 artifact.

## Platform build env

| Platform | Where `VITE_API_URL` lives |
|---|---|
| Vercel / Netlify | Env vars → **Production** build scope |
| GitHub Actions | Secret or `env:` before `npm run build` |
| Local upload | Prefix on shell command above |

`.gitignore` `dist/` by default — never hand-edit minified JS. CORS errors → fix server `CLIENT_ORIGIN` (23.8), not rebuild alone.

## Failure modes

| Symptom | Likely cause |
|---|---|
| Live client calls localhost | Built without production `VITE_API_URL` |
| CORS on live client | `CLIENT_ORIGIN` not updated on API |
| Mixed content blocked | `VITE_API_URL` uses `http://` |
| `/shop` refresh 404 | Missing SPA fallback |
| Old URL after fix | Stale `dist/` without rebuild |

## Artifact rule

Usually, do not commit `dist/`. Commit the SPA configuration and README build command. Let the static host or CI build from a specific Git revision so you can identify and roll back the deployed artifact.

## ✅ Do / ❌ Don't

**✅ Do:** Rebuild when API URL changes; grep `dist/` for localhost.

**✅ Do:** Include SPA fallback in deploy artifact.

**❌ Don't:** Edit minified JS; assume runtime env affects Vite output.

**❌ Don't:** Use HTTP API URL when client is HTTPS.

## Verify checklist

- [ ] `npm run build` exits 0 with production `VITE_API_URL`  
- [ ] Grep shows production URL — not localhost  
- [ ] SPA fallback documented for your host  
- [ ] `dist/` ready for 23.12  
- [ ] README records build command  

## Next

**Sub-chapter 23.10:** Deploy the API and worker, capture the real API origin, then produce the final client build if necessary.
