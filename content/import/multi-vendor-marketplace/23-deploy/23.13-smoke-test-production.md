---
id: multi-vendor-marketplace-23-13
title: "23.13 — Smoke test production"
module: 23
lesson: 13
stepType: Build
phase: "Module 23"
summary: "**Why this step matters:** Deployed is not done — smoke tests are the gate. Row 6 (worker log after checkout) catches the API-only deploy mistake that health checks alone never reveal."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 23.13 — Smoke test production

> **[Build]** · Chapter 23 — Deploy · *Health, login, Stripe test checkout, worker on live URL*

**Why this step matters:** Deployed is not done — smoke tests are the gate. Row 6 (worker log after checkout) catches the API-only deploy mistake that health checks alone never reveal.

**Where the project is now:** Client and API on HTTPS (22.10–22.12); env for API and worker (22.8); worker deployed — **must be running**.

**After this step:** Critical paths on **live HTTPS** verified; worker processes checkout job; results in README.

Prove the **deployed system** — API, client, MongoDB, Redis, Cloudinary, **and worker**. Health-only fails gate. Chapter 22.15 proved async locally — repeat on public URLs.

## Prerequisites

Worker running in host dashboard (not just API). Same `REDIS_URL` on both (22.8). Test customer `test+prod@...`. Incognito browser — no stale dev tokens.

## Smoke test script — mandatory

| # | Test | Pass criteria |
|---|---|---|
| 1 | `GET https://api.../health` | 200; `status: ok`, `db: connected` |
| 2 | Login customer | Works in **browser**; no CORS |
| 3 | Browse `/shop` | Grid from production API |
| 4 | Add to cart | Persists after refresh |
| 5 | Checkout + payment | 201; order visible; cart cleared; Stripe test card succeeds in browser |
| 6 | **Worker smoke** | Worker logs job after paid checkout |
| 7 | Search | Results if wired (Ch 21) |
| 8 | Vendor login | Dashboard loads |

Rows 5–6 prove async pipeline. Row 6 fails on API-only deploy even when row 5 passes.

## Worker smoke test — contract

Verify worker **consuming** — not just Redis up or checkout 201.

| Method | Pass criteria |
|---|---|
| Logs | Tail worker **during** row 5 paid checkout |
| Pattern | `[NOTIFY]` from Ch 22.12 |
| Timing | Within ~30s of 201 |
| Multi-store | Two-store cart → multiple notify lines |
| Negative | Stop worker → 201, no log; restart → new checkout logs |

**No logs?** Check: worker service running; identical `REDIS_URL`; queue `notifications` (Ch 22); startup log present.

## curl and browser

```bash
curl -s https://api.yourdomain.com/health
curl -s -X POST https://api.yourdomain.com/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test+prod@yourdomain.com","password":"..."}'
```

Rows 3–5, 8 in browser — Network tab shows 22.9 API host, never localhost. curl can pass row 2 while browser fails (CORS/cookies Ch 7).

## README contract

Table: date, client URL, API URL, **worker service name**, pass/fail rows 1–8. Fix row 5 or 6 before gate.

## Failure modes

| Symptom | Likely cause |
|---|---|
| Health OK, else fails | Wrong `VITE_API_URL` in client |
| 201, no worker log | Worker down; wrong `REDIS_URL` |
| Checkout 500 | MongoDB; stock; missing API env; Stripe misconfig |
| Login browser-only fail | Cookie / CORS |
| Search empty | Redis index — Ch 21 |

## ✅ Do / ❌ Don't

**✅ Do:** Full eight rows after every redeploy; tail worker during checkout.

**❌ Don't:** Health-only gate; skip row 6 because 201 passed.

**❌ Don't:** Test localhost post-deploy; assume deploy = worker running.

## Verify checklist

- [ ] Row 1: `/health` 200, DB connected  
- [ ] Row 2: Auth in browser  
- [ ] Rows 3–5: Shop, cart, Stripe test checkout on live client  
- [ ] Row 6: Worker log within ~30s of checkout  
- [ ] Rows 7–8 if applicable  
- [ ] README table with worker service name  
- [ ] No localhost in Network tab  
- [ ] Worker running before script started  

## Next

**Sub-chapter 23.14:** Restart survival — API **and worker** recover after kill or redeploy.
