---
id: multi-vendor-marketplace-04-17
title: "4.17 — Checklist and learning log"
module: 4
lesson: 17
stepType: Gate
phase: "Module 4"
summary: "Gate for Chapter 4 — Config and database. Tick every box and answer the learning log on this page before continuing."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 4.17 — Checklist and learning log
> **[Gate]** · Chapter 04 — Config and database

This is the **gate** for Chapter 4. Tick every box honestly and answer the learning log below before opening Chapter 5.

## Chapter 4 — Config and database checklist (gate)

Do **not** open Chapter 5 until every box is ticked and learning log answers are written below.

### Secrets & environment

- [ ] `server/.env.example` committed with `MONGODB_URI`, `PORT`, `NODE_ENV` placeholders
- [ ] `server/.env` exists locally with **working** connection string — **not** committed
- [ ] Root `.gitignore` excludes `.env` files
- [ ] `git check-ignore -v server/.env` shows ignore rule matches
- [ ] `git log --all --full-history -- "**/.env"` returns **empty**
- [ ] README documents `cp server/.env.example server/.env` and client env copy steps
- [ ] No `MONGODB_URI` or database password in client code or `VITE_*` env vars

### Server config & database

- [ ] `server/src/shared/config.js` validates required env (minimum: `MONGODB_URI`)
- [ ] Missing or empty `MONGODB_URI` prevents normal startup with **clear error message**
- [ ] `server/src/shared/db.js` exports `connectDB()` using URI from config
- [ ] `mongoose` installed and listed in `server/package.json`
- [ ] `server/src/server.js` awaits database connection **before** `app.listen`
- [ ] Successful boot logs MongoDB connection and listen on configured port
- [ ] No Mongoose models or schemas added yet (still Chapter 5)

### Health & integration

- [ ] `curl -s http://localhost:5000/health` returns JSON with `"status":"ok"` and `"db":"connected"` when MongoDB is up
- [ ] Health does **not** expose connection string or credentials
- [ ] CORS still allows client to fetch health from Vite origin
- [ ] Client Home displays **live** API `status` and `db` fields from health response
- [ ] Client shows error state when server is stopped (Chapter 2 behaviour preserved)

### Failure-mode verification (4.15)

- [ ] Tested startup with missing `MONGODB_URI` — fail-fast confirmed
- [ ] Tested MongoDB unavailable (stop service, bad URI, or Atlas equivalent) — system does **not** claim `db: connected` falsely
- [ ] Restored happy path after failure tests without code changes

### Prior chapter gates still true

- [ ] `docs/erd-draft.md` still in repo from Chapter 3
- [ ] Chapter 2 skeleton intact — module folders exist under `server/src/modules/`
- [ ] I can explain **12-factor config** as it applies to `MONGODB_URI`
- [ ] I can explain **boot order**: config → connect → listen
- [ ] I can explain why **`config.js` and `db.js` are separate files**
- [ ] I can explain why **`db: connected` in health does not mean seed data exists**
- [ ] I can point to where Chapter 5 will import `connectDB` for the seed script

### Learning log — write now

Answer **in your own words** in the fields below:

1. **Environment setup:** Walk through how configuration flows from `server/.env` into running code — include env loading mechanism (dotenv or `--env-file`), `config.js`, and `db.js`. What happens if `MONGODB_URI` is missing?

2. **Secrets boundary:** Why must `MONGODB_URI` never appear in `client/.env` or React components? What is safe in `VITE_API_URL` and why?

3. **Boot order:** Why does this chapter require MongoDB connection before `app.listen`? What bug appears if you reverse the order?

4. **Health contract:** Paste your actual `/health` JSON from curl (redact nothing — it should contain no secrets). Explain what `db: connected` checks and what it does **not** prove about your data.

5. **Failure test:** Describe one test you ran when MongoDB or config was wrong (4.15). What did the server do? What did the client show?

6. **Local vs Atlas:** Which did you use for development? One advantage and one disadvantage for your choice on this project.

7. **Chapter 5 preview:** How will `scripts/seed.js` reuse `connectDB` and the same `MONGODB_URI`? What collections from your ERD draft will get documents first?

8. **Git verification:** What commands did you run to confirm `.env` was never committed? What would you do if you accidentally committed a URI?


## All boxes ticked?

Mark this chapter complete in the platform, then continue to **Data model and seed** (Chapter 5).

## Phase progress — Week 1

| Week 1 demo target | After Ch 4 gate |
|---|---|
| Both apps run | ✓ |
| Client hits `/health` | ✓ |
| MongoDB connected | ✓ |
| Seed visible in Compass | Chapter 5 |
| ERD finalized | Chapter 5 |

---

## Troubleshooting before you leave Chapter 4

| Blocker | Where to look |
|---|---|
| `MONGODB_URI` undefined | 4.9, 4.10 — env load order |
| Server listens but DB down | 4.12 — await connectDB |
| Health missing `db` | 4.13 |
| Client old UI | 4.14 — read `db` from response |
| `.env` in git status | 4.2, 4.9 — gitignore |
| Mongoose auth failed | 4.5 — Atlas password URL encoding |
| Compass empty | **Expected** until Chapter 5 seed |

---

## Optional stretch

Add graceful shutdown: on `SIGINT`, call `disconnectDB()` before exit. Document in learning log. Not required for gate.

Implement optional config warning when `NODE_ENV=production` but URI contains `localhost` — catches deploy mistakes early.

---

## Final gate statement

Optional confirmation statement:

*"I confirm MongoDB connects from server/.env via shared/config.js and shared/db.js, the server connects before listen, GET /health reports db status honestly, client displays live db status without database secrets, and .env is not in Git. I am ready for Chapter 5."*

**All boxes ticked? Open Chapter 5 — Data model & seed.**

Your API remembers how to reach the database. Next you teach it **what** to store.
