---
id: multi-vendor-marketplace-24-03
title: "24.03 — What you'll build"
module: 24
lesson: 3
stepType: Learn
phase: "Module 24"
summary: "A reservation state machine, checkout integration, webhook confirm, and expiry worker — with ATP exposed on product reads."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---

# 24.03 — What you'll build

> **[Learn]** · Chapter 24 — Inventory reservations · *Blueprint*

This module adds a small but complete **inventory reservation subsystem**. It is intentionally sized: one collection, one denormalized counter on Product, three service functions, two integration points (checkout + webhook), and one worker handler.

## Reservation state machine

```
                    reserve()
         ┌──────────────────────────────┐
         │                              │
         ▼                              │
      [active] ──confirm(paid)──► [confirmed]
         │                              │
         │ expire / abandon             │ (terminal)
         ▼                              │
     [released] ◄───────────────────────┘
         (terminal)
```

| Status | Meaning | Stock impact |
|---|---|---|
| `active` | Hold for a checkout session | Reduces ATP, not `stockQty` yet |
| `confirmed` | Payment succeeded | Decrement `stockQty`, reservation terminal |
| `released` | TTL or user abandoned | ATP restored |

Only **one** transition out of `active` wins — enforced with a conditional update on `{ _id, status: 'active' }`.

## New collection: `StockReservation`

Fields you will implement (names are canonical for this course):

| Field | Type | Purpose |
|---|---|---|
| `productId` | ObjectId | Which SKU |
| `storeId` | ObjectId | Vendor scope for audits |
| `quantity` | number | Units held |
| `status` | enum | `active` \| `confirmed` \| `released` |
| `checkoutId` | string | Correlates cart/checkout attempt |
| `paymentIntentId` | string | Set when PI created; used on confirm |
| `expiresAt` | Date | Worker releases after this instant |
| `confirmedAt` / `releasedAt` | Date | Audit |

**Indexes:** unique `{ checkoutId: 1, productId: 1 }` for retry safety; `{ expiresAt: 1, status: 1 }` for expiry sweeps.

## Product gains one concurrency counter

Add `reservedQty` to Product with default `0`. FreshMarket can then claim stock with one guarded
MongoDB update:

```text
availableQty = stockQty - reservedQty
reserve only when availableQty >= requestedQty
```

This denormalized counter is not decorative cache. It is the value that lets two checkout requests
compete on the same Product document, so MongoDB can allow exactly one winner. Aggregating active
reservation rows and then inserting a new row would be a read-then-write race: two requests could
both observe the same availability and both create holds.

## API behaviour changes

| Endpoint | Before Ch 24 | After Ch 24 |
|---|---|---|
| `POST /checkout` (or start-payment) | Creates PI + orders | **Reserve** each line first; 409 if ATP insufficient |
| Stripe webhook `payment_intent.succeeded` | Marks orders paid | **Confirm** reservations + decrement `stockQty` atomically per line |
| `DELETE /cart` or checkout cancel | Clears cart | **Release** active reservations for that `checkoutId` |
| `GET /products` | `stockQty` | Also `availableQty = stockQty - reservedQty` |

## Worker: `reservation.releaseExpired`

- Query `status: active` and `expiresAt <= now` in batches.
- For each: transactionally change the reservation to `released` and decrement Product `reservedQty`.
- Log counts; safe to retry.

## Verification you owe yourself

1. Product with `stockQty: 1` — two parallel checkout starts; exactly one gets 201, one gets 409.
2. Pay the winner — `stockQty` becomes 0; reservation `confirmed`.
3. Start checkout, wait past TTL without paying — `reservedQty` returns to 0 and ATP returns to 1 without manual DB edit.

<BigWordAlert term="Available to promise (ATP)" plainEnglish="Units you are willing to sell right now — on-hand minus active holds." whyItMatters="Catalogue must show ATP, not raw stock, or customers build carts that checkout rejects." />

**Next:** Reserve vs decrement — when each number moves.
