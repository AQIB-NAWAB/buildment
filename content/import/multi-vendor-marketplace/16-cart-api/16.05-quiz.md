---
id: multi-vendor-marketplace-16-05
title: "16.5 — Quiz"
module: 16
lesson: 5
stepType: Check
phase: "Module 16"
summary: "**Why this step matters:** Optional self-check on `storeId`, user scoping, and stock timing catches conceptual gaps **before** you mount cart routes in 16.7 — security bugs in cart are hard to unwind after UI depends on them."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 16.5 — Quiz

> **[Check]** · Chapter 16 — Cart API · *Reserved*

**Why this step matters:** Optional self-check on `storeId`, user scoping, and stock timing catches conceptual gaps **before** you mount cart routes in 16.7 — security bugs in cart are hard to unwind after UI depends on them.

<BigWordAlert term="Cart line item" plainEnglish="One row in the cart: a product, quantity, and computed subtotal for that vendor's item." whyItMatters="Checkout later groups line items by vendor to create split orders from one payment." />

**Status:** Reserved — excluded from this course pass.

Continue to **Where The Project Is Now** in the sidebar.

When quizzes are added, questions may cover storeId validation, user scoping, stock checks, and cart vs order snapshots.

## Mini self-check (optional)

1. Why store storeId on cart line?  
2. What middleware on every cart route?  
3. Status when User B PATCHes User A's cart itemId?  
4. When is stock decremented — add to cart or checkout?  
5. POST /cart body fields?

Answers from 16.1–16.4.

## Next

**Sub-chapter 16.6:** Where the project is now — quiz done; check the ground state before the build cluster starts.

Build cluster starts.
<InterestingRead title="Cart merge on login" hook="Guest cart + user cart = one headache if you plan late." readMinutes={5}>

Shoppers browse before signing in. When they log in, anonymous cart items must merge without duplicating quantities or dropping selections. FreshMarket handles merge in the API so every client behaves consistently.

</InterestingRead>
