---
id: multi-vendor-marketplace-16-09
title: "16.9 — Get cart route"
module: 16
lesson: 9
stepType: Build
phase: "Module 16"
summary: "**Why this step matters:** GET /cart is what Chapter 17's page will render — populated product and store names avoid N+1 and give the UI everything for grouped display without extra fetches per line."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 16.9 — Get cart route

> **[Build]** · Chapter 16 — Cart API · *`GET /cart` — populated products*

**Why this step matters:** GET /cart is what Chapter 17's page will render — populated product and store names avoid N+1 and give the UI everything for grouped display without extra fetches per line.

<BigWordAlert term="GET /cart" plainEnglish="Returns current user's cart with populated product details and computed totals." whyItMatters="Cart page and header badge load from this route on every navigation." />

**Where the project is now:** POST /cart creates lines; no list endpoint.

**After this step:** `GET /cart` returns current user's items with populated product and store — ready for Chapter 17 UI grouping.

## GET /cart contract

| Field | Value |
|---|---|
| Method | GET |
| Path | `/cart` |
| Auth | Required |

**Query:** none — always current user's cart from JWT.

## Service spec

```javascript
// getCartForUser(userId)
// CartItem.find({ userId })
//   .populate('productId', 'name price unit imageUrl stockQty')
//   .populate('storeId', 'name')
// Map to DTO
```

Apply N+1 discipline — populate both refs in one find, not loops.

## Response shape

```json
{
  "items": [
    {
      "id": "cartLineId",
      "quantity": 2,
      "storeId": "674a...",
      "product": {
        "id": "...",
        "name": "Organic Tomatoes",
        "price": 3.5,
        "unit": "kg",
        "imageUrl": "https://...",
        "stockQty": 10
      },
      "store": {
        "id": "...",
        "name": "Green Valley Grocer"
      }
    }
  ]
}
```

Include raw `storeId` on line for checkout grouping in Ch 17 UI — even if `store` nested present.

**Subtotal:** optional `{ subtotal: 7.0 }` — sum of price * quantity; exclude if not implemented yet.

## Empty cart

**200** with `{ items: [] }` — not 404.

## Deleted product handling

If productId broken ref:

| Strategy | Pick |
|---|---|
| Omit line + log | cleanup job |
| Return line with product: null | UI shows remove |
| Auto-delete orphan line | aggressive |

Document choice — gate prefers no 500 on GET.

## curl test

<ApiRequestPanel title="Get cart — populated items">

<ApiRequest
  label="Customer A cart after two-store adds"
  method="GET"
  url="http://localhost:5000/api/cart"
  bearer="CUSTOMER_A_TOKEN"
  responseStatus={200}
  responseBody='{"items":[{"id":"...","quantity":2,"storeId":"...","product":{"id":"...","name":"Organic Tomatoes","price":3.5,"unit":"kg","imageUrl":"https://...","stockQty":10},"store":{"id":"...","name":"Green Valley Grocer"}}]}'
/>

</ApiRequestPanel>

After two POSTs from different stores — two items, two storeIds.

## Commit

```bash
git commit -m "feat(cart): add GET /cart with populated product and store"
```

## ✅ Do / ❌ Don't

**✅ Do:** Filter `{ userId }` only — never return all cart items in DB.

**✅ Do:** Select minimal product fields for cart display.

**❌ Don't:** Expose other users' carts via query param ?userId=.

**❌ Don't:** Loop populate per item.

## Verify checklist

- [ ] Empty cart → 200 []  
- [ ] After add → populated product name and store name  
- [ ] Two stores in one cart visible in JSON  
- [ ] 401 without token

<RealWorldEvent title="Target cart persistence across channels" when="2010s–present" summary="Retailers invested in unified carts so web, app, and in-store pickup saw the same basket — reducing abandoned purchases." lesson="Treat cart mutations as concurrent — two tabs updating quantity should not corrupt totals." />

## Next

**Sub-chapter 16.10:** PATCH /cart/:itemId — update quantity.

List works; mutating quantity next.