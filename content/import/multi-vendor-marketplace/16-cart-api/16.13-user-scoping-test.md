---
id: multi-vendor-marketplace-16-13
title: "16.13 — User scoping test"
module: 16
lesson: 13
stepType: Build
phase: "Module 16"
summary: "**Why this step matters:** Cart isolation is a **security gate** — User B must never read or mutate User A's itemIds. This explicit test prevents IDOR bugs that are embarrassing in demo and dangerous in production."
estimatedMinutes: 20
prerequisites: []
resources: []
media: []
---
# 16.13 — User scoping test

> **[Build]** · Chapter 16 — Cart API · *User A cannot see User B's cart*

**Why this step matters:** Cart isolation is a **security gate** — User B must never read or mutate User A's itemIds. This explicit test prevents IDOR bugs that are embarrassing in demo and dangerous in production.

<BigWordAlert term="User-scoped cart" plainEnglish="Cart documents keyed by userId — user A never sees user B's cart items." whyItMatters="FreshMarket test: two accounts, add items separately, confirm carts do not merge." />

**Where the project is now:** Cart CRUD implemented; isolation may be untested explicitly.

**After this step:** Documented proof that **User A cannot read or mutate User B's cart** — IDOR/BOLA class bug prevented.

## Threat model

Cart items are **object-level resources**. Knowing another user's `itemId` (MongoDB ObjectId) must not grant access.

Attack pattern:

1. Customer A adds item — note `itemId` from response  
2. Customer B uses A's `itemId` in PATCH or DELETE  
3. **Must fail** — 404 or 403  

Same for GET /cart — only own lines returned.

## Test matrix

<ApiRequestPanel title="Cart user isolation">

<ApiRequest
  label="Customer A — GET /cart after adding two-store items"
  method="GET"
  url="http://localhost:5000/api/cart"
  bearer="TOKEN_A"
  responseStatus={200}
  responseBody='{"items":[{"_id":"ITEM_ID_A","storeId":"STORE_A","quantity":1},{"_id":"ITEM_ID_B","storeId":"STORE_B","quantity":1}]}'
/>

<ApiRequest
  label="Customer B — PATCH Customer A itemId (must 404 or 403)"
  method="PATCH"
  url="http://localhost:5000/api/cart/ITEM_ID_A"
  bearer="TOKEN_B"
  body='{"quantity":99}'
  responseStatus={404}
  responseBody='{"message":"Cart item not found"}'
/>

<ApiRequest
  label="Customer B — DELETE Customer A itemId (must 404 or 403)"
  method="DELETE"
  url="http://localhost:5000/api/cart/ITEM_ID_A"
  bearer="TOKEN_B"
  responseStatus={404}
  responseBody='{"message":"Cart item not found"}'
/>

<ApiRequest
  label="No token — GET /cart"
  method="GET"
  url="http://localhost:5000/api/cart"
  responseStatus={401}
  responseBody='{"message":"Unauthorized"}'
/>

</ApiRequestPanel>

| # | Actor | Action | Expected |
|---|---|---|---|
| 1 | Customer A | POST two products (2 stores) | 201/200 |
| 2 | Customer A | GET /cart | 2 items, A's userId in Compass |
| 3 | Customer B | GET /cart | empty or B-only items |
| 4 | Customer B | PATCH A's itemId | 404 or 403 |
| 5 | Customer B | DELETE A's itemId | 404 or 403 |
| 6 | Customer A | GET /cart after B attack | unchanged 2 items |
| 7 | No token | GET /cart | 401 |

Record itemId from step 1 for steps 4–5.

## Compass verification

CartItem documents:

- All lines for Customer A have `userId: A`  
- No query in code uses `find({})` without userId filter on cart  

## Implementation audit checklist

Review cart.service.js for:

- [ ] `find({ userId })` on GET  
- [ ] `findOne({ _id: itemId, userId })` on PATCH/DELETE  
- [ ] POST sets userId from JWT only  
- [ ] No admin bypass accidentally mounted  

## Multi-vendor cart proof

Same session Customer A:

| Line | storeId |
|---|---|
| Product 1 | Store A |
| Store B product | Store B |

GET /cart JSON shows **two distinct storeId** values — checkout split preview.

Paste snippet into learning log.

## README — cart API

Document all four endpoints with auth requirement and example curls — copy from 16.8–16.11.

Note: **login required** for all cart operations.

## Commit

```bash
git add README.md
git commit -m "docs: cart API user scoping tests and multi-vendor examples"
```

Fix any isolation bugs found before commit.

## ✅ Do / ❌ Don't

**✅ Do:** Use 404 for cross-user item access — avoids enumeration.

**✅ Do:** Test with two real JWT tokens — not same user twice.

**❌ Don't:** Pass gate if PATCH without userId in query ever succeeds for wrong user.

**❌ Don't:** Return 403 with message "not your cart" if you prefer uniform 404 — either OK; be consistent.

## Verify checklist

- [ ] Full test matrix passed  
- [ ] Multi-vendor two storeIds in one cart  
- [ ] Code audit confirms userId in every write query  
- [ ] README cart section added

<RealWorldEvent title="Target cart persistence across channels" when="2010s–present" summary="Retailers invested in unified carts so web, app, and in-store pickup saw the same basket — reducing abandoned purchases." lesson="Treat cart mutations as concurrent — two tabs updating quantity should not corrupt totals." />

## Next

**Sub-chapter 16.14:** Recap and what's next — Chapter 17 cart UI.

API gate almost complete; learning log in 16.15.