---
id: multi-vendor-marketplace-16-08
title: "16.8 — Add to cart route"
module: 16
lesson: 8
stepType: Build
phase: "Module 16"
summary: "Implement POST /cart: validate product and storeId, upsert a cart line for the logged-in customer. This is where multi-vendor cart lines are created."
estimatedMinutes: 30
prerequisites: []
resources: []
media: []
---
# 16.8 — Add to cart route

> **[Build]** · Chapter 16 — Cart API · *POST /cart*

When a customer adds rice from Green Valley and spices from Spice Corner, both lines land in **one cart** — but each line carries a **`storeId`**. That field is what lets checkout split into separate orders in Chapter 18.

<BigWordAlert term="Add to cart" plainEnglish="POST endpoint incrementing quantity or inserting new line for productId — validates stock." whyItMatters="FreshMarket PDP and product cards call this — first step toward checkout." />

This lesson implements `POST /cart`.

---

## Before you start

| Check | How |
|---|---|
| Cart module scaffolded | 16.7 — routes mounted |
| Customer auth works | Bearer token from Chapter 7 |
| Products exist in seed | Compass — published products with storeId |

---

## Step 1 — Define the request body

```
POST /api/cart
Authorization: Bearer <customer-token>
Content-Type: application/json

{
  "productId": "<mongo-id>",
  "storeId": "<mongo-id>",
  "quantity": 1
}
```

`userId` comes from JWT — **never** from the body.

---

## Step 2 — Validate in this order

1. **Schema** — productId, storeId, quantity required; quantity ≥ 1
2. **Load product** — 404 if missing
3. **Publish + stock** — reject unpublished or zero stock (400/404)
4. **Store match** — `product.storeId` must equal body `storeId` → else **400** (prevents tampering)
5. **Quantity vs stock** — `quantity <= product.stockQty` → else 400

<Callout type="warning" title="Why validate storeId?">
A malicious client could send the wrong storeId. You always check it against the product document — the server is the source of truth.
</Callout>

---

## Step 3 — Upsert the cart line

**Recommended:** one line per user + product.

```javascript
// findOne { userId, productId }
// if exists: quantity += incoming (re-check stock cap)
// else: create { userId, productId, storeId, quantity }
```

Copy `storeId` from the validated product — the body storeId must match.

**Do not** decrement stock here — that happens at checkout (Chapter 18).

---

## Step 4 — Return a clear response

**201** for new line, **200** if quantity merged:

```json
{
  "id": "...",
  "productId": "...",
  "storeId": "...",
  "quantity": 2
}
```

Stay consistent with whatever shape `GET /cart` will return in 16.9.

---

## Step 5 — Test with curl

<ApiRequestPanel title="Add to cart — POST /cart">

<ApiRequest
  label="Add product to cart"
  method="POST"
  url="http://localhost:5000/api/cart"
  bearer="$CUSTOMER_TOKEN"
  body='{"productId":"PRODUCT_ID","storeId":"STORE_ID","quantity":1}'
  responseStatus={201}
  responseBody='{"id":"...","productId":"PRODUCT_ID","storeId":"STORE_ID","quantity":1}'
/>

</ApiRequestPanel>

Use real ids from seed — storeId must match the product's store.

---

## Step 6 — Run error tests

| Test | Expected |
|---|---|
| Wrong storeId for product | 400 |
| Unpublished product | 400/404 |
| quantity: 0 | 400 |
| No auth header | 401 |
| quantity > stockQty | 400 |

Document status codes in your learning log.

---

## Step 7 — Multi-vendor proof

Add products from **two different stores** for the same customer:

<ApiRequestPanel title="Multi-vendor cart — two stores">

<ApiRequest
  label="Product from Store A"
  method="POST"
  url="http://localhost:5000/api/cart"
  bearer="$CUSTOMER_TOKEN"
  body='{"productId":"PRODUCT_A_ID","storeId":"STORE_A","quantity":1}'
  responseStatus={201}
  responseBody='{"id":"...","productId":"PRODUCT_A_ID","storeId":"STORE_A","quantity":1}'
/>

<ApiRequest
  label="Product from Store B"
  method="POST"
  url="http://localhost:5000/api/cart"
  bearer="$CUSTOMER_TOKEN"
  body='{"productId":"PRODUCT_B_ID","storeId":"STORE_B","quantity":1}'
  responseStatus={201}
  responseBody='{"id":"...","productId":"PRODUCT_B_ID","storeId":"STORE_B","quantity":1}'
/>

</ApiRequestPanel>

Compass: two CartItem rows, same userId, different storeIds.

---

## Common mistakes

| Mistake | Fix |
|---|---|
| Trust client storeId without lookup | Always compare to product.storeId |
| Decrement stock on add | Wait for checkout |
| userId from body | JWT only |
| Skip upsert | Duplicate lines for same product |

---

## Commit

```bash
git commit -m "feat(cart): add POST /cart with storeId validation"
```

## Verify checklist

- [ ] Valid add creates CartItem with userId + storeId in Compass
- [ ] Duplicate add merges quantity (if implemented)
- [ ] Store mismatch → 400
- [ ] Two-store add works for one customer

<RealWorldEvent title="Target cart persistence across channels" when="2010s–present" summary="Retailers invested in unified carts so web, app, and in-store pickup saw the same basket — reducing abandoned purchases." lesson="Treat cart mutations as concurrent — two tabs updating quantity should not corrupt totals." />

## Next

**Sub-chapter 16.9:** GET /cart — return populated products and stores for the UI.

Add works. Next, list the cart.